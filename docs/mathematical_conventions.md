# Mathematical Conventions

## Problem setting

This document records mathematical conventions shared by proofs and implementations.

## PageRank formulation

The project should explicitly define transition matrices, residuals, local error measures, and work complexity before implementation.

## Invariants

- SOR relaxation parameter uses 1 < omega < 2.
- Experiments must record alpha, epsilon, graph, seed, and stopping criteria.
- Generated figures should never be manually edited.
