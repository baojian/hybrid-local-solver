# Final review: 2026-09-06 09:06:34 UTC

The investigation is complete after 6 hours, 16 minutes, 45 seconds.
The 36-page proof note has been built and visually reviewed. Its strongest
direct hybrid result is the coarse orthant prefix followed by reserved
signed-coordinate delivery; a signed composite-AESP endpoint can use the
same handoff certificate without lower retraction. The unchanged AESP
prefix-work theorem remains open.

All recorded audits have completed. The all-seed atlas run passed 27,120
cases on 995 connected graph representatives, checking 2,461,140 exact
steps. The final parameter-corner sweeps passed 512 deterministic handoffs
and 1,114 directed-rounded cases plus four implicit stars, using alpha
values that are not dyadic squares. The ideal-AESP level recurrence also
matches direct shifted solves on three materialized small unit graphs.
The coverage manifest contains 35 overlapping runs and verifies 29 saved
source snapshots; the limitations of early provenance remain explicit.

The 12-audit focused suite, focused style checks, agent-boundary check,
and TeX build passed. Repository reproduction still has the same three
pre-existing test failures, and full lint has the same two unrelated
findings. No active-paper source, older note, baseline, or provider was
changed. The dated note is ready for independent review; it does not
replace the older accepted controller snapshot.

The earlier checkpoints below are historical records, including statements
that a result or audit was then still open or running.

---

# 08:29 UTC checkpoint: a signed AESP interface and an ideal-AESP obstruction

The new gap-to-excess lemma also holds for signed inputs measured by the
full weighted-l1 composite objective. Such a composite-AESP endpoint needs
no lower retraction before the reserved signed-coordinate tail. Four exact
or fixed-grid supplied negative-coordinate states passed the interface
check, each with a nonempty tail and both signs of coordinate updates.
They are interface witnesses, not outputs of an implemented AESP prefix.

The final clipping proof was strengthened to lower order against RPPR at
eps/3 and nonnegative PPR residual. This revealed an endpoint correction:
at alpha=1, return the regularized seed value instead of exact unregularized
PPR. The dedicated audit passed 256 actual runs, 12 above-PPR tail witnesses,
and 14 alpha=1/zero endpoint cases. The later composite run adds four signed
composite-gap witnesses. All archived source scopes remain explicit.

A further return to the old R_J proof template led to a separate result:
the ideal exact-inner unregularized AESP recurrence has unbounded weighted
residual mass divided by alpha, even at alpha=1/4097. On the outward-walk
limit, its resolvent is g(z)=2(1-1/64^2)/(3-z). At z=(12+5i)/13 the temporal
quadratic violates an elementary necessary unit-disk inequality by the exact
positive rational 6227645511749975/59180113853415424. One pole is outside the
unit disk and its initial coefficient is nonzero. Absolutely summable
resolvent series transfer any fixed-stage witness to finite regular unit
trees. This proves the graph/stage unboundedness without assuming a uniform
long-time graph limit.

The corresponding integer audit passed three finite trees. At stage 213 on
the depth-64, branch-10 tree, the ratio is strictly above 138610690219.
Equitable level equations are offline verification, never a free local
inverse. This obstruction covers the ideal exact-inner recurrence and what
can follow from accuracy certificates alone. It does not prove a failure
of the actual thresholded AESP policy or decide its early Lambda_J conjecture.

The document builds to 36 pages. A new 12-audit fast run and final PDF review
are in progress. The six-hour minimum still ends at 08:49:49 UTC.

---

# 07:42 UTC checkpoint: two final proof developments

The six-hour minimum still ends at 08:49:49 UTC; the task is in progress.

The separated-front limit is now proved. Levelwise energy bounds establish
activation separation without assuming a nonlinear history. A uniformly
stable finite input system bounds every older normalized error, after which
causal-kernel convergence gives the exact limit. The finite gain mechanism
therefore proves an asymptotic no-constant tracking obstruction. The twelve
new integer sign checks passed, including 65,000-step cases.

The more direct answer to the original two-stage question is now in
`13_deterministic_signed_handoff.tex`. Signed reserve-preserving coordinate
updates decrease the thresholded absolute residual potential by at least
alpha times their weighted absolute movement. With sigma=eps/3,
rho=sigma/2 and r_*=rho/2, an orthant-prefix gap G=alpha^2 theta rho
makes that potential at most alpha theta. The resulting signed GS tail has
accelerated work even when the prefix overshoots PPR. Final clipping by
2 sigma gives semantic error eps, lower PPR order and output volume 3/eps.
The frozen prefix needs only log(1/alpha), not log(1/eps). Its rounded
version uses the same directed prefix and a fixed-grid signed tail.

A conservative 256-case prefix/tail audit passed, but every small-graph tail
was empty. The optional directly observed residual gate passed 256 cases,
with 248 nonempty tails. The expanded early-gate run passed 1,096 actual
cases, with 1,025 nonempty tails and 1,083 early handoffs. Twelve separately
labelled external tail-only witnesses start above PPR and exercise negative
coordinate updates; those are not asserted to be prefix-generated states.
All source snapshots and scopes are retained in the raw records.

The positive residual-excess interface separately passed 256 cases, including
128 prefixes above the RPPR optimum and 256 with objective gap larger than
the earlier relative-gap handoff budget. The randomized-face extension with
the optional excess gate passed 36 cases and 27 nonempty tails. Earlier
handoff can greatly increase coordinate work (up to 655,442 updates in this
finite collection); no blanket practical speedup is claimed.

---

# Research log

## 06:14 UTC checkpoint: a nontrivial randomized handoff and large tracking failure

The coarse-face interface audit passed 36 path cases with n in
{16,32,64,128}, alpha in {1/4,1/64,1/1024}, and eps in
{2^-12,2^-24,2^-40}. Nineteen required nonempty reserved GS tails.
For P64, alpha=1/64, eps=2^-24, the tail made 1,112 rounded coordinate
updates. Every proposed dyadic face candidate passed the source theorem's
exact residual test; global reference optima checked lower order, objective
gap, and final ACL accuracy. This is an audit of the certified face-solve
interface, not an implementation or timing claim for randomized SDD.

The general-source OP2 extended run completed 10,228 cases, including four
zero/diagonal endpoints. Its source snapshot predates the later extra
KKT/maximum-principle assertions; those passed a separate 652-case run.

The integer-only radial tracking campaign continued below floating-point
resolution. Its first 81 cases completed in about 201 seconds. A fixed
T(10,2,128) tree, alpha=1/4096 and step 19129, has certified tracking ratio
strictly in (2700097454229948,2700097454229950). The separate fixed
counterexample script now reproduces this as its fourth case with grid
2^-700 and exact rational PPR. Its trajectory error radius is below 4e-174.
This is a large finite obstruction to a ramp-only shortcut; it does not
yet prove unboundedness over an infinite family. A four-case deeper-tree
search (depths256,512; T32,64) is running to examine the scaling, and an
asymptotic lower bound remains a research question.

Primary-source reading for that open question: Basic, Smajlovic and Sabanac,
"Discrete space-time wave kernels on regular trees", arXiv:2606.27083v1,
has explicit radial wave kernels, but its forward second-difference
recurrence differs from ours. No theorem is imported from it. Also opened
the authors' PDF of "Robustness of Accelerated First-Order Algorithms for
Strongly Convex Optimization Problems" for possible noise-amplification
context; no transfer to the nonlinear cooling recurrence is asserted.

## 06:02 UTC checkpoint: further results and remaining verification

This checkpoint is appended near the top to make the current state visible;
the earlier chronological record below is preserved. The six-hour minimum
still ends at 08:49:49 UTC. About 3h12 have elapsed, not six hours.

New sections now include:

- `09_sparse_sources.tex`: discard source entries of degree density at most
  eps/2, then jointly accelerate the retained subprobability source. Input
  reads cost O(|T|); retained source exceptions cost at most the target
  local product scale. This gives additive sparse-input work for OP1.
- `10_general_source_objective.tex`: retain source density above rho/2 and
  solve at regularizer rho/2 to obtain an approximate envelope. The load
  comparison proves that its optimum dominates the ORIGINAL RPPR optimum
  at rho. Restore the original source on the clipped envelope and solve
  that principal obstacle problem. Total gap is at most 4 delta^2/rho and
  total work is soft(|T|+1/(rho sqrt(alpha))), including a separately
  rounded fixed-face realization. A principal-obstacle maximum principle
  adds full normalized error <=4 delta, KKT norm <=4 delta, and positive
  PPR residual when delta<=alpha rho/4.
- `11_randomized_coarse_handoff.tex`: use the randomized threshold-batch
  face contract at G/2, with G=alpha mu rho/2 and rho=eps/2. Clip relative
  to its EXACT FACE solution, rather than invoking the existing stronger
  full-optimum repair. This keeps lower order and nonnegative residual
  without already asserting the final residual upper bound. A computed
  KKT norm can terminate discovery early; the reserve-preserving GS tail
  then has the accelerated product-size work bound. This is a composition
  of the imported randomized face theorem, not an implementation of SDD.
  Its exact interface audit is running; do not promote measured speedup.

The general-source OP2 smoke passed 652 exact/rounded cases. In 216 cases
the first stage alone exceeded the final objective budget, and in 108 cases
the second stage restored omitted source entries. Eight cases omitted
positive optimal coordinates, exercising the approximate-envelope proof.
A second 652-case run added the maximum-principle/KKT/residual assertions
and passed. The 10,224-case larger run is nearing completion, but began
before those final extra assertions; its saved source snapshot makes that
distinction explicit. The independent point-source objective/KKT/residual
audit passed 780 cases. Directed rounding completed 4,872 graph cases and
four implicit stars. Nonsquare-alpha exact checks completed; extract their
final count for the verification report.

The tempting ramp-only shortcuts now have exact COUNTEREXAMPLES:

- `ramp_four_counterexample.py`: the 512-vertex lollipop has error/r in
  (4.01807,4.01809), as previously recorded.
- `radial_ramp_counterexamples.py`: a rooted unit tree T(10,3,12) on
  33,331 vertices has a decreasing root density at step 2250, alpha=1/16384.
  The decrease is about 1.5948077762e-11, rigorously positive.
- T(10,4,128), alpha=1/4096, step 3037 has root PPR overshoot in
  (4e-13,5e-13) and negative normalized PPR residual in
  (-1.93e-14,-1.92e-14). Thus intermediate PPR lower order and nonnegative
  PPR residual are false, not merely unproved.
- T(2,4,128), alpha=1/4096, step 3498 has error/r in
  (6.82360,6.82361). A larger universal constant remains open.

Here T(b,s,L) branches b-fold at levels divisible by s and otherwise has
one child, through depth L. The exact certificates use level symmetry only
offline, integer grid 2^-200, the proved pairwise-contraction radius, and
an exact rational tridiagonal PPR solve. They are NOT floating-point claims
or weighted graphs substituted for canonical unit graphs. The larger
quotient diagnostic that found them is separately labelled numerical.
The main two-phase theorem assumes none of the refuted properties.

Remaining operational work: finish/audit the randomized coarse handoff;
update the stale README, STATUS, abstract and Section 04; register its
audit; compile and visually inspect the expanded PDF; create a durable
verification summary; run required checks, including `make reproduce`.
Known full-test failures remain in pre-existing AESP notation/oversized
files; two pre-existing lint findings are also recorded in the raw logs.
Do not edit those unrelated files just to turn the checks green.

- User request: revisit the two-stage attempts, using both newer randomized
  and deterministic solutions, and work for at least six hours.
- Start: **2026-09-06 02:49:49 UTC** (10:49:49 Asia/Shanghai).
- Earliest six-hour completion: **2026-09-06 08:49:49 UTC** (16:49:49 local).
- A final result must distinguish elapsed investigation time from CPU time,
  and must not claim six hours before that time.
- Existing worktree: `main`, with substantial unrelated/uncommitted active
  manuscript and literature edits. Preserve all those edits.

## Initial reading

Read repository and note instructions, mathematical conventions and residual
decision, shared summaries, the three requested notes' entrypoints and key
sections, and the new deterministic/randomized manuscript proofs. Shared
August summaries still call OP1/OP2 open; the September active manuscript
now states both existence results. Treat mathematical proof sources as
authority; do not conflate a solved existence target with the unchanged old
recurrences' open locality claims.

The existing `two_stage_point_source_aesp_cd` note already contains the
randomized set-only handoff, a direct leakage certificate, exact structural
tree solves, and a mass-capture barrier. Repackaging those is not a new idea.

## Candidate A: orthant-only accelerated continuation

For a nonnegative source h and t=Q^{-1}h, take p=[q]_+ and n=q-p.
On a nonzero normal coordinate, p_i=0, n_i<0, and
(Qp-h)_i<=0 by the off-diagonal signs. Therefore
(Qp-h)^T n>=0, exactly the second-metric sector condition.
Ordinary projection also supplies the first Euclidean sector condition.
This suggests that **both** the upper box and mass cap may be removed,
not merely one of them. The source-diffuseness assumption is still needed
to initialize the response bound; positivity of intermediate iterates
preserves the lower bound on the auxiliary energy.

Next checks: reconstruct every proof dependency without an iterate mass
bound, verify the exact recurrence in rational density coordinates, derive
the simplified local reporter, and check repair/continuation and finite
representation separately. This changes the projected kinetic trajectory;
it does not establish the old FISTA or AESP conjecture.

## Candidate B: a single coarse warm-up and one accelerated stage

Try a monotone RPPR coordinate prefix making the residual source diffuse at
a coarser level H. The new response bank suggests retaining its decaying
initial transient instead of bounding every step by its initial maximum.
Tentative optimization gives an alpha^(-3/4) two-stage tradeoff, rather than
the desired alpha^(-1/2). Derive this carefully before interpreting it as a
barrier: an upper-bound tradeoff alone proves no algorithm lower bound.

## Candidate C: continuous regularization with no intermediate repair

The orthant sector suggests a stronger route. Keep the source fixed at b,
start r_0=1/d_v, and use r_{k+1}=max(rho, eta r_k),
eta=1-theta/2 and chi=1-theta. At each step use the same orthant kinetic
coupling with lambda_k=alpha r_k. No center retraction or primal reset.
The inverse-source comparator t=Q^{-1}b is fixed.

Let B_k use the second energy at lambda_k. The fixed-lambda comparison and
nonnegativity give

    B_{k+1} <= chi B_k + alpha (lambda_k-lambda_{k+1}).

Since B_0<=0 and lambda_{k+1}>=eta lambda_k, induction gives
B_k<=alpha lambda_k, and hence ||Q xi_k-b||^2<=4 alpha lambda_k.
Against u_k=x*_{r_k}, the squared response is at most
10 alpha lambda_k. This initialization needs no diffuse fixed source: the
regularizer is initially large enough and decreases with the energy.

For signed flow, compare z_k to u_k but select using z_{k+1}>u_{k+1}.
Monotonicity u_{k+1}>=u_k supplies an extra favorable term. Divide each
step's selected-flow inequality by lambda_k. Since
lambda_k>=eta lambda_{k-1}>=beta_0 lambda_{k-1}, the weighted excess mass
still telescopes. The analytical core supp(x*_{r_k/2}) contains u_{k+1}
because r_{k+1}>=r_k/2. Weighted Cauchy then suggests

    sum_{k<K} vol(supp z_{k+1}) <= 44 sum_{k<K} 1/r_k.

The geometric ramp costs O(1/(theta rho)); freezing r=rho for
O(theta^{-1} log(1/eps_obj)) steps should finish with the same soft product
bound. Need a fully explicit first-energy bound at the freeze, final safe
repair (only once), and the actual lazy heap implementation. The key
threshold now changes with lambda_k, but the source-exception set is only
the seed. All of this remains a candidate until the exact audit and detailed
proof are complete.

## 03:12 UTC checkpoint: the direct semantic theorem

The main exact-real proof is now written in `main.tex` and its sections.
The bank also proves actual primal mass at most two, without imposing it
as a constraint. At the ramp endpoint r=rho the ordinary accelerated
objective energy is at most 9 alpha rho. This uses the response bound,
the kinetic Q-energy, and ||x_0^*-x_rho^*||_2^2<=rho.

For OP1, choose rho=eps_ppr/2 and freeze until objective gap is at most
alpha eps_ppr^2/32. Starting from 9 alpha rho, this needs at most
1+theta^{-1} log(144/eps_ppr) steps. Return

    x_hat = [x - (eps_ppr/4) sqrt(d)]_+.

Strong convexity gives ||x-x_rho^*||_2<=eps_ppr/4. Final clipping is
therefore below x_rho^*, has support volume at most 2/eps_ppr, and adds
at most eps_ppr/2 to the normalized bias rho. No intermediate or terminal
projected-gradient repair is needed for this semantic certificate. Do not
infer an ACL nonnegative-residual certificate from coordinatewise lower order.

The initial 64-case/1536-step exact smoke run passed. An expanded exact run
has passed more than 1,100 instances, continuing through all connected
graph-atlas representatives up to seven vertices, every seed, alpha in
{1/4,1/16,1/64,1/256}, rho=1/(32 d_v), and 40 frozen steps. A sparse-state
equivalence audit and the actual terminal accuracy test remain to be built.

The first LaTeX build found that the shared note preamble does not provide
mathscr; change the local reporter notation to mathcal instead of modifying
the shared preamble. This is a typesetting issue, not a mathematical change.

## Further structural simplification: a retained-coordinate FISTA form

Eliminate z using theta z_k=x_k-chi x_{k-1}. The exact primal recurrence is

    y_k = x_k + ((1-theta)/(1+theta)) (x_k-x_{k-1}),
    x_{k+1} = max{ (1-theta) x_k, (I-Q)y_k+b-lambda_k sqrt(d) }.

Thus the algorithm is ordinary-looking accelerated projected gradient with
a multiplicative retention floor rather than the zero floor. The sparse
updates are the positive excess above this shared decay. This equivalence
is proved in `prop:revisit-floor-form`; it must not be confused with a
confinement theorem for unchanged FISTA, whose full-primal scan work is
different.

A possible useful extension is to suppress vertices with d_i>1/rho after
their degree is read. Such vertices cannot belong to any relevant optimum
x*_r for r>=rho. The proof should extend to the principal operator M on
the admissible set, using M w>=alpha w and the auxiliary linear term
lambda (M w)^T x instead of alpha lambda w^T x. This is not yet promoted;
check its moving analytical core and sparse reporter separately.

## 04:00 UTC checkpoint: implementation and intermediate-state limits

The exact lazy-state audit passed 1,618 full runs through six vertices.
Every kinetic and primal coordinate and every represented key agreed with
the independent dense recurrence. Final output satisfied lower order to
the exact full RPPR optimum and normalized error to exact PPR. Separate
implicit stars with up to a trillion leaves exposed only the seed leaf
and the hub degree; the hub row was never opened. Additional endpoint
cases include alpha=1 and the zero-return branch. The alpha=1 branch was
corrected to return the exact RPPR coordinate, not exact PPR, so the stronger
output statement `x_hat<=x_rho*` holds in that branch too.

An exact P16 witness (endpoint seed, alpha=1/4096, step 224) has positive
seed RPPR gradient between 9e-9 and 1e-8 during cooling. This disproves the
positive-coordinate RPPR subsolution condition for every intermediate
state. This particular iterate is still below its current optimum; the
witness does not refute order or monotonicity. It also does not establish
a negative *PPR* residual: the regularizer shift matters here.

Two preliminary monotonicity searches found no decreasing primal coordinate:
2,427 exact small-graph cases and 600 floating structured/random cases.
Those exploratory runs preceded the registered ramp probe and are only
motivation, not the main durable evidence for a theorem.

During this session, the pre-existing manuscript work was committed by
another task as `7e1b4e77351b361e0c700ce3186c1534d2023f0e`. We preserved it.
The new audit provenance records that commit and the dirty-worktree state.
From the later sparse endpoint run onward, the provenance helper also saves
the exact Python source snapshot alongside each raw output.

## 04:15 UTC checkpoint: a bounded-arithmetic construction

The no-cap variant initially appeared harder to round because kinetic mass
is not explicitly constrained. The following construction closes the loop:

1. Use primal grid h but kinetic grid h/theta.
2. Round neighbor records downward at a scalar rebase by subtracting d_i h
   after scaling and flooring, then clamp at zero. Their normalized deficit
   stays between zero and 4h.
3. The represented raw kinetic vector is therefore below the ideal raw
   vector, within 2h/theta in each degree density. Both this error and the
   kinetic floor error help the selected-flow inequality.
4. Rebase the old primal records *before* adding new kinetic increments.
   A positive kinetic increment contributes at least half theta p to the
   actual primal vector. Thus new primal mass <=3 implies kinetic mass
   <=6/theta, closing the induction needed by the perturbation bound.

The two per-step energy perturbations are at most 256h; choose h so that
Gamma=256h/theta is at most alpha^2 rho and half the final tolerance.
The rounded bank yields response constant six and comparator-response
constant fourteen, leading to kinetic work `60 sum 1/r_k`.
Regularizers are rounded upward to the fixed grid g=theta rho/8; they still
decrease between factors 1-theta/2 and 1-3theta/8, and their integer
encodings do not grow with iteration count. A predetermined block schedule
avoids storing a growing contraction power in the frozen phase as well.

The first exact audit of the actual rounded reporter and states passed
146 graph cases and four implicit-star cases, with and without fixed pruning.
It verifies both perturbed energies, directed neighbor deficits, closed-tail
ties, actual dyadic state, selected flow, full-graph output accuracy/order,
and original-degree incidence charges. A larger six-vertex audit is running.
The formal precision and pruning proofs are now written in sections 05/06.

## 04:30 UTC checkpoint: OP2 and a stronger ramp-only hypothesis

The direct OP1 clipping argument also yields OP2 without an extra PG scan.
For independently requested rho and eps_obj choose dyadic delta with
eps_obj rho/8 < delta^2 <= eps_obj rho/2, freeze until gap <=alpha delta^2/2,
and output [x-delta sqrt(d)]_+. The output lies below x_rho*, and its error
is supported on supp(x_rho*). KKT makes the linear objective term vanish,
so the final gap is at most 2 delta^2/rho <=eps_obj. This is
`cor:revisit-rppr`; the rounded variant uses its own doubled scalar budget.
An independent audit of these objective stopping rules is running.

The first registered floating ramp-only probe finished 98 cases with maximum
normalized error/current-r approximately 3.79026 and no primal decrease.
A larger deterministic collection now tests alpha down to 2^-20, structured
graphs through 1,024 vertices, and 200 generated graph instances. Reference
linear-solve residual bounds are recorded so small-alpha numerical error is
visible. A uniform `error<=4r` statement would eliminate the final accuracy
phase, but has no proof yet and is not used anywhere in the current theorem.

For a fixed linear face, the exponential particular solution has a shifted
Stieltjes resolvent. At the smallest eigenvalue the tracking factor approaches
four when theta^2=alpha and eta=1-theta/2. This explains the empirical scale,
but does not control activation changes or the homogeneous inertial transient.
Those are the next falsifiable mathematical targets.
