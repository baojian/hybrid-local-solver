\begin{proposition}[Master-certified high Moreau window]
\label{prop:aesp-cd-psi-high-window}
Consider an exact fixed full positive face, and let $P$ be $D$-orthogonal
projection off the constant mode.  Put $h_t=Pe_t$,
$u_t=h_t-(1-q)h_{t-1}$, and
\begin{equation}
 \mathcal C_t^{\mathrm h}
 :=\langle h_t,Q_AM_Ah_t\rangle_D
   +c\langle u_t,M_Au_t\rangle_D.
 \label{eq:aesp-cd-psi-high-bank}
\end{equation}
Assume the high gap is at least $2q$ and that the exact clipped master form
$\Psi$ is nonpositive for every admissible state on this graph.  Then,
regardless of the realized partial/full correction word,
\begin{equation}
 \mathcal C_{b+L}^{\mathrm h}
 \leq(1+q)(1-q)^{2L}\mathcal C_b^{\mathrm h}.
 \label{eq:aesp-cd-psi-high-contraction}
\end{equation}
In particular, for $0<q\leq1/2$ and
$L=\lceil(\log 2)/q\rceil$, the contraction factor is at most $3/8$.
This applies directly to the complete-multipartite and certified
cycle-blow-up families in the companion master-inequality note.
\end{proposition}

\begin{proof}
Define the master energy
\begin{equation*}
 \mathcal V_t
 :=\norm{h_t}_D^2
 -(1+\beta_{\mathrm A})\langle h_t,M_Ah_{t-1}\rangle_D
 +\beta_{\mathrm A}\langle h_{t-1},M_Ah_{t-1}\rangle_D.
\end{equation*}
The relations
\begin{equation*}
 Q_AM_A=\kappa_{\mathrm A}(I-M_A),\qquad
 2c(1-q)=\kappa_{\mathrm A}(1+\beta_{\mathrm A}),\qquad
 c(1-q)^2=\kappa_{\mathrm A}\beta_{\mathrm A}
\end{equation*}
give the exact identity
\begin{equation}
 \mathcal C_t^{\mathrm h}
 =\kappa_{\mathrm A}\mathcal V_t
  +\alpha\langle h_t,M_Ah_t\rangle_D.
 \label{eq:aesp-cd-psi-high-identity}
\end{equation}
On a high eigenmode with resolvent eigenvalue $m$, the gap hypothesis gives
$m\leq1-q$.  Minimizing the scalar $\mathcal V(h,v)$ over the history $v$
gives $(1-m/m_0)h^2$, where $m_0=1-q^2$.  Therefore
\begin{equation*}
 \mathcal V_t
 \geq\frac{q}{1-q^2}\langle h_t,M_Ah_t\rangle_D.
\end{equation*}
Since $\alpha(1-q^2)/q=\kappa_{\mathrm A}q$,
\begin{equation*}
 \kappa_{\mathrm A}\mathcal V_t
 \leq\mathcal C_t^{\mathrm h}
 \leq\kappa_{\mathrm A}(1+q)\mathcal V_t.
\end{equation*}
The exact master identity and $\Psi\leq0$ give
$\mathcal V_{t+1}\leq(1-q)^2\mathcal V_t$ on every included transition.
Iterating $L$ transitions and using the two comparisons proves
\cref{eq:aesp-cd-psi-high-contraction}.  Finally,
$(1-q)^{2L}\leq e^{-2qL}\leq1/4$ and $1+q\leq3/2$.
\end{proof}

The scope is deliberately narrow: the face, optimum, and projection must be
fixed; shifted solves are exact; and $\Psi\leq0$ must be proved for the graph
class.  The proposition neither controls the constant low mode nor crosses
admissions, proper faces, or finite-inner perturbations.  Its significance is
that arbitrarily dense positive signed events cannot damage the certified
high bank; all remaining obstruction is forced into the low/controller
channel.

\begin{corollary}[Finite-inner perturbation of the certified high root]
\label{cor:aesp-cd-psi-high-finite}
Under the hypotheses of \cref{prop:aesp-cd-psi-high-window}, put
$H_t:=\sqrt{\mathcal V_t}$.  Suppose the finite shifted solve adds a high
error $\varepsilon_t$ but stays on the same face.  Then
\begin{equation}
 H_{t+1}^{\rm fin}
 \leq(1-q)H_t^{\rm fin}+\norm{\varepsilon_t}_D.
 \label{eq:aesp-cd-psi-high-finite-root}
\end{equation}
If $\bar\zeta_t=D^{-1/2}\zeta_t$ is the normalized end residual of the inner solve,
then
\begin{equation*}
 \norm{\varepsilon_t}_D
 \leq\frac{m_0}{\kappa_{\mathrm A}}\norm{\bar\zeta_t}_2.
\end{equation*}
Consequently, for $L\geq\lceil(\log 4)/q\rceil$, the condition
\begin{equation}
 \sum_{t=0}^{L-1}(1-q)^{L-1-t}
 \frac{m_0}{\kappa_{\mathrm A}}\norm{\bar\zeta_t}_2
 \leq\frac18H_0
 \label{eq:aesp-cd-psi-high-finite-budget}
\end{equation}
implies $H_L^{\rm fin}\leq3H_0/8$.  The uniform tolerance
$\norm{\bar\zeta_t}_2\leq\kappa_{\mathrm A}qH_0/(8m_0)$ is sufficient.
\end{corollary}

\begin{proof}
Apply the structural master certificate to the actual finite input but its
exact shifted output.  In the pair norm defined by $\mathcal V$, perturbing
the new component by $\varepsilon_t$ has norm exactly
$\norm{\varepsilon_t}_D$.  The triangle inequality proves
\cref{eq:aesp-cd-psi-high-finite-root}.  The resolvent norm gives the residual
bound.  Unroll, use $(1-q)^L\leq1/4$, and bound the geometric sum by $1/q$.
No separate Lipschitz stability of the clipping decision is needed, although
face stability remains an explicit hypothesis.
\end{proof}

The constant mode has an exact complementary dichotomy.  Let
$\mathsf v_G=\langle\one,\one\rangle_D$ and write
\begin{equation*}
 a_t:=\frac{\langle e_t,\one\rangle_D}{\mathsf v_G},\qquad
 \delta_t:=a_{t-1}-a_t\geq0,\qquad
 \omega_t:=a_t-(1-q)a_{t-1}.
\end{equation*}

\begin{proposition}[Low-mode overshoot or high trigger]
\label{prop:aesp-cd-low-overshoot-trigger}
On an exact monotone safeguarded trajectory on a fixed full positive face,
so $e_t\geq0$ and $d_t\geq0$, let
$\widetilde a_t:=a_t-\beta_{\mathrm A}\delta_t$ be the trial mean.  The
constant-mode Moreau bank is
\begin{equation*}
 \mathcal C_t^0
 =\kappa_{\mathrm A}\mathsf v_G
   (q^2a_t^2+\omega_t^2).
\end{equation*}
If $\widetilde a_t<0$, then for $q\leq1/2$ the actual corrected step obeys
\begin{equation}
 \mathcal C_{t+1}^0<(1-q)\mathcal C_t^0.
 \label{eq:aesp-cd-low-overshoot-contract}
\end{equation}
If $\widetilde a_t\geq0$, write
$\widetilde h_t=P(e_t-\beta_{\mathrm A}d_t)$ and let $\Delta_t$ be the common
cap.  Then the mean correction
$\bar r_t:=\langle r_t,\one\rangle_D/\mathsf v_G$ satisfies
\begin{equation}
 \alpha\bar r_t
 \leq\alpha\Delta_t
 =[-\alpha\widetilde a_t-min_i(Q_A\widetilde h_t)_i]_+
 \leq\norm{Q_A\widetilde h_t}_\infty.
 \label{eq:aesp-cd-low-high-trigger}
\end{equation}
Thus every low event not already giving one-step accelerated contraction is
caused by a high coordinate trigger.
\end{proposition}

\begin{proof}
Since $0\leq r_t\leq\beta_{\mathrm A}d_t$, the mean output is
$a_{t+1}=m_0(\widetilde a_t+\bar r_t)$.  On the overshoot branch,
$\delta_t>a_t/\beta_{\mathrm A}$, hence $\omega_t<-a_t$, while monotone safety gives
$0\leq a_{t+1}\leq m_0a_t$.  Therefore
$-(1-q)a_t\leq\omega_{t+1}\leq(1-q)qa_t$ and
\begin{equation*}
 \mathcal C_{t+1}^0
 \leq\kappa_{\mathrm A}\mathsf v_G
 [q^2m_0^2+(1-q)^2]a_t^2
 <(1-q)\kappa_{\mathrm A}\mathsf v_Ga_t^2
 <(1-q)\mathcal C_t^0.
\end{equation*}
On the other branch, decompose the trial error as
$\widetilde a_t\one+\widetilde h_t$.  The exact common-cap rule gives the
middle equality in \cref{eq:aesp-cd-low-high-trigger}, and
$\bar r_t\leq\Delta_t$ gives the first inequality.
\end{proof}

The high trigger is essential.  If one keeps only the scalar recurrence and
$0\leq\bar r_t\leq\beta_{\mathrm A}\delta_t$, the artificial word
$a_t=m_0^ta_0$, $\bar r_t=\beta_{\mathrm A}\delta_t$ satisfies every scalar
constraint but contracts $\mathcal C_t^0$ only by $m_0^2=1-O(q^2)$ per
stage.  It is not a reachable high-free controller word, because
\cref{eq:aesp-cd-low-high-trigger} would force its correction to vanish.

\begin{proposition}[Master-high plus forced-mean epoch]
\label{prop:aesp-cd-master-mean-epoch}
Under the master-certified exact fixed-face hypotheses, perform a same-point
restart at the epoch boundary, so $e_{-1}=e_0$.  Put
\begin{equation*}
 H_t:=\sqrt{\mathcal V_t},\qquad
 Z_t:=H_t+\sqrt{\mathsf v_G}\,a_t.
\end{equation*}
For $T=\lceil2/q\rceil$,
\begin{equation}
 Z_T
 \leq3e^{-2}Z_0
 +\sqrt{\mathsf v_G}\,m_0
  \sum_{t=0}^{T-1}(T-t)(1-q)^{T-1-t}\bar r_t.
 \label{eq:aesp-cd-master-mean-epoch}
\end{equation}
Hence a correction-free epoch contracts $Z$ by a factor below $0.407$, and
if the displayed weighted deficit is at most $Z_0/4$, then
$Z_T<3Z_0/4$.  More sharply, writing
$\rho_T=(1+qT)(1-q)^T$, the observable gate
\begin{equation}
 m_0\sum_{t=0}^{T-1}(T-t)(1-q)^{T-1-t}\bar r_t
 \leq(1/2-\rho_T)a_0
 \label{eq:aesp-cd-same-point-mean-gate}
\end{equation}
implies $Z_T\leq Z_0/2$.
\end{proposition}

\begin{proof}
The master theorem gives $H_{t+1}\leq(1-q)H_t$.  Because
$M_A\one=m_0\one$, the exact mean recurrence is
\begin{equation*}
 a_{t+1}=(1-q)(a_t+\omega_t)+g_t,\qquad
 \omega_{t+1}=(1-q)\omega_t+g_t,\qquad
 g_t=m_0\bar r_t.
\end{equation*}
The repeated-root Green formula and $a_{-1}=a_0$ give
\begin{equation*}
 a_T=(1+qT)(1-q)^Ta_0
 +m_0\sum_{t=0}^{T-1}(T-t)(1-q)^{T-1-t}\bar r_t.
\end{equation*}
Since $qT\geq2$, both $(1-q)^T$ and
$(1+qT)(1-q)^T$ are at most $3e^{-2}$.  Combine the two components.
For the sharper gate, apply $(1-q)^T\leq\rho_T<1/2$ to the high root and
the displayed budget to the mean component separately.
\end{proof}

A same-point restart on the same face increases the high root by at most
$\sqrt{1+q}$ while leaving $a_t$ unchanged.  Indeed, modewise
\begin{equation*}
 \frac{\mathcal V(h,h)}{\min_v\mathcal V(h,v)}
 =\frac{m_0(1-m)}{m_0-m}\leq1+q,
 \qquad m\leq1-q.
\end{equation*}
Thus a restarted epoch passing
\cref{eq:aesp-cd-same-point-mean-gate} has net factor at most
$\sqrt{1+q}/2<0.613$ relative to the pre-restart value.  This statement is
same-face only; on a proper or changing face the constant projection need not
commute with the resolvent.

The preceding scalar formula can be strengthened to a root norm that does not
require a restart at the beginning of the measured block.  Define
\begin{equation*}
 R_t:=\sqrt{q^2a_t^2+\omega_t^2},\qquad
 \mathcal Z_t:=H_t+\sqrt{\mathsf v_G}\,R_t,
\end{equation*}
and, for a block of $L$ transitions beginning at $b$, put
\begin{equation}
 \mathcal D_{b,L}
 :=\sum_{j=0}^{L-1}(1-q)^{L-1-j}
 \sqrt{1+q^2(L-j)^2}\,m_0\bar r_{b+j}.
 \label{eq:aesp-cd-observable-mean-deficit}
\end{equation}

\begin{corollary}[Observable accepted two-scale window]
\label{cor:aesp-cd-observable-two-scale-window}
Under the exact fixed-full-face master hypotheses, assume $0<q\leq1/2$ and let
$L=\lceil2/q\rceil$.  Then
\begin{equation}
 \mathcal Z_{b+L}
 \leq \rho_\star\mathcal Z_b
       +\sqrt{\mathsf v_G}\,\mathcal D_{b,L},
 \qquad
 \rho_\star:=e^{-2}(1+\sqrt2)<0.327.
 \label{eq:aesp-cd-observable-two-scale-window}
\end{equation}
Consequently the fully observable gate
\begin{equation}
 \mathcal D_{b,L}\leq\frac16R_b
 \label{eq:aesp-cd-observable-two-scale-gate}
\end{equation}
implies $\mathcal Z_{b+L}<\mathcal Z_b/2$.  The gate uses only the current
constant residual state and the realized correction means; it does not use
the unknown high component of the optimum.  Indeed, on the full face the
active KKT residual is $y_t=Q_Ae_t$, and
\begin{equation*}
 a_t=\frac{\langle y_t,\one\rangle_D}
 {\alpha\mathsf v_G},
\end{equation*}
so $a_t$, $\omega_t$, and $R_t$ are obtained from current and previous
residuals, while $\bar r_t$ is recorded by the controller.
\end{corollary}

\begin{proof}
In the coordinates $(qa_t,\omega_t)$, the homogeneous low recurrence over
$n$ transitions is
\begin{equation*}
 (1-q)^n
 \begin{pmatrix}1&nq\\0&1\end{pmatrix}.
\end{equation*}
Its Euclidean operator norm is
$\sigma(nq)$, where
\begin{equation*}
 \sigma(s):=\frac{\sqrt{s^2+4}+s}{2}.
\end{equation*}
An impulse $g_j=m_0\bar r_{b+j}$ contributes
$(1-q)^{L-1-j}(q(L-j)g_j,g_j)$ to the final low state.  The triangle
inequality therefore gives
\begin{equation*}
 R_{b+L}\leq(1-q)^L\sigma(Lq)R_b+\mathcal D_{b,L}.
\end{equation*}
The function $e^{-s}\sigma(s)$ is decreasing for $s>0$.  Since $Lq\geq2$,
$(1-q)^L\sigma(Lq)\leq\rho_\star$.  The master high root obeys the same
bound because $H_{b+L}\leq(1-q)^LH_b$.  Adding the two estimates proves
\cref{eq:aesp-cd-observable-two-scale-window}; the gate adds at most
$\mathcal Z_b/6$, and $\rho_\star+1/6<1/2$.
\end{proof}

An even less conservative retrospective gate uses only the two endpoint low
states.  For any $0<\gamma<1$ and any $L$ such that
$(1-q)^L\leq\gamma$, the observable condition
\begin{equation}
 R_{b+L}\leq\gamma R_b
 \label{eq:aesp-cd-two-scale-endpoint-gate}
\end{equation}
implies $\mathcal Z_{b+L}\leq\gamma\mathcal Z_b$, because the master theorem
already gives $H_{b+L}\leq(1-q)^LH_b$.  In particular,
$L=\lceil(\log 2)/q\rceil$ and an endpoint low-root reduction by one half
certify a total half-contraction.  Unlike
\cref{eq:aesp-cd-observable-two-scale-gate}, this endpoint test cannot reject
early, but it avoids the triangle-inequality loss in the forcing convolution.

There is a direct finite-inner version.  Let $\varepsilon_t$ be the primal
error added by the finite shifted solve and put
$s_t=\langle\varepsilon_t,\one\rangle_D/\mathsf v_G$.  Define
\begin{equation}
 \mathcal E_{b,L}
 :=\sum_{j=0}^{L-1}(1-q)^{L-1-j}
 \left[1+\sqrt{1+q^2(L-j)^2}\right]
 \norm{\varepsilon_{b+j}}_D.
 \label{eq:aesp-cd-two-scale-finite-budget}
\end{equation}

\begin{corollary}[Finite-inner accepted two-scale window]
\label{cor:aesp-cd-finite-two-scale-window}
Assume $0<q\leq1/2$ and suppose every finite solve stays on the same
master-certified full face.  Let $R_b$ and $\mathcal Z_b$ be formed from the
actual finite starting states, and let $\bar r_t$ in
\cref{eq:aesp-cd-observable-mean-deficit} be the actual finite controller's
recorded correction mean.  Then
\begin{equation*}
 \mathcal Z_{b+L}^{\rm fin}
 \leq\rho_\star\mathcal Z_b^{\rm fin}
 +\sqrt{\mathsf v_G}\,\mathcal D_{b,L}
 +\mathcal E_{b,L}.
\end{equation*}
In particular, the certified conditions
\begin{equation}
 \mathcal D_{b,L}\leq\frac1{12}R_b,
 \qquad
 \mathcal E_{b,L}\leq\frac1{12}\sqrt{\mathsf v_G}\,R_b
 \label{eq:aesp-cd-finite-two-scale-gate}
\end{equation}
imply $\mathcal Z_{b+L}^{\rm fin}<\mathcal Z_b^{\rm fin}/2$.
If $\norm{\varepsilon_t}_D\leq(m_0/\kappa_{\mathrm A})
\norm{\bar\zeta_t}_2$, the uniform normalized residual tolerance
\begin{equation*}
 \norm{\bar\zeta_t}_2
 \leq\frac{\kappa_{\mathrm A}q\sqrt{\mathsf v_G}R_b}{48m_0}
\end{equation*}
is sufficient for the second gate.
\end{corollary}

\begin{proof}
The finite mean impulse contributes the same low Green vector with $g_j$
replaced by $s_j$, while
$\sqrt{\mathsf v_G}|s_j|\leq\norm{\varepsilon_j}_D$.
The high root pays an additional
$\sum_j(1-q)^{L-1-j}\norm{\varepsilon_j}_D$.
This is exactly \cref{eq:aesp-cd-two-scale-finite-budget}.  The two gates add
at most $\mathcal Z_b/6$.  Finally, for $q\leq1/2$ and
$L=\lceil2/q\rceil$, the bracket in
\cref{eq:aesp-cd-two-scale-finite-budget} is less than four and its geometric
sum is at most $1/q$.
\end{proof}

There is also a positive alignment route that eliminates all later correction
events, at the price of an observable pure-prox warmup.

\begin{proposition}[Observable pure-prox alignment gives a permanent exact tail]
\label{prop:aesp-cd-observable-alignment-tail}
Assume $0<q<1$, an exact fixed full positive face, degree weights $d_i\geq1$,
$Q_A\preceq_D I$, and resolvent spectrum at most $1-q$ on the
$D$-orthogonal complement of the constant vector.  Let an exact pure-prox prefix satisfy
$e_{j+1}=M_Ae_j$, and define its observable residual decomposition
\begin{equation*}
 y_j:=Q_Ae_j,\qquad
 a_j:=\frac{\langle y_j,\one\rangle_D}{\alpha\mathsf v_G},\qquad
 w_j:=y_j-\alpha a_j\one.
\end{equation*}
If $a_0>0$, then
\begin{equation}
 a_J=m_0^Ja_0,qquad
 \norm{w_J}_D\leq(1-q)^J\norm{w_0}_D,qquad
 \frac{\norm{w_J}_D}{a_J}
 \leq(1+q)^{-J}\frac{\norm{w_0}_D}{a_0}.
 \label{eq:aesp-cd-observable-alignment-rate}
\end{equation}
At stage $J$, make a same-point restart and reindex the following momentum
tail from $t=0$.  If
\begin{equation}
 \norm{w_J}_D
 \leq\frac{\alpha a_J}{\sqrt2}
       \sqrt{\frac q{1+q}},
 \label{eq:aesp-cd-observable-alignment-gate}
\end{equation}
then every subsequent exact global-$\beta_{\mathrm A}$ trial residual is
componentwise nonnegative.  Hence the entire momentum tail is
correction-free.  A sufficient stopping index is
\begin{equation}
 J\geq
 \left\lceil
 \frac{\log_+\!\left(
  \sqrt2\norm{w_0}_D/(\alpha a_0\sqrt{q/(1+q)})
 \right)}{\log(1+q)}
 \right\rceil.
 \label{eq:aesp-cd-observable-alignment-work}
\end{equation}
Thus the alignment phase is $O(q^{-1})$ up to logarithmic factors and uses
only the active residual, not unknown eigendata or the optimum.
\end{proposition}

\begin{proof}
The pure-prox identities follow from $M_A\one=m_0\one$, commutation of
$M_A$ and $Q_A$, and the high spectral bound.  For a high resolvent
eigenvalue $m$, define
\begin{equation*}
 \mathcal V_m(u,v)
 :=u^2-(1+\beta_{\mathrm A})muv+\beta_{\mathrm A}mv^2,
 \qquad s:=(1+\beta_{\mathrm A})u-\beta_{\mathrm A}v.
\end{equation*}
Direct dual-norm calculation gives
\begin{equation*}
 s^2\leq
 \frac{\beta_{\mathrm A}}{m(1-m/m_0)}\mathcal V_m(u,v).
\end{equation*}
Here $1-m/m_0\geq q/(1+q)$ and, because the $Q_A$ spectrum is at most one,
$m\geq\kappa_{\mathrm A}/(\kappa_{\mathrm A}+1)$.  Consequently
\begin{equation*}
 \norm{Q_A\widetilde h_t}_D
 \leq\frac{\sqrt2}{(1+q)\sqrt{q/(1+q)}}H_t^w,
 \qquad
 H_t^w:=\sqrt{\mathcal V(w_t,w_{t-1})}.
\end{equation*}
On a correction-free prefix,
$\mathcal V_{t+1}^w=\beta_{\mathrm A}m\mathcal V_t^w$ modewise, so after
the same-point restart $H_t^w\leq(1-q)^t\norm{w_J}_D$.
The constant trial residual is exactly
\begin{equation*}
 \alpha\widetilde a_t
 =\alpha\frac{1+q(t+1)}{1+q}(1-q)^ta_J
 \geq\frac{\alpha}{1+q}(1-q)^ta_J.
\end{equation*}
Use \cref{eq:aesp-cd-observable-alignment-gate} and
$\norm{z}_\infty\leq\norm{z}_D$ to get
$\norm{Q_A\widetilde h_t}_\infty\leq\alpha\widetilde a_t$.
Thus $Q_A\widetilde e_t\geq0$ coordinatewise.  Induction proves that every
cap is zero and extends the correction-free prefix forever.
\end{proof}

\begin{corollary}[Finite-horizon stability of the aligned tail]
\label{cor:aesp-cd-finite-alignment-tail}
Under the hypotheses of
\cref{prop:aesp-cd-observable-alignment-tail}, strengthen the gate to
\begin{equation*}
 \norm{w_J}_D
 \leq\frac{\alpha a_J}{2\sqrt2}\sqrt{\frac q{1+q}}.
\end{equation*}
Consider $K$ finite momentum solves which remain in the same relative
interior.  Suppose the active residual deviation from the exact aligned tail
satisfies
\begin{equation*}
 \delta y_s=M_A[(1+\beta_{\mathrm A})\delta y_{s-1}
                 -\beta_{\mathrm A}\delta y_{s-2}]+\zeta_s,
 \qquad \delta y_{-1}=\delta y_0=0,
\end{equation*}
with
\begin{equation}
 \norm{\zeta_s}_D\leq\xi_y
 \leq\frac{\alpha a_Jq^2(1-q)^K}{12(1+q)}.
 \label{eq:aesp-cd-finite-alignment-budget}
\end{equation}
for $s=1,\ldots,K$.  Thus the first trial, indexed by $t=0$, has no
preceding forcing.
Then the first $K$ finite trial residuals remain componentwise nonnegative,
with at least one half of the exact alignment margin.  If the normalized inner
end residual is $\bar b_s$, then
$\zeta_s=(I-M_A)\bar b_s$ and
$\norm{\zeta_s}_D\leq\norm{\bar b_s}_D$, so the same bound may be imposed
directly on the inner certificate.
\end{corollary}

\begin{proof}
The strict exact gate leaves coordinate margin at least
$\alpha a_J(1-q)^t/[2(1+q)]$ at trial $t$.  The residual Green kernel obeys
$\norm{g_r(M_A)}\leq(r+1)(1-q)^r$.  Indeed, on a mode $m$ it satisfies
\begin{equation*}
 g_0=1,\qquad g_1=m(1+\beta_{\mathrm A}),\qquad
 g_r=m(1+\beta_{\mathrm A})g_{r-1}-m\beta_{\mathrm A}g_{r-2}.
\end{equation*}
Writing $x=\sqrt{m/m_0}\in[0,1]$ and using
$m_0(1+\beta_{\mathrm A})^2=4\beta_{\mathrm A}$ and
$\beta_{\mathrm A}m_0=(1-q)^2$ gives
$g_r(m)=(1-q)^r x^r U_r(x)$.  Therefore
$|g_r(m)|\leq(r+1)(1-q)^r$, and
$\sum_{r\geq0}(r+1)(1-q)^r=q^{-2}$.  Hence
\begin{equation*}
 \norm{\delta y_t}_D\leq\frac{\xi_y}{q^2},
 \qquad
 \norm{(1+\beta_{\mathrm A})\delta y_t
       -\beta_{\mathrm A}\delta y_{t-1}}_D
 \leq\frac{3\xi_y}{q^2}.
\end{equation*}
Use \cref{eq:aesp-cd-finite-alignment-budget},
$\norm{z}_\infty\leq\norm{z}_D$, and $t\leq K$.
For an interior finite solve, the residual forcing identity follows by
multiplying its end-residual equation by $Q_A(Q_A+\kappa_{\mathrm A}I)^{-1}
=I-M_A$.
\end{proof}

The alignment theorem is exact and full-face, while its finite corollary is
finite-horizon and relative-interior.  A fixed absolute tolerance cannot
preserve a margin forever because the residual itself tends to zero.  On a
proper face, $M_A\one=m_0\one$ generally fails, so the alignment gate does not
cross admissions without a separate replay theorem.
\Cref{lem:aesp-cd-point-source-ground-state-normalization} gives every
connected proper face an exact positive ground state after changing the
diagonal mass matrix.  That removes eigenpair estimation for a separately
formulated no-correction Perron tail, but it does not change the common-cap
direction \(\one\) in the present master and hence is not a proof of this
full-face gate on \(A\).
If the controller itself is changed to the \(W_A\)-shift and \(h_A\)-cap,
\cref{prop:aesp-cd-proper-face-ground-conjugacy} gives an exact diagonal
conjugacy back to the present constant-mode/common-cap algebra.  The
alignment and master interfaces then transfer, but the transformed high gap
and \(\Psi\leq0\) hypotheses remain additional obligations.

Combining the alignment gate with the correction-free Moreau contraction in
\cref{prop:aesp-cd-moreau-epoch-restart} gives an explicit exact fixed-face
outer bound.  To reduce the post-restart bank from $\mathcal C_J$ to
$\tau>0$, it suffices to take
\begin{equation*}
 K\geq\left\lceil\frac1q\log_+\frac{\mathcal C_J}{\tau}\right\rceil
\end{equation*}
momentum stages after the $J$ pure-prox stages in
\cref{eq:aesp-cd-observable-alignment-work}.  Thus the total number of exact
face-resolvent applications is
\begin{equation*}
 O\!\left(\frac1q\left[
  \log_+\frac{\norm{w_0}_D}{\alpha a_0\sqrt q}
  +\log_+\frac{\mathcal C_J}{\tau}
 \right]\right).
\end{equation*}
If one face application costs $W_A$, the corresponding charged work is this
quantity times $W_A$; factorization, finite precision, state scans, and output
remain explicit parts of $W_A$ rather than free operations.

\begin{corollary}[Exact fixed-face gate-or-align hybrid]
\label{cor:aesp-cd-fixed-face-gate-align}
Assume the master-certified full-face and alignment hypotheses.  At each
checkpoint run $L=\lceil(\log2)/q\rceil$ actual controller stages.  Accept the
window if the endpoint test
$R_{b+L}\leq R_b/2$ passes; otherwise discard momentum history once and switch
permanently to the pure-prox alignment phase in
\cref{prop:aesp-cd-observable-alignment-tail}.  Every accepted window halves
$\mathcal Z$.  If
\begin{equation*}
 S:=\left\lceil\log_2\frac{\mathcal Z_0}{\mathcal Z_{\rm tar}}\right\rceil,
\end{equation*}
then the protocol either reaches $\mathcal Z_{\rm tar}$ within $SL$ stages,
or reaches the correction-free aligned tail after at most
\begin{equation}
 (S+1)L+J
 \label{eq:aesp-cd-fixed-face-gate-align-count}
\end{equation}
face-resolvent applications.  Adding
$\lceil q^{-1}\log_+(\mathcal C_J/\tau)\rceil$ stages reaches Moreau-bank target
$\tau$.  Hence the exact fixed-face outer count is $O(q^{-1})$ up to the
displayed logarithms, without any assumption on how many raw correction
events occur before the first failed window.
\end{corollary}

The hybrid is a genuine exact fixed-face result, not yet an end-to-end local
solver theorem.  Its finite implementation uses
\cref{cor:aesp-cd-finite-two-scale-window,cor:aesp-cd-finite-alignment-tail}
and a face-stability guard.  With fixed exposed volume $V_A$, a local inner
solve whose absolute polish costs only logarithmically in its tolerance would
give soft-$O(V_A/q)$ work.  Face changes, an unavailable relative-interior
certificate, or an inner method whose cost is polynomial in the requested
absolute tolerance remain explicit failure conditions.

\begin{proposition}[Fixed-rank low-mode deflation does not restore the master gap]
\label{prop:aesp-cd-fixed-rank-deflation-stop}
Fix an integer $k\geq0$ and $0<q\leq1/2$.  Put
\begin{equation*}
 \lambda:=\frac{q^2}{1-q^2},
 \qquad s:=k+2,
 \qquad
 Q_s:=\lambda I+\frac{\lambda}{s}L_{K_s},
 \qquad \kappa:=1.
\end{equation*}
Then $Q_s$ is a positive definite Stieltjes matrix with a ground eigenvalue
$\lambda$ and eigenvalue $2\lambda$ of multiplicity $s-1$.  The ground
resolvent eigenvalue is $m_0=1-q^2$.  After deflating any $k$ lower
directions, at least one $2\lambda$ mode remains, with
\begin{equation}
 m=\frac{1-q^2}{1+q^2}>1-q,
 \qquad
 m-(1-q)=\frac{q(1-q)^2}{1+q^2}>0,
 \label{eq:aesp-cd-fixed-rank-deflation-stop}
\end{equation}
Thus the high-gap hypothesis $m\leq1-q$ still fails, and the one-step master
certificate cannot be invoked on the surviving spectral cluster.  No
graph-independent fixed deflation rank can repair that hypothesis.

The failure is substantive for the mixed-clipping master, not merely a loose
spectral estimate.  At $q=1/2$, take
$\alpha=1/5$, $\kappa=3/5$, and
$Q=\alpha I+(\alpha/2)L_{K_2}$.  Its ground and high resolvent values are
$3/4$ and $3/5>1-q$.  For
\begin{equation*}
 y=(1,-1),\qquad h=(4,-4),
\end{equation*}
the exact master variables satisfy
$z=Py=(1,-1)$,
$w=P(y_-)=(-1/2,1/2)$, and
$b=z-[q/(1+q)]h=(-1/3,1/3)$.  Direct substitution gives
\begin{equation}
 \Psi(y,h)=\frac2{25}>0.
 \label{eq:aesp-cd-two-node-master-gap-stop}
\end{equation}
Thus the same clipped-master inequality genuinely fails already on the
two-node member of the family.
\end{proposition}

The proposition is a spectral-cluster STOP for fixed-dimensional deflation,
not a lower bound against rank-adaptive clustering, recursive graph
decomposition, or a different windowed bank.  It complements
\cref{prop:aesp-cd-positive-polynomial-stop}: acceleration may use signed
scratch space, but a universal proof must also account for an unbounded
number of nearly slow directions when the face geometry supplies them.

The remaining universal question is now precise: pay the observable weighted
mean deficit in \cref{eq:aesp-cd-master-mean-epoch} using the high trigger in
\cref{eq:aesp-cd-low-high-trigger} without a graph-size loss.  The complete-
graph family in \cref{eq:aesp-cd-kn-high-to-low-stop} rules out payment from a
plain global $L_2$/high-Moreau reserve, even with an extra factor $1/q$; a
successful interface must be coordinate-local, volume-sensitive, or retain a
low endpoint spend.
