\section{Scope and claim status}
\label{sec:direct-scope}

The repository-wide residual convention remains open. In this note only, use
the shared unregularized quadratic $f$ and define
\begin{equation}
 r(x):=b-Qx,
 \label{eq:direct-objective-residual}
\end{equation}
and use the document-scoped terminal certificate
\begin{equation}
 \norm{D^{-1/2}r(x)}_\infty\leq\tau,
 \qquad \tau:=\alpha\epsppr.
 \label{eq:direct-terminal-gate}
\end{equation}
This is the convention used by the measured two-rung note. It is not an
adopted repository stopping rule, and $\epsppr$ is not identified with the
RPPR regularizer $\rho$ or an objective-gap tolerance.

To avoid conflicting with the reserved right-hand side $b$, write $B>1$ for
the two-rung band factor. The measured policy uses $B=2.5$ and the schedule
\begin{equation}
 \bigl[(B\tau,\omega_\star),(\tau,1)\bigr],
 \qquad
 \omega_\star
 =\frac{2(1+\alpha)}{(1+\sqrt\alpha)^2}.
 \label{eq:direct-two-rung-schedule}
\end{equation}
One executed coordinate operation at $u$ is charged
\begin{equation}
 c_u:=1+d_u.
 \label{eq:direct-charge}
\end{equation}

\begin{center}
\footnotesize
\begin{tabularx}{\textwidth}{@{}p{0.19\textwidth}X@{}}
\toprule
Status & Statement \\
\midrule
\statusproved & The empirical score
$|r_u|\sqrt{d_u}/(1+d_u)$ is a factor-two approximation to exact quadratic
decrease per charge, on every graph and at every signed residual. The same
factor holds for a top-$k$ snapshot. \\
\statusproved & A fully refreshed score maximizer has a fixed-region linear
contraction in charged work. Selecting only from the live threshold frontier
changes the constant but not the conclusion. \\
\statusproved & Every literal two-rung run terminates, with explicit but
nonaccelerated fallback bounds for both phases. \\
\statusproved & On a single edge the optimal-SOR residual is available in
closed form. Every $B<3.33019\ldots$ has eventual one-push exact settlement;
$B=2.5$ needs at most one terminal push after at least twenty spreading
pushes. \\
\statusproved & On a sufficiently long endpoint-seeded path, fully refreshed
absolute-residual ranking executes an exact parity-wave event set and never
selects a negative waiting packet. At depth $L$ it has
$\lfloor(L+1)^2/4\rfloor$ pushes, an explicit signed plateau, and a
no-repeat terminal exact sweep for every $B<3.33019\ldots$. \\
\statusproved & A live positive-sign guard extends the endpoint-path count
and terminal theorem to the literal snapshot-batch structure for arbitrary
batch size, without extra charged coordinate work. \\
\statusproved & For $\alpha>1/49$, including the measured values $0.025$ and
$0.04$, the original unguarded top-$1/32$ batches preserve the complete path
event and terminal counts.  The proof is independent of equal-rank tie
breaking. \\
\statusrefuted & Without the sign guard, arbitrary batch sizes do not
preserve the path event schedule: a full-frontier policy charges a negative
endpoint in its third batch.  Moreover, top-$1/32$ parent closure is false
on the class of all legally reachable corner interfaces, so any all-$\alpha$
literal theorem needs a dynamic history invariant. \\
\statusproved & On a symmetric $q$-arm spider, refreshed ranking has an exact
center-echo prefix of $\Theta(1/\sqrt\alpha)$ consecutive center pushes and
$\Theta(q/\sqrt\alpha)$ charged work. For small enough $\alpha$, its second
center push is already a negative-residual revisit. \\
\statusproved & Grouping each symmetric spider depth into a radial block
reduces both SOR rungs exactly to the endpoint-path recurrence and proves
$\widetilde O(\mathcal V_{\rm exp}/\sqrt\alpha)$ work for every
$B<B_{\rm edge}$. \\
\statusproved & Every level-regular rooted tree has an exact shell-energy
quotient: optimal-SOR block pushes form a symmetric tridiagonal chain with
edge impedance $2\sqrt{\phi_i/(d_i d_{i+1})}$. On a homogeneous $g$-ary
bulk, the path two-level parity wave is critically matched only for $g=1$;
its exact branch-mismatch debt is
$\lambda^2((g-1)/(g+1))^2$. The root has a matching
$\Theta_g(1/\sqrt\alpha)$ echo, and geometric shell growth converts a
triangular shell-visit bound into $O_g(K\mathcal V_L)$ charged work. \\
\statusproved & For every homogeneous $g>1$, all finite explored balls have
Dirichlet gap at least
$\delta_g=(\sqrt g-1)^2/[2(g+1)]$.  Retrospective contraction on the final
explored ball proves both rungs terminate and gives
$\Work=\widetilde O_g(\mathcal V_{\rm exp}/\sqrt\alpha)$ for every fixed
$B>1$, without a per-shell visit hypothesis. \\
\statusproved & A positive Jacobi supersolution with ratio $\rho<1$ gives the
same theorem for variable level-regular profiles.  In particular, arbitrary
offspring counts $\phi_i\geq g>1$ retain the homogeneous gap $\delta_g$.
An explicit unary-corridor Rayleigh vector proves that unbounded unary runs
admit no uniform-gap extension. \\
\statusproved & Without any level symmetry, every rooted tree with at least
$g>1$ children per vertex has the same $\delta_g$ gap on all ancestor-closed
prefixes.  The fully refreshed individual-coordinate two-rung method has
$\widetilde O_g(\mathcal V_{\rm exp}/\sqrt\alpha)$ work on this class. \\
\statusrefuted & The unrestricted literal graph-uniform theorem is false.
On the center-seeded three-vertex path, with $B=2.5$ and
$\epsppr=10^{-2}$, singleton top-$1/32$ batches have
$\Work_{\rm spread}\geq\alpha^{-3/2}/1408$ for
$\alpha\leq10^{-4}$ although $\mathcal V_{\rm exp}=4$. \\
\statusproved & Certified pendant-forest Schur deflation has linear work,
unit forest preconditioned eigenvalues, a canonical graph-$2$-core reduction,
and an accelerated finite-spider radial bound. \\
\statusproved & On a cycle core, certified pendant forests all attached at one
fixed core vertex admit an exact Green crossing reporter.  After one cyclic
solve and one sort per gate, every absorption costs $O(1)$ plus its newly
reported crossings and requires no global boundary rekey. \\
\statusrefuted & A proof based only on self-reflection cannot explain a fixed
$B=2.5$ as $\alpha\downarrow0$, and a proof using only monotone objective
decrease loses the factor
$\omega_\star(2-\omega_\star)=\Theta(\sqrt\alpha)$. \\
\statusopen & The actual top-$1/32$ endpoint-path trajectory for
$\alpha\leq1/49$ remains unresolved.  A broader positive theory must now
state an explicit no-reflecting-leaf, persistent-expansion, or corridor
decomposition condition; no unrestricted graph-uniform accelerated bound is
possible for the literal method. \\
\bottomrule
\end{tabularx}
\end{center}
