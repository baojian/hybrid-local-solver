# Research round 001: three hard interfaces

Date: 2026-08-21
Controller snapshot: branch `agent/prove-aesp-cd-inner-oracle`, dirty concurrent
worktree; no staging, commit, push, rename, or folder move is authorized.
Round state: redistributed

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `response_preconditioned_hybrid` | Prove or rigorously narrow an output-sensitive within-epoch finite-band reporter on one high-cut-rank nonequitable cyclic family | `response_preconditioned_hybrid/` | Proved an exact-representation obstruction on a prescribed notched-double-sun structural trace; finite-band and RPPR-trajectory claims remain open. |
| `aesp_cd_l1_rppr` | Amortize cumulative correction log-inflation using the sharp-collapse identity, or prove why the current collapse ledger is insufficient | `aesp_cd_l1_rppr/` | Refuted raw correction-count amortization on an exact singleton-support family; positive normalized log-inflation remains open. |
| `local_solver_oracle_hierarchy` | Formalize the smallest defensible recurrence/response/materialization separation with every access, memory, preprocessing, and output clause stated | `local_solver_oracle_hierarchy/` | Defined an eleven-coordinate ledger, proved a narrow free-inverse collapse, and proved an exact-cell path materialization analogue; the recurrence/response separation remains open. |

## Direction handoffs

- `response_preconditioned_hybrid`: `prop:notched-double-sun-loss-rank` proves
  density for every `alpha in (0, 1)` and, for each fixed `n`, independence in
  an `n`-dependent interval near one. With `K_gamma = Theta_gamma(n)`,
  `cor:notched-sun-exact-tag-obstruction` forces dimension `Omega(n)` for an
  exact fixed linear decoder. The handoff explicitly disclaims an RPPR
  admission trace, a uniform fixed-`alpha` family, a finite-band lower bound,
  and nonlinear or scale-truncated representations.
- `aesp_cd_l1_rppr`: `prop:aesp-cd-singleton-corrections` gives an exact
  single-edge family with one support discovery and
  `N_T = floor((T - 1) / 2)`. Every correction has negative defect and
  `I_T = 0`, so the result rejects raw-count charging while preserving the
  positive-log-inflation route.
- `local_solver_oracle_hierarchy`: `lem:free-preconditioner-collapse` applies
  only after complete exposure and only when inverse construction, encoding,
  and application are all free. `thm:online-path-resource-separation` gives
  an `Omega(tau_stop^2)` exact-cell materialization coordinate versus one
  linear append-only response. It is explicitly not word/bit,
  finite-precision, adjacency-adaptivity, recurrence, or
  `nu_fin / sqrt(alpha)` complexity.

## Controller adjudication

- The notched-double-sun algebra, asymptotic regime, determinant argument, and
  decoder implication received an independent read-only review. The
  controller required and applied the fixed-`n`, `alpha_n`, and
  `K = K_gamma` qualifications before promotion.
- The AESP-CD recurrence, KKT support condition, two-stage ratios, full-reset
  inequality `4q^2 + 5q <= 1`, count indexing, and negative-defect conclusion
  were checked directly by the controller. The promoted conclusion is only
  that raw correction count is the wrong amortization variable.
- The first oracle-model draft received an independent review that found seven
  scope/model defects. The direction owner fixed the undefined volume symbol,
  stopping-time quantifier, universal-Theta overclaim, precision model,
  incomplete ledger, interaction/adaptivity conflation, and free-inverse
  headline. The controller checked the revised proof and added an explicit
  distinct coordinate-cell convention.
- Concurrent coded-bank and shifted-debt additions appeared in the response
  direction outside this round's assigned notched-double-sun proof. They were
  preserved but not promoted by this adjudication; they require a separate
  proof audit.

## Redistribution messages

- To AESP, volume-gated acceleration, and synthesis agents: do not charge all
  nonzero safe corrections to support growth. Pack only rounds with
  `gamma_t > 1`; the singleton family has arbitrarily many accuracy-driven
  corrections and zero normalized inflation.
- To response, delayed-debt, and finite-band agents: stop the universal exact
  fixed-linear-tag route. The surviving target is a scale-aware one-sided
  additive group oracle or an implicit coded/transfer/separator state with
  every construction, old-row read, validation, and output charged.
- To lower-bound agents: use the eleven-coordinate execution ledger. The path
  theorem is a representation witness, not the desired spectral recurrence
  separation, and an arbitrary graph-dependent inverse cannot be free.

## Metadata and shared-state changes

- Results ledger: the three reviewed results and their exact qualifiers were
  added.
- Broadcast: the round conclusions, stopped routes, and next targets were
  redistributed to all directions.
- Related work: targeted primary-source search rechecked the comparison
  frontier on 2026-08-21; Wei--Yang's 2026 growing-active-set SDD method
  remains the newest directly relevant comparator found.
- Taxonomy/dependencies: the response target now names aggregate coded-bank or
  shifted-debt propagation; the oracle entry names the eleven-resource ledger
  and records its response-note dependency.
- Manifest/index: no direction was created, moved, or deleted. The oracle
  manifest summary now records the exact-cell materialization result and open
  recurrence/response target.

## Validation

- Direction builds: forced controller rebuilds passed for all three affected
  notes. The response note retained only its existing font/float warnings.
- `make note-audit`: passed after final metadata edits, with 18 notes across 5
  tracks.
- Tests: the complete repository suite passed, 182 tests; pytest reported only
  temporary-directory cleanup warnings.
- Lint and diff checks: `make lint`, `git diff --check`, and the conflict-marker
  scan passed.
- Concurrent-work caveats: the working tree already contains theorem, source,
  test, documentation, and generated-PDF changes from other work; this round
  uses strict one-directory worker ownership.

## Next round queue

1. Positive-only AESP-CD log-inflation packing, using the benign singleton and
   positive P3 witnesses as opposite test cases.
2. Aggregate coded-bank or shifted-debt implementation on a high-cut-rank
   cyclic core, with a separate audit of the concurrent coded-bank theorem.
3. Instantiate one fully specified `RowRec` budget and try to falsify a
   same-task recurrence/response candidate before attempting a lower bound.
