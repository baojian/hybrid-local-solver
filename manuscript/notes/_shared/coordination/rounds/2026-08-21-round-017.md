# Research round 017: amplitude mass, incremental lower diagnostics, and face-switch banks

Date: 2026-08-21
Controller snapshot: updated `main` at `14f3e60`; reviewed Rounds 014--016 are
retained in the intentional dirty worktree, and Round 016 was redistributed and
validated before this dispatch.
Round state: redistributed

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `response_preconditioned_hybrid` | Keep the frozen template, pairing, fixed `n`, exact-real model, and one common label schedule, but replace unit logical events by declared positive amplitudes. Charge total/per-label mass, multiplicity, capacity, fragments, first threshold crossings, and certificates. Prove an output-sensitive no-response reporter or return the first exact amplitude obstruction. | `response_preconditioned_hybrid/` | GO: `thm:notched-sun-amplitude-delta-reporter` proves that `NotchedSunAmplitudeDelta` handles every verified interleaving of one finite declared positive occurrence schedule. A mass-capacity margin identifies each label's first exact crossing, including delayed crossings after earlier subcapacity occurrences, with zero response, recurrence, and numerical-materialization work. |
| `hybrid_aesp_locsor` | Determine whether the all-layer anchored lower diagnostic can avoid repeated full old-prefix sweeps via a named implicit/incremental state. Prove a fully charged update/query construction, or a representation-specific repeated-sweep lower bound with an implicit comparator and exact interface. | `hybrid_aesp_locsor/` | GO for the diagnostic only: `lem:branch-caterpillar-implicit-lower-heap` maintains the exact anchored lower point implicitly under local numerical writes, and `thm:branch-caterpillar-implicit-lower-handoff` removes every query-time old-face diagnostic sweep. One raw-product initialization and one transported-anchor materialization per face, fresh AESP state, candidate-row exposure, and the native post response remain charged. |
| `volume_gated_acceleration` | Choose one explicit face-dependent two-phase bank with coefficients satisfying the `U_3` and `U_4` held-pair constraints, then include the literal stage-8 face reset/coefficient switch and test whether its jump is exactly chargeable by the admission shock. | `volume_gated_acceleration/` | Mixed finite result: `prop:path-tight-pair-schur-bank` gives a squared-bank GO for the isolated frozen-candidate stage-8 reset and a raw-linear-bank STOP. The tight coefficients flatten the two named held pairs; the raw switch exceeds the exact Schur drop, while the squared switch is strictly paid by it. This is not a global stagewise potential. |

## Direction handoffs

### `response_preconditioned_hybrid`

- Stable source labels: `thm:notched-sun-amplitude-delta-reporter`,
  `eq:notched-sun-amplitude-schedule`,
  `eq:notched-sun-amplitude-total-mass`,
  `eq:notched-sun-amplitude-capacity`,
  `eq:notched-sun-amplitude-crossing-index`,
  `eq:notched-sun-amplitude-capacity-gap`,
  `eq:notched-sun-amplitude-future-capacities`,
  `eq:notched-sun-amplitude-margin`,
  `eq:notched-sun-amplitude-prefix-state`,
  `eq:notched-sun-amplitude-certificate`,
  `eq:notched-sun-amplitude-reporter-state`,
  `eq:notched-sun-amplitude-fragments`, and
  `eq:notched-sun-amplitude-eleven-vector`.
- For each label `i`, declare positive exact occurrence amplitudes
  `a_(i,1),...,a_(i,mu_i)` and prefix mass
  `A_(i,k)=sum_(s<=k) a_(i,s)`. The same occurrence schedule is used by every
  logical column and every physical stream. Put `L=sum_i mu_i`,
  `A_i=A_(i,mu_i)`, and `A=sum_i A_i`; event count, logical mass, and exact-cell
  fragment count remain distinct ledgers.
- With gate `g=vartheta^(5/2)`, define
  `lambda_i=vartheta^2/[d_(u_i)(1+c_eta)]`,
  `epsilon=9vartheta^3/(1-3vartheta)`, and capacity
  `kappa_i=g/lambda_i=d_(u_i)(1+c_eta)sqrt(vartheta)`. The first certified
  occurrence is `k_i*=min{k:A_(i,k)>kappa_i}`; it need not be one.
- The exact pre-crossing gap and worst interleaving horizons are
  `gamma_i=kappa_i-A_(i,k_i*-1)>0`,
  `H_i=L-mu_i+k_i*-1`, and
  `F_i=A-A_i+A_(i,k_i*-1)`. The schedule-dependent test
  `m_A=min_i{lambda_i gamma_i-epsilon F_i}>0` makes every unreported label,
  including a previously seen but still subcapacity one, at most `g-m_A`.
  Occurrence `k_i*` then exceeds `g`, and is simultaneously its first
  certified and first exact strict crossing for every accepted interleaving.
  Every fixed finite positive schedule passes at all sufficiently small
  positive `vartheta`; failure of this displayed test rejects only the scalar
  certificate and is not a response lower bound.
- The reporter preprocesses all `L` amplitude cells and prefix sums, atomically
  checks the common label, next exact amplitude, per-label cap, and total cap,
  and retains the common schedules, crossing indices, counts, and total prefix
  mass. It performs no future-label scan and stores no response, cumulative
  response, exterior slack, or numerical coordinate.
- The complete vector is
  `(Theta(n),2,L,Theta(n+J+p+L),Theta(C_frag+rL+L+n),0,0,Theta(n+J+p+L),O(1),0,Theta(rL))`.
  The emission term contains `rL` certificate records and exactly `rn` delta
  records. For nonnegative columns, `p=r`, `C_frag=rL`, and the absolute
  supplied coefficient mass is `rA`; for separately checked signed
  `2`-minus-`1` streams, `p=2r`, `C_frag=2rL`, and the absolute mass is `3rA`.
  No cancellation is credited before both signed fragments are checked.
- This GO relaxes only unit logical amplitudes. It keeps the frozen face,
  rank-`n` cut, complete ladder, pairing, common cross-stream label
  interleaving, exact multiplicities, complete label coverage, fixed `n`,
  schedule-dependent near-one scale, and exact-real arithmetic. Undeclared or
  unbounded mass, arbitrary signed logical amplitudes, different per-column
  schedules, batches, arbitrary fragment support, template mutation, RPPR/KKT
  chronology, fixed-`alpha` uniformity, terminal solving, finite precision,
  rounding, word-RAM, coefficient-bit, and PPR claims remain outside scope.

### `hybrid_aesp_locsor`

- Stable source labels: `sec:branch-caterpillar-implicit-lower-heap`,
  `eq:branch-caterpillar-implicit-lower-state`,
  `lem:branch-caterpillar-implicit-lower-heap`,
  `eq:branch-caterpillar-implicit-lower-equality`,
  `eq:branch-caterpillar-implicit-lower-query`,
  `eq:branch-caterpillar-implicit-lower-update`,
  `eq:branch-caterpillar-implicit-lower-local-cost`,
  `eq:branch-caterpillar-implicit-lower-init-vector`,
  `prop:branch-caterpillar-literal-dense-lower-sweep-cost`,
  `def:branch-caterpillar-implicit-lower-policy`,
  `eq:branch-caterpillar-implicit-lower-stopping-time`,
  `eq:branch-caterpillar-implicit-lower-stage-bound`,
  `eq:branch-caterpillar-implicit-lower-shock`,
  `eq:branch-caterpillar-implicit-lower-budgets`,
  `thm:branch-caterpillar-implicit-lower-handoff`,
  `eq:branch-caterpillar-implicit-lower-prefix-eleven-vector`,
  `eq:branch-caterpillar-implicit-lower-post-eleven-vector`, and
  `eq:branch-caterpillar-implicit-lower-total-work`.
- On one canonical face `U`, `ImplicitLowerHeap(U,a,z)` stores the raw lower
  residual `r=ell_(rho,U)-Q_(UU)z` and indexed exact max-heap keys
  `h_i=[-r_i]_+/(alpha sqrt(d_i))`. Its maximum is exactly the lower-map scalar
  `Delta_U^low(z)`. Together with the transported anchor, a requested parent
  coordinate equals the corresponding coordinate of
  `a vee L_U(z)=T_(k,a)(z)`; the three cached boundary parent/coupling records
  therefore give the exact conservative complete-batch query in `O(1)` work.
- A write `z_j<-z_j+delta` changes and rekeys exactly
  `U intersect ({j} union N(j))`, by `r_i<-r_i-Q_(ij)delta`. With the host's
  already charged stencil this adds `O(d_j+1)` recurrence/materialization work
  and `O((d_j+1)log(2+C(U)))` control work, with no adjacency, interaction,
  preprocessing, response, or emission increment. The diagnostic state uses
  `Theta(C(U))` persistent cells and `O(1)` scratch.
- From a materialized point alone, one raw-product pass and heapify have vector
  `(vol(U),1,0,0,O(C(U)),O(vol(U)),0,Theta(C(U)),O(1),Theta(C(U)),0)`.
  A host-supplied charged `Q_(UU)z` removes the first two coordinates. The
  composed policy nevertheless charges an independent initialization pass on
  every face. A bulk endpoint that does not expose the product pays one extra
  full product; this is absorbed in `O(A_k^ih)` because
  `Lambda_k^tr>=2` rather than being treated as free sharing.
- `LiteralDenseLowerSweep` is the named representation that stores neither
  residual nor maximum and reconstructs the product and full anchored point at
  every test. Its repeated-sweep formula is an accounting identity for that
  interface only, not an algorithm-class lower bound. The implicit heap replaces
  those diagnostic passes by one face initialization, local rekeys, and
  constant-size queries.
- For every fixed `m>=2`, branch seed, `0<alpha<1/2`,
  `0<rho<rho_can(m,alpha)`, and `2<=q<=m`,
  `ImplicitLowerHeap-BC-AESP_(0:q-1)` certifies the first `q` canonical batches,
  commits the first `q-1` without settlement, and preserves the same
  coordinatewise lower anchor as the Round-016 transported policy. Candidate
  rows remain pre-exposed. Every new face initializes fresh momentum, proximal,
  center, estimate-sequence, residual, and heap state; no accelerated auxiliary
  record crosses admission.
- The explicitly surviving face shocks are
  `S_<q^row=sum_(k<q) vol(Uhat_k)=3q^2` raw-residual/heap initialization work and
  `S_<q^adm=sum_(k=1)^(q-1)|Uhat_k|=(q-1)(3q+2)/2` transported-anchor writes.
  Thus `q=m` still has a quadratic restart/admission shock even though every
  diagnostic query is constant-size.
- Before interaction `q`, the complete prefix vector is
  `(V_q^can+O(S_<q^row+A_<q^ih),2q+1+O(A_<q^ih),q-1,0,O(H_<q^ih),O(D_<q^ih),0,O(C_q^can),O(C_(q-1)^can),O(D_<q^ih),Theta(q))`.
  It emits exactly the first `3(q-1)` labels and one constant-size reply per
  interaction, and has `C_resp=0` exactly. The direct native response and
  canonical continuation have vector
  `(vol(S* setminus Uhat_q),m-q,m-q+2,0,O(C(S*)),0,O(C(S*)+(m-q+1)log(2+m)),Theta(C(S*)),O(C(S*)),Theta(card(S*)),card(S* setminus Uhat_(q-1))+card(S*)+Theta(m-q+2))`.
  Total interactions remain `m+1`, both memory peaks are `O(C(S*))`, and total
  work is `O(H_<q^ih+C(S*)log(2+C(S*)))`.
- This is an exact-real, fixed-`m`, branch-seeded, strict-range
  delta/certificate diagnostic GO, not a subquadratic growing-face prefix or a
  speedup. It transports no momentum or estimate-sequence energy, removes no
  AESP stage work, raw-product initialization, admission materialization,
  candidate-row exposure, margin term, or `log(1/(1-2alpha))` factor. It proves
  no other-range/seed/graph/policy, hidden-row, graph-uniform, finite-precision,
  bit, PPR, or RPPR-to-PPR result.

### `volume_gated_acceleration`

- Stable source labels: `prop:path-tight-pair-schur-bank`,
  `eq:path-tight-pair-coefficients`,
  `eq:path-tight-pair-schur-bank`,
  `eq:path-tight-pair-held-values`,
  `eq:path-tight-pair-reset-components`,
  `eq:path-tight-pair-schur-data`,
  `eq:path-tight-pair-schur-shock`,
  `eq:path-tight-pair-linear-switch-stop`,
  `eq:path-tight-pair-squared-switch`,
  `eq:path-tight-pair-switch-slack`,
  `eq:path-tight-pair-switch-rule`,
  `eq:path-tight-pair-feasible-rectangle`, and
  `eq:path-tight-pair-schur-bank-eleven-vector`.
- On the actual `q=1/5` chronology, take the unique tight held-pair endpoints
  `c_3=2978273417354/42112483166425` and
  `c_4=95554102960761584/1567701294665491845`. The named
  `TightPair-Schur_(3->4)` bank
  `b_t(U_j)=delta(x^(t))+c_j q^(-1)e_t(U_j)` is exactly flat on held pair
  `6->7` on `U_3` and held pair `9->10` on `U_4`.
- Freeze the literal stage-8 candidate. Its old/new correction and normalized
  errors are
  `delta_8^-=delta_8^+=28990137728/3755908203125`,
  `e_8^-=101253632777792/25604026220703125`, and
  `e_8^+=13229/1501825`. At the old restricted optimum the normalized new-row
  KKT value is `-13229/4260625` and the one-coordinate Schur pivot is
  `60073/170425`, so the exact restricted-optimum drop is
  `Delta_8=175006441/6398713140625`.
- Exact subtraction gives `b_8^+-b_8^->Delta_8`, stopping a unit objective-shock
  charge for the raw linear bank. Exact squaring instead gives
  `J_8=(b_8^+)^2-(b_8^-)^2<Delta_8`, with the exact positive slack recorded in
  `eq:path-tight-pair-switch-slack`. Equivalently,
  `Psi_t(U_j)=b_t(U_j)^2+H_j`, with `H_3=Delta_8` and `H_4=0`, strictly decreases
  across the isolated reset while the two reviewed held pairs are flat.
- Over the locally feasible rectangle `c_3'>=c_3` and `0<=c_4'<=c_4`, increasing
  the pre-reset coefficient only raises the old bank and decreasing the
  post-reset coefficient only lowers the new bank. Hence the tight endpoints
  maximize the squared reset jump, so the proved unit Schur charge covers every
  locally feasible pair at this frozen candidate.
- The literal execution remains `J=4`, `T=16`, `n_fin=5`, `nu_fin=9`, swept
  volume `114`, no intermediate external emission, and one terminal return. Its
  vector remains
  `(Theta(9),Theta(5),1,0,Theta(114),Theta(114),Theta(114),Theta(9),O(9),Theta(114),Theta(9))`.
- This is a finite exact-real squared-reset GO and linear-switch STOP for one
  named two-phase bank, two held pairs, literal event order, and the
  instantaneous frozen-candidate stage-8 reset. Held-face evolution and reset
  accounting are separate. It asserts no monotonicity on every held pair, the
  fixed-face step producing stage 8, or the next step to stage 9; supplies no
  online coefficient rule, multi-admission telescope, global block horizon,
  logarithm removal, asymptotic upper/lower bound, or spectral result; and says
  nothing about zero-padded momentum, nonpaths, alternate orders, intermediate
  returns, finite precision, or bit complexity. It does not conflict with the
  hybrid coordinatewise lower-state invariant, whose accelerated auxiliaries
  restart.

## Controller adjudication

- Promote `NotchedSunAmplitudeDelta` only as a declared-positive-occurrence-
  schedule extension of the frozen promised-template GO. The mass-capacity
  margin is schedule dependent; a failed margin is not evidence that response
  state is necessary. Keep event count, logical mass, fragments, certificates,
  and first-crossing deltas separate.
- Promote `ImplicitLowerHeap` as an exact incremental implementation of the
  already proved anchored lower diagnostic. It removes query-time full-face
  diagnostic construction, not the face initialization, transported-anchor
  write, AESP restart/stage work, row exposure, or paid native suffix. The
  `LiteralDenseLowerSweep` formula is representation-specific.
- Promote `TightPair-Schur_(3->4)` only as an exact squared-bank charge for one
  instantaneous reset and the accompanying raw-linear STOP. Do not call it a
  global potential, conflate held evolution with reset, or infer removal or
  necessity of the path logarithm.
- No new formal taxonomy dependency edge is warranted: all three proofs use
  direction-local predecessors and already recorded shared/source contracts.

## Independent review

- The response audit independently rederived every matching/off-matching
  envelope, the tight event and full-mass horizons `H_i,F_i`, capacity indices,
  schedule margin, simultaneous unseen/seen-but-unreported certificate,
  first-certified/first-exact crossing identity, stream validation,
  nonnegative/signed fragment masses, every vector coordinate, and all
  exclusions. It reran the seeded checker, forced the 79-page build, and added
  an exhaustive audit of 1,024 count states and 3,840 legal transitions. It
  returned clean and required no edit.
- The hybrid audit independently rederived `r=ell_rho-Q_(UU)z`, the heap keys,
  exact maximum/parent query, closed-neighborhood rekeys, and both initialization
  interfaces. It verified absorption of an extra bulk-endpoint product through
  `Lambda_k>=2`, the `q=2` and `q=m` edges, `3q^2` row-initialization shock,
  `(q-1)(3q+2)/2` anchor-write shock, both vectors, interactions, emissions,
  storage peaks, direct native response, and representation-only dense-sweep
  scope. The checker, Ruff, and forced 65-page build passed with only three
  pre-existing overfull warnings. It returned clean and made no edit.
- The volume audit used an independent dense exact-rational replay. It
  reproduced the literal chronology, both tight coefficients and held values,
  every stage-8 component, KKT value, Schur pivot/drop, raw linear STOP,
  squared GO and slack, reserve identity, feasible-rectangle extremality,
  complete vector, and reset-local exclusions. The supplied checker and forced
  44-page build passed. It returned clean and made no edit.

## Redistribution messages

1. Response to hybrid and volume: a dynamic certificate can be maintained from
   declared local scalar capacity while a phase transition still requires a
   separately charged materialization or shock. The reporter's future-mass
   bound depends on the full positive occurrence schedule and cannot be reused
   as a generic correction potential or undeclared-mass certificate.
2. Hybrid to response and volume: local numerical writes can update an exact
   global diagnostic through a persistent residual/heap and constant-size
   queries, but every new face still pays raw-product initialization, anchor
   materialization, and fresh accelerated state. Distinguish incremental
   within-phase maintenance from transition work in every proposed reporter or
   potential.
3. Volume to response and hybrid: held evolution, coefficient switching, and
   face reset are separate obligations. Squaring the scalar bank makes this one
   reset dimensionally chargeable by the exact Schur drop; the raw linear jump
   is not. This finite identity is not evidence for a generic scalar correction
   potential and does not contradict coordinatewise lower-state transport.
4. All directions: preserve the exact access interface, event/face chronology,
   stream or path promises, first-crossing versus first-occurrence distinction,
   response-free prefix versus paid suffix, and all eleven resource coordinates
   when reusing any Round-017 result.

## Metadata and shared-state changes

- This round record, `BROADCAST.md`, and the shared results ledger record the
  three independently reviewed outcomes and their cross-implications.
- The shared results gain one narrowly scoped row per promoted direction. The
  response, hybrid, and acceleration frontier text is narrowed from arbitrary
  amplitudes, repeated lower-diagnostic sweeps, and any face-dependent switch to
  their exact remaining interfaces.
- Direction notes retain their stable theorem/equation labels and reproducible
  checkers. Controller reconciliation synchronized the root notes index,
  taxonomy, manifest, and three research-roadmap documents. The related-work
  ledger, shared problem definition, and formal dependency arrays remain
  unchanged; no new edge is added.

## Validation

- Round-start `make test`: 187 passed with 15 known temporary-directory cleanup
  warnings.
- Round-start `make lint`: green.
- Round-start `make note-audit`: 18 notes across 5 tracks.
- Round-start `make note-graph`: 18 nodes and 26 acyclic formal edges.
- Round-start full `git diff --check`, conflict scan, staging scan, and root-
  artifact scan: green.
- Response direction: the seeded 100-trial amplitude checker passed through
  total length `102` and logical mass `0.1674662`, including `1,084` delayed
  crossings and `5,459` seen-but-unreported prefixes. It reported maximum
  future-tail/gap fraction `0.003377205`, minimum derived-band/gate fraction
  `0.6478048`, minimum certified-crossing/gate ratio `1.200001`, and maximum
  actual-pre-crossing/(gate-band) ratio `0.9994058`. Focused Ruff, labels, diff,
  inventory, and the 79-page build passed; independent exhaustive review was
  clean.
- Hybrid direction: the exact-rational checker matched the dense lower maps
  after signed updates on every face `m=2,...,7` and verified locality, shocks,
  ledgers, and labels. Round-014--017 checkers, Ruff, inventory/graph, scoped
  diff, and the 65-page build passed; the build retained only three pre-existing
  overfull warnings. Independent review was clean.
- Volume direction: all Round-013--017 exact checkers, Ruff, inventory/graph,
  scoped diff, the 44-page build, and visual page review passed. Independent
  dense rational replay was clean.
- Shared promotion reran `make note-audit` and `make note-graph` successfully,
  checked the three-file edit scope, whitespace, conflict markers, staging, and
  the repository root for the accidental `=2` artifact; no path was staged.
- Controller durable reconciliation completed the six root-metadata/roadmap
  edits with dependency arrays unchanged. TOML counts remained `18/18` across
  5 tracks; note inventory, dependency graph, lint, diff, conflict-marker,
  root-artifact, and staging scans were green, and no path was staged.
- Controller final validation reran all three Round-017 checkers, the full test
  suite (`187` passed with the same `15` known temporary-directory cleanup
  warnings), lint/format, note inventory (`18` notes across `5` tracks), the
  unchanged `26`-edge acyclic dependency graph, TOML `18/18` ID reconciliation,
  full diff/conflict/staging/root-artifact scans, and forced fresh direction
  builds (`79`, `65`, and `44` pages). All passed; the retained recovery stash
  was left untouched and no path was staged, committed, or pushed.

## Next round queue

1. Response: relax exactly one remaining promise--undeclared or unbounded
   positive mass, arbitrary signed logical amplitudes, different per-column
   schedules, simultaneous batches, arbitrary fragment support, or template
   mutation--while retaining charged first-crossing certificates, or isolate
   the first exact response/state obstruction. Do not reuse a failed displayed
   margin as a lower bound.
2. Hybrid: amortize or remove the surviving `3q^2` face-initialization and
   `(q-1)(3q+2)/2` anchor-write shocks, or prove a representation-specific
   obstruction for a named interface. Any acceleration claim must additionally
   transport or amortize AESP state rather than only the lower diagnostic.
   The remaining `rho_can<=rho<rho_cat`, arbitrary pre-backbone, and trusted-
   metadata row-access routes stay separate.
3. Volume: extend the squared bank through the fixed-face step producing the
   stage-8 candidate and the step from reset to stage 9, derive a face-general
   coefficient rule and multi-admission telescope, or return the shortest exact
   failure. A renewed lower route still needs literal full-face entry
   coefficients, anti-cancellation, and uniform projection/envelope margins.
4. Preserve separately a generic partial reporter, shock-aware accelerated
   transport, and a genuine global path potential. The three Round-017
   fixed-template/representation/reset results do not compose into a
   graph-uniform PPR theorem.
