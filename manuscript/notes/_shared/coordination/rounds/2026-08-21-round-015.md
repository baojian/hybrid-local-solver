# Research round 015: permutation robustness, local margin certificates, and phase-aware path debt

Date: 2026-08-21
Controller snapshot: updated `main` at `14f3e60`, with reviewed Round-014 work
restored from the retained automatic WIP stash and redistributed before this
dispatch; the live worktree is intentionally dirty with those promoted source
and documentation edits.
Round state: redistributed

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `response_preconditioned_hybrid` | Keep the frozen notched-sun template, verified pairing, unit amplitudes, exact-real model, and explicit near-one scale, but replace the prescribed event order by an arbitrary verified permutation. Prove an output-sensitive delta/certificate reporter with a complete eleven-vector, or give the first exact permutation that forces a response query or broader state. | `response_preconditioned_hybrid/` | GO: `thm:notched-sun-permutation-delta-reporter` proves that `NotchedSunPermutationDelta` handles every online-verified common permutation with the same vector as Round 014 and no response query, slack cell, future-label scan, or numerical materialization. |
| `hybrid_aesp_locsor` | At the actual branch-caterpillar `k=1` checkpoint, replace the paid exact four-coordinate response in the sharp `mu_1` certificate by a strictly cheaper local one-sided certificate, or prove a named obstruction; separately test whether the policy can reach `Uhat_1` without checkpoint pre-exposure. | `hybrid_aesp_locsor/` | GO for the gate and scoped STOP for row exposure: the local lower map certifies the `k=0,1` batches without an exact response, and `LowerGate-BC-AESP_(0:1)` gives a genuine two-gate numerical prefix whose response coordinate is zero through the pre-second-interaction endpoint. Charged row validation necessarily pre-exposes the first candidate rows; the direct post phase deliberately builds and pays the native response on `Uhat_2`. |
| `volume_gated_acceleration` | On the exact constant-ratio endpoint path, formulate and test one explicit nonmonotone, phase-aware, or space--time block potential that survives the Round-014 same-face correction increase. Prove a charged lemma or return the smallest exact countertrace to that named potential. | `volume_gated_acceleration/` | STOP: the coefficient-one face-local bank `Phi_t^bank=delta_t+q^(-1)e_t` passes the held `6->7` pair on `U_3` but first increases on held stages `9->10` on `U_4`, even though `delta_t` decreases. This finite exact countertrace leaves other coefficients and longer, signed, or aggregate potentials open. |

## Direction handoffs

### `response_preconditioned_hybrid`

- Stable source labels: `thm:notched-sun-permutation-delta-reporter`,
  `eq:notched-sun-permutation-prefix`,
  `eq:notched-sun-permutation-certificate`,
  `eq:notched-sun-permutation-state`, and
  `eq:notched-sun-permutation-eleven-vector`.
- Keep the frozen face, rank-`n` cut, complete ladder, verified label pairing,
  gate `g=vartheta^(5/2)`, band `g/2`, unit logical amplitudes, fixed `n`, and
  exact-real range `0<vartheta<=(1296n^2)^(-1)`. At interaction `t`, every
  stream must name the same valid unused petal `pi_t` with its required
  amplitude. The reporter atomically rejects an invalid label, mismatch,
  amplitude, or repeat before changing state or emitting a record.
- The Round-014 matching and off-matching estimates are pointwise and
  order-blind. For every accepted prefix, the matching label `w_(pi_t)` crosses
  strictly, while every unseen label has cumulative tail at most
  `9t vartheta^3/(1-3vartheta)<=g/2`. A seen-label set and the prefix length
  therefore instantiate one universal future-safe certificate per logical
  column without a response application or future-label scan.
- The complete vector remains
  `(Theta(n),2,n,Theta(n+J+p),Theta(C_frag+rn+n),0,0,Theta(n+J+p),O(1),0,Theta(rn))`.
  For nonnegative logical columns, `p=r` and `C_frag=rn`; for separately
  checked signed `+2/-1` streams, `p=2r` and `C_frag=2rn`. The linear seen set
  is absorbed in validated template memory. No response/slack cell or
  numerical materialization is stored or queried.
- This GO relaxes only the prescribed common permutation. It does not cover
  repetitions or missing petals, arbitrary amplitudes/fragments, different
  per-column schedules, simultaneous batches, template mutation, RPPR/KKT
  chronology, fixed-`alpha` uniformity, terminal solution, finite precision,
  or word/bit complexity.

### `hybrid_aesp_locsor`

- Stable source labels: `lem:branch-caterpillar-local-lower-gate-certificate`,
  `eq:branch-caterpillar-local-lower-map`,
  `eq:branch-caterpillar-local-lower-certificate-vector`,
  `eq:branch-caterpillar-local-lower-stopping-time`,
  `eq:branch-caterpillar-local-lower-stage-bound`,
  `def:branch-caterpillar-two-face-lower-policy`,
  `eq:branch-caterpillar-lower-first-gate-arenas`,
  `prop:branch-caterpillar-first-layer-row-preexposure-obstruction`,
  `eq:branch-caterpillar-two-face-lower-budgets`,
  `thm:branch-caterpillar-two-face-lower-handoff`, and its prefix/post
  eleven-vectors.
- For a signed trial point `z` on `Uhat_k`, `k in {0,1}`, the charged map
  `L_U(z)=[z-Delta_U^low(z)D_U^(1/2)1]_+` is a nonnegative restricted
  subsolution and hence lies coordinatewise below the exact restricted point
  `x_k`. Because each tree-boundary row has a unique active parent, its demand
  at `L_U(z)` is an exact one-sided lower bound on the true demand. Strictly
  positive lower demands certify the complete canonical batch without any
  coordinate of `x_k`, optimum-value scalar, `mu_k`, response factor, or
  future margin table.
- At `k=1`, once the face rows and lower point are charged, the three live-row
  scan and strict sign tests have incremental vector
  `(nu_1,1,0,0,O(1),0,0,O(1),O(1),O(1),0)`. This preserves the Round-014
  boundary exposure while making `C_resp=0` exactly; construction of the lower
  point and every repeated numerical/lower-map sweep remain separately
  charged. Here `nu_1=3` for `m=2` and `nu_1=6` for `m>2`, and
  `C_2=16+nu_1`.
- The observed lower-sign stopping rule is online. Zero restricted gap needs
  zero stages; a positive gap may also pass immediately. The named zero starts
  at both `k=0` and `k=1` fail initially and therefore run at least one genuine
  numerical stage. Its finite analysis-side bound still contains `mu_k`:
  `T_k^low<=1+floor((2/sqrt(chi_A))log_+(4(alpha+sqrt(3))^2 Delta_(k,0)/(alpha^3 mu_k^2)))`.
  Every relative-oracle stage retains its explicit
  `log(1/(1-2alpha))` factor.
- Put `V_0=3,C_0=4,V_1=9,C_1=13` and
  `L_k^rel=2+(3/4)log_+(16(1-alpha)^2V_k/(delta_A(1-2alpha)(1-alpha)))`.
  The fully charged budgets are
  `A_k^low=(T_k^low+1)V_kL_k^rel`,
  `A_<2^low=A_0^low+A_1^low`,
  `D_<2^low=C_2+A_<2^low`, and
  `H_<2^low=C_2+(1+log(2+C_0))A_0^low+(1+log(2+C_1))A_1^low`.
  The added one pays the initial/terminal lower-map and sign sweep even when no
  numerical stage is required.
- `LowerGate-BC-AESP_(0:1)` certifies and commits the first canonical batch
  without exact settlement, deliberately resets to a charged zero numerical
  start on `Uhat_1`, and certifies the second batch before the second external
  interaction. Its pre-second-interaction prefix is response-free and has
  vector
  `(9+nu_1+O(A_<2^low),3+O(A_<2^low),1,0,O(H_<2^low),O(A_<2^low),0,O(C_2),O(C_1),O(D_<2^low),Theta(1))`.
  The sole prefix interaction emits the first three labels plus one constant-
  size certificate; internal sign tests emit nothing.
- At the second interaction the first committed face is deliberately still
  unsettled. The post phase consumes the already scanned second-boundary rows,
  emits that batch, builds and settles the native response directly on
  `Uhat_2`, and continues without replaying an earlier face. Its vector is
  `(vol(S* setminus Uhat_2),m-2,m,0,O(C(S*)),0,O(C(S*)+(m-1)log(2+m)),Theta(C(S*)),O(C(S*)),Theta(card(S*)),card(S* setminus Uhat_1)+card(S*)+Theta(m))`.
  Total work is
  `O(H_<2^low+C(S*)log(2+C(S*)))`.
- Charged validation of the three first-batch degrees and incidences requires
  the degree-six rows of `b_2,a_1,r_1` before commitment. Thus the policy
  reaches admitted/numerical `Uhat_1` with `E_row=Uhat_1`, not with an
  unexposed row arena. This is a scoped access STOP, not a lower bound against
  trusted free metadata.
- The independent audit caught and repaired one scope defect: an earlier title
  and summaries called the full execution response-free even though the post
  vector has nonzero `C_resp`. Every reconciled statement now says only the
  two-gate/pre-second-interaction prefix is response-free and explicitly
  charges the direct `Uhat_2` native response. No proof, formula, vector, or
  chronology changed. The result remains an exact-real, fixed-`m`, branch-
  seeded, strict-range RPPR comparison, not a speedup, cross-face acceleration,
  automatic unexposed exploration, `k>=2`, finite-precision, PPR, arbitrary-
  graph, or graph-uniform theorem.

### `volume_gated_acceleration`

- Stable source labels: `prop:path-correction-error-bank-potential-fails`,
  `eq:path-correction-error-bank`,
  `eq:path-correction-error-bank-stage-seven-pass`,
  `eq:path-correction-error-bank-components`,
  `eq:path-correction-error-bank-stage-ten-stop`, and
  `eq:path-correction-error-bank-stop-eleven-vector`.
- On the actual `q=1/5` zero-start internally gated transported-center path,
  define on each held face
  `Phi_t^bank(U)=delta(x^(t))+q^(-1)||D_U^(-1/2)(x^(t)-x_U*)||_infinity`,
  reinitializing the restricted optimum after transport. The literal stage
  order and face origin are fixed; no alternative phase alignment or
  permutation is assumed.
- The bank absorbs the reviewed Round-014 spike on held `U_3`:
  `Phi_7-Phi_6=-156536838996284/5120805244140625<0`. Its first later held-pair
  failure is stages 9 and 10 on `U_4`, where
  `Phi_10-Phi_9=11777177533637842088/2957905129146728515625>0`, even though
  `delta_10-delta_9=-24266663296/93897705078125<0`. Both stages retain the
  actually clipped safe envelope, fail active certification, have positive
  outside KKT value, and neither admit nor certify.
- The unchanged complete run has admissions at stages `1,2,4,8`, terminal
  certification at stage `16`, `J=4`, `T=16`, `n_fin=5`, `nu_fin=9`, swept
  volume `114`, no intermediate external emission, and one terminal return.
  Its full vector is
  `(Theta(9),Theta(5),1,0,Theta(114),Theta(114),Theta(114),Theta(9),O(9),Theta(114),Theta(9))`.
- This is a finite exact-real coefficient-one bank STOP only. It proves no
  asymptotic lower bound and does not refute another coefficient, a signed
  phase variable, a longer space--time block, a nonlocal sum across faces, an
  amortized potential allowed to rise locally, the reviewed one-log upper
  bound, logarithm removal, or logarithm necessity.

## Controller adjudication

- Promote `NotchedSunPermutationDelta` exactly as an arbitrary verified
  common-permutation extension of the frozen promised-template GO. The
  exclusions on amplitudes, repeats, per-column schedules, and batches remain
  operative.
- Promote the hybrid lower-map certificate as a strict response-coordinate
  improvement and the two-gate execution only as a pre-second-interaction
  response-free numerical comparator. Promote the charged row-preexposure
  STOP at the stated validation contract. The direct post response prevents a
  full-run no-response or speedup interpretation.
- Promote the volume countertrace only against the coefficient-one finite
  bank. It neither settles the logarithm nor revives the withdrawn Round-013
  lower theorem.
- No new formal taxonomy dependency edge is warranted: the proofs use only
  direction-local predecessors and already recorded shared/source contracts.

## Independent review

- The response audit independently rederived the `M=B_0-vartheta R`/Neumann
  constants, entrywise positivity, arbitrary common-permutation matching and
  unseen-tail bounds, atomic streamed seen-set validation, nonnegative and
  signed stream counts, every vector coordinate, and the certificate/no-query/
  no-materialization scope. It found no defect and required no edit.
- The hybrid audit independently checked the Stieltjes lower-map comparison,
  unique-parent boundary identity, `k=0,1` chronology, zero- and positive-gap
  branches, degree and row-preexposure charges, deliberately unsettled first
  commitment, direct charged `Uhat_2` suffix, analysis-only margin use,
  prefix/post vectors, rounds, emissions, and memory peaks. It required the
  one wording repair recorded above, then re-audited the result clean with no
  mathematical or ledger change.
- The volume audit used an independent dense exact-`Fraction` replay. It
  reproduced admissions `1,2,4,8`, holds `3,5,6,7,9--15`, the common `U_4`
  face at stages 9 and 10, the gate/envelope facts, the exact `6->7` bank pass,
  the first later `9->10` increase while correction falls, `J=4`, `T=16`,
  `n_fin=5`, `nu_fin=9`, swept volume `114`, and every vector coordinate. It
  found no defect and required no edit.

## Redistribution messages

1. Response to hybrid: order-blind pointwise bounds remove only a common
   permutation promise. Any different per-column schedule, repeat, batch, or
   amplitude still needs explicit validation and a new future-safe argument;
   do not treat template metadata as a free general gate certificate.
2. Hybrid to response: a charged lower retraction plus strict one-sided sign
   tests can replace a constant-size exact response at a local tree boundary,
   but constructing the lower point and exposing every validated row remain
   work. The pattern does not imply a generic no-response partial reporter.
3. Volume to both: even `delta_t+q^(-1)e_t` can increase on consecutive held
   steps while `delta_t` falls. Do not use the coefficient-one bank as a
   monotone response debt, signed-gate potential, or cross-face charge.
4. All directions: keep logical promises disjoint from access promises.
   `NotchedSunPermutationDelta` validates a frozen template and common stream
   order; `LowerGate-BC-AESP_(0:1)` must still pre-expose candidate rows; the
   path bank uses one literal phase origin. None licenses a free oracle or a
   reordered chronology elsewhere.

## Metadata and shared-state changes

- This round record, `BROADCAST.md`, and the shared results ledger record the
  three independently reviewed outcomes and the hybrid audit repair.
- The shared results gain one narrowly scoped row per promoted GO/STOP, and
  the current-frontier text is narrowed accordingly.
- The controller synchronized `manuscript/notes/README.md`, `taxonomy.toml`,
  `manifest.toml`, `docs/literature/acceleration.md`, `docs/research_notes.md`,
  and `docs/solver-family-roadmap.md` with the three reviewed outcomes and next
  targets. Related work, the shared problem definition, and all formal
  dependency arrays are unchanged; no new edge is added.

## Validation

- Round-start `make test`: 187 passed with 15 known temporary-directory cleanup
  warnings.
- Round-start `make lint`: green.
- Round-start `make note-audit`: 18 notes across 5 tracks.
- Round-start `make note-graph`: 18 nodes and 26 acyclic formal edges.
- Round-start full `git diff --check`, conflict scan, and staging scan: green.
- Response direction: 74-page build; 100 seeded permutation trials plus all
  120 permutations at `n=5`; Ruff and scoped diff checks green. The independent
  audit rederived the theorem and vector without requiring an edit.
- Hybrid direction: 57-page build with only three pre-existing overfull-box
  warnings; focused lower-map/stage checker, Ruff, and scoped diff checks
  green. After the scope wording repair, the independent re-audit was clean.
- Volume direction: 39-page build; all three finite exact checkers, Ruff, and
  scoped diff checks green. The independent dense rational replay was clean.
- Redistribution repeated all three Round-015 focused checkers: the response
  run passed 100 seeded arbitrary permutations, the hybrid lower-point gate
  checks passed, and the exact path replay reproduced the `Phi_7<Phi_6` and
  `Phi_10>Phi_9` STOP with `J=4`, `T=16`, `nu_fin=9`, and swept volume `114`.
- Redistribution `make note-audit`: 18 notes across 5 tracks.
- Redistribution `make note-graph`: 18 nodes and 26 acyclic formal edges.
- Redistribution scoped whitespace, conflict-marker, staging, and edit-scope
  checks: green; no file was staged.
- Post-promotion TOML parsing found 18 taxonomy entries and 18 manifest notes;
  `make note-audit`, `make note-graph`, full `git diff --check`, conflict scan,
  and staging scan all remained green.

## Next round queue

1. Response: relax exactly one remaining frozen-stream axis—repetitions,
   arbitrary amplitudes, different per-column schedules, or simultaneous
   batches—and either retain an output-sensitive future-safe certificate or
   exhibit the first exact query/state obstruction. Capacity and slack-weight
   changes must be charged.
2. Hybrid: test whether the lower-map certificate can extend to `k>=2` while
   preserving numerical progress across faces instead of zero-resetting, or
   give the first exact failure. Any trusted-metadata alternative to the row-
   preexposure STOP must name and charge its access contract; the post response
   may not be hidden.
3. Volume: test a different coefficient, a signed phase variable, or the
   shortest explicit multi-step/space--time block on the literal path. Promote
   only a charged proof or the earliest exact countertrace; a lower route still
   needs actual entry coefficients, anti-cancellation, and uniform projection/
   envelope margins.
4. Preserve separately the new cyclic-legality gadget and a same-task
   recurrence/response family that defeats both residual-slack spreading and
   singleton-basis synthesis.
