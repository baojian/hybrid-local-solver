# Research round 014: implicit partial response, charged gate margins, and the path logarithm

Date: 2026-08-21
Controller snapshot: Round 013 is independently reviewed and redistributed.
The notes inventory has 18 directions across 5 tracks, the formal import graph
has 26 acyclic edges, and the inventory audit now enforces exact agreement
between every status card and `taxonomy.toml`. The existing dirty worktree is
shared; no staging, commit, push, rename, or directory move is authorized.
Round state: active

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `response_preconditioned_hybrid` | On the frozen notched-double-sun face from Round 013, replace `EagerExactSlack` by one precisely named implicit, batched, scale-aware, or on-demand partial reporter and give an output-sensitive eleven-vector, or prove the first exact obstruction against a strictly broader named representation. | `response_preconditioned_hybrid/` | pending |
| `hybrid_aesp_locsor` | At the actual `FirstLayer-BC_1` checkpoint, derive and charge a locally checkable lower certificate for the sharp signed margin `mu_1` and compose a genuinely numerical capped prefix, or prove the smallest exact parameter/margin obstruction. | `hybrid_aesp_locsor/` | pending |
| `volume_gated_acceleration` | Remove the logarithm from the constant-ratio path chronology upper bound, or derive a rigorous matching fixed-face lower using the actual entry state, moving correction, projection, and envelope. Return the first exact failed lemma if neither route closes. | `volume_gated_acceleration/` | pending |

## Direction handoffs

Pending. Each direction must return stable labels, exact graph/parameter and
interface scope, the shared eleven-coordinate ledger, explicit exclusions, a
resume-ready `STATUS.md`, focused checks, and a compact redistribution message.
Numerical evidence may guide a proof but remains `Measured`.

## Controller adjudication

Pending independent direction work and cyclic review. Round-013 results are
inputs, not claims to be reproved or widened. No candidate enters accumulated
knowledge before its proof, model, resource ledger, and summaries agree.

## Redistribution messages

1. Dense exact increments refute only separately addressed eager slack cells;
   every broader response obstruction must name what implicit computation and
   on-demand validation are still allowed.
2. `FirstLayer-BC_1` is response-native, not accelerated. A numerical splice
   must pay or certify `mu_1`; strict positivity alone is not a complexity
   bound.
3. The withdrawn path theorem may not be revived from characteristic roots or
   finite traces. Any lower route needs actual entry coefficients,
   anti-cancellation, and uniform projection/envelope validity.

## Metadata and shared-state changes

- This round record and its rounds-index entry are added at dispatch.
- `BROADCAST.md` records the active assignments while preserving the verified
  Round-013 redistribution and dependency/provenance contract.
- Results, literature, taxonomy, manifest, and durable roadmaps remain
  unchanged until independent review completes.

## Validation

- Round-start `make test`: 187 passed with 15 known temporary-directory cleanup
  warnings.
- Round-start `make lint`: green.
- Round-start `make note-audit`: 18 notes across 5 tracks.
- Round-start `make note-graph`: 18 nodes and 26 acyclic formal edges.
- Round-start full `git diff --check`, conflict scan, and staging scan: green.

## Next round queue

Pending the exact GO/STOP outcomes above. Preserve as separate future targets
the new cyclic legality gadget and a same-task recurrence/response family that
defeats both residual-slack spreading and singleton-basis synthesis.
