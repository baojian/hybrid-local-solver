# Research round 014: implicit partial response, charged gate margins, and the path logarithm

Date: 2026-08-21
Controller snapshot: Round 013 is independently reviewed and redistributed.
The notes inventory has 18 directions across 5 tracks, the formal import graph
has 26 acyclic edges, and the inventory audit enforces exact agreement between
every status card and `taxonomy.toml`. The existing dirty worktree is shared;
no staging, commit, push, rename, or directory move is authorized.
Round state: redistributed

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `response_preconditioned_hybrid` | On the frozen notched-double-sun face from Round 013, replace `EagerExactSlack` by one precisely named implicit, batched, scale-aware, or on-demand partial reporter and give an output-sensitive eleven-vector, or prove the first exact obstruction against a strictly broader named representation. | `response_preconditioned_hybrid/` | GO: `thm:notched-sun-scale-delta-reporter` proves the template-aware `NotchedSunScaleDelta` reporter in the explicit range `0<vartheta<=(1296n^2)^(-1)`. It stores no response or slack cells and has vector `eq:notched-sun-scale-delta-eleven-vector`. |
| `hybrid_aesp_locsor` | At the actual `FirstLayer-BC_1` checkpoint, derive and charge a locally checkable lower certificate for the sharp signed margin `mu_1` and compose a genuinely numerical capped prefix, or prove the smallest exact parameter/margin obstruction. | `hybrid_aesp_locsor/` | GO with comparator qualifier: `lem:branch-caterpillar-first-layer-margin-certificate` computes `underline(mu)_1=mu_1` from one paid three-row scan and a separately charged four-coordinate exact response. `MarginCert-BC-AESP_1` then runs a capped signed numerical prefix and hands off by `thm:branch-caterpillar-first-layer-numerical-handoff`; it is genuine numerical work but no speedup or automatic exploration theorem. |
| `volume_gated_acceleration` | Remove the logarithm from the constant-ratio path chronology upper bound, or derive a rigorous matching fixed-face lower using the actual entry state, moving correction, projection, and envelope. Return the first exact failed lemma if neither route closes. | `volume_gated_acceleration/` | STOP on the first pointwise upper route: `prop:path-monotone-correction-potential-fails` proves on the actual `q=1/5` path execution that the moving correction increases from stage 6 to 7 and that `delta_7/e_7>q^(-1)`. This neither removes nor proves necessity of the logarithm. |

## Direction handoffs

### `response_preconditioned_hybrid`

- Stable source labels: `thm:notched-sun-scale-delta-reporter`,
  `eq:notched-sun-scale-regime`, `eq:notched-sun-scale-matching`,
  `eq:notched-sun-scale-future-tail`, `eq:notched-sun-scale-state`, and
  `eq:notched-sun-scale-delta-eleven-vector`.
- The fixed template, exposed cut, complete ladder, unit event order, gate
  `g=vartheta^(5/2)`, and band `g/2` are exactly those of Round 013. For
  `0<vartheta<=(1296n^2)^(-1)`, the entrywise-nonnegative Neumann kernel has
  operator norm at most three; no positive-semidefinite order is claimed. The
  length-one matching term exceeds `g`, while the cumulative future-label tail
  is at most `9 k vartheta^3/(1-3 vartheta)<=g/2`.
- `NotchedSunScaleDelta` verifies the promised template, labels, ladder, order,
  and every fragment amplitude. It retains only that metadata and an event
  counter, then emits `(q,w_k)` and one universal future-safe certificate per
  logical column without response application, future-label scans, slack
  cells, or numerical materialization.
- With `C_frag=pn`, use `p=r` and `C_frag=rn` for nonnegative columns, or
  separately verify `+2/-1` streams with `p=2r` and `C_frag=2rn`. The complete
  vector is
  `(Theta(n),2,n,Theta(n+J+p),Theta(C_frag+rn+n),0,0,Theta(n+J+p),O(1),0,Theta(rn))`.
  This is exact-real, fixed-`n`, near-one-`alpha`, fixed-template, fixed-order,
  and fixed-amplitude debt/query reporting. It is not an RPPR/KKT chronology,
  arbitrary dynamic reporter, terminal solver, fixed-`alpha` uniform family,
  or finite-precision/word/bit theorem.

### `hybrid_aesp_locsor`

- Stable source labels: `sec:branch-caterpillar-first-layer-numerical-splice`,
  `lem:branch-caterpillar-first-layer-margin-certificate`,
  `eq:branch-caterpillar-first-layer-margin-certificate-vector`,
  `def:branch-caterpillar-first-layer-numerical-policy`,
  `eq:branch-caterpillar-first-layer-numerical-arenas`,
  `eq:branch-caterpillar-first-layer-numerical-stage-cap`,
  `eq:branch-caterpillar-first-layer-numerical-oracle-log`,
  `thm:branch-caterpillar-first-layer-numerical-handoff`, and its prefix/post
  eleven-vectors.
- At the actual `Uhat_1={b_1,b_2,a_1,r_1}` checkpoint, one scan of the three
  live rows computes the exact sharp margin `underline(mu)_1=mu_1`; the
  incremental certificate vector is
  `(nu_1,1,0,0,O(1),0,O(1),O(1),O(1),O(1),0)`, where `nu_1=3` for `m=2` and
  `nu_1=6` for `m>2`.
- For initial restricted gap `Delta_(1,0)`, the stage cap is zero at zero gap
  and otherwise
  `T_1=1+floor((2/sqrt(chi_A)) log_+(4 Delta_(1,0)/(alpha mu_1^2)))`.
  The zero start has positive gap and therefore runs genuine numerical stages;
  every relative-oracle stage retains
  `L_1^rel=2+(3/4)log_+(144(1-alpha)/(delta_A(1-2alpha)))`.
- Before the second interaction, admitted and numerical arenas are `Uhat_1`,
  rows are scanned through `Uhat_2`, and only labels extend to `Uhat_3` when
  `m>2`. The exact response used to obtain the margin is separately charged
  and retired; the signed numerical endpoint decides the gate. Because the
  paid prequery violates the older checkpoint endpoint clause, the suffix is
  proved directly rather than imported silently.
- With `A_1=T_1 V_1 L_1^rel`, `D_1=C_2+A_1`, and
  `H_1=C_2+(1+log(2+C_1))A_1`, the prefix vector is
  `(9+nu_1+O(A_1),3+O(A_1),1,0,O(H_1),O(A_1),O(1),O(C_2),O(C_1),O(D_1),Theta(1))`.
  The direct post vector is
  `(vol(S* setminus Uhat_2),m-2,m,0,O(C(S*)),0,O(C(S*)+(m-1)log(2+m)),Theta(C(S*)),O(C(S*)),Theta(card(S*)),card(S* setminus Uhat_1)+card(S*)+Theta(m))`.
  Total work is `O(H_1+C(S*)log(2+C(S*)))`.
- This is exact-real, fixed-`m`, branch-seeded, `alpha<1/2`,
  `rho<rho_can`, fixed-face RPPR comparison. It proves no speedup, automatic
  exploration, other checkpoint/range/seed/policy, repeated full-list,
  finite-precision, PPR, arbitrary-graph, or graph-uniform result.

### `volume_gated_acceleration`

- Stable source labels: `prop:path-monotone-correction-potential-fails`,
  `eq:path-stage-seven-correction-growth`,
  `eq:path-stage-seven-correction-error-stop`, and
  `eq:path-monotone-correction-stop-eleven-vector`.
- On the actual `q=1/5` zero-start internally gated transported-center path,
  stages 6 and 7 are consecutive held steps on the same face `U_3`, yet
  `delta_7-delta_6=1747556648/751181640625>0` and
  `delta_7/e_7=5361340160387/858557821215>5=q^(-1)`. The proximal and envelope
  states are strictly unclipped; no conditional Round-013 premise is used.
- The complete run has `J=4`, `T=16`, `n_fin=5`, `nu_fin=9`, swept volume
  `114`, no intermediate emission, and one terminal return. Its vector is
  `(Theta(9),Theta(5),1,0,Theta(114),Theta(114),Theta(114),Theta(9),O(9),Theta(114),Theta(9))`.
- The proposition refutes only monotone held-face correction debt and the
  coefficient-one pointwise inequality `delta_t<=q^(-1)e_t`. It does not
  refute nonmonotone, phase-aware, or amortized potentials, the reviewed
  moving-maximum upper bound, logarithm removal, logarithm necessity, a
  matching lower bound, or any asymptotic extension of the finite trace.

## Controller adjudication

- Promote the response GO exactly as a promised-template scale certificate.
  It is constructive evidence that the Round-013 `EagerExactSlack` STOP cannot
  be widened to every template-aware or on-demand representation.
- Promote the hybrid GO only as a response-assisted numerical comparison at
  `k=1`. The paid exact margin response prevents any speedup or automatic-
  exploration interpretation, while the carried signed endpoint makes the
  numerical gate genuine.
- Promote the volume finite STOP, not an asymptotic path theorem. The logarithm
  remains unresolved, and the quarantined Round-013 lower candidate remains
  withdrawn.
- No new formal taxonomy dependency edge is warranted: all three arguments use
  direction-local results and shared contracts already recorded as source or
  provenance prerequisites.

## Independent review

- The response audit independently rederived the decomposition, entrywise
  Neumann argument, norm and scale constants, matching and tail inequalities,
  event logic, nonnegative and signed stream accounting, complete vector, and
  exclusions. It found the reconciled direction clean.
- The hybrid audit independently rederived the `m=2` and `m>2` four-row
  formulas, one-scan sharp margin, chronology, arenas, stage and oracle caps,
  response retirement, prefix/post rounds and emissions, peak composition,
  and total work. It confirmed the zero-start instance is numerical but
  response-assisted and found the reconciled direction clean.
- The volume audit used an independent dense exact-Fraction elimination replay.
  It reproduced admissions at stages `1,2,4,8`, held `U_3` at stages `6,7`,
  certification at stage `16`, both displayed rational inequalities, strict
  unclipping, `J=4`, `T=16`, `n_fin=5`, `nu_fin=9`, swept volume `114`, and the
  vector specialization. It found the restored direction clean.

## Redistribution messages

1. Response to hybrid: the fixed-template GO shows that a paid structural and
   scale certificate can replace eager cell maintenance, but only when order,
   amplitudes, and every future-label claim are verified. Treat a locally
   computed margin similarly as an explicit query/certificate cost, never as
   free state.
2. Hybrid to response: the `mu_1` result is a concrete on-demand pattern—scan
   exactly the live rows, compute one sharp scalar from paid response data,
   retire the response arena, and carry only the certified endpoint. It does
   not justify a general no-response reporter.
3. Volume to both: the moving global correction is neither pointwise monotone
   on a held face nor bounded by `q^(-1)` times the normalized face error with
   coefficient one. Do not use it as a decreasing debt or residual surrogate
   in response or signed-gate compositions.
4. All directions: the withdrawn path theorem may not be revived from roots or
   finite traces. A lower route still needs actual entry coefficients,
   anti-cancellation, and uniform projection/envelope validity.

## Metadata and shared-state changes

- This round record, `BROADCAST.md`, and the shared results ledger now record
  the three independently reviewed outcomes while preserving Round-013 history
  and the dependency/provenance contract.
- The shared results gain one narrowly scoped row per promoted GO/STOP. No
  literature fact, problem definition, or formal dependency edge changes.
- At 15:58 the host switched automatically from branch
  `agent/prove-aesp-cd-inner-oracle` at `513e848` to updated `main` at
  `14f3e60` after PR #26, preserving the uncommitted Round-014 direction work
  in stash `e47d263`, named
  `WIP Round 014 before switching to latest main (2026-08-21)`. The controller
  applied that stash non-destructively to current `main`; all 12 direction files
  were restored without conflict, the stash was retained, and both affected
  audits repeated their build, checker, and source checks on the restored
  files. This is operational provenance only and records no data loss or
  mathematical change.

## Validation

- Round-start `make test`: 187 passed with 15 known temporary-directory cleanup
  warnings.
- Round-start `make lint`: green.
- Round-start `make note-audit`: 18 notes across 5 tracks.
- Round-start `make note-graph`: 18 nodes and 26 acyclic formal edges.
- Round-start full `git diff --check`, conflict scan, and staging scan: green.
- Response direction: 73-page build; 100-trial focused checker; Ruff, inventory,
  graph, reference, whitespace, conflict, and scoped-diff checks green.
- Hybrid direction: 52-page build; exact four-row/stage-cap checker; Ruff,
  inventory, graph, reference, whitespace, conflict, and scoped-diff checks
  green. The independent audit repeated the restored-file checks.
- Volume direction: 37-page build; both exact finite checkers; Ruff, inventory,
  graph, reference, whitespace, conflict, and scoped-diff checks green. The
  independent audit repeated its exact replay and restored-file checks.
- Redistribution `make note-audit`: 18 notes across 5 tracks.
- Redistribution `make note-graph`: 18 nodes and 26 acyclic formal edges.
- Redistribution scoped whitespace, conflict-marker, staging, and diff-scope
  checks: green.

## Next round queue

1. Response: relax exactly one promised-template axis—event order, fragment
   amplitude, or one unverified label pairing—and either retain a universal
   future-safe output certificate with output-sensitive work or exhibit the
   first exact trace that forces a response query or broader state.
2. Hybrid: replace the separately charged exact four-coordinate response in
   the `mu_1` certificate by a strictly cheaper local one-sided certificate, or
   prove a named obstruction; then test whether the resulting policy explores
   to `Uhat_1` without pre-exposing the checkpoint.
3. Volume: formulate and test one explicit nonmonotone, phase-aware, or
   space--time block potential on the exact constant-ratio path. A renewed
   lower route must instead derive the actual full-face entry coefficients and
   anti-cancellation before invoking the conditional roots.
4. Preserve as separate future targets the new cyclic legality gadget and a
   same-task recurrence/response family that defeats both residual-slack
   spreading and singleton-basis synthesis.
