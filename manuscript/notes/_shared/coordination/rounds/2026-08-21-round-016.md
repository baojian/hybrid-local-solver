# Research round 016: repetition budgets, transported lower points, and constant-coefficient path banks

Date: 2026-08-21
Controller snapshot: updated `main` at `14f3e60` with reviewed Rounds 014--015
retained in the intentional dirty worktree; Round 015 was redistributed and
fully validated before this dispatch.
Round state: redistributed

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `response_preconditioned_hybrid` | Keep the frozen face/cut/ladder, verified pairing, common cross-stream schedule, unit logical amplitudes, fixed `n`, and exact-real model, but permit repeated labels under an explicit total-length or per-label multiplicity contract. Derive an output-sensitive future-safe reporter and scale condition, or give the first exact repetition obstruction requiring response/state. | `response_preconditioned_hybrid/` | GO: `thm:notched-sun-multiplicity-delta-reporter` proves that `NotchedSunMultiplicityDelta` handles every online-verified common permutation of a declared positive multiset. Its exact last-unseen scale preserves zero response, slack, and numerical-materialization work; first occurrences emit deltas and repeats do not. |
| `hybrid_aesp_locsor` | Extend the Stieltjes lower-point gate certificate beyond `k=1` and preserve numerical lower-point progress across face admissions instead of zero-resetting. Prove a charged transport/retraction invariant and direct continuation, or identify the smallest exact loss of feasibility/lowerness or forced settlement/private replay. | `hybrid_aesp_locsor/` | GO for coordinatewise lower-state transport: the padded lower point and anchored maximum remain below every successive exact restricted solution. For each fixed `2<=q<=m`, `TransportLower-BC-AESP_(0:q-1)` carries the lower vector through the first `q-1` admissions, restarts all AESP auxiliaries, and gives a response-free prefix followed by an explicitly paid native response on `Uhat_q`. |
| `volume_gated_acceleration` | On the actual `q=1/5` path chronology, derive the exact constraints imposed on every constant coefficient `c>=0` by held pairs `6->7` and `9->10` for `Phi_t(c)=delta_t+c q^(-1)e_t`. Prove a finite no-constant theorem if incompatible, or test the shortest block for the surviving interval. | `volume_gated_acceleration/` | STOP: `prop:path-no-constant-coefficient-bank` proves that the two exact held-pair half-lines are disjoint. No fixed `c>=0` makes the bank nonincreasing on both literal pairs, but this finite pairwise obstruction gives no block-horizon or asymptotic conclusion. |

## Direction handoffs

### `response_preconditioned_hybrid`

- Stable source labels: `thm:notched-sun-multiplicity-delta-reporter`,
  `eq:notched-sun-multiplicity-contract`,
  `eq:notched-sun-multiplicity-scale`,
  `eq:notched-sun-multiplicity-prefix`,
  `eq:notched-sun-multiplicity-certificate`,
  `eq:notched-sun-multiplicity-state`,
  `eq:notched-sun-multiplicity-fragments`, and
  `eq:notched-sun-multiplicity-eleven-vector`.
- Declare exact positive multiplicities
  `mu=(mu_1,...,mu_n)`, total accepted length `L=sum_i mu_i`, and
  last-unseen horizon `H=L-min_i mu_i`. The entries are both exact final
  multiplicities and online caps; a maximum multiplicity alone is not a
  substitute for `L` or `H`.
- The unrelaxed uniform-envelope condition is
  `18 H sqrt(vartheta)+3 vartheta<=1`, equivalently
  `0<vartheta<=(sqrt(81H^2+3)+9H)^(-2)`. It is the exact positive root of the
  displayed scalar Neumann-tail inequality and is sharp only for that uniform
  template envelope, not a generic reporter lower bound.
- Every stream must name the same verified label and its prescribed
  nonnegative-unit or signed `+2/-1` amplitude.
  The reporter atomically checks the common label/amplitude, total length, and
  per-label cap before incrementing one exact-cell counter. At a label's first
  occurrence its matching response crosses and the reporter emits `(q,w_j)`;
  at a repeat that label is already reported and no delta is emitted. Every
  still-unseen label has cumulative tail at most
  `9H vartheta^3/(1-3vartheta)<=g-m`, while an empty unseen complement receives
  the corresponding verified-vacuous certificate.
- The complete vector is
  `(Theta(n),2,L,Theta(n+J+p),Theta(C_frag+rL+n),0,0,Theta(n+J+p),O(1),0,Theta(rL))`.
  For nonnegative logical columns, `p=r` and `C_frag=pL=rL`; for separately
  checked signed `+2/-1` streams, `p=2r` and `C_frag=pL=2rL`. This charges
  `rL` certificate records and the `rn` first-occurrence deltas inside
  `Theta(rL)` emission.
- One counter per template label replaces the Round-015 seen bit without
  changing the linear persistent-state order. No response application,
  future-label scan, response/slack cell, or numerical materialization is
  stored or queried.
- The GO relaxes only declared bounded repetition. The frozen face, rank-`n`
  cut, complete ladder, pairing, unit logical amplitudes, common cross-stream
  schedule, complete petal coverage, fixed `n`, multiplicity-dependent
  near-one scale, and exact-real model remain promised. Arbitrary amplitudes
  or fragments, undeclared/unbounded repetitions, incomplete epochs,
  different per-column schedules, batches, template mutation, RPPR/KKT
  chronology, fixed-`alpha` uniformity as `L` grows, terminal solving, finite
  precision, and word/bit complexity remain outside scope.

### `hybrid_aesp_locsor`

- Stable source labels: `lem:branch-caterpillar-anchored-lower-transport`,
  `eq:branch-caterpillar-padded-lower-transport`,
  `eq:branch-caterpillar-anchored-lower-map`,
  `eq:branch-caterpillar-anchored-lower-invariant`,
  `eq:branch-caterpillar-transported-anchor-update`,
  `def:branch-caterpillar-transported-lower-policy`,
  `eq:branch-caterpillar-transported-lower-arenas`,
  `eq:branch-caterpillar-transported-lower-stage-bound`,
  `eq:branch-caterpillar-transported-lower-budgets`,
  `thm:branch-caterpillar-transported-lower-handoff`, and its prefix/post
  eleven-vectors.
- Principal Stieltjes face monotonicity gives
  `x_(k+1)|_(Uhat_k)>=x_k` coordinatewise. Hence zero-padding a charged lower
  point `y_k<=x_k` on the newly admitted three coordinates remains below
  `x_(k+1)` and preserves every old lower coordinate exactly.
- On a fixed face, the anchored map
  `T_(k,a)(z)=a vee L_(Uhat_k)(z)` remains between `a` and `x_k`, never
  decreases an old anchor coordinate, and gives conservative strict demands
  on every unique-parent tree-boundary row. The update
  `a_(k+1)=iota_k y_k` therefore transports a nonzero lower vector through all
  fixed canonical admissions without a restricted response or future-margin
  table.
- For every fixed `m>=2`, branch seed, `0<alpha<1/2`,
  `0<rho<rho_can(m,alpha)`, and `2<=q<=m`, the named policy certifies the
  first `q` batches, commits the first `q-1` without settlement, and stops
  before interaction `q` with
  `U_adm=U_num=Uhat_(q-1)`, `E_row=Uhat_q`, and
  `E_lab=Uhat_(min(q+1,m))`. Candidate rows are pre-exposed; every anchored
  test pays a full old-face sweep.
- Every new face starts a fresh AESP run at the transported nonzero anchor.
  Momentum, proximal, center, and estimate-sequence records are retired and
  restarted rather than transported. The online stopping rule observes only
  strict lower-demand signs; its finite analysis cap retains the unknown
  `mu_k`, initial gap, and explicit `log(1/(1-2alpha))` relative-oracle factor.
- With the budgets in
  `eq:branch-caterpillar-transported-lower-budgets`, the response-free prefix
  vector is
  `(V_q^can+O(A_<q^tr),q+1+O(A_<q^tr),q-1,0,O(H_<q^tr),O(A_<q^tr),0,O(C_q^can),O(C_(q-1)^can),O(D_<q^tr),Theta(q))`.
  Its `q-1` interactions emit exactly the first `3(q-1)` labels and one
  constant-size reply per interaction; internal lower tests emit nothing.
- The direct suffix reuses the retained rows, commits the certified `q`th
  batch, builds and settles the native response on `Uhat_q`, and continues
  canonically with vector
  `(vol(S* setminus Uhat_q),m-q,m-q+2,0,O(C(S*)),0,O(C(S*)+(m-q+1)log(2+m)),Theta(C(S*)),O(C(S*)),Theta(card(S*)),card(S* setminus Uhat_(q-1))+card(S*)+Theta(m-q+2))`.
  Thus total interactions remain exactly `m+1`, memory peaks are
  `O(C(S*))`, and total work is
  `O(H_<q^tr+C(S*)log(2+C(S*)))`.
- This is all-layer coordinatewise lower-state preservation and a direct exact
  continuation, not transported acceleration. It proves no AESP energy or
  momentum carryover, sublinear diagnostic, speedup, hidden-row exploration,
  margin-uniform or graph-uniform work, other range/seed/graph/policy,
  finite-precision/bit claim, PPR statement, or RPPR-to-PPR conversion.

### `volume_gated_acceleration`

- Stable source labels: `prop:path-no-constant-coefficient-bank`,
  `eq:path-constant-coefficient-bank`,
  `eq:path-constant-coefficient-stage-seven-constraint`,
  `eq:path-constant-coefficient-stage-seven-lower`,
  `eq:path-constant-coefficient-stage-ten-constraint`,
  `eq:path-constant-coefficient-stage-ten-upper`,
  `eq:path-constant-coefficient-gap`, and
  `eq:path-no-constant-bank-stop-eleven-vector`.
- On each currently held face define
  `Phi_t(c;U)=delta(x^(t))+c q^(-1)e_t(U)` for fixed `c>=0`, with
  `e_t(U)=||D_U^(-1/2)(x^(t)-x_U^*)||_infinity`. The restricted optimum is
  reset after the literal stage-8 admission; errors are never compared across
  that face transport.
- On the actual `q=1/5` held `U_3` pair `6->7`, exact affine algebra gives
  nonincrease iff
  `c>=c_6,7=2978273417354/42112483166425`. After the face-local reset, the
  actual held `U_4` pair `9->10` gives nonincrease iff
  `c<=c_9,10=95554102960761584/1567701294665491845`.
- These half-lines are incompatible because
  `c_6,7-c_9,10=129004507967154213801509972186/13203958876316640794781123060825>0`.
  Hence every fixed nonnegative coefficient makes at least one of the two
  literal consecutive held pairs strictly increase the bank.
- The unchanged complete run has admissions at stages `1,2,4,8`, terminal
  certification at stage `16`, `J=4`, `T=16`, `n_fin=5`, `nu_fin=9`, swept
  volume `114`, no intermediate external emission, and one terminal return.
  Its full vector is
  `(Theta(9),Theta(5),1,0,Theta(114),Theta(114),Theta(114),Theta(9),O(9),Theta(114),Theta(9))`.
- This is a finite exact-real pairwise representation STOP for one constant
  family, literal phase/order, complete gate, and face-local reset convention.
  It proves no global block horizon, stage/work lower bound, logarithm
  necessity, spectral claim, or asymptotic result. It does not exclude a
  time- or face-dependent coefficient, multi-step or signed-phase bank,
  nonlocal space--time sum, locally rising amortized potential, literal
  zero-padded momentum, or nonpath route. In particular it does not refute the
  hybrid coordinatewise lower-point invariant, whose accelerated auxiliary
  state is explicitly restarted.

## Controller adjudication

- Promote `NotchedSunMultiplicityDelta` only as a declared-positive-multiset
  extension of the frozen promised-template GO. `L` controls interactions and
  fragment work; `H` controls the last-unseen tail. The scale's sharpness is
  restricted to the displayed uniform Neumann envelope.
- Promote the branch-caterpillar result as a coordinatewise lower-vector
  transport invariant and a response-free prefix/paid-response suffix for
  each fixed `2<=q<=m`. Do not describe restarted AESP auxiliaries as carried
  acceleration, omit the full old-face sweeps, or hide the native `Uhat_q`
  response.
- Promote the path result only as a finite incompatibility of two exact local
  constant-coefficient constraints. It neither removes nor proves necessary
  the logarithm and does not extend to variable coefficients, blocks, or
  aggregate potentials.
- No new formal taxonomy dependency edge is warranted: all three proofs use
  direction-local predecessors plus already recorded shared/source contracts.

## Independent review

- The response audit independently rederived `H=L-min_i mu_i` and its
  attainment, the scalar-envelope equivalence and exact positive root, the
  envelope-only sharpness qualifier, first-occurrence/repeat/certificate
  semantics, atomic common-stream counts, nonnegative and signed fragment
  totals, every eleven-vector coordinate, and all exclusions. It returned
  clean and required no edit. It also reran the seeded multiplicity checker
  and forced the 76-page direction build; only the known font/float warnings
  remained.
- The hybrid owner delegated a nested independent read-only audit. That audit
  rederived the all-face lower-map algebra, principal-face monotonicity,
  zero-padding and anchored-maximum invariants, finite stopping caps, the
  `q=m` edge, every prefix/post coordinate, round and emission count,
  response-free prefix and paid `Uhat_q` suffix, full-sweep charges, restarted
  AESP state, and absence of hidden response/energy carryover. The Round-016
  checker, Ruff checks, 61-page build, note inventory, and scoped diff were
  clean; no repair was required.
- The volume audit used an independent exact-rational replay. It reproduced
  both held faces, the stage-8 admission/reset, the exact affine slopes,
  lower and upper coefficient thresholds, positive gap, unchanged chronology,
  every vector coordinate, and finite pairwise scope. The supplied exact
  checker and 41-page build also passed. It returned clean and made no edit.

## Redistribution messages

1. Response to hybrid and volume: repeated accepted events must be charged by
   total stream length `L`, not only by distinct labels `n`; the count state
   can suppress repeated delta output only because the full template,
   amplitudes, common schedule, and last-unseen envelope are verified. Do not
   infer a generic scalar correction potential or free repeated certificate.
2. Hybrid to response and volume: coordinatewise lower state can survive a
   face admission while every accelerated auxiliary record is restarted. A
   valid transport claim must name exactly which state crosses the boundary
   and separately charge candidate-row exposure, full lower sweeps, and the
   eventual response; it supplies no generic response-free acceleration.
3. Volume to both: no fixed nonnegative coefficient makes
   `delta+c q^(-1)e` decrease on both reviewed held pairs. This rules out that
   scalar bank family only and does not conflict with the hybrid monotone
   lower-vector invariant or the response template's order/multiplicity
   certificate.
4. All directions: distinguish event multiplicity from distinct output,
   coordinatewise invariant state from accelerated state, and pairwise local
   monotonicity from a global amortized block. Preserve the exact access,
   chronology, scale, and eleven-vector qualifiers when reusing any result.

## Metadata and shared-state changes

- This round record, `BROADCAST.md`, and the shared results ledger record the
  three independently reviewed outcomes and their cross-implications.
- The shared results gain one narrowly scoped row per promoted GO/STOP; the
  response, acceleration, and hybrid frontier text is narrowed accordingly.
- Direction notes retain their stable theorem/equation labels and reproducible
  checkers. The controller reconciliation synchronizes the root notes index,
  taxonomy, manifest, and three research-roadmap documents without changing
  the related-work ledger, shared problem definition, or formal dependency
  arrays. No new edge is added.

## Validation

- Round-start `make test`: 187 passed with 15 known temporary-directory cleanup
  warnings.
- Round-start `make lint`: green.
- Round-start `make note-audit`: 18 notes across 5 tracks.
- Round-start `make note-graph`: 18 nodes and 26 acyclic formal edges.
- Round-start full `git diff --check`, conflict scan, and staging scan: green.
- Response direction: seeded 100-trial multiplicity checker passed through
  total length `94`; 76-page build completed with only known font/float
  warnings, and focused Ruff, label, conflict, inventory, and diff checks were
  green. The independent audit rederived the theorem and vector without edit.
- Hybrid direction: exact rational checker passed for `m=2,...,7` and
  `alpha in {1/20,1/5,49/100}`; 61-page build had no undefined references or
  new overfull boxes, and focused Ruff/inventory/diff checks were green. The
  nested independent audit returned clean.
- Volume direction: exact `Fraction` checker reproduced both coefficient
  half-lines and the positive gap; all four finite checkers, 41-page build,
  Ruff, graph/inventory, conflict, and scoped diff checks were green. The
  independent exact-rational audit returned clean.
- Redistribution reran all three Round-016 focused checkers successfully.
- Redistribution `make note-audit`: 18 notes across 5 tracks.
- Redistribution `make note-graph`: 18 nodes and 26 acyclic formal edges.
- Redistribution scoped whitespace, conflict-marker, staging, and edit-scope
  checks: green; no file was staged.
- The controller removed one accidental empty root-level `=2` shell-redirection
  artifact. A later promotion search briefly recreated the same empty path; it
  was removed immediately. The final root-artifact and staging scans found only
  expected repository files and no staged path.

## Next round queue

1. Response: relax exactly one remaining stream promise--declared unit
   amplitude, a common per-column schedule, or singleton event batches--while
   retaining a charged future-safe certificate, or exhibit the first exact
   response/state obstruction. Any amplitude route must make total absolute
   event mass, multiplicity, capacity, and fragment charges explicit.
2. Hybrid: determine whether the all-layer anchored lower map admits a
   sublinear incremental diagnostic, or give an exact family forcing repeated
   old-prefix sweeps. A constructive route must additionally transport or
   amortize the AESP face-admission shock; preserving only the lower vector is
   now closed.
3. Volume: test the shortest explicit time-/face-dependent, signed-phase, or
   multi-step bank on the literal chronology, or derive actual full-face entry
   coefficients with uniform anti-cancellation and projection/envelope
   margins. No fixed nonnegative coefficient should be retried.
4. Preserve separately a generic partial reporter, shock-aware accelerated
   transport, and a global path potential. The three Round-016 finite/fixed-
   family results do not compose into a graph-uniform PPR theorem.
