# Research round 005: test trajectory relevance and handoff costs

Date: 2026-08-21
Controller snapshot: all three Round-005 outcomes have focused builds,
independent proof/scope audits, corrected ledgers, and reconciled direction
summaries on the live dirty worktree; no staging, commit, push, rename, or
folder move is authorized.
Round state: redistributed

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `propagate_settle_framework` | On the existing unweighted notched double sun, either give explicit single-seed RPPR parameters whose canonical faces realize a linear sequence of distinct petal admissions while report leaves remain outside the active face through one epoch, or prove the first exact KKT/symmetry/bulk-admission contradiction. | `propagate_settle_framework/` | Proved the exact twin-demand/co-admission obstruction; independently reviewed and corrected. |
| `evolving_support_cg` | On the endpoint-seeded path at `alpha=n^-2`, prove or refute that literal exact CG with genuinely supported row products needs all `n` iterations before its actual degree-normalized PPR residual certificate holds; give the complete sparse-work and eleven-coordinate ledger. | `evolving_support_cg/` | Proved earliest certificate `K=n` and a `Theta(n^2)` chosen literal ledger; independently reviewed. |
| `hybrid_aesp_locsor` | On the center-seeded unweighted three-arm spider, for `0<alpha<1` and `0<rho<1/3` in exact real-cell arithmetic, prove a fully charged conversion from any finite gate-compatible Phase-I prefix into the common affine activation-token response with incremental cost `O(C(S*(rho)))`, or exhibit the smallest signed handoff/order forcing replay, repeated boundary scans, or uncharged conversion. The note's particular composite Catalyst/AESP prefix is restricted to `alpha<1/2`. | `hybrid_aesp_locsor/` | Proved one-pass common-state conversion after any fully charged gate-compatible prefix; product scale remains conditional; independently reviewed. |

## Direction handoffs

- `propagate_settle_framework`: `prop:notched-sun-rppr-trace-obstruction`
  proves that every unseeded `(f_i,w_i)` pair has equal exact demand at a
  common anchor-containing face and is co-admitted by the canonical
  all-positive gate. At most one seed pair is exceptional. The structural
  `F`-only epoch is therefore not a canonical single-seed RPPR trace while
  `W intersect U = empty`. The finite-band consequence is only for one
  simultaneous uniform point-estimate call at one common face; arbitrary
  asynchronous interval refinement remains open.
- `evolving_support_cg`: `thm:endpoint-path-exact-cg-certificate` proves by
  unreduced-tridiagonal Krylov/Galerkin and continuants that, for
  `alpha_n=n^-2` and `eps_ppr=1/10`, exact ordinary CG from zero has a
  frontier-singleton residual, a full-prefix live direction, and first
  certifies at `K=n`. The selected sequential rereading/materialized
  implementation has all eleven coordinates reported and costs
  `Theta(n^2)=Theta(nu_n/sqrt(alpha_n))`.
- `hybrid_aesp_locsor`: `thm:three-arm-composite-response-handoff` discards
  signed Phase-I numerical/momentum state on the center-seeded unweighted
  three-arm spider, for `0<alpha<1` and `0<rho<1/3` in exact real-cell
  arithmetic, retains the paid irreversible arm prefixes, and constructs the
  common affine response in one charged pass. The note's particular composite
  Catalyst/AESP prefix requires `alpha<1/2`. Post-work is
  `O(C(S*(rho)))`, total work is
  `B_J^full+O(C(S*(rho)))`, and
  `cor:three-arm-composite-product-conditional` reaches the product scale only
  under an independent fully charged bound on `B_J^full`.

## Controller adjudication

All three scoped results are promoted to the accumulated-results ledger and
the verified broadcast. The first retires only the symmetric notched-sun
trajectory witness, not its structural response audit or any reporter class.
The second is an exact-CG/selected-implementation calibration, not a
recurrence-class lower bound or finite-precision claim. The third is a
one-branch exact-RPPR handoff whose accelerated scale remains conditional on
the separately charged prefix.

## Redistribution messages

- Trace realizability: the symmetric notched-double-sun `F`-only epoch cannot
  supply a canonical single-seed RPPR trace. Future cyclic families must first
  prove an asymmetric legal gate chronology. The simultaneous point-estimate
  consequence does not restrict asynchronous interval refinement, and the
  high-rank structural representation results remain valid.
- Supported CG: a frontier-singleton residual can coexist with a full-prefix
  live direction. The selected literal exact-CG implementation meets the
  product scale, but no class lower bound follows for arbitrary supported
  polynomials, implicit/rational responses, prefetching, or finite precision.
- Hybrid handoff: irreversible gate admissions, not signed trial support, are
  the reusable common state. Discarding numerical energy permits a one-pass
  three-arm conversion, but the total is only `B_J^full+O(C(S*))` until an
  independent theorem bounds the fully charged prefix. No shock-free energy
  carryover is implied.

## Metadata and shared-state changes

- Results ledger: three reviewed Round-005 rows added with their exact
  trajectory, implementation, and conditional boundaries.
- Broadcast: active Round-005 assignment replaced by verified redistribution
  and a reconciled next-priority queue.
- Related work, root notes README, taxonomy, and manifest were synchronized
  with the reviewed outcomes. The formal taxonomy dependency
  `hybrid_aesp_locsor -> adaptive_revisit_control` was added.
- No direction was created, moved, renamed, staged, committed, or pushed.

## Validation

- Round-start status cards, shared memory, and the relevant proof owners were
  read from the live worktree.
- `make note-audit`: passed with 18 notes across 5 tracks immediately before
  dispatch.
- Full tests: 182 passed with 15 temporary-directory cleanup warnings.
- Lint and repository diff checks: passed.
- Concurrent-work caveat: every worker must re-read live files immediately
  before editing and preserve unrelated changes.
- Round-end direction builds passed. Independent audits rederived the
  twin-demand/seed cases and point-estimate boundary, the exact-CG
  Krylov/continuant residual and eleven-coordinate ledger, and the three-arm
  common-state conversion/resource accounting.
- Review fixes use active-face rather than graph-exterior terminology, separate
  simultaneous point estimates from asynchronous intervals, identify
  `tau_fb` as a structural pruning threshold, give separate fresh/stored
  eleven-coordinate trace-audit ledgers, remove mandatory scan/output claims,
  and retain every prefix charge. The CG and handoff scopes were found sound.
- Repository note audit and scoped shared-file diff check passed after
  promotion.
- Final controller validation: 182 tests passed with 15 temporary-directory
  cleanup warnings; lint and formatting were clean; `make note-audit` reported
  18 notes across 5 tracks; the full diff check, focused direction builds, and
  log/reference scan were clean.

## Next round queue

1. Construct an asymmetric nonequitable cyclic family and prove its canonical
   single-seed RPPR admission chronology before analyzing a finite-band
   reporter; keep asynchronous interval refinement live.
2. Extend common-state handoff to the smallest tree with two branching
   vertices, while separately seeking a fully charged bound on the Phase-I
   prefix `B_J^full`.
3. Separate a supported growing-prefix recurrence class from fully charged
   response methods on one common task, or build the guarded exact-CG baseline
   using the endpoint path as its residual/work regression case.
