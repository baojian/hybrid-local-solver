# Research round 018: asynchronous amplitudes, carried lower state, and adjacent Schur-bank steps

Date: 2026-08-21
Controller snapshot: `main` at `14f3e60`; reviewed Rounds 014--017 remain in
the intentional dirty worktree, Round 017 was redistributed and fully
validated before this dispatch, and recovery stash `e47d263` remains retained.
Round state: redistributed

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `response_preconditioned_hybrid` | Relax exactly the common logical-column schedule in `NotchedSunAmplitudeDelta`: allow each logical column to declare its own finite positive label/amplitude schedule, with the two physical streams of a signed column still paired. Accept an arbitrary asynchronous interleaving of those declared column events. Either prove first-certified equals first-exact crossing with all per-column mass, fragment, state, interaction, and emission charges, or return the first exact cross-column obstruction. Keep the frozen face/cut/ladder/pairing, fixed `n`, declared bounded positive mass, and exact-real model. | `response_preconditioned_hybrid/` | GO: `thm:notched-sun-columnwise-amplitude-delta-reporter` proves that `NotchedSunColumnAmplitudeDelta` handles arbitrary one-column-at-a-time interleavings of independently declared positive column schedules. Each event emits one affected-column certificate; every cell emits one delta at its first exact crossing. Other-column events enlarge only global latency and do not enter the local response-mass horizon. |
| `hybrid_aesp_locsor` | Test whether `ImplicitLowerHeap` and the transported lower anchor can be carried across each canonical face expansion without a fresh full old-face raw-product/heap initialization or eager copy of every old anchor coordinate. Charge the three new rows, local residual/key changes, persistent/versioned anchor representation, candidate exposure, AESP endpoint products, and all eleven coordinates. Prove a named incremental face-transition construction or a representation-specific obstruction with an implicit comparator; do not hide fresh accelerated-state or stage work. | `hybrid_aesp_locsor/` | Diagnostic-transition GO with a surviving restart shock: `lem:branch-caterpillar-lazy-anchor` and `lem:branch-caterpillar-incremental-face-transition` give lazy range tags plus a three-key `FaceCarryLowerHeap` expansion with no old-row read, old-key rebuild, or old-anchor copy. `thm:branch-caterpillar-incremental-face-handoff` composes all `q` expansions, while the named `DenseFreshAESP` host still writes `q(3q-1)/2` fresh auxiliary records. An independent audit found and repaired one adjacency-round wording contradiction; the repaired ledger has exact `R_int=m+1` but `R_adj=m+1+O(A_fc)`. No speedup follows. |
| `volume_gated_acceleration` | Extend the exact finite `TightPair-Schur_(3->4)` audit beyond the isolated frozen-candidate reset: separately include the actual fixed-face step producing the stage-8 candidate, the stage-8 reset, and the first post-reset step to stage 9. Determine whether the same squared bank plus remaining-optimum-drop reserve is nonincreasing across the complete local chain, or give the first exact rational failure. Keep literal order, face-dependent coefficients, and reset accounting separate; make no global or asymptotic inference. | `volume_gated_acceleration/` | Finite local-chain STOP: `prop:path-tight-pair-local-chain-stop` proves that at the canonical tight pair the fixed-`U_3` production step rises, the isolated reset still decreases by the exact Schur slack, the first fixed-`U_4` follow-up rises, and the net stage-7--9 change is positive. Both adjacent failures hold for every coefficient pair in the held-pair-feasible rectangle, but the net sign is not uniform there. The complete vector is unchanged. |

## Direction handoffs

### `response_preconditioned_hybrid`

- Stable source labels:
  `thm:notched-sun-columnwise-amplitude-delta-reporter`,
  `eq:notched-sun-columnwise-amplitude-schedule`,
  `eq:notched-sun-columnwise-amplitude-totals`,
  `eq:notched-sun-columnwise-amplitude-capacity`,
  `eq:notched-sun-columnwise-amplitude-crossing-index`,
  `eq:notched-sun-columnwise-amplitude-gap`,
  `eq:notched-sun-columnwise-amplitude-event-horizons`,
  `eq:notched-sun-columnwise-amplitude-mass-horizon`,
  `eq:notched-sun-columnwise-amplitude-margin`,
  `eq:notched-sun-columnwise-amplitude-event`,
  `eq:notched-sun-columnwise-amplitude-prefix-state`,
  `eq:notched-sun-columnwise-amplitude-response`,
  `eq:notched-sun-columnwise-amplitude-certificate`,
  `eq:notched-sun-columnwise-amplitude-reporter-state`,
  `eq:notched-sun-columnwise-amplitude-fragments`,
  `eq:notched-sun-columnwise-amplitude-eleven-vector`, and
  `eq:notched-sun-columnwise-amplitude-prefix-envelope`.
- For every logical column `q` and label `i`, independently declare positive
  occurrence amplitudes `a_(q,i,k)`, multiplicity `mu_(q,i)`, prefix mass
  `A_(q,i,k)`, cell mass `A_(q,i)`, column length
  `L_q=sum_i mu_(q,i)`, and column mass `A_q=sum_i A_(q,i)`. Put
  `L_Sigma=sum_q L_q` and `A_Sigma=sum_q A_q`. The external stream is any
  one-column-at-a-time interleaving of all `L_Sigma` declared events.
- Retain
  `lambda_i=vartheta^2/[d_(u_i)(1+c_eta)]`,
  `epsilon=9vartheta^3/(1-3vartheta)`, gate `g=vartheta^(5/2)`, and structural
  capacity
  `kappa_(q,i)=g/lambda_i=d_(u_i)(1+c_eta)sqrt(vartheta)`. Let
  `k*_(q,i)=min{k:A_(q,i,k)>kappa_(q,i)}` and
  `gamma_(q,i)=kappa_(q,i)-A_(q,i,k*_(q,i)-1)>0`.
- The local and global event horizons and the response-relevant mass horizon
  are
  `Hloc_(q,i)=L_q-mu_(q,i)+k*_(q,i)-1`,
  `Hasync_(q,i)=L_Sigma-mu_(q,i)+k*_(q,i)-1`, and
  `F_(q,i)=A_q-A_(q,i)+A_(q,i,k*_(q,i)-1)`. Other-column events contribute
  `L_Sigma-L_q` only to `Hasync_(q,i)`: they do not change response column `q`
  and therefore do not enter `F_(q,i)`.
- The exact per-column test is
  `m_q=min_i{lambda_i gamma_(q,i)-epsilon F_(q,i)}>0`, with
  `m_Sigma=min_q m_q>0`. Every unreported cell, whether unseen or already seen
  but below capacity, is then at most `g-m_q`; occurrence `k*_(q,i)` is its
  first certified and first exact strict crossing. Every fixed finite family of
  positive schedules passes at sufficiently small positive `vartheta`. A failed
  displayed margin rejects only this scalar certificate.
- Each accepted event updates one cell counter and emits one universal
  certificate for the affected column only. Unchanged columns retain their
  previous certificates without re-emission. The full epoch therefore emits
  exactly `L_Sigma` certificate records and exactly `rn` delta records. In the
  nonnegative interface `p=r`, `C_frag=L_Sigma`, and absolute physical mass is
  `A_Sigma`; in the atomically paired signed `2`-minus-`1` interface `p=2r`,
  `C_frag=2L_Sigma`, and absolute physical mass is `3A_Sigma`.
- Before simplifying with `L_Sigma>=rn`, the complete vector is
  `(Theta(n),2,L_Sigma,Theta(n+J+p+L_Sigma),Theta(C_frag+L_Sigma+rn+n),0,0,Theta(n+J+p+L_Sigma),O(1),0,Theta(L_Sigma+rn))`.
  Its canonical simplified form is
  `(Theta(n),2,L_Sigma,Theta(n+J+p+L_Sigma),Theta(C_frag+L_Sigma),0,0,Theta(n+J+p+L_Sigma),O(1),0,Theta(L_Sigma))`.
  It stores all declared schedules and counters, but no response, cumulative
  response, slack, recurrence, or numerical-materialization state.
- This GO relaxes only the common logical-column schedule. It keeps the frozen
  graph, face, cut, ladder, label pairing, fixed `n`, complete label coverage,
  declared finite positive mass, exact-real arithmetic, and atomic signed
  pairing within each column. Undeclared or unbounded mass, arbitrary signed
  logical amplitudes, signed pairs split across interactions, simultaneous
  batches, arbitrary fragment support, incomplete labels, template/ladder
  mutation, RPPR chronology, fixed-`alpha` uniformity, terminal solving,
  finite precision, rounding, word-RAM, coefficient-bit, and PPR claims remain
  excluded.

### `hybrid_aesp_locsor`

- Stable source labels:
  `sec:branch-caterpillar-incremental-face-transition`,
  `eq:branch-caterpillar-lazy-anchor-state`,
  `lem:branch-caterpillar-lazy-anchor`,
  `eq:branch-caterpillar-lazy-anchor-new-records`,
  `eq:branch-caterpillar-lazy-anchor-transition`,
  `lem:branch-caterpillar-incremental-face-transition`,
  `eq:branch-caterpillar-incremental-face-residual`,
  `eq:branch-caterpillar-incremental-face-transition-vector`,
  `prop:branch-caterpillar-fresh-auxiliary-shock`,
  `eq:branch-caterpillar-fresh-auxiliary-shock`,
  `def:branch-caterpillar-incremental-face-policy`,
  `eq:branch-caterpillar-incremental-face-arenas`,
  `eq:branch-caterpillar-incremental-face-stopping-time`,
  `eq:branch-caterpillar-incremental-face-stage-bound`,
  `eq:branch-caterpillar-incremental-face-budgets`,
  `thm:branch-caterpillar-incremental-face-handoff`,
  `eq:branch-caterpillar-incremental-face-prefix-eleven-vector`,
  `eq:branch-caterpillar-incremental-face-post-eleven-vector`,
  `eq:branch-caterpillar-incremental-face-total-rounds`, and
  `eq:branch-caterpillar-incremental-face-total-work`.
- The exact lazy anchor stores every successful heap maximum `delta_t` in an
  append-only range-minimum structure. Each coordinate stores a resolved lower
  value `bar(a)_i`, its current numerical value `z_i`, and a marker `sigma_i`.
  Pending tags represent
  `Phi_i(s,k)=[z_i-sqrt(d_i) min_(s<t<k) delta_t]_+`, or zero for an empty
  range, and `a_i^lazy=max{bar(a)_i,Phi_i(sigma_i,k)}`. A coordinate is flushed
  before its next numerical write. At admission, one new scalar tag and three
  zero anchor/numerical records implement exactly
  `a_(k+1)=iota_k(a_k vee L_(Uhat_k)(z_k))` without visiting any old anchor
  coordinate.
- Zero-padding the successful signed endpoint leaves every old residual and
  heap key unchanged:
  `r_i^+=r_i` for `i in Uhat_k`, while each of the three new rows has
  `r_v^+=ell_(rho,v)+beta_(k,v) z_(p_k(v))`. The already retained candidate
  rows therefore determine exactly three new keys
  `[-r_v^+]_+/(alpha sqrt(d_v))`. `FaceCarryLowerHeap` performs no old-row read,
  old residual rekey, fresh old-face product, or eager old-anchor copy.
- Given the retained candidate rows, one face transition has vector
  `(0,0,0,0,O(log(2+C_(k+1)^can)),O(1),0,Theta(C_(k+1)^can),O(1),O(log(2+C_(k+1)^can)),0)`.
  The persistent-memory entry is the peak state after the transition rather
  than an allocation of that size; the materialization entry includes the
  three numerical/residual/key records, anchor markers, heap, and range-tree
  writes. Unexposed candidate rows retain their separate charged exposure.
- For the named `DenseFreshAESP` host, the fresh momentum, proximal, center,
  and estimate-sequence arrays write
  `Xaux_<q=sum_(k=0)^(q-1)|Uhat_k|=q(3q-1)/2` coordinate records, up to a fixed
  number of arrays. This is a definition-level identity for that dense host,
  not a lower bound for a lazy or different accelerated method. The carried
  lower diagnostic itself adds only `3q` residual/key records, `3q` zero
  numerical/anchor records, and `q` scalar tags.
- For fixed `m>=2`, branch seed, `0<alpha<1/2`,
  `0<rho<rho_can(m,alpha)`, and `2<=q<=m`,
  `FaceCarryLowerHeap-BC-AESP_(0:q)` commits all first `q` canonical batches
  and carries the exact residual, heap, signed point, and lower anchor through
  all `q` expansions, including `q=m`. Its response-free prefix vector is
  `(V_q^can+O(A_<q^fc),q+1+O(A_<q^fc),q,0,O(H_<q^fc),O(A_<q^fc+Q_<q^fc+q),0,O(C_q^can),O(C_(q-1)^can),O(D_<q^fc),3q+Theta(q))`.
  Candidate-row exposure, every numerical stage/local update, all missing bulk
  endpoint products, and all fresh auxiliary arrays remain charged inside the
  displayed budgets.
- The paid direct-response/canonical suffix has vector
  `(vol(S* setminus Uhat_q),m-q,m-q+1,0,O(C(S*)),0,O(C(S*)+(m-q)log(2+m)),Theta(C(S*)),O(C(S*)),Theta(card(S*)),card(S* setminus Uhat_q)+card(S*)+Theta(m-q+1))`.
  Total interactions are exactly `R_int=m+1`, whereas the full adjacency-round
  coordinate is `R_adj=m+1+O(A_<q^fc)`. Only the first-exposure component of
  adjacency rounds is exactly `m+1`; numerical endpoint products remain
  charged. Both final memory peaks are `O(C(S*))`, and exact total work is
  `O(H_<q^fc+C(S*)log(2+C(S*)))`.
- This exact-real, fixed-`m`, branch-seeded, strict-range RPPR result removes the
  Round-017 raw-product and eager-anchor-copy transition shocks. It does not
  remove candidate-row pre-exposure, dense AESP initialization, numerical
  stages, missing endpoint products, analysis margins, or the relative-oracle
  logarithm. No momentum, proximal, center, estimate-sequence energy, or
  objective-gap certificate crosses admission; `Xaux_<m=Theta(m^2)`. It proves
  no speedup, subquadratic growing-face prefix, scalar energy/potential
  monotonicity, other-range/seed/policy/graph result, graph-uniform work,
  finite precision, bit complexity, PPR, or RPPR-to-PPR conversion.

### `volume_gated_acceleration`

- Stable source labels: `prop:path-tight-pair-local-chain-stop`,
  `eq:path-tight-pair-local-chain-chronology`,
  `eq:path-tight-pair-local-chain-components`,
  `eq:path-tight-pair-local-chain-bank-values`,
  `eq:path-tight-pair-production-stop`,
  `eq:path-tight-pair-local-chain-reset-go`,
  `eq:path-tight-pair-followup-stop`,
  `eq:path-tight-pair-local-chain-net-stop`,
  `eq:path-tight-pair-production-coefficient-stop`,
  `eq:path-tight-pair-followup-coefficient-stop`, and
  `eq:path-tight-pair-local-chain-eleven-vector`.
- Retain the exact `q=1/5` run, the tight held-pair coefficients
  `c_3=2978273417354/42112483166425` and
  `c_4=95554102960761584/1567701294665491845`, scalar bank
  `b=delta+c_j q^(-1)e`, and reserve `H_3=Delta_8`, `H_4=0`. Keep separate the
  literal chronology
  `(7,U_3,c_3,H_3)->(8^-,U_3,c_3,H_3)->(8^+,U_4,c_4,H_4)->(9,U_4,c_4,H_4)`:
  fixed-`U_3` candidate production, frozen-candidate reset, and first fixed-
  `U_4` follow-up are three different transitions.
- For `Psi=b^2+H_j`, production already fails before the paid reset:
  `Psi_8^- - Psi_7 = 4798070852000000223973860697504672676758942656 / 1000717813631998065466639280969584524631500244140625 > 0`.
  The reset remains a strict GO by the exact Round-017 slack:
  `Psi_8^+ - Psi_8^- = -13153352075844141929930465447315402255043873003664815010264484652155446410873317194853 / 5858303218542070115616289966440099116412997639614861194042012716938884968578815460205078125 < 0`.
- The first follow-up also fails:
  `Psi_9 - Psi_8^+ = 185418883451039233414483427688468628491104384871211336448 / 2666939846939373614068372111818330675602595627307891845703125 > 0`.
  Consequently the complete stage-7--9 comparison is positive:
  `Psi_9-Psi_7 = 212119118576066028022993097362480113913974152962498585225695096903541477328114649390919 / 2943060211404325822565068748572471445947270419141748880957478373466923217833042144775390625 > 0`.
- The STOP is uniform over the held-pair-feasible rectangle. Production
  nonincrease would require
  `c_3<=2103479690463/41819574955745<c_3^tight`, whereas held-pair feasibility
  requires `c_3>=c_3^tight`. Follow-up nonincrease would require
  `c_4>=2617155474971384896/14508305905763575885>c_4^tight`, whereas feasibility
  requires `0<=c_4<=c_4^tight`. The exact positive gaps are recorded in the
  two coefficient-stop equations. Every feasible pair still passes the reset
  because the tight endpoints maximize its squared jump, but every such pair
  fails both adjacent fixed-face comparisons.
- The complete run remains `J=4`, `T=16`, `n_fin=5`, `nu_fin=9`, swept volume
  `114`, no intermediate external emission, and one terminal return. The
  canonical vector is unchanged:
  `(Theta(9),Theta(5),1,0,Theta(114),Theta(114),Theta(114),Theta(9),O(9),Theta(114),Theta(9))`.
- This is finite exact-real local-chain evidence for one named `q=1/5` path,
  literal phase origin/order, face-local restricted-optimum error, and the
  held-pair-feasible coefficient rectangle. It refutes pointwise monotonicity
  of this squared bank plus the sole remaining-optimum-drop reserve. It does
  not refute a bank allowed to rise locally, an added production reserve,
  signed phase terms, cross-state cancellation, a longer/nonlocal space--time
  bank, or another coefficient rule. It proves no all-held-pairs, global,
  multi-admission, block-horizon, logarithm, asymptotic, nonpath, alternate-
  order, intermediate-return, finite-precision, or bit result.

## Controller adjudication

- Promote `NotchedSunColumnAmplitudeDelta` only as an independent-declared-
  column-schedule extension of the frozen promised-template reporter. The
  proof separates local response mass from global asynchronous event latency
  because unrelated events provably leave the response column unchanged. It
  does not justify the same separation under cross-column coupling, batches,
  undeclared mass, or a shared transition state. Keep one certificate per
  event, exactly `rn` deltas, fragment mass, and schedule storage explicit.
- Promote `FaceCarryLowerHeap` as an exact transition implementation for the
  carried lower diagnostic. It removes old-face raw-product/key rebuilds and
  eager anchor copies at admission, but it transports no accelerated
  auxiliary. `DenseFreshAESP` remains a named representation with
  `q(3q-1)/2` fresh records, and the theorem is no speedup. The audit repair is
  authoritative: `R_int=m+1`, while the full `R_adj` ledger retains
  `O(A_<q^fc)` numerical rounds and is not exactly `m+1`.
- Promote `prop:path-tight-pair-local-chain-stop` as a finite local STOP on
  both sides of the still-paid reset. Do not merge production, reset, and
  follow-up; do not promote the reset-local GO to a stagewise or global
  potential; and do not infer logarithm necessity or an asymptotic lower bound.
- No new formal taxonomy dependency edge is warranted. The cross-direction
  comparisons are accounting analogies and use only already recorded
  shared/source contracts, not new theorem dependencies.

## Independent review

- The response audit independently rederived the local column envelope,
  capacities, `F_(q,i)`, `Hloc_(q,i)`, `Hasync_(q,i)`, and every unseen or
  seen-but-unreported first-certified/first-exact crossing. It checked equality
  cases, atomic signed pairing, exactly `L_Sigma` certificates and `rn` deltas,
  fragment counts, physical masses, all eleven coordinates, and every scope
  exclusion. The official checker covered `2,304` exhaustive count states,
  `12,288` legal transitions, and 100 randomized asynchronous families; a
  supplemental `r=1` audit covered 48 states, 128 transitions, and all 360
  complete interleavings. Ruff/format, the forced 83-page build, and scoped
  diff checks passed. The audit returned clean and required no edit.
- The hybrid audit independently checked the lazy-anchor induction,
  range-minimum tags and flush-before-write rule, three-key residual transition,
  absence of old-row/key/anchor rebuild work, bulk-product and stage charges,
  `q(3q-1)/2` dense-auxiliary identity, every vector coordinate, `q=2` and
  `q=m`, native suffix, memory peaks, and exclusions. It found one exact ledger
  wording contradiction: numerical adjacency rounds make
  `R_adj=m+1+O(A_<q^fc)`, while only `R_int=m+1` and the structural first-
  exposure component are exact. The theorem, proof, README, STATUS, and checker
  were repaired together. Re-audit was clean; extended exact chains through
  `m=10`, all checkers, Ruff, diff checks, and the forced 70-page build passed
  with only the three pre-existing overfull warnings.
- The volume audit used a separate dense exact-rational solve. It reproduced
  admissions at stages `1,2,4,8`, the four-state chronology, every transition
  fraction and sign, the production STOP, prior negative reset slack,
  follow-up STOP, positive stage-7--9 net, Schur row/pivot/drop, production
  coefficient cap, follow-up coefficient floor, exact gaps, feasible-rectangle
  argument, unchanged vector, and finite-local exclusions. The official
  checker, forced 46-page build, visual theorem-page review, and scoped diff
  checks passed. The audit returned clean and required no edit.

## Redistribution messages

1. Response to hybrid and volume: unrelated events may enlarge a global event
   horizon without entering a certificate's mass horizon only after exact state-
   block separation is proved. One affected-block certificate per event does
   not pay a face transition, shared reset, or cross-column coupling.
2. Hybrid to response and volume: zero-padded numerical state plus lazy tags can
   make a transition local when old residuals and lower coordinates are truly
   invariant. The result does not transport accelerated auxiliaries; fresh
   dense records, numerical products, and stage rounds remain separate charges.
3. Volume to response and hybrid: a reset-local reserve can pay the
   instantaneous coefficient/state switch while both adjacent fixed-state
   steps fail. Event-local certification, coordinatewise lower transport, and
   reset payment therefore imply no neighboring-step or scalar-potential
   monotonicity.
4. All directions: preserve local versus global horizons, interaction versus
   adjacency rounds, structural exposure versus numerical products, held
   evolution versus production/reset/follow-up, and all eleven resource
   coordinates. None of the three Round-018 results composes into a graph-
   uniform PPR theorem.

## Metadata and shared-state changes

- This record, `BROADCAST.md`, and the accumulated-results ledger replace the
  active Round-018 dispatch with the independently reviewed redistributed
  snapshot while preserving every prior round.
- The results ledger gains one narrowly scoped row per outcome and updates the
  response, acceleration, and composition frontiers to the exact remaining
  interfaces. The broadcast records the same stable labels, canonical vectors,
  audit repair, exclusions, and next queue.
- The controller completed the durable metadata and documentation
  synchronization in `manuscript/notes/README.md`,
  `manuscript/notes/manifest.toml`, `manuscript/notes/taxonomy.toml`,
  `docs/research_notes.md`, `docs/solver-family-roadmap.md`, and
  `docs/literature/acceleration.md`. Those six files now mirror the three
  reviewed outcomes, including the final volume qualifier that both adjacent
  failures are rectangle-uniform while the positive stage-7--9 net change is
  proved only at the canonical tight pair. Note IDs, track counts, and formal
  dependency arrays are unchanged.
- The related-work ledger and shared PPR problem definition remain unchanged.
  The formal graph stays at 18 direction nodes and 26 acyclic edges; no edge is
  added for comparison, provenance, or cross-redistribution alone.

## Validation

- Round-start full suite inherited from the redistributed Round 017: `187`
  tests passed with `15` known temporary-directory cleanup warnings; lint and
  format passed; note audit found 18 notes across 5 tracks; note graph found 18
  nodes and 26 acyclic formal edges; fresh direction builds were 79, 65, and 44
  pages. Staging was empty, recovery stash `e47d263` was retained, and no root
  `=2` artifact existed.
- Response owner checks: Round-014--018 checker regressions, focused Ruff
  check/format, 356-label duplicate scan, conflict/staging/root-artifact scans,
  scoped diff, inventory/graph checks, and a fresh 83-page build passed. The
  Round-018 checker covered one deterministic delayed three-column case, 100
  randomized families, `2,304` exhaustive states, `12,288` transitions,
  maximum asynchronous length `388`, `12,272` column switches, `3,307` delayed
  crossings, and `54,651` seen-but-unreported states. Independent audit and
  supplemental exhaustive replay were clean.
- Hybrid owner and repaired-audit checks: all Round-014--018 exact checkers,
  focused Ruff check/format, labels, conflict/root-artifact/scoped-diff scans,
  inventory/graph checks, and the 70-page build passed with only three
  pre-existing overfull warnings. The exact checker covered every expansion for
  `m=2,...,7` and all three declared `alpha` values; the independent repaired
  audit extended exact chains through `m=10` and returned clean.
- Volume owner checks: all six finite exact checkers, focused Ruff check/format,
  label/control/conflict/root-artifact/scoped-diff scans, inventory/graph checks,
  fresh 46-page build with no overfull boxes, and visual inspection of theorem
  pages passed. Independent dense exact-rational reproduction and forced build
  were clean.
- After the six-file synchronization, controller validation completed: the
  full suite passed `187` tests with `9` temporary-directory cleanup warnings
  of the same known kind; lint and format passed; all three Round-018 checkers
  passed; fresh response, hybrid, and volume builds produced 83, 70, and 46
  pages; note audit found 18 notes across 5 tracks; note graph found 18 nodes
  and 26 acyclic formal edges; both TOML inventories parsed with 18 matching
  unique IDs; and full/scoped diff, whitespace, conflict-marker, staging, and
  root-artifact scans were clean. The recovery stash `e47d263` remains
  retained. No validation or promotion step staged, committed, pushed, or
  dropped that stash.

## Next round queue

1. Response: admit simultaneous batches, undeclared/unbounded positive mass,
   arbitrary signed logical amplitudes, or a genuinely cross-column coupled
   response state while retaining exact first-crossing certificates and full
   schedule/fragment/state/output charges. A failed per-column margin is not a
   lower bound, and the current local-mass separation cannot be assumed under
   coupling.
2. Hybrid: amortize or avoid the named `DenseFreshAESP` restart cost
   `q(3q-1)/2`, or transport and charge actual momentum, proximal, center, and
   estimate-sequence auxiliaries across admission. Preserve the repaired
   `R_adj` ledger, candidate exposure, missing endpoint products, margin/oracle
   terms, and separate native suffix; no speedup follows from the lower-state
   transition alone.
3. Volume: abandon this held-pair-feasible squared-bank-plus-sole-reserve form,
   or add an explicit production/follow-up reserve, signed phase term,
   cross-state cancellation, or longer space--time block that survives the two
   exact adjacent failures. A global/multi-admission route remains open, as does
   the quarantined lower route only after literal entry coefficients,
   anti-cancellation, and uniform projection/envelope margins are proved.
4. Preserve the three distinct frontiers: a general partial reporter beyond
   frozen separability, accelerated-state rather than diagnostic-only
   transport, and a genuine global path potential. Round 018 narrows all three
   but proves no graph-uniform PPR solver or lower bound.
