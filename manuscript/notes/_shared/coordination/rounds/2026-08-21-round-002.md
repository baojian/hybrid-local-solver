# Research round 002: falsify the surviving interfaces

Date: 2026-08-21
Controller snapshot: round 001 redistributed on the same dirty concurrent
worktree; no staging, commit, push, rename, or folder move is authorized.
Round state: redistributed

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `aesp_cd_l1_rppr` | Use the benign singleton and positive P3 witnesses to prove a positive-log normalized-inflation upper bound, or construct a family that refutes the next natural charge. | `aesp_cd_l1_rppr/` | Proved the collateral-clipping upper bound `lem:aesp-cd-collateral-charge`; cumulative packing remains open. |
| `response_preconditioned_hybrid` | Independently audit `thm:coded-bucket-row-locator`, `cor:shared-coded-harmonic-bank`, and the concurrently added `cor:notched-sun-finite-band-separation`, including probability, adaptivity, dimensions, threshold scaling, and hidden-work clauses. | Read-only audit of `response_preconditioned_hybrid/` | Coded theorem accepted after adaptive-freshness/work qualifiers; finite-band corollary accepted after fixed-`n` quantifier and constant repairs. |
| `local_solver_oracle_hierarchy` | Fix one concrete `RowRec` budget and candidate same-task family, then try first to defeat the proposed recurrence barrier with allowed Krylov/preconditioned actions. | `local_solver_oracle_hierarchy/` | Exact CG defeats the fully exposed endpoint-path candidate at `alpha=n^-4`; charged LDL remains linear. |

## Direction handoffs

- `aesp_cd_l1_rppr`: `lem:aesp-cd-collateral-charge` proves
  `log max(1,gamma_t) <= C_t^col <= 1` and
  `I_T <= sum_t C_t^col`. Only coordinates clipped while their extrapolates
  remain below `x*` enter the numerator. For `t >= 2`, the clipping amplitude
  has the exact proximal-displacement collapse form. The charge is
  a-posteriori, may overcharge a benign collateral round, and has no proved
  cumulative accelerated-scale packing.
- `response_preconditioned_hybrid`: `thm:coded-bucket-row-locator` is sound
  for fixed `X`, and `cor:shared-coded-harmonic-bank` gives a one-fixed-event
  candidate family of dimension
  `O_tilde(1 + |F| E^2 / m^2)`. Adaptive use must refresh both Gaussian-source
  and hash/sign-target layers, allocate a summable failure schedule, and
  charge capacity, workspace, membership state, construction/application, and
  validation. For `cor:notched-sun-finite-band-separation`, the valid regime
  is: fixed `n,beta`, `alpha > alpha_bar_(n,beta)`, and
  `((1-alpha)/2) < 1/50`; at the displayed threshold exactly one matching leaf
  survives. It is a query-count calibration, not a uniform family or RPPR
  trajectory.
- `local_solver_oracle_hierarchy`: under budget
  `(0,floor(sqrt(nu_n)),32 nu_n)` and the exact full-vector endpoint-path task,
  `thm:path-krylov-counteralgorithm` gives a complete eleven-coordinate CG
  ledger with `O(n^2)` nonmemory work at `alpha=n^-4`, whereas
  `nu_n/sqrt(alpha)=Theta(n^3)`. The same-task fully charged LDL response costs
  `Theta(nu_n)`. The dimension condition applies only after a fixed-support
  exact system is fully exposed with `O(nu)` representation/matvec cost and
  enough vector memory.

## Controller adjudication

All three handoffs are promoted at their narrowed scopes. The AESP result is a
per-round analytical inequality, not cumulative amortization or an algorithmic
certificate. The coded bank is a fixed-event measurement/candidate theorem,
not a dynamically charged reporter. The notched-sun finite-band result is
pointwise in fixed size and near-one `alpha`. The CG result defeats the named
fully exposed path candidate; it neither refutes arbitrary hard graph families
nor imposes its dimension condition on every local support-discovery problem.

## Redistribution messages

- AESP agents should start from the collateral fractions, explicitly allowing
  benign overcharge, and seek a cumulative packing theorem or local surrogate;
  they must not revert to raw correction count.
- Response agents may reuse the fixed-event coded bank only with fresh
  two-layer randomness and a summable failure budget. The active data-
  structural target is aggregate construction/application plus exact
  validation, with all workspace and membership state charged.
- Lower-bound agents must report all eleven resources. For a fully exposed
  exact-system candidate, first test the finite-dimensional CG cap; keep this
  route separate from local information/support-discovery lower bounds.

## Metadata and shared-state changes

- Results ledger: added the collateral-charge lemma, fixed-event coded bank,
  pointwise notched-sun finite-band calibration, and scoped path/CG
  counteralgorithm.
- Broadcast: advanced to the round-002 verified redistribution.
- Taxonomy/dependencies: next targets narrowed; no dependency edge was added.
- Manifest/index: all three direction summaries and this round record were
  synchronized; no direction was created, renamed, or moved.

## Validation

- Independent proof audits: AESP algebra, coded probability/dimensions,
  notched-sun asymptotics, CG ledger, budget, and asymptotics were checked.
- Focused builds: all three round-002 proof notes passed after corrections.
- Repository validation: `make note-audit` passed with 18 notes across 5
  tracks; `make test` passed all 182 tests with 15 temporary-directory cleanup
  warnings; `make lint` and repository `git diff --check` passed. No unresolved
  references were present in focused builds.
- Concurrent-work caveat: preserve every unrelated live edit and re-read diffs
  immediately before any patch.

## Next round queue

1. Prove or refute cumulative packing of `sum_t C_t^col`, or find a local
   surrogate with the same one-sided guarantee.
2. Build and charge the fixed-event coded measurements across a genuine
   adaptive high-rank epoch, or replace them with an equivalent aggregate
   shifted-debt/transfer primitive.
3. Stress-test the exact-system RowRec candidate in the surviving
   `n=Theta(1/sqrt(alpha))` regime while independently pursuing killed-Green
   information packing for local support discovery.
