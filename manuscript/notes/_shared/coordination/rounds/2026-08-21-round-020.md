# Research round 020: slack reweighting, nonsettled continuation, and causal recovery

Date: 2026-08-21
Controller snapshot: Round019 redistributed baseline on
`agent/coordinate-research-rounds-14-20`
Round state: redistributed

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `response_preconditioned_hybrid` | Test an actual checkpoint-slack weight change in the fixed coded bank, charging validation, rejection, replay, state, and output. | direction `main.tex`, `README.md`, `STATUS.md`, checker | Reviewed named-state STOP: four retained bucket norms do not determine an exact three-label positive reweight; rejection and six-read replay comparators are fully charged. |
| `hybrid_aesp_locsor` | Cross a canonical face from a genuinely nonsettled point, register the positive estimate shock, run at least one accelerated continuation stage, and preserve the native suffix ledger. | direction `main.tex`, `README.md`, `STATUS.md`, checker | Reviewed finite GO/rate STOP: the first nonsettled admission, KKT reset, and one enlarged-face relative-accuracy stage are fully charged, but no cross-face contraction or speedup follows. |
| `volume_gated_acceleration` | Give a causal realized-credit rule and test it through a second canonical admission without borrowing future decreases. | direction `main.tex`, `README.md`, `STATUS.md`, checker | Reviewed finite two-admission GO/local STOP: the face-general scalar identity is exact; carried stage-4 credit keeps the named path ledger solvent through stage 12, while a stage-7 restart stops at 11 and first closes at 12. |

## Direction handoffs

### `response_preconditioned_hybrid`

- Stable authority: `prop:three-label-norm-only-reweight-obstruction`,
  `eq:three-label-reweight-histories`, `eq:three-label-old-norm-state`,
  `eq:three-label-reweight-flip`, and the rejection/replay eleven-vectors.
- In one bucket with identifiers `00,01,10`, the signed histories
  `(-4,1,1)` and `(-2,-1,-1)` retain the same four unit-weight squared norms
  `(1,9,1,9)`. Reweighting to `(1,3,2)` makes the first decoder comparison
  `9>4` in one history and `9<16` in the other. A two-source nonnegative
  witness similarly gives `9>8` versus `9<10`.
- Three labels are minimal for this named state. With at most two injectively
  coded labels, separating and grouped bit sides recover every Gram
  coefficient needed by a later positive diagonal reweight.
- `NormOnlyWeightRefresh` rejects before mutation with vector
  `(0,0,1,0,Theta(1),0,0,Theta(1),Theta(1),0,1)`.
  `ReplayThreeWeightRefresh`, when all six old response cells exist, has
  vector `(0,0,1,0,Theta(1),0,6,Theta(1),Theta(1),12,1)`.
  Missing cells require the actual cut scan, response reconstruction, and any
  newly exposed adjacency in addition to that comparator.
- This exact-real result is representation-specific. It is not a lower bound
  on richer Gram state, replay/rebuild, collision-free banks, other decoders,
  general partial reporting, RPPR chronology, finite precision, or bits.

### `hybrid_aesp_locsor`

- Stable authority: `lem:branch-caterpillar-first-nonsettled-admission`,
  `lem:branch-caterpillar-nonsettled-shock-reset`,
  `thm:branch-caterpillar-first-nonsettled-continuation`, and the transition,
  prefix, post, and total-work equations carrying the same stem.
- For fixed `m>=2`, branch seed, `0<alpha<1/2`, and
  `0<rho<min{rho_can(m,alpha),1/30}`, one shifted exact stage from zero ends
  strictly below the first restricted optimum and makes all three first
  canonical demands positive. The complete gate commits the first
  three-label layer from a genuinely nonoptimal point.
- The carried primal, momentum, center, and estimate point zero-pad. The
  proximal warm start retains its old cell and appends three positive cells.
  Two paid products, twelve appended cells, and a reset marker have vector
  `(2V_1,2,0,0,O(1),Theta(V_1),0,Theta(C_1),O(C_1),12+Theta(1),0)`.
- The observable KKT reset
  `B_ns=K_ns^2/(2alpha)+mu_E D_ns^2+mu_E K_ns^2/alpha^2`
  is a valid positive enlarged-face estimate budget. One genuine extrapolated
  relative-accuracy AESP-CD stage is then charged before the native response
  suffix. The complete prefix/post vectors are
  `eq:branch-caterpillar-nonsettled-prefix-eleven-vector` and
  `eq:branch-caterpillar-nonsettled-post-eleven-vector`.
- This registers rather than amortizes the shock. It proves no multi-face
  rate, speedup, graph-uniform theorem, other seed/range/policy claim, PPR
  conversion, finite-precision guarantee, or bit bound.

### `volume_gated_acceleration`

- Stable authority: `prop:path-causal-two-admission-recovery`,
  `eq:causal-face-score`, `eq:causal-recovery-update`,
  `eq:causal-recovery-invariant`, the stage-11/12 STOP/GO equations, and
  `eq:path-causal-two-admission-eleven-vector`.
- Define `Xi_t=delta(x^(t))^2`; credit a fixed-face decrease only after the new
  score is observed and a restricted-optimum drop only after the admission
  and its charged Schur/response query. The exact balance is
  `C_k=C_0+Xi_0-Xi_k+sum_(t<k) Gamma_t`.
- Nonnegative balance means only scalar-ledger closure: already realized
  decreases and charged optimum drops pay every realized score-increase debt.
  It is not convergence, objective monotonicity, or a work certificate.
- On the literal `q=1/5` endpoint path, starting at held stage 3 with zero
  balance, the stage-4 and stage-8 drops are `858637/6497453125` and
  `175006441/6398713140625`. Unused first-admission credit keeps the balance
  positive through stage 12. Restarting at stage 7 removes that credit:
  stage 11 remains negative and stage 12 is the first local ledger GO.
- The run/vector remain `J=4`, `T=16`, `nu_fin=9`, `n_fin=5`, swept volume
  `114`, and `(Theta(9),Theta(5),1,0,Theta(114),Theta(114),Theta(114),Theta(9),O(9),Theta(114),Theta(9))`.
  This finite exact-real result is not a uniform recovery time,
  all-admission/global potential, asymptotic/logarithmic theorem, nonpath
  result, alternate-order result, finite-precision statement, or bit bound.

## Controller adjudication

All three reviewed conclusions are promoted with their exact exclusions. The
response result is a named-state impossibility, the hybrid result is a finite
one-transition/one-continuation witness with a fresh reset, and the volume
result is a conditional accounting identity with one finite path
instantiation. None supplies a graph-uniform solver theorem, speedup, response
lower bound, uniform recovery horizon, or finite-precision result.

## Independent review

- Response review independently rederived both collisions, the two-label
  reconstruction argument, and both eleven-vectors. It ran 28,000 exact
  reconstruction checks and confirmed the 88-page build. No repair was needed.
- Hybrid review independently rederived the endpoint, margins, KKT reset,
  nonzero carried state, continuation, and every ledger coordinate. It
  required one wording repair: the first four carried arrays zero-pad, but
  the proximal warm start appends three positive cells. The repaired 77-page
  build and checker passed.
- Volume review independently replayed the exact chronology, both Schur drops,
  all balances, stage-11 STOP/stage-12 GO, and the unchanged vector. It
  required one scope repair defining nonnegative balance as scalar-ledger
  closure only, not convergence, objective decrease, or work. The repaired
  51-page build and checker passed.

## Redistribution messages

| Finding | Recipient directions | Required action |
|---|---|---|
| A genuine weight change cannot reuse only four old bucket norms on the minimal three-label collision. | response, hybrid | Test the smallest richer invariant or a paid replay/rebuild schedule; charge transition information rather than treating old aggregate state as universal. |
| A nonsettled sparse transition is possible, but the positive estimate shock is a fresh budget. | hybrid, volume | Continue to a second actual admission or prove the first obstruction; do not infer stagewise contraction from a registered reset. |
| Earlier realized Schur credit can cancel a later local deficit on one path. | volume, hybrid, response | Test the causal balance on a distinct graph/order and distinguish all-history solvency from restarted per-block recovery. |

## Metadata and shared-state changes

- Shared results, BROADCAST, and this record carry the reviewed Round020
  outcomes. The six durable project/documentation files are synchronized in
  the combined Round020--021 promotion.
- Manifest and taxonomy retain 18 matching IDs and the same 26 formal acyclic
  dependency edges. No new proof/import edge follows from these lessons.

## Validation

- All three Round020 exact checkers, focused direction builds, and independent
  derivations passed after the two wording repairs above.
- Final combined Round020--021 validation is recorded in the Round021 record;
  it includes inventory/graph, lint/format, full tests, direction builds,
  stable-label checks, diff/conflict scans, and staging inspection.

## Next round queue

1. Response: add the smallest sufficient richer state to the three-label
   witness, test a second refresh, and determine its collision-count scaling.
2. Hybrid: continue through a second actual admission and register the next
   fresh estimate shock without hiding products, candidate rows, or logs.
3. Volume: test whether restarted scalar debt can survive the next admission
   on a distinct graph/order even when the all-history balance stays solvent.
