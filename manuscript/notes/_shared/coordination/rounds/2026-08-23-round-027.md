# Research round 027: weighted-reserve boundary and spectral-window survivor

Date: 2026-08-23
Controller snapshot: dirty reviewed Round020--026 tree on
`agent/coordinate-research-rounds-14-20`; Round027 source work was added only
to `aesp_cd_l1_rppr` without staging, committing, pushing, stashing, resetting,
or branch mutation.
Round state: redistributed
Review sequence: direction frozen, two independent audits exposed and checked
two hard indexing repairs, both final audits returned CLEAN, controller
reviewed the accepted package, then redistributed it

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `aesp_cd_l1_rppr` low-Dirichlet owner | Test the surviving `q^(-1)`-weighted route on the actual finite recurrence and prove a useful reserve or a rigorous proof-template STOP, retaining the fresh gate and C2-plus-absolute residual interface. | Direction `main.tex`, `README.md`, `STATUS.md`, checker | Proved an actual-finite lagged Euclidean reserve with only `q^2` graph-uniform drift, an exact two-family STOP for the simplest lagged unsplit `q^(-1)Q` reserve, and a compatible exact `K_8` high-band payment limit. |
| Independent adversarial/exact track | Seek a reachable family defeating every permitted additive soft-logarithmic term or narrow the surviving spectral/nonlinear route with exact rational reductions. | Read-only source analysis and scratch exact arithmetic only | Found the reachable uniform-seed `K_2` critical low-mode family and the exact `K_8` high-band payment formula. No additive-term-resistant obstruction, net-exponent counterexample, solver, or vector resulted. |
| Independent theorem/checker audits | Recheck finite-reserve indices, `K_2` chronology and fresh gate, the `K_8` coefficient floor and high-band degree scaling, global normalization, checker guardrails, scope, and build. | Read-only review; owner applies accepted repairs | The audits caught a current-versus-lagged bank mismatch and then an entry-versus-persistent stage mismatch. After both hard repairs, two independent final readbacks returned **CLEAN**. |

## Direction handoffs

### `aesp_cd_l1_rppr`: actual-finite lagged Euclidean reserve

- Claim-status change: `lem:aesp-cd-q-weighted-euclidean-reserve` works on the
  realized finite sequence. For `t>=1`, put
  `e_t=x*-x_t`,
  `omega_q^E=(1-q)^2 mu_E/(2q)`,
  `Psi_t^E=Phi_t^fin+omega_q^E||e_(t-1)||_2^2`, and
  `eta_t=2 kappa_A xi_t+kappa_A xi_t^2`.
- Every stage covered by the finite-inner potential comparison satisfies
  `Psi_(t+1)^E<=Psi_t^E-q Phi_t^fin+eta_t`.
  The proof retains the actual finite state, end error, correction, and
  shifted-solve error; it does not import an exact shadow trajectory.
- The unconditional comparison is
  `Psi_t^E<=((1+q+q^2)/q)Phi_t^fin`, hence
  `Psi_(t+1)^E<=(1-q^2/(1+q+q^2))Psi_t^E+eta_t`.
  Exact shifted solves therefore give a monotone reserve, but the
  graph-uniform drift is only order `q^2`, yielding `O(q^(-2))` stages rather
  than an accelerated `O(q^(-1))` proof.
- Scope: this is an exact actual-finite reserve lemma, not the missing net
  exponent, an accelerated solver, or an eleven-coordinate vector.

### `aesp_cd_l1_rppr`: two-family STOP for the lagged unsplit reserve

- Claim-status change: `prop:aesp-cd-unsplit-q-energy-stagewise-stop` compares
  the same absolute coefficient on two exact reachable families. For rational
  `0<q<=1/100`, put
  `alpha=q^2/(1+q^2)`,
  `kappa_A=(1-q^2)/(1+q^2)`, and
  `mu_E=kappa_A q^2`.
- Directly absorbing a stage defect by the Round026 unsplit drop requires
  `(1-q)mu_E D_t^fin/2<=(A/q)(E_(t-1)^Q-E_t^Q)`. The reachable `K_8` stage-two
  bounds force
  `A>14(1-q)kappa_A/197>=A_*`, where
  `A_*=6929307/98509850`.
- On uniform-seed `K_2`, take `q=1/n`, `n>=100`, and
  `rho=tau=1/16`. Exact shifted solves are valid zero-residual finite outputs,
  `x*=(7/16)1`, and
  `e_t=(1+tq)(1-q)^t e_0`,
  `x*-z_t=(1-q)^t e_0`. The support is full from stage one, all corrections
  vanish, and stage two is genuinely persistent.
- The absorbing index is necessarily lagged:
  `Psi_t^A=Phi_t+(A/q)E_(t-1)^Q`, `t>=1`. With
  `b_q=(1-q^2)/2`, the first genuinely persistent transition is
  `Psi_3^A/Psi_2^A`, not `Psi_2^A/Psi_1^A`, and equals
  `(1-q)^2(b_q(1-q)^2((1+3q)^2+1)+(A/q)(1+2q)^2)`
  divided by
  `b_q(1-q)^2((1+2q)^2+1)+(A/q)(1+q)^2`.
- Its exact component estimates are
  `1-E_2^Q/E_1^Q<=4q^2`,
  `1-Phi_3/Phi_2<=2q`, and a `Phi` weight at most `q/A`. Therefore, for
  every `A>=A_*`,
  `0<=1-Psi_3^A/Psi_2^A<=(4+2/A_*)q^2`.
  No absolute `c>0` gives a uniform one-step
  `Psi_(t+1)^A<=(1-cq)Psi_t^A` for this proof template.
- Scope: this is a stagewise unsplit-bank STOP only. It does not refute a
  windowed spectrally split bank, nonlinear transfer, different normalization,
  an additive soft-logarithmic term, the actual-finite net exponent, a solver,
  or a resource vector.

### `aesp_cd_l1_rppr`: the witnesses are not additive-resistant

- The complete `K_2` trajectory satisfies, for `t>=1`,
  `Psi_t^A/Psi_1^A<=(4/e)exp(-q(t-1))` and
  `Psi_1^A/Phi_0<=1+A/(q(1-q^2))`.
  Relative to `Phi_0`, the startup cost is only
  `q+log(4/e)+log(1+A/(q(1-q^2)))=O(log(q^(-1)))`, which is permitted by the
  frozen additive allowance.
- The exact trial-center error is
  `x*-ell_t=(1+(t+1)q)(1-q)^t e_0/(1+q)`. For every `0<=t<=n`, the fresh
  normalized gate residual is larger than `alpha*7/64>alpha*tau`, so the
  displayed persistent transition occurs before the fresh gate.
- No reachable Round027 family forces
  `J_T^fin-(1-c)qT` to exceed every permitted soft-logarithmic additive term.
  Numerical reconnaissance is not used in any promoted statement.

### `aesp_cd_l1_rppr`: exact `K_8` high-band compatibility

- In the Round026 rational `K_8` family, let `P_h` project onto
  `v=(1,-1/7,...,-1/7)` in degree-scaled coordinates and put
  `E_t^h=<P_h e_t,Q P_h e_t>`.
- The exact modal data are
  `lambda_h=(4+7q^2)/(7(1+q^2))`,
  `m_h=7(1-q^2)/11`, and
  `s_h=(1-q)(3+14q)/(11(1+q))`.
  With `a_0=1/112`,
  `q^(-4)a_0^(-2)(E_1^h-E_2^h)->516096/14641` and
  `q^(-1)a_0^(-2)D_2^fin->32`.
- Consequently the exact high-band payment ratio
  `R_h(q)=((1-q)mu_E D_2^fin/2)/(q^(-1)(E_1^h-E_2^h))`
  satisfies `R_h(q)->14641/32256` as `q` decreases to zero. The common
  original-coordinate degree factor cancels.
- Scope: one constant `q^(-1)` high-band payment survives this exact pulse.
  This is live evidence for spectral splitting, not a general spectral-window
  theorem; the coordinatewise positive part still does not commute with the
  modal projectors.

## Hard audit repairs

1. The first frozen draft paired `D_t` with `E_(t-1)^Q-E_t^Q` but defined a
   current-index bank `Phi_t+(A/q)E_t^Q`. The absorbing bank was repaired to
   the lagged index `Phi_t+(A/q)E_(t-1)^Q` throughout the source, proof,
   checker, README, and STATUS.
2. The first lagged repair tested `Psi_2/Psi_1`, but
   `P_1=supp(x_1) intersect supp(x_0)` is empty. The proof and checker were
   repaired to the genuinely persistent stage-two transition
   `Psi_3/Psi_2`, with `E_2/E_1` and the exact constant
   `(4+2/A_*)q^2`.
3. The checker now asserts the fresh gate at `ell_t`, not merely at `x_t`,
   and asserts the combined time-one global normalization. Both independent
   auditors rederived the repaired formulas and returned **CLEAN**.

## Controller adjudication

| Candidate conclusion | Decision | Exact evidence checked | Shared destination |
|---|---|---|---|
| A lagged Euclidean reserve can absorb every realized correction defect. | Promote narrowly | `lem:aesp-cd-q-weighted-euclidean-reserve`; the reserve retains `eta_t` but has only `q^2` graph-uniform drift. | Results ledger and broadcast, without an accelerated-rate claim. |
| The same absolute coefficient gives both direct `K_8` payment and uniform one-step accelerated contraction of the lagged unsplit reserve. | Reject | `prop:aesp-cd-unsplit-q-energy-stagewise-stop`; `A>=A_*` and the persistent `K_2` transition loses at most `(4+2/A_*)q^2`. | Refuted-routes ledger and broadcast. |
| The reachable `K_2` family defeats the permitted additive term or net exponent. | Reject | Its all-time decay is accelerated after only `O(log(q^(-1)))` startup normalization. | Explicit non-obstruction scope in every destination. |
| The `K_8` pulse also refutes a `q^(-1)` high-band reserve. | Reject | `R_h(q)->14641/32256`. | Preserve as live evidence for a split/windowed route. |
| Round027 proves a spectral transfer, graph-uniform net exponent, accelerated solver, or eleven-coordinate vector. | Reject | Modal projectors do not commute with the positive part; no cumulative split transfer was proved. | Shared frontier remains **Open**. |

## Redistribution messages

| Finding | Recipient directions | Required action |
|---|---|---|
| The exact Euclidean reserve is actual-finite but has only `q^2` drift. | AESP-CD and exact-to-finite acceleration directions | Reuse its finite-error-safe identity if useful, but do not call it accelerated. |
| The simplest lagged unsplit `q^(-1)Q` reserve fails one-step accelerated contraction on a genuinely persistent low mode. | `aesp_cd_l1_rppr` and every low-Dirichlet packing attempt | Keep the lagged index and stage-two chronology; do not revive a current-index or entry-stage version. |
| The exact `K_8` pulse is compatible with a constant high-band payment. | Spectral/windowed obstruction and Lyapunov searches | Preserve the high/low split and solve the positive-part/nonlinear transfer; do not call the limit a general theorem. |
| Neither exact family is additive-resistant. | Every obstruction search | Do not infer a net-exponent counterexample, solver lower bound, or resource conclusion from Round027. |

## Metadata and shared-state changes

- Results ledger: added the actual-finite Euclidean reserve, the repaired
  persistent two-family STOP, the global non-obstruction, and the exact
  high-band compatibility limit.
- Broadcast: redistributed the reviewed Round027 package and restricted the
  live Route-B queue to a windowed spectrally split, nonlinear-transfer, or
  differently normalized actual-finite low-Dirichlet Lyapunov.
- Round ledger: added this Round027 record and index entry without rewriting
  any earlier round.
- Direction and durable summaries: unchanged by this promotion; they were
  already frozen and independently audited before redistribution.
- Taxonomy/dependencies: no formal import edge added or removed.
- Manifest/index: no note identity or ownership change.

## Validation

- Direction evidence before promotion: exact Round022--027 checkers passed.
  `check_round027.py` uses `Fraction` for the finite reserve samples, reachable
  `K_2` recurrence, lagged persistent ratio, exact pre-gate chronology,
  coefficient floor, high-band samples, and rational precursors to the global
  bound. An independent scratch `Fraction` checker separately reproduced the
  formulas.
- Ruff check and format passed for Round022--027. The focused LaTeX build
  produced a clean 51-page PDF with no final warnings, undefined references,
  or overfull/underfull boxes.
- Two independent final read-only audits returned **CLEAN** after the hard lag
  and persistent-stage repairs. Diff, conflict-marker, and staging checks
  passed before shared promotion.
- Shared inventory validation passed after promotion: 18 notes across 5
  tracks. The dependency graph remains acyclic with the unchanged 26 formal
  edges. Full diff whitespace, scoped conflict-marker, and staging checks also
  passed.
- Nothing was staged, committed, pushed, stashed, reset, rebased, or otherwise
  used to mutate branch history.

## Next round queue

1. The only surviving Route-B queue is a windowed spectrally split,
   nonlinear-transfer, or differently normalized actual-finite
   low-Dirichlet Lyapunov that retains both finite residuals, persistent
   partial corrections, every retraction/rekey, and the fresh terminal gate.
2. It must survive the Round026 reachable pulse and same-unsplit-drop STOP,
   the Round027 `K_2` persistent low-mode STOP, and the exact
   `14641/32256` `K_8` high-band compatibility check.
3. Do not claim an additive-term-resistant obstruction, graph-uniform net
   exponent, accelerated solver, or eleven-coordinate vector from Round027.
4. Do not open Round028 automatically.
