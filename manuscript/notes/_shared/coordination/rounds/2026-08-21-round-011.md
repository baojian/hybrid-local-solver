# Research round 011: final relay seed, caterpillar hybrid handoff, and constant-ratio path work

Date: 2026-08-21
Controller snapshot: round 010 independently reviewed and redistributed before
dispatch; round 011 direction work and cyclic independent review are complete
on the live dirty worktree. The concurrent response-preconditioned
aggregate-Chebyshev/sleep continuation is preserved but remains separate and
provisional pending its next audit. No staging, commit, push, rename, or folder
move is authorized.
Round state: redistributed

## Assignments

| Direction | Exact target | Allowed edit scope | Outcome |
|---|---|---|---|
| `propagate_settle_framework` | Exhaust the relay-seeded double-cycle feed under the exact canonical all-violations gate; prove a long report-free attachment epoch or the last seed-orbit obstruction, then decide whether to retire this graph family. | `propagate_settle_framework/` | STOP: every propagating relay seed reports in its first batch; retire this graph family only for the prescribed long report-free later-petal witness. |
| `hybrid_aesp_locsor` | Compose a fully charged canonical-checkpoint Phase-I prefix on the branch caterpillar with `CaterpillarCanonicalLayerDelta`, using one paid support-only conversion and no private batch replay. | `hybrid_aesp_locsor/` | GO in the strict canonical range: direct paid support-only conversion, exact prefix/post ledgers, and unconditional prefix-plus-post total; product remains conditional. |
| `volume_gated_acceleration` | Prove or refute the constant-ratio zero-start endpoint-path swept-work behavior at `rho=tau=q/5`, including the global safe-envelope correction. | `volume_gated_acceleration/` | Exact product-scale floors plus STOP for frontier-only gate logic; no `q^-3` swept law or separation proved. |

## Direction handoffs

### `propagate_settle_framework`

- Stable result: `prop:double-cycle-relay-seed-obstruction`, with exhaustive
  first-batch split `eq:double-cycle-relay-first-batch` and ledger anchor
  `eq:double-cycle-relay-audit-vectors` inside
  `rem:double-cycle-feed-trace-ledger`.
- Exact scope: every even `n>=4`, site `j`, relay seed `s=e_(r_j)`,
  `alpha in (0,1)`, `rho>0`, exact-real canonical all-violations batching.
  Put
  `t=(1+alpha)/(1-alpha)`,
  `rho_W=1/(2(2t+1))`, and
  `rho_B=1/(2(4t+1))`. The initialized face/batch is absent for
  `rho>=1/2`; `({r_j},empty)` for `rho_W<=rho<1/2`;
  `({r_j},{w_j})` for `rho_B<=rho<rho_W`; and
  `({r_j},{w_j,b_j})` for `0<rho<rho_B`. At equality the zero-demand vertex
  stays inactive under the strict gate.
- Consequence: every relay-seeded trace that propagates meets `W` in its first
  batch before any petal. Together with the backbone- and anchor-seed stops,
  this retires the double-cycle family only for the prescribed long
  report-free later-petal witness, not all cyclic witnesses.
- Ledger: `P_firstW^relay` pays every face solve, row read, gate decision,
  state write, materialization, and required emission through the first report,
  singleton termination, or absent initialization. Only the fixed-site proof
  audit is incremental:
  `(O(1),1,0,0,O(1),0,0,0,O(1),0,0)` with fresh rows and
  `(0,0,0,0,O(1),0,0,0,O(1),0,0)` with already paid retained rows. An
  all-rotation audit restores linear scan/control.
- Exclusions: arbitrary positive-subset timing, post-report behavior,
  finite-band response, response directions, reporter/rank/work lower bounds,
  finite precision, and stability.

### `hybrid_aesp_locsor`

- Stable result: `thm:branch-caterpillar-composite-response-handoff`, with
  prefix definition `def:branch-caterpillar-canonical-checkpoint-prefix`,
  vectors `eq:branch-caterpillar-composite-prefix-eleven-vector` and
  `eq:branch-caterpillar-composite-eleven-vector`, work bound
  `eq:branch-caterpillar-composite-work`, and conditional product corollary
  `cor:branch-caterpillar-composite-product-conditional`.
- Exact scope: fixed `m>=2`, branch seed `s=e_(b_1)`, `alpha in (0,1)`, exact
  canonical all-violations checkpoints, exact-real delta/certificate
  interface, and
  `0<rho<rho_can(m,alpha)<=rho_cat(m,alpha)<=1/3`. A prefix at `Uhat_k` must
  have paid the actual common layer/support records, all prior exact
  settlements, `3k` delta labels, and `k` certificate replies.
- Conversion: one direct traversal of the current support builds the long-arm
  and leaf absorptions, the `min(k+1,m)` tridiagonal branch cells, and the
  balanced transfer tree. It reconstructs, queries, and emits no earlier face
  and retains no numerical estimate-sequence energy.
- Exact prefix vector:
  `(C_adj^J,R_adj^J,k,C_pre^J,C_ctl^J,C_rec^J,C_resp^J,`
  `M_pers^J,M_tmp^J,C_mat^J,C_emit^J)`.
- With `J_post=m-k`, one exact post realization is
  `(O(vol(S*)),O(J_post+2),J_post+1,0,O(C(S*)),0,`
  `O(C(S*)+J_post log(2+m)),O(C(S*)),O(C(S*)),Theta(|S*|),`
  `|S* minus Uhat_k|+|S*|+Theta(J_post+1))`.
  Additive and round coordinates sum; persistent and scratch coordinates use
  the stated retained/released-arena peak rules.
- Work conclusion:
  `B_J^full+O(C(S*) log(2+C(S*)))` unconditionally. Product work follows only
  if an independent theorem proves
  `B_J^full=O_tilde(C(S*)/sqrt(alpha))`. The note's particular
  Catalyst/AESP prefix separately requires `alpha<1/2`.
- Exclusions: `rho_can<=rho<rho_cat`, another seed or policy, arbitrary
  pre-backbone interleavings, repeated complete-positive lists, an
  `m`-uniform positive `rho` range, `kappa=1`, finite precision/bit complexity,
  transfer conditioning, RPPR-to-PPR conversion, arbitrary graphs, and
  graph-uniform acceleration.

### `volume_gated_acceleration`

- Stable results: `lem:path-global-envelope-gate`,
  `prop:path-constant-ratio-floor`, complete ledger
  `eq:path-constant-ratio-eleven-vector`, and
  `prop:path-frontier-only-gate-fails`.
- Exact floor: for `0<q<=1/4`, set `alpha=q^2`, `rho=tau=q/5`,
  `chi=(1-q)/(1+q)`, and
  `L_q=floor(log(5/3)/(-log chi))`. On the named exact-real zero-start,
  internally gated endpoint-path execution,
  `L_q<=J_q<=5/q`, `T_q>=J_q`,
  `1+2L_q<=nu_fin<=5/q`, and
  `mathfrak V_(T_q)>=L_q^2`. Thus
  `J_q,nu_fin=Theta(q^-1)` and only the product-scale swept floor
  `mathfrak V_(T_q)=Omega(q^-2)=Omega(nu_fin/q)` is proved.
- Complete literal-implementation vector:
  `(Theta(nu_fin),Theta(J_q+1),1,0,Theta(mathfrak V_T),`
  `Theta(mathfrak V_T),Theta(mathfrak V_T),Theta(nu_fin),O(nu_fin),`
  `Theta(mathfrak V_T),Theta(nu_fin))`.
- Nonlocal STOP: the general strong gate is
  `p_j(x)>delta(x)+2 alpha (rho+tau)/eta`. At exact `q=1/5`, a correction
  maximized at the old seed suppresses a raw stage-6 frontier violation; after
  the next admission the maximizer switches to old interior vertex `v_2` at
  stage 9. Neither a raw-frontier rule nor a correction pinned to one old
  coordinate determines the chronology.
- Exclusions: no `T_q=Theta(q^-2)` theorem, no
  `mathfrak V_T=Theta(q^-3)` theorem, no separation above realized product
  scale, no asymptotic conclusion from the nine-stage trace, and no nonpath,
  finite-precision, or bit-complexity claim.

## Controller adjudication

- The relay result passed independent rederivation of the singleton settlement,
  normalized demands, strict threshold equalities, every rotation/parity
  class, family-retirement qualifier, and incremental-versus-prefix ledger.
- The caterpillar result passed independent endpoint and ledger review. The
  adversarial pass caught and repaired two defects before promotion: at `k=m`
  there is no next canonical layer, and the direct full-support build has `m`,
  not `m+1`, branch cells. It also confirmed the `3k` prior delta labels,
  `k` prior replies, exact ranges `m>=2` and `alpha in (0,1)`, no-replay
  semantics, both vectors, and memory composition.
- The path result passed exact independent replay of all nine displayed
  stages and every strict rational comparison. Review corrected the general
  gate threshold from a `rho=tau`-specialized expression to
  `2 alpha (rho+tau)/eta`; the specialization, theorem constants, and trace
  remain unchanged.
- Adjudication preserves exact policy, output, arithmetic-model, and accuracy
  scopes. It promotes no `q^-3` swept law, graph-uniform product theorem,
  arbitrary positive-subset cyclic claim, or uncharged Phase-I budget.

## Redistribution messages

1. The relay seed closes the last candidate orbit of the double-cycle graph:
   every propagating trace reports immediately. Retire this graph family only
   for the prescribed canonical long report-free later-petal witness, and move
   to a genuinely different coupling or settlement gadget. Do not generalize
   the STOP to all cyclic witnesses or past the first report.
2. A strict-range canonical caterpillar checkpoint can hand off without replay,
   but Phase I is not free. Preserve both exact eleven-vectors and the
   unconditional `B_J^full+O(C(S*) log(2+C(S*)))` total. Redistribute product
   work only as a corollary conditional on an independent fully charged prefix
   bound.
3. At `rho=tau=q/5`, support discovery and swept work are proved only to the
   realized product scale. Any next path proof must track the moving global
   correction, including the stage-9 maximizer switch; it may not advertise
   the measured `q^-2` stage or `q^-3` swept exponents as theorems.

## Metadata and shared-state changes

- `_shared/results/README.md` now contains the relay thresholds and scoped
  family retirement, the strict-range caterpillar checkpoint handoff with
  both exact ledgers, and the constant-ratio floors plus nonlocal-correction
  STOP.
- `_shared/coordination/BROADCAST.md` now carries the verified Round-011
  redistribution and revised controller priorities while preserving the
  concurrent response-preconditioned aggregate-Chebyshev/sleep text as a
  separate provisional continuation.
- This durable round record now contains assignments, handoffs, audits,
  corrections, exclusions, validation provenance, and redistribution.
- The later controller synchronization updated
  `manuscript/notes/{README.md,taxonomy.toml,manifest.toml}` and
  `docs/{literature/acceleration.md,research_notes.md,solver-family-roadmap.md}`
  with the same three reviewed scopes.  It added no dependency edge and
  preserved the concurrent response-preconditioned continuation as separate
  provisional work.  Direction files were not changed by either promotion
  phase.

## Validation

- Round-start full tests: 182 passed with 15 known temporary-directory cleanup
  warnings.
- Round-start lint and format checks: green.
- Round-start `make note-audit`: 18 notes across 5 tracks.
- Round-start `make note-graph`: 18 nodes and 25 acyclic dependency edges.
- Round-start full `git diff --check`: green.
- Relay direction: exact rational checks at
  `alpha in {1/20,1/5,7/10,19/20}` covered both equality thresholds and one
  interior point of every interval; the algebraic proof remains primary.
- Caterpillar direction: focused build; `make note-audit` (18 notes/5 tracks);
  `make note-graph` (18 nodes, 25 acyclic edges); stable-label,
  undefined-reference, conflict-marker, and scoped tracked/untracked
  whitespace scans all passed after the two review repairs.
- Path direction: exact `Fraction` replay verified all nine chronology rows,
  every displayed stage-6/stage-8/stage-9 rational value, all strict
  comparisons, and the moving maximizer. The focused build, reference/label
  scans, control-character scan, `make note-audit` (18/5), and scoped
  tracked/untracked whitespace checks passed after the general gate fix.
- Independent mathematical, policy/model, scope, and resource audits are
  recorded above and in all three direction status cards.
- Final shared promotion validation: `make note-audit` reports 18 consistent
  notes across 5 tracks; `make note-graph` reports 18 nodes and 25 acyclic
  dependency edges; scoped tracked/untracked whitespace checks on all three
  promoted shared files are green.
- Final post-synchronization repository validation: `make test` reports 182
  passing tests with the same 15 temporary-directory cleanup warnings;
  `make lint` reports clean Ruff lint and format checks.  Both TOML inventories
  parse with 18 unique records, the dependency graph remains the same acyclic
  25-edge graph, and the full worktree whitespace check is green.
- Concurrent-work caveat: workers may touch only their assigned direction
  files and must preserve all user and prior-round dirty work; no staging,
  commit, push, or unrelated normalization is authorized.

## Next round queue

1. Redirect cyclic work to a genuinely different bounded-degree coupling or
   settlement gadget; prove a legal long report-free trace before reporter
   algebra.
2. Seek an independent fully charged Phase-I bound for the strict-range
   caterpillar handoff, and separately extend or refute the conversion beyond
   `rho_can` or canonical checkpoints.
3. Prove/refute the remaining constant-ratio `T=Theta(q^-2)` and
   `mathfrak V_T=Theta(q^-3)` laws with the moving global correction, or prove
   a soft-order upper bound; do not infer a separation from the current floor.
