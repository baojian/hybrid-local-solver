The proof-oriented hybrid method is:

\begin{enumerate}
\item Run AESP until a certified coarse accuracy level is reached.
\item Remove Catalyst momentum.
\item Run a monotone local relaxation method such as $\omega=1$ \LOCSOR{}.
\end{enumerate}

The momentum reset is essential: acceleration state cannot automatically be
transferred into a local relaxation recurrence.
