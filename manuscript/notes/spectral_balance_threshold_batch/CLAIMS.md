# Audit map: what is closed and what is not

This file is the short claim map for `README.md`.  It deliberately separates
an implemented/end-to-end work theorem, a proved algebraic or oracle theorem,
and a conjectural producer.  The notation is

```text
M=vol(S*),   alpha=PageRank floor,   sqrt(alpha)<=tau<=1.
```

**Superseding update (2026-09-04).**  The row labelled conditional
`InputConeFrontierNAG` remains a valid implication, but its literal
quarter-stop premise is now **refuted** on the canonical point-source,
zero-start chronology.  Exact cells refute every fixed
`0<beta<0.4897917473484638...`.  The surviving completion routes are a beta
in the remaining interval, deterministic support-linear `ExactBoxMaintenance`
through the proved positive-barrier repair, or an equivalent persistent
response/safeguarded-center theorem.  See `DETERMINISTIC_HALO_INTEGRATION.md`
for the current claim map; legacy rows below preserve the chronology of the
exploration.

Here `S*=supp(x_rho*)` for the canonical point-source RPPR instance, and all
work is exact-real adjacency-list work with repeated scans, response
applications, validation, materialization, and output charged.  The accuracy
namespace is `eps_obj`; no `eps_ppr` conversion or deterministic bit bound is
claimed.  In repository vocabulary, Sections A and the unconditional parts of
B are **Proved here**, premise-dependent rows are **Conditional**, Section C
is **Refuted** only for each named inference, and the missing producer in
Section D is **Open**.

## A. Closed end-to-end deterministic branches

| Branch | Certified work | Scope |
|---|---:|---|
| Boundary-exposed faces | `O_tilde(M/(alpha+phi_trace))` | `phi_trace=min d_out,U(i)/d_i` over the realized face trace.  It reaches `M/sqrt(alpha)` when `phi_trace=Omega(sqrt(alpha))`. |
| Endpoint path | `O(M)` for the exact response recurrence | The one-dimensional frontier is retained rather than re-solved. |
| Trees via shifted obstacle prox | `O_tilde(M/tau+M tau/alpha)` | Exact scalar Schur messages and the zero-threshold depth theorem; optimize at `tau=sqrt(alpha)`. |
| Supplied constant treewidth | `O_tilde(M/tau+M tau/alpha)` up to width factors | Exact bounded-width elimination. |
| Feedback-edge rank `k` | `O_tilde((M+k^omega_mat)/sqrt(alpha))` | `omega_mat` is an admissible exact matrix-multiplication exponent over the ordered input field.  Rebuild a grounded-tree Green factor plus a dense `k x k` Woodbury core on each capped maximal face and solve the core by recursive block inversion.  This returns the certified approximate/capped output; it does not claim the last face is the full exact support, bit complexity, or faster singleton-persistent maintenance. |
| Bounded chronological fill | `O_tilde(R_chol+(F_chol+M)/tau+(F_chol+M)tau/mu_*)` | `R_chol,F_chol` are measured elimination-tree reach/fill parameters; optimize the displayed expression. |
| Small final support | `O(s^3+sM)`, `s=|S*|` | Exact dense principal solve and boundary validation; useful as a portfolio branch. |

Every approximate fixed-face branch may use the proved residual/retraction
adapter.  It turns a normwise CG/Chebyshev residual into a nonnegative lower
subsolution and preserves no-miss admission and quiet-KKT correctness.

## B. Closed theorems that do not by themselves implement the general case

| Theorem | Exact conclusion | Missing operation |
|---|---|---|
| Current-gap high branch | A charged certificate `Q_U>=beta I` gives at most `O_tilde(1/sqrt(beta))` prefix faces and fresh-CG work `O_tilde(M/beta)`. | No theorem bounds the low-gap suffix by `beta/alpha`; the broom refutes that inference. |
| Shifted prox reduction | If one entire `tau^2`-shifted obstacle closure costs `O_tilde(M/tau)`, outer contraction gives `O_tilde(M/tau+M tau/alpha)`. | `ShiftedProxClosure(tau)` on a general sparse growing face. |
| Global positive-boundary subgradient stop | If `d>=0`, `e_U=r_U-(Q_UU+tau^2 I)d_U>=0`, and `s_out` is the current exterior score, then `(-e_U,-(s_out)_+)` is the minimum-norm full obstacle subgradient.  Its squared norm is `||e_U||^2+||(s_out)_+||^2`, and dividing this by `2(alpha+tau^2)` gives the global prox-objective gap bound. | The canonical point-source one-hop scan computes the full exterior term because the source is seeded and far nonneighbors have negative floor score.  Quietness is unnecessary for capped output; exact support identification is not supplied. |
| Arbitrary-feasible final cap | For any `d>=0`, the minimum-norm orthant subgradient equals `h_i=(Ad-r)_i` where `d_i>0` and `min{h_i,0}` where `d_i=0`.  Its norm gives the same strong-convexity objective and distance bounds. | A two-mask method can certify terminal accuracy at a nonnegative raw accelerated state while reserving the lower state for safe face publication.  An overshooting raw prox point cannot be committed as the lower base of the next outer call; the cap neither authorizes transient raw-positive rows nor proves the moving-face root clock. |
| Proximal-gradient final cap | For `L>=lambda_max(A)`, one projected-gradient point `d_plus=[d-(Ad-r)/L]_+` has gap at most `2||G_L(d)||^2/(alpha+sigma)` and distance at most `2||G_L(d)||/(alpha+sigma)`.  The mapping is `2L`-Lipschitz and is computable, together with `d_plus`, by a one-hop scan from a materialized raw support. | This removes the minimum-subgradient discontinuity at vanishing raw coordinates and gives a genuine distance lower bound when the certificate fails.  It is a terminal certificate, not a safe lower-state commit for an intermediate shifted-prox call. |
| False-hub-safe evaluation | In the canonical point-source model, scanning the rows of the current face `U` accumulates every possibly positive exterior score and the exact exterior contribution to the minimum-subgradient norm in `O(vol(U))` work.  Nonneighbors have the negative floor score. | No exterior row is read.  A raw-positive high-degree hub is only a boundary label until its lower-state score certifies true-support membership; this evaluates the cap but does not accelerate its decay. |
| Capped quiet-face timeout | If the projected raw state `y` and quiet certified-lower state `ell` are both within `delta/(sqrt(5)L)` of the exact restricted center, then the full minimum-subgradient norm at `y` is at most `delta`.  Fixed-face Chebyshev plus retraction reaches this premise in `O_tilde(1/tau)` products for shift `tau^2>=alpha`. | Every unchanged face therefore publishes or stops within one root window.  The number of root windows separated by certified singleton expansions is not bounded; this is not `SourceClockNoReuse`. |
| Mapping-to-packet charge | If raw and lower states are within `delta/(8L)` of the restricted center and the raw proximal-gradient mapping still exceeds `delta`, the lower state has positive exterior score norm at least `5delta/8`.  Appending all such rows releases an exact orthogonal center packet of energy at least `25delta^2/(64L)`. | At most `64LE_tot/(25delta^2)` fully equilibrated nonterminal timeouts occur at one absolute scale.  This can be polynomial at the final accuracy, so a scale-relative avalanche/volume argument is still needed. |
| Scalar shifted-debt envelope | For nonnegative solve debt `e`, every exterior response obeys `0<=C_v(Q_UU+tau^2 I)^-1e<=sqrt((a+tau^2)/(alpha+tau^2))||e||_2`.  Visible positive batches append at correction zero, leave old scores unchanged, and have `sum_B||g_B||_2^2<=(1+tau^2)alpha`. | Leverage is needed only for the exact restricted center or zero-margin support.  A norm-trigger count requires reset-and-zero-append epochs; it is not valid for arbitrary residual-changing rent steps. |
| Deterministic group reporter | One positive inverse-square-root probe dominates every row leverage; canonical conservation and first crossing charge all group visits and exact leaves in `O_tilde(M tau/alpha)`. | The additive response summary must first be produced. |
| Rank--port balance | A charged response bank with coefficients `A,B` gives `O_tilde(M+A/tau+B tau/alpha)` and optimizer `tau=sqrt(A alpha/B)`. | Construction and persistent application of the bank. |
| Supplied laminar hierarchy | One-low-mode clusters, mean-zero gap `Omega(tau^2)`, bounded overlap, and retained state give `O_tilde(A_H/tau+B_H tau/alpha)`. | Online support-local hierarchy plus coarse Schur response. |
| Canonical Green amplitude spine | For the final point-source support, every non-source row of `H_Sp=alpha e_v` has a strictly larger-`p` support neighbor.  Thus all nonempty Green superlevel sets are connected and contain the source.  Also `sum_S d_i p_i<=1`, while `alpha rho/a<p_i<=1`, so dyadic superlevels form only `O(log(1/(alpha rho)))` nested amplitude bands whose shell volumes sum to `M`. | This removes a supplied laminar *amplitude* chain from the assumptions.  It does not prove that long online epochs cross a band or spend disjoint within-band volume, nor does it supply the signed mean-zero response transport. |
| Solution-amplitude constant-shave spine | Every non-source positive coordinate of `y*(rho)` has a strictly larger solution neighbor, so all `{y*>eta}` are connected and source-rooted and have volume at most `1/eta`.  The oracle shave `(y*(rho)-eta 1)_+` is a lower subsolution for floor `rho+eta` and has original-objective gap at most `alpha eta/2`.  Moreover `(y(rho)-eta 1)_+<=y(rho+eta)<=y(rho)` and `||y(rho)-y(rho+eta)||_infinity<=eta`, while `y(rho+eta)` itself has original gap at most `alpha eta/2`. | This uses exactly the known constant direction of the implemented retraction and puts every final coordinate above `eta` in the shifted-floor support.  The missing `ConstantShaveShadow` theorem must show that the computable accelerated lower envelope shadows these oracle shaves without replaying dense face responses. |
| Constant-shave port gate | If `0<=y_U^*-ell_U<=eta 1`, then in every component of `S*\U` whose maximum is at least `eta`, a maximizing port `i` has exterior score at least `alpha d_i y_i^*+b d_iU(y_i^*-eta)+b d_i0y_i^*>=alpha d_i eta`. | A uniform shadow therefore publishes all high-amplitude branches in parallel.  Zero-appending those ports can violate the shadow on the new rows; restoring it without a fresh whole-face solve is the exact remaining update/no-reuse clause. |
| Missing-solution face cap | With `m_y(U)=max_(S*\U)y_i^*`, the exact restricted center satisfies `0<=y_U^*-x_U^U<=m_y(U)1` and `F(x^U)-F(y*)<=alpha m_y(U)/2`. | One offline solution maximum controls both uniform center error and all future packet energy.  Parallel components can keep the global maximum unchanged, so the exact cap is not an event count. |
| Computable constant bracket | For any certified lower state `ell`, `c=max_i(f-Hell)_i^+/(alpha d_i)` is computable from the face and one-hop boundary and satisfies `ell<=y*<=ell+c1`; hence all coordinates with `y_i^*>c` are already materialized and the maximum missing solution amplitude is at most `c`. | The scalar bracket's direct energy bound costs `c^2M`.  An exact two-vertex example has certified `ell_0<=ell_1` but `c(ell_0)=1/5<c(ell_1)=6/25`, so arbitrary lower monotonicity cannot prove decay.  A schedule-specific `ObservableConstantShaveDecay` or local port-repair charge remains open. |
| Retained shifted-prox bracket | If `ell<=y*<=ell+delta 1`, the exact obstacle prox centered at `ell` satisfies `P_tau(ell)<=y*<=P_tau(ell)+[tau^2/(alpha+tau^2)]delta 1`.  For a certified-lower inner point `z`, its local shifted residual bracket `zeta` gives the inexact recurrence `z<=y*<=z+(q_tau delta+zeta)1`; all final coordinates above the new width are in `supp(z)`. | This closes `ObservableConstantShaveDecay` at the scalar-comparison level and avoids storing a dense upper response.  Achieving each requested `zeta` in total `O_tilde(vol(U_out)/tau)` work across inner face growth is precisely the still-open `AmortizedLowerShiftedApply` producer. |
| Two-certificate prox invariant | If `kappa=c_H(ell)`, an inner shifted-lower point of width `zeta` satisfies `delta^+=q delta+zeta` and `kappa^+<=zeta/(1-q)+q kappa`.  Therefore `kappa<=delta/(1-q)` is invariant.  At shift `sigma=alpha`, inner target `zeta<=delta/4` gives `delta^+<=3delta/4` and `kappa^+<=2delta^+`, so the next shifted problem starts with bracket at most `delta^+`. | This prevents the strict nonmonotone scalar rebound from accumulating and reduces every inner task to constant-relative bracket decay.  Proving `QuarterBracketMaskedNAG` through all safe zero-appends would now close the target; experiments support it but do not bound arbitrary mask forcing. |
| Retained-prox experiment | A projected estimate-sequence recurrence is continued without restart inside each shifted-prox phase, expands only on positive certified-lower residual, and stops at the observable quarter-bracket.  Across the named structured graphs and evolutionary searches, phase time stayed below about `1.67` normalized root units over `alpha in [10^-4,10^-2]`, while paths safely admitted every vertex. | Dense floating-point evidence only.  It neither proves the masked recurrence theorem nor charges boundary accumulation in a sparse implementation. |
| Same-time omniscient-domain stop | On the four-vertex graph `E={01,02,12,13}`, `alpha=1/100`, `rho=1/10`, an exact `Q(sqrt(202))` retained-prox trace has the omniscient final-prox-face lower state exceed the masked lower state by `0.000110470130852219...` at phase 6, product 3. | The failure is exactly the newly admitted coordinate's zero-append lag.  It refutes same-time coordinate domination, not a one-product-lag comparison or the quarter-bracket phase bound. |
| Canonical one-step domain dominance stop | On the stored 30-vertex simple connected unit graph `lag30`, `alpha=1/10000`, `rho=17/2000`, an exact `Q(sqrt(20002))` trace has a strict lag-one lower deficit in retained-prox phase 8, product 86.  The degree-coordinate value is `0.0002892921587903060918853871424203...`; the normalized maximum is `0.0007417822237238216`, with simultaneous primal and next-extrapolate failures. | This refutes `OneStepDomainDominance` inside the exact canonical single-source class, despite exhaustive six-vertex evidence at one parameter choice.  The witness phase still ends in `1.6405` root-time, so it does not refute `QuarterBracketMaskedNAG` or the target bound; it only closes the pointwise-shadow transfer route. |
| Certified diagonal/block residual push | For Stieltjes `B`, a certified lower `u`, and positive residual `r=h-Bu`, simultaneously adding `p_i=r_i/B_ii` on any visible positive-residual batch preserves the lower certificate.  The block variant `p_W=B_WW^-1r_W` is also nonnegative, zeros the residual on `W`, and increases it outside.  Against any other certified lower `w`, an ambient all-coordinate diagonal Jacobi push contracts `max(w-u)^+` by factor at most `1-min_i(B1)_i/B_ii`; for `H+alpha D`, the improvement is `4alpha/(1+3alpha)`. | Diagonal positive append and one active push cost only a constant extra product plus one scan per newly admitted coordinate and repair all current lag witnesses.  The ambient contraction is an algebraic benchmark, not permission to scan unread rows; the block identity likewise needs a charged deterministic batch solver.  The proved diagonal factor is only `1-O(alpha)`, so it cannot by itself establish `O(alpha^-1/2)` phase time; a joint NAG/push potential remains open. |
| Pushed-domain route | If the masked lower state after one safe active Jacobi push and maximal diagonal positive-append closure dominates the same-time unpushed omniscient lower envelope, fixed-face NAG transfers directly and proves `QuarterBracketMaskedNAG`, hence the target. | This comparison has zero deficit on lag4/lag30, structured graphs, 1500 random graphs, all 26,704 labelled connected six-vertex graphs at one parameter, and a 17,472-run five-vertex grid.  Fraction implementations check 4,596 exact histories, including all 728 connected five-vertex graphs at both `alpha=1/7` and `alpha=1/199` over three thresholds and two additional four-vertex small-root grids.  It remains a candidate, not a consequence of the generic `1-O(alpha)` push contraction. |
| Shadow residual cover reduction | For pre-push masked envelope `u`, same-time omniscient envelope `w`, and `r=h-Bu`, the coordinate inequalities `B_ii(w_i-u_i)<=[r_i]_+` imply `PushedDomainDominance`.  With `delta=w-u`, `C=diag(B)-B`, and omniscient slack `r(w)=h-Bw`, the nontrivial case is exactly `C delta<=r(w)`.  An independent set of positive-deficit vertices is sufficient. | This is a proved local reduction, not a proof that paired NAG histories satisfy it.  The verifier/search now records residual-cover and adjacent-deficit violations; tested enhanced canonical traces remain nonpositive, but a history theorem or strict counterexample is still missing. |
| Strong pre-push lead candidate | Every tested enhanced history already has `u>=w` immediately after retraction and before its current push, making shadow residual cover vacuous.  The all-five-vertex floating grid is nonnegative up to `2.50e-16` roundoff, and the rational exhaustive histories are exact. | `PrePushDomainDominance` would replace the shadow-cover clause, but it is stronger and remains unproved.  The canonical-load retraction-isotonicity stop prevents deriving it from current order alone. |
| Pushed one-step lead candidate | After the synchronized first product, tested histories satisfy `ell_masked^push(t)>=w_omn(t+1)`, so monotonicity gives the next pre-push dominance.  Structured relevant margins are strict, and the exact `alpha=1/199` four-vertex enumeration has minimum lead about `3.91e-6`. | First-product repair plus this single temporal inequality proves pushed-domain dominance and the target.  It is not implied by a static Jacobi cap, which has an exact canonical counterexample; the accelerated masked history is essential. |
| One-step state-order stop | In the exact `alpha=1/199` four-vertex audit, prior masked lower/current have positive lead over the next omniscient lower/current, but one-step auxiliary and extrapolate gaps are strictly negative (about `-0.01388` and `-0.000434`). | `PushedOneStepLead` concerns the retracted envelope only.  A time-shifted full-state order cannot prove it; the certificate/retraction geometry must be used directly. |
| Proved first-product pushed dominance | At a synchronized phase start, active residual `r_A>=0` gives masked raw current `ell_A+r_A/L` with residual `R_AA r_A>=0`, so its shave is zero.  The omniscient common current is identical but shaved nonnegatively.  Any positive omniscient exterior lower is at most `r_i/L`, while masked exterior residual is at least `r_i` and diagonal append gives at least `r_i/B_ii>=r_i/L`. | The first enhanced product dominates unconditionally.  Therefore the compact temporal route has exactly one open premise: `PushedOneStepLead`; if it holds after the base product, pushed-domain dominance and the target follow. |
| Proved common-face second-product lead | For a synchronized fixed face and nonnegative start residual `r`, the first pushed lower and second omniscient raw current satisfy `U_1-c_2^o=s(1-s)Rr/[B_ii(1+s)]>=0`; the omniscient retraction can only lower the envelope. | This proves the first-to-second `PushedOneStepLead` exactly.  It does not iterate: later state and admission history remain essential. |
| Exact point-source supplied-face stop | On a 12-vertex simple connected unit graph at `s=1/250`, a point-source zero start with the full face supplied has exact third-product lead `min(U_2-w_3)=-0.120502...`; products 1--3 have no masked shave, and product 3 is the first quarter-residual stopping product. | Single-source load, fixed face, and `MaskedNoShave` do not imply the temporal lead.  This is not a counterexample to the local algorithm because the supplied face pre-certifies zero rows.  Starting from the minimal source face and applying the prescribed positive-append cascade reaches the full face after product 1 yet keeps strict lead `0.0039679` through product 3. |
| Certified source arborescence | Exact-positive closure keeps the certified face equal to the positive lower support after the initial source update.  Every shifted-prox lower `u` also obeys the original certificate `Qu<=load`; hence each positive non-source row has a neighbor of strictly larger `u`, so every positive lower superlevel is connected and source-rooted. | This rigorously excludes the supplied-zero-face stop and identifies first-admission values/parents as the missing chronology.  Connectivity/ascent alone has not yet been converted into `PushedOneStepLead` or a no-reuse work bound. |
| Temporal residual-shield equivalence | With preceding pushed lower `U`, next omniscient input `q`, residual `r_U=h-BU`, shave `theta`, and `R=I-B/L`, the exact candidate gap is `U-c+theta=R(U-q)-r_U/L+theta`.  Given old-envelope order, `PushedOneStepLead` is therefore equivalent on candidate-growth rows to `L[R(U-q)]_i+Ltheta>=r_U,i`. | This turns the single open premise into a local flux payment: post-push slack is incoming `Cp` plus later one-time append flux.  Charging append flux to source-arborescence admission edges and repeated flux to the momentum cone is a concrete route, but the combined inequality is unproved. |
| Chronological flux decomposition | Within one enhanced product, label the active push batch `0` and append batches increasingly.  If `p_j` is the diagonal increment and `t(j)` its batch, the final certified residual is exactly `r_i=sum_{t(j)>=t(i)}C_ij p_j`: row `i`'s own push cancels every earlier contribution and only same/later increments remain. | Append terms are globally one-time and directed by admission chronology; active-batch terms can recur.  This supplies the exact debt representation needed by `TemporalResidualShield`, but a source-arborescence plus momentum-flux payment inequality remains open. |
| Causal cancellation identity | With pre-push `V`, post-closure `U=V+p`, next omniscient extrapolate `q`, and `gamma=L-B_ii`, exact substitution gives `LR(U-q)_i-r_U,i=gamma(U_i-q_i)+C(V-q)_i+sum_{t(j)<t(i)}C_ij p_j`. | Same/later push debt cancels completely.  Adding omniscient shave `Ltheta` yields an exact four-term equivalent of `PushedOneStepLead`: diagonal gap, pre-push neighbor flux, positive earlier-admission credit, and shave.  Proving its sign on canonical histories is the sharpest remaining local task. |
| Stopped post-append cleanup / two-level lead candidate | One extra diagonal push after maximal append, followed by a second append cascade, changes the exact shield to `gamma(U^(2)-q)+C(U^(1)-q)+early_credit+Ltheta`.  Cleanup scans charge to the next enlarged core face except once per outer phase, so total work stays `O(W_core+M polylog)`. | The exact supplied-face stop becomes `+0.083327...`; lag30 gains about 100x lead margin, structured phases stay below `0.99` root-time, and 10,000 arbitrary fixed-face histories show no reversal before their first quarter-residual crossing (minimum `5.78e-9`).  A generic product-seven reversal exists only after its phase should have stopped.  Proving the stopped sign is a single-premise route to the target; static two-Jacobi capping is exactly false on `K_1,3` by `0.0004853298...`. |
| Exact stop-guard separation for two cleanup pushes | A Fraction-exact 12-row arbitrary-residual fixed-face trace at rational root `430/51139` crosses the quarter-residual threshold after product one (`0.163995...`) but, if illegally continued, reverses product-three temporal lead by `0.000122140052...`.  A separate exact enumeration of all 38 connected four-vertex graphs and all 15 nonzero binary residuals passes at roots `1/2,1/10,1/50`; two skewed 80-profile grids add 6,080 histories. | Two pushes do not imply an all-time lead.  The exact open statement is the stop-guarded implication `predecessor bracket>delta/4 => next-envelope lead`; the finite audits do not prove it. |
| Masked input-residual normal form | If the masked extrapolate has `h-Bq>=0`, then its raw current has residual `R(h-Bq)>=0`, the shave is zero, pre-push lower equals the raw current, and the push/clamp resets `current=lower`.  Exact small-graph canonical audits and adversarial searches satisfy this condition (worst floating value `-7.2e-25`). | This is a proved implication and a strong candidate reachable-state invariant, not a proved history theorem.  Generic fixed-face starts violate it, and the exact supplied-face lead stop fails while still no-shave, so the normal form alone cannot prove temporal dominance. |
| Input-residual frontier safety | If `A subseteq supp(x*)`, `q` is supported on `A`, and `h_A-B_AAq_A>=0`, then `q_A<=B_AA^-1h_A<=x_A*`; consequently every exterior row with `h_i-(Bq)_i>0` belongs to `supp(x*)`.  After admitting all such rows, every ignored exterior gradient is nonnegative. | This makes extrapolate-frontier discovery one-hop, deterministic, and output-safe.  It depends on the active input-residual cone; arbitrary accelerated states need not satisfy it. |
| Conditional InputConeFrontierNAG theorem | Under `StoppedMaskedInputResidual`, active partial-gradient NAG with input-residual frontier admission contracts `E=f(x)-f(x*)+(2alpha/2)||z-x*||^2` by `1-s`, `s=sqrt(2alpha/(1+alpha))`.  Omitted exterior gradient terms are nonnegative in the strong-convexity lower model, while certified lower push/clamp is a Stieltjes/Euclidean Fejer improvement.  Safe face admissions change no state.  Therefore each prox phase costs `O_tilde(M/sqrt(alpha))`, and the two-certificate outer loop closes the target. | The implication is proved, but its literal quarter-stop premise is refuted on the prescribed canonical zero-start history.  Exact chronology cells refute every fixed `beta<0.4897917473484638...`; only the remaining interval to `1/2` survives this route. |
| Pushed-state order-only stop | In exact scalar arithmetic with NAG root `1/2`, omniscient `(current,auxiliary)=(8,14)` and masked `(9,12)` have the same extrapolate `10`.  The legal map `T(q)=q+(2-q/2)` gives common next current `7`; common retraction gives lower `4`, yet next extrapolates are `20/3` and `19/3`, reversing the desired order by `1/3`. | Lower/current/extrapolate coordinate orders alone are not an inductive invariant because the next extrapolate has a negative old-current coefficient.  This state is not proved reachable from the canonical zero initialization, so it scopes the proof obligation rather than refuting `PushedDomainDominance`. |
| Retained momentum-flux identity | For `sigma=alpha`, `s=sqrt(2alpha/(1+alpha))`, `R=I-B/(1+alpha)=((1-s^2)/2)I+K`, `K>=0`.  If `g,d,z` are masked-minus-omniscient extrapolate/current/auxiliary gaps, the next raw extrapolate gap on a common active row is exactly `[s(1-s)z+2Kg]/(1+s)`. | This identifies how neighboring extrapolate surplus can pay a negative auxiliary gap and explains the enhanced traces.  It does not prove that the paired source chronology preserves the momentum-flux cone. |
| Reachable-clamp compatibility | For a NAG raw state whose old current is above the old lower and whose shaved candidate is no larger than the raw current, the clamped extrapolate is exactly `max{q_raw,ell,(c_raw+s ell)/(1+s)}`.  Therefore raw-current, raw-extrapolate, and retracted-lower order imply post-clamp current/extrapolate order. | This closes the clamp link in the conditional state induction.  Arbitrary current/auxiliary/lower triples do not obey the identity; reachability through the NAG auxiliary formula is essential. |
| Clamp/push-flux preservation stop | With `s=1/2` and `K_12=K_21=1/8`, an exact paired state using genuine NAG raw auxiliaries has ordered raw current/extrapolate/lower and flux `(7/12,23/12)`, but legal clamps give `(-1/6,1/3)`.  For retained-form `B=[[5,-1],[-1,5]]/7`, a common load certifies both lowers and one legal masked diagonal push still gives `(-2/15,13/30)`. | State order survives, but neither reachable clamping nor generic certified push preserves the stronger momentum cone.  The common load/state is not asserted canonical single-source zero-start reachable, so the actual source chronology may still re-establish it. |
| Boundary-flux exterior gate | On a zero-history exterior row, `(Cq^o)_i<=(Cu)_i` implies `r_i(u)>=L c_i^{o,+}`.  Positive omniscient current therefore forces safe diagonal admission whose value dominates both the current and next extrapolate; nonpositive current clamps to zero.  The parameter inequality is exact for `0<alpha<=1`. | This proves `ExteriorPrimalGate` from a local weighted-front premise.  Boundary-layer order `u>=q^o` on active exterior neighbors is sufficient and survives the five-vertex grid and moving-path searches, but its chronological preservation is open. |
| Conditional three-clause closure | If `MomentumFlux`, `ShadowResidualCover`, and `BoundaryFluxGate` hold at every enhanced transition, then raw-current order, the momentum identity, reachable-clamp compatibility, and safe append give full same-time pushed-domain dominance.  Fixed-face NAG, the two-certificate outer loop, and `W_charged<=2W_core+M` then prove `O_tilde(M/sqrt(alpha))`. | This is a rigorous conditional theorem.  The only missing proof is preservation of the three local premises by the prescribed zero-start source chronology. |
| One-Jacobi envelope-cap stop | On the canonical two-vertex unit edge at `alpha=1/7,rho=1/10`, the retained root is `1/2` and the fourth exact omniscient lower envelope exceeds one positive-residual Jacobi push from its predecessor by `(1/3200,1/3200)`. | The Jacobi map is isotone, but one push does not prepay a whole accelerated envelope round.  This refutes only `OneJacobiEnvelopeCap`, not the paired enhanced recurrence. |
| Pushed-retraction isotonicity stop | On the canonical unit edge with `alpha=1/10000,rho=1/1000`, use the certified constant-shave old lower `(69873,69853)/200000` and ordered phase currents `(494953,494866)/10^6 <= (645553,640166)/10^6`.  Uniform retraction plus a full diagonal push reverses order by exactly `(1693660617,1693140669)/40012000000`. | The graph, retained load, and old lower certificate are canonical, so the composite repair map is not isotone even in that phase namespace.  The ordered currents are not proved reachable from zero-start NAG; `PushedDomainDominance` remains open but must use the reachable source history. |
| Enhanced diagonal sparse ledger | For the positive-append/active-residual-push recurrence, push work is at most core scratch-product work and append work is at most final support volume: `W_charged<=2W_core+M`. | Exact adjacency-list accounting, with no randomization.  It applies only to diagonal repair; dense block solves remain uncharged.  The missing theorem is now solely an `O_tilde(M/sqrt(alpha))` bound on core products/history. |
| Canonical support-radius theorem | The reversible-walk formula `d_i pbar_i=(alpha/a)sum_t(b/a)^tP^t(v,i)` and the Varopoulos--Carne Gaussian bound give `pbar_i<=C(d_i d_v)^-1/2 exp(-c sqrt(alpha)dist(v,i))`.  Since every `i in S*` has `pbar_i>alpha rho/a`, the final support radius is `O(alpha^-1/2 log(1/(alpha rho)))`; `{y*>eta}` has radius `O(alpha^-1/2 log(1/eta))`. | This closes serial graph-distance depth without randomness.  Ambient ball exploration is not output-local because of false hubs, and the theorem does not bound same-layer port multiplicity or implement their Schur response. |
| Canonical complement-port forest | For every nested source-containing trace `U_t subseteq S*`, the connected components of `S*\U_t` only split and shrink, so all such components form a laminar forest.  Strict ascent for both the Green profile and `y*` implies that each component's corresponding maximum is attained at a port adjacent to `U_t`, with a strictly higher matching amplitude already in `U_t`. | This localizes a Green or constant-shave charge to the branch in which an event occurs and avoids masking by untouched parallel components.  It does not bound branch depth: tiny boundary removals may preserve the component maximum and almost all its volume. |
| Green ground-state upper obstacle | With the full point-source Green profile `pbar=alpha H^-1e_v`, the substitution `q=pbar-y` converts RPPR exactly to `min_(q<=pbar){q^THq/2-alpha rho d^Tq}`; its solution has `0<=q<=pbar` and `S*={i:q_i<pbar_i}`.  For `q=diag(pbar)w`, the energy is `alpha pbar_v w_v^2+b sum_E pbar_i pbar_j(w_i-w_j)^2`, and `sum d_i pbar_i=1`. | The support is the noncontact set of the constant relative obstacle `w<=1`, with total transformed load `alpha rho`.  The profile is dense/ambient (or final-support dependent), so constructing its weights is not a free local algorithm or a response producer. |
| Relative-slack connected cap | For `s=y*/pbar=1-w`, every non-source positive row has a strictly larger-`s` neighbor, so each positive superlevel `{s>eta}` is connected and source-rooted.  The lower path `y^(eta)=diag(pbar)(s-eta)_+` is an active-row subsolution and satisfies `F(y^(eta))-F(y*)<=alpha pbar_v eta/2<=alpha eta/(2d_v)`. | Only logarithmically many relative-slack bands are objective-significant and their shells partition `S*`.  This is an oracle comparison path: computing or uniformly shaving in the `pbar` direction is the missing dense Green response/`CenterLift`. |
| Missing-slack face cap | For any source-containing `U subseteq S*`, let `m(U)=max_(S*\U)y_i*/pbar_i`.  Since the oracle truncation at `m(U)` is supported in `U`, restricted optimality gives `F(x^U)-F(y*)<=alpha pbar_v m(U)/2`. | The largest remaining relative slack certifies all future exact-center energy offline.  The actual algorithm neither knows this slack nor guarantees that its global maximum falls at each parallel-component event. |
| Green--Schur capacity ledger | On the final support, Green normalization gives `K1=kappa e_v`.  For the Schur complement `Sigma_U` onto `R=S*\U`, `h_U=Sigma_U1=-kappa K_RU K_UU^-1e_v>=0` and `cap(U)=1^Th_U<=kappa`.  If `i` maximizes the final relative slack on `R`, its exact gate obeys `g_i>=(h_U)_i s_i`.  Admitting `B` drops capacity exactly by `(h_U)_B^T(Sigma_U)_(BB)^-1(h_U)_B`, so all such packets telescope to at most `kappa=alpha p_v`. | This is a deterministic threshold-free port bank.  Turning `g_i>=s_i h_i` into the displayed quadratic capacity payment by Cauchy squares the gate and reintroduces the absolute publication margin; a scale-relative dwell or harmonic-mass work bound remains open. |
| Single-source amplitude homotopy | For `F_t(y)=y^THy/2-(t alpha e_v-alpha rho d)^Ty`, the solutions have nested support, equal `t` times the ordinary `rho/t` path, and on each face move in the one Green direction `alpha H_UU^-1e_v`.  The full path has `H`-length at most `sqrt(alpha/d_v)`, dominates `p(s-(1-t))_+`, and satisfies `F_1(y(t))-F_1(y(1))<=alpha p_v(1-t)/2`. | All exact activation thresholds lie on one global scalar clock.  A local two-mask method still needs `SourceRampShadow`: safe lower-state tracking through face events, without false exterior materialization or repeated whole-face scans.  Bounded path length alone is only an additive budget. |
| Source-gate slope bank | On a held face, every exterior exact gate has nonnegative source-amplitude derivative `-H_iU p^U`, and the sum of all those derivatives is `b sum_U d_out,U(i)p_i^U<=alpha`.  Hence the continuous gate variation over one global source ramp has total `l1` mass at most `alpha`, regardless of breakpoint count. | Schur jumps at face admissions are not source-ramp variation; they require the orthogonal center/capacity bank.  An `l1` gate budget alone still cannot count full products without a scale-relative crossing/tracking inequality. |
| Landscape hierarchy | The local potential `V_U(i)=alpha+c d_out,U(i)/d_i` identifies monotone low wells; all dyadic well forests cost `O_tilde(M)` structural work.  A supplied closure gives `O_tilde(min_tau[A_fast(tau)/tau+B_land(tau)tau/alpha])`. | A Bessel-stable frame/implicit Schur state through well entries and merges. |
| Projected two-mask NAG | Correctness, non-deadlock, lattice projection, immediate scratch readiness, held contraction, event debt, and the full-spectrum `F0` cut bank are proved. | A scale-relative publication/occupancy clock converting the additive bank into elapsed products. |
| Certified-gap adaptive NAG | For any charged `mu<=lambda_min(Q_U)`, `F_mu=(1+mu)Q-Q^2-mu aI` is Stieltjes/lattice compatible, contracts by `1-sqrt(mu)`, and keeps the exact principal-growth cut drop.  Nonincreasing dyadic gap changes are nonexpansive after physical-state rescaling, and all recenter debts sum to `O_tilde(alpha)`. | The same publication/occupancy conversion; a cheap certificate may also be loose.  At `mu=alpha` this is the original open clock. |
| Certified-gap fixed-face square function | Tuned NAG on one face obeys `sum_t||Cq_t||^2<=[(3+sqrt(5))/(8sqrt(mu))]J_mu` for every fixed exterior cut; the scalar constant is sharp. | The exact future-output Gramian acquires a shifted harmonic Schur lift when the face grows; `P4` shows zero instantaneous interface output does not pay it. |
| Quarter-scale response epochs | Exact fixed-face Gramians give full cut-square and quadratic-variation bounds.  Their moving-face term is one resolvent shifted by `Theta(tau)`, with Chebyshev radius `tau^-1/2`; `tau^(3/2)/alpha` epochs would cost `M tau/alpha`. | Output-sensitive persistence/no-reuse packing of the shifted harmonic response through face events. |
| Positive Poisson clock | `d'=A^-1/2(g-Ad)` is positive, monotone, domain ordered, and root-time convergent. | Sparse application of `A^-1/2`; universal positive walk polynomials need degree `Omega(1/alpha)`. |

These statements may be composed only when the missing operation in the last
column is explicitly supplied and charged.  In particular, neither the
positive reporter nor a spectral sparsifier makes `CenterLift` free.

## C. Strict stops and counterexamples

| Tempting inference | Witness / reason it is false |
|---|---|
| “The formed face has a narrow spectrum.” | Every diagonal is `(1+alpha)/2`, hence `lambda_max>=1/2`; a face with minimum eigenvalue `Theta(alpha)` has condition number `Theta(1/alpha)`. |
| “Measure every current gap and restart CG.” | The canonical ballasted broom has `Theta(1/sqrt(alpha))` large faces, each with gap `Theta(alpha)`, for `Omega(M/alpha)` fresh work. |
| “Choose the largest scalar Richardson step that keeps the residual nonnegative.” | On an endpoint path from `r_0=e_1`, residual parity forces every Collatz-safe maximal step to equal `1/a`; the method is exactly `r_t=(B/a)^t e_1`.  A path of length `Theta(1/sqrt(alpha+sigma))` therefore needs `Omega((alpha+sigma)^-1 log(1/(alpha+sigma)))` steps for polynomial residual accuracy. |
| “Warm-started short CG recurrence survives a new coordinate.” | Padded directions keep mutual conjugacy but acquire defect `P^T H_UBg_B`; exact repair is the dense Schur lift.  A three-coordinate path already breaks the unmodified recurrence. |
| “A connected landscape well has one slow scalar.” | A hub joining `r` large cliques is one well but has at least `r` eigenvalues below `alpha+c/W`. |
| “Union--find is the well response state.” | One endpoint added to a path needs the dense vector `(A^-1e_n)_i proportional sinh(i kappa)` with `kappa=Theta(sqrt(alpha))`. |
| “Dynamic terminal Schur equals the active face.” | It maintains `Q_U-Q_UWQ_W^-1Q_WU`, not `Q_U`; on one edge the two values are `alpha/a` and `a`, separated by `Theta(1/alpha)`. |
| “Future Schur energy is relatively bounded by active error energy.” | A reachable canonical two-vertex checkpoint with zero unopened residual has ratio `c^2/(2alpha)`.  The valid repair contains the spectral factor `(A-alpha I)(I-A)/alpha`. |
| “Retune the critical `F0` metric only by scalar weights and replace `sqrt(alpha)` with `sqrt(alpha+tau^2)`.” | The floor-mode energy ratio tends to two; three scalar modes rule out that natural diagonal family.  It does not rule out the proved certified-gap operator `F_mu`, which adds `-c(mu-alpha)S`; `mu` must be a safe face-gap lower bound, not a free cutoff. |
| “A deterministic dynamic hierarchy follow-up removes the small `o(1)`.” | The 2026 proper-router data structure retains `n^(o(1))` overlap/congestion/update factors and stores routes, not a Schur response. |
| “Immediate scratch readiness means the next publication occurs in one product.” | An exact `Q(sqrt(10))` canonical trace on a 34-vertex simple-unit graph has publication times `[0,1,2,3,4,38]` at `alpha=1/1000,rho=1/100000`; every exterior residual at products 5--37 is nonpositive and the final batch is strictly positive.  All lower-subsolution and early-stop tests are certified, and exact elimination proves `S*=V`.  Scratch dominance closes admission only.  This strict recurrence-specific counterexample remains compatible with a shared root clock because `34sqrt(alpha)=1.075`; it is not a lower bound for all algorithms or the target runtime. |
| “One product of lag makes the masked lower state dominate the fixed-final-prox-face omniscient lower state.” | The exact `Q(sqrt(20002))` canonical `lag30` trace at `alpha=1/10000,rho=17/2000` has a phase-8/product-86 degree-coordinate lag deficit `0.000289292158790306...`; normalized lower, primal, and next-extrapolate lag comparisons all fail.  The same phase still finishes in `1.6405` root-time, so the pointwise transfer is false while the aggregate quarter-bracket bound remains open. |
| “Coordinatewise lower/current/extrapolate domination is automatically preserved by one common NAG step and retraction.” | The exact scalar pushed-state witness above starts with all three desired orders and reverses the extrapolate order by exactly `1/3`.  Reachability is deliberately not claimed; the stop is against a state-order-only induction. |
| “Retraction followed by one safe diagonal push is an isotone map of the input current.” | The exact retained two-vertex witness above has ordered inputs but strict reversed outputs in both coordinates.  It is a generic Stieltjes-map stop, not a canonical chronology counterexample. |
| “Every long publication releases at least `1/30` of the remaining center-packet energy, or else the global missing Green/solution maximum falls or face volume grows by at least `105/104`.” | An exact `Q(sqrt(10))` canonical thresholded trace on a fixed 40-vertex simple-unit graph has events `[0,1,2,41,91,92,109]` and `S*=V`.  The event-41 packet fraction is `0.0312985369...<1/30`.  At events 41 and 91 both the global missing Green maximum and the global missing final-solution maximum are unchanged, while face volume grows only by `26/25` and `105/104`.  Thus the displayed packet constant and every such global amplitude/volume dichotomy demanding growth at least `105/104` are strictly false.  This finite witness does not rule out a smaller universal packet constant, an asymptotic aggregate clock, or a component-sensitive constant-shave charge. |
| “The fixed-face quadratic-variation bank splices through face growth.” | On unit `P_3`, adding the third vertex makes the exact remaining-variation Gramian jump strictly for `h=e_1,w=0`, although `Ch=Cw=Cq=0`.  Its hard term is a shifted harmonic resolvent, so no finite multiple of the instantaneous `F0` cut square pays the jump. |
| “Explore an ordinary `tau^-1/2` graph ball to implement the shifted response.” | In the canonical seed--hub--`N`-leaf tree with `rho=2/(N+1)`, the exact obstacle support is only the degree-one seed (`M=1`), while its radius-two ball touches `N+1` edges.  Hop locality alone is not output locality. |
| “One positive point-source Green reservoir dominates every raw NAG hard lift.” | An explicit canonical simple-unit tree makes a raw critical-NAG coordinate negative while both it and the exact solution exceed the publication threshold by more than 25 orders; coefficient isolation on high-degree trees rules out every universal pointwise constant under the same support/threshold hypotheses.  A 39-vertex tree makes the hard lift larger than the full reservoir increment, and at fixed `alpha=.01` high-private-degree paths make their ratio `Omega(1.22745543^t/t)`.  This also defeats payment by past/current raw cut squares on that row.  Exact maximal-batch chronology is not shown, so stopped low-band, future-tail, and chronology-specific routes remain open. |
| “Coordinate clipping restores a significant-coordinate Green upper bracket.” | A finite simple-unit tree has a fixed principal face where exact projected NAG overshoots the exact Green coordinate by `26.5766...`; canonical tiny `rho,epsilon_obj` give `S*=V` and keep both values far above the publication threshold.  The face/time is not proved to occur in the exact maximal-batch chronology. |
| “Fixed-face square functions telescope through mask growth using instantaneous cut and center charges.” | Tuned `P4` has zero old cut, zero expansion-interface output, and zero first two exterior outputs, followed by a strictly positive exterior output.  A canonical `P3` event makes the center charge arbitrarily small while a legal old packet has fixed positive future shifted-resolvent lift.  The `P3` state is not proved reachable from zero, so this is a local-payment stop rather than an algorithmic lower bound. |
| “The shifted `alpha^-1/4` depth implies only `alpha^-1/4` response builds.” | It bounds memory for one fixed load.  A unit path admits `Theta(alpha^-1/2)` significant fresh positive loads with only `O(alpha)` total square mass; independent clique modules add fresh response directions.  Load aggregation/disjoint attenuation is an additional premise. |

The counterexamples are scoped.  They refute the named proof templates or
interfaces, not every possible deterministic graph algorithm.  Paths in
particular compress their dense response by a transfer recurrence, showing
why “dense vector” is not an unconditional lower bound.

## D. General-graph decision boundary

The general target

```text
O_tilde(M/tau+M tau/alpha),
```

and hence `O_tilde(M/sqrt(alpha))`, is proved after either of the following
producer contracts, but neither contract currently has a deterministic
support-local general-graph implementation:

1. `CenterLift(tau)`: persist old-face inverse/Schur responses and emit the
   certified group summaries in `O_tilde(M/tau)` total work; or
2. `AmortizedLowerShiftedApply(tau)`: maintain a certified-lower accelerated
   shifted solve through any chosen visible zero-appends, reducing the lower
   state's joint active-debt/positive-boundary norm to each intermediate
   outer-call tolerance, with total response/rescan work
   `O_tilde(M/tau)`.  A parallel nonnegative raw state may use its global
   minimum-subgradient cap only for terminal exit.  Tiny positive exterior
   scores need not be appended; exact zero-margin discovery additionally
   requires an exact solve or interval refinement.

The low-side reporting ledger `O_tilde(M tau/alpha)` is closed once those
summaries exist.  Therefore the unresolved issue is response production,
not Gaussian candidate detection, normwise solve accuracy, or advance choice
of `tau`.

For reference, the exact clipped optimizer of `A/tau+B tau/alpha` on
`sqrt(alpha)<=tau<=1` is `tau=sqrt(alpha)` when `A<=B`,
`tau=sqrt(A alpha/B)` when `B<=A<=B/alpha`, and `tau=1` when
`A>=B/alpha`.  The respective values are `(A+B)/sqrt(alpha)`,
`2sqrt(AB/alpha)`, and `A+B/alpha`.

With a common certified final-face lower bound `mu_bar`, every supplied
producer contract and the shifted-prox contraction replace the low
denominator by `mu_bar`, giving the conditional curve

```text
A/tau+B tau/mu_bar,       sqrt(mu_bar)<=tau<=1.
```

The same clipped formula holds with `alpha` replaced by `mu_bar`; in
particular `A=B=O_tilde(M)` is minimized at `tau=sqrt(mu_bar)` with value
`O_tilde(M/sqrt(mu_bar))`.  The new `F_mu` theorem makes nonincreasing
certified-gap state transport consistent with this improvement, but it does
not implement either general-graph producer contract.
