# Verification manifest

This manifest records what the executable checks certify.  A passing script
is evidence only for the statement listed here; it must not be promoted to a
stronger end-to-end claim.

| Artifact | Certified statement | Deliberate scope limit |
|---|---|---|
| `exact_delayed_clock.py` | Exact `Q(sqrt(10))` moving projected-NAG trace with events `0,1,2,3,4,38`; finite threshold, lower-subsolution, early-stop, connectedness, and `S*=V` checks. | Refutes immediate next-product publication for this recurrence only; not a target-runtime lower bound. |
| `green_band_stress_exact.py` | Exact `Q(sqrt(10))` canonical moving trace on a fixed 40-vertex simple unit graph, including the finite `eps_obj=10^-20` publication threshold, the event sequence `0,1,2,41,91,92,109`, `S*=V`, exact center-packet energies, exact final-Green/final-solution comparisons, and the complement-port forest. | It proves that the event-41 packet releases less than `1/30` of the remaining energy and that the event-41/event-91 global missing Green and solution maxima do not fall while face volume grows only by `26/25` and `105/104`.  This refutes those constants and the named global dichotomy, not an arbitrarily small constant, an asymptotic lower bound, or the overall shared clock. |
| `gap_adaptive_fmu_exact.py` | Tensor-Bernstein held-step LMI for `F_mu`, nonexpansive decreasing-gap transport, and the stale-gap triangle obstruction. | Algebraic state transport; not publication occupancy or a response implementation. |
| `adaptive_square_function_exact.py` | Sharp fixed-face constant `(3+sqrt(5))/8` and the `P4` delayed exterior output. | Fixed-face observability does not telescope through mask growth. |
| `f0_lmi_exact.py` | Critical `F0` held contraction and positivity certificates. | Gives an additive cut bank, not a count of held products. |
| `input_frontier_potential_exact.py` | Symbolically verifies the partial-gradient strongly-convex NAG potential cancellation; after combining smoothness, convexity, and strong convexity, the remainder beyond `(1-s)E` is exactly `-(1-s)mu||x-q||^2/(2s)`. | The omitted exterior-gradient term is safe only under the separately proved input-frontier sign condition; the script does not prove preservation of `MaskedInputResidual`. |
| `moving_face_smoothing_exact.py` | Fixed-face Gramians, shifted harmonic jump, and canonical `P2/P3` local-payment stops. | The legal `P3` packet is not proved to arise in exact zero-start chronology. |
| `path_gap_certificate_exact.py` | Path-Poincare lower certificate and exact ballasted-broom congestion formula. | Stops fresh per-face relaxation, not retained tree/Schur state. |
| `green_bracket_exact.py` | Exact threshold-significant raw Green-bracket counterexample. | Principal-face/raw-state statement; not exact maximal-batch chronology. |
| `green_lift_exact.py` | Exact hard-lift/source-reservoir ratio `1.126299...`. | Does not rule out a stopped low-band or future-tail reserve. |
| `projected_green_overshoot_exact.py` | Exact fixed-face projected-NAG/Green ratio `26.576618...`. | The face/time is not proved to occur in canonical chronology. |
| `cycle_rank_response_verify.py` | Tree cavity Green formula, `Q=B+cZZ^T`, and Woodbury solution identity on random small graphs. | Numerical identity audit; the `k^omega_mat` recursive-block-solve work theorem is algebraic and proved in `README.md`, including exact ordered-field and capped-output scope. |
| `shifted_debt_envelope_verify.py` | Scalar Schur envelope, global capped objective/distance bounds for lower states with mixed exterior scores, arbitrary nonnegative feasible states, and the one-step proximal-gradient output; zero-append block residual identity; exact packet orthogonality and source-square bank; the exact two-vertex nonmonotone constant-bracket stop; and the point-source solution-amplitude ascent, constant-shave/rho-path cap, retained exact/inexact shifted-prox bracket, Green ascent, mass, upper-obstacle, ground-state, relative-slack path/cap, missing-slack gate, Green--Schur capacity, and source-amplitude homotopy identities on random small normalized graphs. | Numerical normalization audit except for the fraction-exact two-vertex stop; all named formulas are proved algebraically in `README.md`, and the script does not implement `AmortizedLowerShiftedApply`, `SourceRampShadow`, or the missing shifted-response producer. |
| `collatz_safe_richardson_experiment.py` | Reproduces the endpoint-path degeneration of the maximal residual-positive scalar Richardson rule and reports its iteration scaling. | Floating-point illustration only; the parity/Jacobi identity and scoped lower bound are proved algebraically in `README.md`. |
| `source_clock_adversary_search.py` | Mutates connected simple unit graphs from random, path-like, or exact-stop-seeded initial families, optionally co-optimizes the canonical threshold, and searches root-scaled time/work, publication gaps, retained-prox shadow/state deficits, residual cover, momentum/clamp margins, exterior boundary flux/layer order, pre-push order, pushed one-step lead, masked input residual, or masked retraction shave; optional positive append, active residual push, and input-residual frontier admission are included.  Prox searches force exact-positive (`publication_floor=0`) closure. | Heuristic dense floating-point counterexample search only.  It found the canonical `lag30` baseline one-step-domain counterexample and a 50-vertex ramp horizon `8.82`; a 40-generation input-frontier search plus five-alpha crosscheck reached only `-9.1e-20` input-residual roundoff.  Values at machine precision are not theorem counterexamples or sparse work certificates. |
| `source_ramp_experiment.py` | Runs the dual-projected two-mask recurrence under one monotone source-amplitude ramp and compares publication horizon, terminal time, and volume work on the exact-delay graph, Green-band stress graph, layered graph, and lollipop. | Floating-point candidate evidence only.  A one-root-unit ramp collapses the 40-vertex stress publication horizon from `3.4785` to `0.1581` root units, but does not uniformly improve the lollipop or total terminal work; it neither proves `SourceRampShadow` nor removes full-face scans. |
| `retained_prox_experiment.py` | Runs the two-certificate outer loop and continues projected NAG through safe maximal face expansions inside each `sigma=alpha` proximal phase; checks exact prox order, retained bracket, support safety, quarter-bracket stop, lower/current/auxiliary/extrapolate order, shadow residual cover, adjacent deficits, raw momentum flux, reachable-clamp margins, exterior zero state, boundary flux/layer order, masked input/raw residual, and optional certified positive append / active residual pushes.  It can perform a post-append cleanup push plus second closure, or pre-gradient input-residual frontier admission, and audits the standard estimate-potential contraction. | Dense floating-point candidate evidence.  The baseline `lag30` deficits are independently reproduced exactly/high precision; the one-push enhanced `~1.67`-root and post-cleanup structured `~0.99`-root maxima do not prove chronological preservation.  Input-frontier admissions are asserted to stay inside the exact prox support, but finite tests do not prove preservation of the active input-residual cone.  Block-solve diagnostics are not charged algorithms. |
| `retained_prox_exhaustive.py` | Enumerates every connected labelled simple unit graph on two through six vertices over user-selected `alpha` and source-threshold grids, and records worst enhanced lower/state dominance, shadow residual cover, adjacent deficit, exterior boundary flux/layer order, and phase-time metrics. | Finite dense floating-point regression only.  The 17,472-run five-vertex grid has strict nonpositive boundary-flux/layer maxima and zero exterior extrapolate, but absence of a small-graph violation is not a history theorem. |
| `retained_prox_exact_exhaustive.py` | Uses degree coordinates and rational roots `s` (`alpha=s^2/(2-s^2)`) to enumerate complete enhanced histories with Fraction-exact solves, retraction, push, append, stopping, and branch comparisons.  The 4,368 five-vertex histories at `alpha in {1/7,1/199}` and 228 four-vertex histories at `alpha in {1/391,1/4999}` pass the recorded core inequalities (4,596 total; up to 1,003 products).  It also supports post-append cleanup and pre-gradient input-residual frontier admission.  The focused 728-run five-vertex one-push audit has strict relevant lead `2.91e-6`; the 728-run cleanup audit has strict relevant lead `3.92e-5`; 842 input-frontier four/five-vertex histories at `s=1/10` have exact nonnegative input residual. | Finite exact enumeration, not an arbitrary-graph history theorem.  The cleanup variant can have negative input residual, and static two-Jacobi capping fails exactly by `0.0004853298...`; zero margins often come from coordinates where histories coincide. |
| `masked_fixed_face_counterexample_exact.py` | Fraction-exact 12-vertex simple-unit point-source comparison at `s=1/250`: the common-face product-two lead holds, a gratuitously supplied full zero face reverses product-three lead by `0.120502...` before the quarter-residual stop, and the masked raw residual remains nonnegative.  Repeating from the minimal source face with exact-positive append reaches the same full face after product one and keeps strict `0.0039679` lead; two supplied-face diagonal pushes instead give strict `0.083327...` lead. | The supplied-full-face branch deliberately violates the local algorithm's minimal-certification chronology, so it is a strict stop for history-free fixed-face/no-shave proofs, not a canonical algorithm counterexample.  The two-push repair is exact on this witness only. |
| `masked_no_shave_search.py` | Samples arbitrary nonnegative fixed-face residuals and compares the enhanced pushed and unpushed recurrences while recording masked retraction shave, temporal lead, nonterminal lead, same-time lower/extrapolate gaps, and predecessor residual at every lead failure; diagonal-push rounds and first-quarter-residual stopping are configurable. | Floating-point counterexample search outside canonical source chronology.  One push has strict failures.  A 100,000-run two-push stopped sweep found no reversal; unrestricted failures have predecessor residual below the stopping threshold in the recorded searches.  This supports only a stop-guarded conjecture. |
| `two_push_fixed_face_exact_audit.py` | Enumerates all 38 connected labelled four-vertex graphs and configurable exact residual grids under two pushes through the first quarter-residual stop.  The binary grid passes 1,710 histories at roots `1/2,1/10,1/50`; two three-level skew grids add 6,080 exact histories at root `1/10`. | Deterministic finite Fraction audit, not a proof of the stopped implication or a canonical source-history theorem. |
| `two_push_post_stop_counterexample_exact.py` | Gives a Fraction-exact 12-row arbitrary-residual fixed-face trace at root `430/51139` whose two-push residual is already `0.163995...` after product one, but whose temporal lead reverses at product three by `0.000122140052...` if continued. | Strictly refutes unconditional all-time two-push lead.  It is not executed by the stopped phase and is not a canonical point-source/minimal-admission counterexample. |
| `retained_prox_gate_sweep.py` | Samples graph size, connected simple unit graph, `alpha`, and source-threshold fraction and records extrema for boundary-layer order, boundary flux, exterior state, pre-push lower order, shadow cover, pushed dominance, and post-clamp margin. | Finite dense floating-point regression only.  The recorded 2,500-run seed had 669 genuinely growing-face traces and no positive gate/dominance violation; near-zero late-phase margins are not symbolic certificates. |
| `retained_prox_sparse_tree_stress.py` | Runs the enhanced recurrence with sparse products on the canonical 12,286-vertex projected-overshoot tree at `alpha=10^-6,rho=10^-30`, auditing lower/primal/extrapolate order and the raw momentum-flux margin while separately charging core, push, and append work. | Floating-point stress only.  Exact-positive maximal append traverses the entire true support in its first closure, so the run verifies large sparse accounting and numerical stability but is not a hard moving-face dominance instance. |
| `pushed_state_order_exact.py` | Fraction-exact scalar NAG calculation in which masked lower/current/extrapolate order holds before a common gradient/retraction step but the next extrapolate order reverses by exactly `1/3`. | A strict stop for a state-order-only induction.  The input state is not asserted reachable from the canonical single-source zero start and therefore is not a counterexample to the enhanced recurrence. |
| `pushed_retraction_isotonicity_exact.py` | Fraction-exact canonical unit-edge phase calculation: a certified constant-shave old lower and ordered current inputs produce pushed-retraction outputs reversed by `(1693660617,1693140669)/40012000000`. | Strictly refutes isotonicity of the composite repair operator even at a canonical retained load.  The ordered currents are not asserted reachable from the zero-start source chronology. |
| `pushed_clamp_compatibility_exact.py` | Fraction-exact audit of the reachable clamp identity `q_clamp=max{q_raw,ell,(c_raw+s ell)/(1+s)}` on 2,184 states and 493,050 ordered pairs. | Finite audit of a general algebraic proof in `README.md`; it does not establish the raw momentum, lower, or boundary-flux premises. |
| `pushed_clamp_flux_exact.py` | Fraction-exact two-coordinate reachable-clamp state with ordered raw current/extrapolate/lower and flux `(7/12,23/12)`, post-clamp flux `(-1/6,1/3)`, and post-push flux `(-2/15,13/30)` under a retained-form Stieltjes matrix/common load certifying both lowers. | Strict stop for generic clamp/push invariance of the momentum cone.  The common load and paired state are not asserted canonical single-source zero-start reachable. |
| `pushed_exterior_gate_exact.py` | Fraction-exact parameter certificate for `B_ii<=L` and `s>=2alpha/(1+alpha)`, with squared slack `2alpha(1-alpha)/(1+alpha)^2`, which makes a safe exterior append dominate omniscient current and extrapolate under boundary flux. | Proves the parameter part and records the conditional implication; it does not prove `BoundaryFluxGate` along every history. |
| `omniscient_jacobi_cap_exact.py` | Fraction-exact canonical unit-edge trace at `alpha=1/7,rho=1/10` with `w_4-J(w_3)=(1/3200,1/3200)`. | Strictly refutes `OneJacobiEnvelopeCap`; it is not a counterexample to pushed-domain dominance. |
| `retained_prox_lag_exact.py` | Exact `Q(sqrt(202))` four-vertex retained-prox trace: strict same-time omniscient/masked lower-domination failure of `0.000110470130852219...`, together with exact lag-one lower, primal, and next-extrapolate inequalities through the witness phase. | The same-time failure is a theorem for the named recurrence.  The finite lag-one checks are evidence only, not a graph-uniform `OneStepDomainDominance` proof. |
| `retained_prox_lag30_exact.py` | Exact `Q(sqrt(20002))` retained-prox trace on the canonical 30-vertex simple connected unit graph at `alpha=1/10000,rho=17/2000`.  A rational phase-zero solve certifies the full omniscient face; inverse positivity keeps it full later.  The exact phase-8/product-86/vertex-12 lag-one lower deficit is `0.000289292158790306...`. | This is a strict canonical counterexample to pointwise `OneStepDomainDominance`, not to the observed aggregate quarter-bracket runtime or to every possible accelerated growing-face algorithm. |
| `retained_prox_lag30_high_precision.py` | Independently repeats the exact 30-vertex witness at 80 and 140 digits and selects the same proximal supports, publication branches, witness coordinate, and deficit. | Redundant numerical crosscheck for the exact script; it adds no stronger theorem. |
| `witnesses.py` | Numerical regression collection for the remaining finite witnesses. | Floating-point diagnostics, not theorem certificates. |
| `two_mask_experiments.py` | Structured/random chronology and Lyapunov regression, raw minimum-subgradient tracking, volume work, exact-center packet energies, final-Green amplitude ratios, and a fixed 40-vertex stress graph with several root-scale gaps. | Floating-point evidence only; the 34- and 40-vertex traces have separate exact verifiers.  The broader searches do not certify the chronology conjecture or an asymptotic counterexample. |

The exact symbolic suite was rerun with cached isolated dependencies as

```text
uv run --with sympy --with numpy --with scipy python <script>
```

and the exact delayed-clock verifier uses only the Python standard library.
The scalar shifted-debt envelope/local-append theorem is a direct
Schur-complement and block-multiplication proof; its numerical regression is
not treated as authority.  Likewise, the fast dense-core exponent is not
inferred from timing `cycle_rank_response_verify.py`.
The complete `witnesses.py --alpha 1e-4` JSON regression and a 120-random-run
two-mask regression passed.  A separate `alpha=3e-5` stress run was stopped
without a result after entering a 16.6GB dense SVD; it is not counted as a
test pass or as mathematical evidence.

## Repository gate status

The direction-local `ruff check`, `ruff format --check`, `compileall`, and
`git diff --check` pass.  `make agent-audit` also passes.  The repository-wide
gates are not green because of files outside this unregistered working
companion:

- `make lint` has one unused import in
  `problem_definitions/verify_exact_batch_cholesky.py`;
- `make note-audit` reports two over-limit sections in `aesp_cd_l1_rppr`;
- `make test` passes 209 of 213 tests and fails on those two inventory findings
  plus pre-existing notation findings in `related_work.tex` and
  `aesp_cd_l1_rppr`.

Those unrelated owned files were not modified.  The working companion is not
registered and therefore does not claim a controller-approved inventory or
promotion status.  `make reproduce` was also invoked and stopped at the same
four repository-wide pytest failures before reaching its experiment, figure,
and manuscript stages.
