# Direction status: volume_gated_acceleration

Last reviewed: 2026-08-22
State: proved-open
Review: Round 021 independently audited clean and promoted on 2026-08-22

## Exact question and contract

- **Question:** Can safe RPPR support gates preserve accelerated progress across certified subspace additions without a full restart or old-face scan per admission?
- **Model:** Shared PPR plus RPPR safe-support gates. A signed accelerated candidate is retracted to a lower envelope, negative outside KKT violations are admitted, and restricted acceleration continues on expanding certified subspaces.
- **Accuracy namespace:** Final PPR target is ||D^(-1/2)(x_hat-x^0)||_infinity <= eps_ppr. rho is RPPR regularization and tau is a one-sided KKT/error gate; the main choice is rho=tau=eps_ppr/2.
- **Access and charged work:** A full restricted step on U costs vol(U); every admission scan, old-face recurrence/response application, factor or Schur update/query, state write, materialization, validation, and output is charged in the shared eleven-coordinate order.
- **Rounds 012--020 exact path contract:** 0<q<=1/4, alpha=q^2, rho=tau=q/5, zero start, transported centers, ambient degrees, the internal complete all-violations gate, the full moving global correction, and one terminal external certificate/output return. The quarantined Round-013 lower candidate sets q=1/(16m) on the complete endpoint path P_m; the Round-014--020 finite audits use the actual q=1/5 execution on a sufficiently long ambient path. Rounds 015--019 use the literal stage order and a face-local error bank reinitialized after transport; they assume no alternative phase alignment or event permutation. Round 016 compares only two local held pairs. Round 017 keeps held-face evolution separate from the frozen-candidate stage-8 reset. Round 018 further separates held evolution, the fixed-U_3 candidate-production step, the instantaneous reset, and the first fixed-U_4 follow-up. Round 019 retains all four boundaries and then audits only the realized fixed-U_4 decreases through stage 13; its recovery reserve is an analytical telescope, not free state known in advance. Round 020 instead uses the face-general observable score Xi=delta^2 and a causal one-scalar credit ledger from held stage 3 through the consecutive canonical admissions at stages 4 and 8 and through stage 12. It credits a score decrease or exact restricted-optimum drop only after that event is realized and debits every score increase. Its finite balance is not a uniform horizon or all-admission theorem.
- **Round 021 exact nonpath contract:** The same exact recurrence, complete simultaneous all-boundary gate, and causal score ledger run on the asymmetric six-vertex T tree with edges `(0,1),(1,2),(2,3),(2,4),(4,5)`, seed `0`, ambient degrees `(1,2,3,1,2,1)`, and `q=1/5`, `alpha=rho=tau=1/25`. The ledger restarts with zero balance at held stage 3. This finite test charges no nonpath response vector: it proves the scalar STOP only after exact gate scores and restricted-optimum drops are available.
- **Intended result:** Uniform peak volume O(1/eps_ppr) and graph-uniform O_tilde(1/(rho*sqrt(alpha))) work, hence the PPR product scale after rho=Theta(eps_ppr).

## Claim ledger

- **Source:** RPPR/FISTA support and confinement facts, the classical bad-star phenomenon, LocCH, and AESP are source ingredients (`sec:scope`).
- **Proved here:** Principal conditioning and spider geometry; the exact-PPR core; RPPR support/error conversion; safe lower-envelope admission and certificate; exact peak-volume gate; the Schur gain, transported-center, weighted-shock, and endpoint-path append-only-response ledgers; the accuracy-driven swept-prefix lower family; the constant-ratio support/product floor; and the exact q=1/5 failure of frontier-only gate logic. Stable anchors are `lem:principal-conditioning`, `thm:exact-core`, `cor:accuracy-volume`, `prop:transported-schur-ledger`, `lem:weighted-shock-occupation`, `prop:path-implicit-transport-ledger`, `prop:path-zero-start-swept-separation`, `prop:path-constant-ratio-floor`, and `prop:path-frontier-only-gate-fails`.
- **Proved here (Round 012):** For the exact-real constant-ratio path execution, the full moving correction satisfies a pointwise energy bound independent of its maximizer. Every visited face admits or certifies within K_q=O(q^(-1) log(1/q)) steps, so T=O(q^(-2) log(1/q)) and mathfrak V_T=O(q^(-3) log(1/q)); `eq:path-moving-max-eleven-upper` charges all eleven coordinates (`thm:path-moving-max-soft-upper`).
- **Proved here (Round 014 finite STOP):** On the actual q=1/5 execution, stages 6 and 7 are consecutive held steps on U_3, but delta_7-delta_6=1747556648/751181640625>0 and delta_7/e_7=5361340160387/858557821215>1/q. The full run has J=4, T=16, nu_fin=9, n_fin=5, swept volume 114, one terminal return, and the specialized complete vector `eq:path-monotone-correction-stop-eleven-vector` (`prop:path-monotone-correction-potential-fails`). The proof verifies actual positive proximal and unclipped envelope states; it uses none of the Round-013 conditional assumptions.
- **Proved here (Round 015 finite STOP):** The explicit face-local correction--error bank Phi_t^bank(U)=delta(x^(t))+q^(-1)||D_U^(-1/2)(x^(t)-x_U^*)||_infinity absorbs the reviewed stage-6--7 spike: Phi_7-Phi_6=-156536838996284/5120805244140625<0. It nevertheless fails on the earliest later consecutive held pair in the literal chronology, stages 9 and 10 on U_4: Phi_10-Phi_9=11777177533637842088/2957905129146728515625>0 even though delta_10-delta_9=-24266663296/93897705078125<0 (`prop:path-correction-error-bank-potential-fails`). The proof retains the actually clipped safe envelopes and complete gate. The full charged run remains J=4, T=16, nu_fin=9, n_fin=5, swept volume 114, one terminal return, with the complete vector `eq:path-correction-error-bank-stop-eleven-vector`.
- **Proved here (Round 016 finite STOP):** For the full constant family Phi_t(c;U)=delta(x^(t))+c q^(-1)||D_U^(-1/2)(x^(t)-x_U^*)||_infinity, c>=0, the actual held U_3 pair 6--7 is nonincreasing iff c>=2978273417354/42112483166425, while after the literal stage-8 transport and face-local reset the actual held U_4 pair 9--10 is nonincreasing iff c<=95554102960761584/1567701294665491845. The lower endpoint exceeds the upper by 129004507967154213801509972186/13203958876316640794781123060825, so no nonnegative constant works on both (`prop:path-no-constant-coefficient-bank`). This is a finite pairwise representation STOP, not a block-horizon or asymptotic statement. The complete charged run remains J=4, T=16, nu_fin=9, n_fin=5, swept volume 114, one terminal return, with the complete vector `eq:path-no-constant-bank-stop-eleven-vector`.
- **Proved here (Round 017 finite switch GO and linear STOP):** `TightPair-Schur_(3->4)` chooses the unique tight endpoints c_3=2978273417354/42112483166425 and c_4=95554102960761584/1567701294665491845, so its scalar bank b=delta+c_j q^(-1)e is exactly flat on held pairs 6--7 and 9--10. At the frozen literal stage-8 candidate, delta_8^-=delta_8^+=28990137728/3755908203125, e_8^-=101253632777792/25604026220703125, and e_8^+=13229/1501825. The normalized new-row KKT value and Schur pivot are -13229/4260625 and 60073/170425, giving exact optimum drop Delta_8=175006441/6398713140625. The raw linear switch exceeds Delta_8, but the squared-bank switch is strictly below it; `Psi=b^2+H_j` with H_3=Delta_8 and H_4=0 strictly decreases across the isolated reset (`prop:path-tight-pair-schur-bank`). The endpoint choice maximizes the reset jump over every locally feasible pair c_3'>=c_3 and 0<=c_4'<=c_4. This is not a global stagewise potential, automatic coefficient rule, multi-face telescope, or asymptotic claim. The complete vector remains `eq:path-tight-pair-schur-bank-eleven-vector`.
- **Proved here (Round 018 finite complete-chain STOP):** Retaining the same tight coefficients and reserve, the exact chronology is held stage 7 on U_3, fixed-U_3 production of the stage-8 pre-reset candidate, the frozen-candidate reset/coefficient switch, then the first fixed-U_4 step to held stage 9. The production change is the positive fraction in `eq:path-tight-pair-production-stop`, so this is the first STOP; the reset still decreases by the exact Round-017 slack, but the follow-up rises by the positive fraction in `eq:path-tight-pair-followup-stop`, and at those tight coefficients the net stage-7--9 change is positive (`prop:path-tight-pair-local-chain-stop`). Production nonincrease would require c_3<=2103479690463/41819574955745<c_3^tight, while follow-up nonincrease would require c_4>=2617155474971384896/14508305905763575885>c_4^tight. Hence every pair in the held-pair-feasible rectangle passes the reset but fails both adjacent fixed-face comparisons; the net sign is not uniform over that rectangle. The complete vector remains `(Theta(9),Theta(5),1,0,Theta(114),Theta(114),Theta(114),Theta(9),O(9),Theta(114),Theta(9))` at `eq:path-tight-pair-local-chain-eleven-vector`. This is finite exact-real local-chain evidence only.
- **Proved here (Round 019 finite recovery-block GO and stage-12 STOP):** Keep the Round-018 production, reset, and follow-up changes separate. At the canonical tight pair, define the post-reset reserve `R_(9:k)^post=Psi_9-Psi_k=sum_(s=9)^(k-1)(Psi_s-Psi_(s+1))`, sourced only from realized fixed-`U_4` squared-bank decreases. It has the same squared-bank/objective units as `Psi` and is an analytical telescope, not free credit known in advance. The exact order is `Psi_9=Psi_10>Psi_11>Psi_12>Psi_7>Psi_13`; hence the reserve remains insufficient through stage 12 and first pays the stage-7--9 deficit at stage 13 (`prop:path-tight-pair-recovery-block`). Endpoint monotonicity in `c_3` and `c_4` makes the stage-7--13 GO uniform over the held-pair-feasible rectangle, while no rectangle-uniform intermediate net sign is asserted and the Round-018 positive stage-7--9 net remains tight-pair-only. One scalar audit accumulator leaves the complete vector unchanged at `eq:path-tight-pair-recovery-eleven-vector`. This is a finite exact-real single-admission block, not a pointwise potential, online face-general closing rule, multi-admission telescope, global block horizon, logarithm claim, asymptotic result, nonpath theorem, or finite-precision statement.
- **Proved here (Round 020 causal two-admission scalar-ledger GO and local stage-11/12 ledger STOP/GO):** Define the observable score `Xi_t=delta(x^(t))^2`. After any transition, update one scalar by adding the exact score decrease, subtracting the exact score increase, and adding an exact restricted-optimum drop only after its charged admission query. The resulting telescoping identity is face-general; nonnegative balance certifies only that every realized score-increase debt has been paid by the declared initial balance, already realized score decreases, and already realized restricted-optimum drops. It is not a convergence, objective-decrease, or work certificate. On the actual `q=1/5` trace, start at held stage 3. The balance stays nonnegative through the consecutive canonical stage-4 and stage-8 admissions and through stage 12; its smallest positive carried margin is at stage 9. If the unused stage-4 credit is discarded and the account restarts at stage 7, the stage-8 block remains negative through stage 11 and first becomes positive at stage 12 (`prop:path-causal-two-admission-recovery`). Thus the earlier realized Schur credit supplies an exact cross-state cancellation and no future decrease is borrowed. One retained scalar and constant per-checkpoint control leave the complete vector unchanged at `eq:path-causal-two-admission-eleven-vector`. This is a finite exact-real two-admission scalar-ledger result, not a pointwise potential, uniform recovery horizon, all-admission telescope, logarithm claim, asymptotic result, nonpath theorem, alternate-order theorem, or finite-precision statement.
- **Proved here (Round 021 finite nonpath next-admission STOP):** On the named asymmetric T tree, the exact admission stages are `1,2,4,9,15`, certification is stage `17`, and `(J,T,nu_fin,n_fin,mathfrak V_T)=(5,17,10,6,125)`. At stage 4 only branch vertex 3 crosses the gate; vertex 4 first crosses at stage 9. The exact Schur drops are `Delta_4=27889/364463450` and `Delta_9=542750209/8351767328125`. Restarting the causal balance at held stage 3 makes it negative at stage 5 and still negative immediately before and after the next admission at stage 9; stage 13 is the last local STOP and stage 14 the first local GO (`prop:t-tree-causal-next-admission-stop`). Thus uniform per-block recovery before the next admission is false for the named recurrence and gate. This does not refute the all-history ledger, prove convergence failure, or supply a nonpath eleven-vector. The optional exact checker exhausts all 3806 connected labeled rooted graphs on 2--5 vertices and finds no smaller instance under the same test; that minimality is computational scaffolding, not part of the proposition.
- **Conditional:** In Round 013, on a fixed full P_m face and assuming the nonnegativity projection is inactive, the affine normalized residual obeys `eq:path-full-face-two-state` and `eq:path-full-face-residual-recurrence`. Its exact two mode roots are `(1-q) cos(h*pi/(2m)) exp(+/- i h*pi/(2m))` (`lem:path-full-face-linearized-roots`). This calculation does not establish full-face entry coefficients, projection inactivity, envelope unclipping, or a residual-range lower bound for the actual trajectory.
- **Measured:** The floating-point table in `main.tex` suggests T=Theta(q^(-2)) and mathfrak V_T=Theta(q^(-3)); it is scaffolding only.
- **Refuted:** The spider growing-condition-number route; an alpha-uniform logarithmic ordinary-restart count; shock-free zero-padding across admission; universal O(sqrt(alpha) * sum_t Delta_t) packing of the weighted Schur tail; log-free O(nu_fin/sqrt(alpha)) swept-prefix work on the accuracy-driven literal path family; frontier/seed-only gate logic; monotone held-face moving-correction debt; the coefficient-one pointwise bound delta<=q^(-1)||D_U^(-1/2)(x-x_U^*)||_infinity; held-pair monotonicity of the coefficient-one correction--error bank; the existence of any nonnegative constant coefficient c making delta+c q^(-1)e nonincreasing on both reviewed held pairs; a unit objective-shock charge for the raw linear TightPair-Schur reset jump; pointwise monotonicity of the squared TightPair-Schur bank plus sole remaining-optimum-drop reserve across the complete stage-7--9 local chain; and uniform restarted-block recovery before the next admission for the named exact recurrence and gate. See the stable theorem/proposition labels in `sec:ledger`.
- **Refuted proof attempt (Round 013):** Two independent audits rejected the proposed terminal-face logarithmic lower theorem. Correct characteristic roots do not provide the actual packet/cosine coefficients or anti-cancellation, and deleting nonpositive fragments is not monotone for oscillation. The rapid-front/Pascal argument lacked explicit base and endpoint-row derivations; projection inactivity and the unclipped-envelope margin were not proved uniformly through k0=Theta(q^(-1) log(1/q)). The withdrawn theorem and all dependent lower-ledger/refutation claims have been removed.
- **Open:** At rho=tau=q/5, a uniform K_face=O(q^(-1)) bound, a logarithmic fixed-face lower bound, aggregate removal of the upper logarithm, matching Omega(q^(-2)) stages and Omega(q^(-3)) swept work, and the observed exact power laws are all open. The literal zero-padded signed defect, transported remaining-gain occupancy, soft-order separation, nonpath response growth, branching/cyclic extensions, finite precision, and graph-uniform total work also remain open.

## Central blocker

The Round-012 upper bound loses one logarithm. The Round-013 slow-mode lower route stops because it never proves that the actual full-face entry amplitude begins above the terminal tolerance alpha*tau=q^3/5 by a sufficient q-dependent factor, nor that cancellation, projection, and clipping preserve such a lower bound to k0. Round 014 closes no asymptotic route: the actual correction can increase while a face is held and can exceed q^(-1) times normalized face error with unit coefficient. Round 015 additionally stops the first correction-aware repair: adding that coefficient-one face error as a decreasing bank passes the Round-014 spike but fails on the next face's earliest consecutive held pair. Round 016 excludes every fixed nonnegative coefficient in delta+c q^(-1)e on those two literal local pairs. Round 017 shows that the canonical tight face-dependent coefficients admit a unit Schur charge after the scalar bank is squared for the instantaneous stage-8 reset. Round 018 closes its immediate pointwise extension negatively: the fixed-U_3 production step rises before the paid reset and the first fixed-U_4 follow-up rises after it throughout the held-pair-feasible coefficient rectangle; at the canonical tight pair the complete local-chain net change is also positive, but the net sign is not uniform over the rectangle (for example, the feasible pair `(c_3,c_4)=(1,0)` has negative net change). Round 019 gives the first finite longer-block GO: realized fixed-U_4 decreases create an analytical recovery telescope that is too small through stage 12 and first pays at stage 13; endpoint extremality makes only the 7--13 GO uniform over the rectangle. Round 020 supplies a causal face-general accounting condition with score Xi=delta^2. On the exact path it stays solvent across the consecutive stage-4 and stage-8 admissions because unused stage-4 Schur credit gives an exact cross-state cancellation; if that carry is discarded, the stage-8 block stops through stage 11 and first closes at stage 12. Round 021 isolates the first reviewed nonpath failure of the hoped-for uniform block rule: on the asymmetric T tree, stage-5 debt survives the next admission at stage 9 and first closes at stage 14. The upper route must now prove an all-history solvency invariant or a graph/face structural condition that guarantees enough earlier credit, while retaining the moving global correction and charging every exact drop query. A stronger lower test would keep debt negative through several later admissions. The lower chronology route still needs literal entry coefficients and all conditional-regime margins. These finite results determine no global block horizon and do not conflict with a coordinatewise monotone lower-point invariant whose accelerated auxiliary state is restarted.

## Dependencies and reusable outputs

- Formal taxonomy dependencies: `aesp_cd_l1_rppr`.
- Source/shared prerequisites: shared RPPR support and KKT facts.
- Context/provenance: the Round-012 moving-maximum theorem and withdrawn Round-013 terminal-face attempt are folder-local predecessors; response-preconditioned and hybrid-synthesis notes are downstream companions, not proof imports here.
- Supplies to: response-preconditioned and hybrid-synthesis directions through the safe gate, peak-volume cap, restart obstruction, endpoint-edge shock warnings, transported-center and weighted-shock ledgers, path response ledger, and the reviewed moving-maximum upper bound. Round 013 supplies no promotable asymptotic result; its conditional root lemma and finite checks stay direction-local scaffolding. Round 014 supplies only the exact pointwise STOP `prop:path-monotone-correction-potential-fails`: do not model the correction itself as decreasing debt or apply the displayed coefficient-one q^(-1) face-error bound. Round 015 supplies the exact face-local bank STOP `prop:path-correction-error-bank-potential-fails`: the sum of those two terms can rise on consecutive held steps even when the correction falls. Round 016 supplies the stronger pairwise STOP `prop:path-no-constant-coefficient-bank`: no fixed c>=0 satisfies both local monotonicity constraints. Round 017 supplies the dimension-consistent isolated-switch rule `prop:path-tight-pair-schur-bank`: square the face bank and debit the exact remaining restricted-optimum drop at the reset; do not compare the raw linear bank directly with objective shock. Round 018 supplies `prop:path-tight-pair-local-chain-stop`: keep held evolution, candidate production, reset, and follow-up separate, because the sole optimum-drop reserve pays the reset but not either adjacent fixed-face step. Round 019 supplies the finite analytical recovery telescope `prop:path-tight-pair-recovery-block`: realized post-reset fixed-face decreases first pay the canonical deficit at stage 13, and endpoint extremality extends only that longer endpoint GO across the feasible rectangle. Round 020 supplies `prop:path-causal-two-admission-recovery`: for the observable score Xi=delta^2, credit only realized score decreases and charged restricted-optimum drops and debit every score increase. The exact path balance survives a second admission by carrying unused earlier credit; without that carry, stage 11 stops and stage 12 first goes. Round 021 supplies `prop:t-tree-causal-next-admission-stop`: do not assume a restarted negative block recovers before the next admission, even on a six-vertex tree. None of these results prohibits an all-history balance, a graph-structural solvency condition, or coordinatewise lower-point invariants with restarted auxiliary state.

## Resume here

- Exact anchors: `sec:path-constant-ratio`, `lem:path-global-envelope-gate`, `prop:path-constant-ratio-floor`, `prop:path-frontier-only-gate-fails`, `prop:path-monotone-correction-potential-fails`, `prop:path-correction-error-bank-potential-fails`, `prop:path-no-constant-coefficient-bank`, `prop:path-tight-pair-schur-bank`, `prop:path-tight-pair-local-chain-stop`, `prop:path-tight-pair-recovery-block`, `prop:path-causal-two-admission-recovery`, `prop:t-tree-causal-next-admission-stop`, `thm:path-moving-max-soft-upper`, and `lem:path-full-face-linearized-roots`; retain `eq:weighted-shock-occupation` as the separate analytical-shock ledger.
- Next concrete action: Test whether the all-history causal Xi=delta^2 balance stays solvent, or derive a graph/face condition that supplies sufficient prior credit, with every delta scan and exact drop query charged. As a falsification alternative, search for a finite exact trace whose restarted debt survives several subsequent admissions. Separately, derive the literal full-face entry coefficients and a uniform anti-cancellation/range lower while proving the actual projection/envelope margins through k0. Never replace the moving global correction by a frontier or seed surrogate, declare it monotone, use the refuted coefficient-one q^(-1) pointwise error bound, assume its coefficient-one correction--error sum decreases on held pairs, posit one fixed c>=0 for both reviewed held pairs, compare a raw linear bank directly with objective shock, promote the reset-only GO past the adjacent-step STOP, treat `R_(9:k)^post` as advance credit, credit a score/Schur decrease before it is realized, or assert that every restarted block closes before the next admission.
- Stop/go test: GO only after every conditional-regime obligation and any phase origin/order dependence are explicit. Keep held-face evolution, candidate production, instantaneous reset charge, follow-up evolution, and any multi-face telescope separate. Keep pairwise local coefficient feasibility separate from any global block horizon. STOP any logarithmic lower promotion based only on roots, central-binomial size, finite plots, or the m=2,4,8 checker. STOP a pointwise upper proof at `eq:path-stage-seven-correction-growth` or `eq:path-stage-seven-correction-error-stop` if it needs either Round-014 shortcut; STOP a coefficient-one face-error bank at `eq:path-correction-error-bank-stage-ten-stop`; STOP every constant-coefficient repair at `eq:path-constant-coefficient-gap`; STOP a raw linear unit-shock switch at `eq:path-tight-pair-linear-switch-stop`; STOP the squared-bank-plus-sole-reserve local chain at `eq:path-tight-pair-production-stop` (and independently at `eq:path-tight-pair-followup-stop`). For the tested longer TightPair block, STOP through `eq:path-tight-pair-stage-twelve-stop` and use `eq:path-tight-pair-stage-thirteen-go` only as a finite endpoint GO. For the causal delta-squared path block restarted at stage 7, STOP through `eq:path-delta-squared-stage-eleven-stop` and use `eq:path-delta-squared-stage-twelve-go` only after the realized decreases; the carried two-admission GO is `eq:path-two-admission-cross-state-cancellation`. For a recovery-before-next-admission claim, STOP at `eq:t-tree-stage-nine-post-stop`; its later local recovery is only `eq:t-tree-stage-fourteen-go`. Never infer an online/global horizon from either finite result. No nonpath eleven-vector or asymptotic lower eleven-vector currently exists.

## Verification

- Source pointers checked: the existing RPPR/FISTA and Catalyst page pointers remain unchanged; Rounds 014--015 import no new external theorem and use the note's exact transported-center recurrence and gate.
- Rounds 004--012 retain their previously reviewed exact identities, rational checks, direction builds, note audits, graph audits where applicable, control-character/conflict scans, and scoped diff checks.
- Round-013 finite scaffolding: `check_round013.py` uses exact `Fraction` arithmetic at (m,q)=(2,1/32),(4,1/64),(8,1/128). It verifies those actual finite admission prefixes, the conditional full-face residual recurrence, inactive projection and unclipped moving envelope over 16,32,64 terminal steps, and non-certification over those same finite prefixes. It does not test the asymptotic k0 range or prove a packet coefficient, anti-cancellation, or a theorem.
- Round-014 exact STOP: `check_round014.py` replays the actual q=1/5 path with `Fraction` arithmetic. It verifies the held U_3 face at stages 6 and 7, both exact correction values and their strict increase, the stage-7 residual and restricted-optimum error, delta_7/e_7>1/q, positive proximal/envelope margins, admissions at stages 1,2,4,8, terminal certification at stage 16, J=4, nu_fin=9, swept volume 114, and one terminal return. It makes no asymptotic claim.
- Round-015 exact STOP: `check_round015.py` replays the same actual q=1/5 path with `Fraction` arithmetic. It verifies Phi_7^bank<Phi_6^bank, then the earliest later consecutive held-pair failure Phi_10^bank>Phi_9^bank on U_4; the exact delta/error components; delta_10<delta_9; the actually clipped envelopes; active non-certification and positive outside KKT values at stages 9 and 10; admissions at stages 1,2,4,8; terminal certification at stage 16; J=4, nu_fin=9, swept volume 114; and one terminal return. It makes no asymptotic, alternate-phase, or reordered-event claim.
- Round-016 exact STOP: `check_round016.py` replays the same actual q=1/5 path with `Fraction` arithmetic. It verifies the exact delta and face-error values at stages 6,7,9,10; actual held U_3 and U_4 pairs; the stage-8 admission and face-local optimum reset; unclipped stage-6--7 and actually clipped stage-9--10 envelopes; all admission/non-certification gate inequalities; the affine coefficient slopes; the exact lower threshold c_6,7=2978273417354/42112483166425 and upper threshold c_9,10=95554102960761584/1567701294665491845; their strictly positive exact gap; admissions at stages 1,2,4,8; terminal certification at stage 16; J=4, nu_fin=9, swept volume 114; and one terminal return. Its continuum conclusion is exact affine half-line algebra, not numerical sampling. It makes no asymptotic, block-horizon, alternate-phase, or reordered-event claim.
- Round-017 exact switch audit: `check_round017.py` replays the same actual q=1/5 path with `Fraction` arithmetic. It rederives both tight half-line endpoints and flat held-pair banks; freezes the actual stage-8 candidate; verifies the unchanged correction, old/new face errors, zero-padding, center transport, and optimum reset; derives normalized shock row -13229/4260625, Schur pivot 60073/170425, and exact optimum drop 175006441/6398713140625 from both the Schur formula and objective values; proves the raw linear bank jump exceeds that drop; proves the squared-bank jump is smaller by the exact positive slack in `eq:path-tight-pair-switch-slack`; and checks the complete admissions, terminal stage, final volume, swept volume, and one-return ledger. It makes no all-held-pairs, stage-boundary, multi-face, asymptotic, alternate-phase, or reordered-event claim.
- Round-018 exact local-chain audit: `check_round018.py` replays the same actual q=1/5 path with `Fraction` arithmetic. It keeps the held 6--7 pair, fixed-U_3 production 7 to 8^-, frozen-candidate reset 8^- to 8^+, fixed-U_4 follow-up 8^+ to 9, and held 9--10 pair distinct; verifies every displayed correction, face error, bank value, squared transition change, and net change; rederives the production coefficient cap and follow-up coefficient floor and their strict separation from the held-pair-feasible rectangle; and checks admissions 1,2,4,8, terminal stage 16, J=4, nu_fin=9, swept volume 114, and one terminal return. It makes no global, multi-admission, asymptotic, logarithmic, alternate-phase, nonpath, or finite-precision claim.
- Round-019 exact recovery-block audit: `check_round019.py` replays the same actual q=1/5 path with `Fraction` arithmetic. It retains the Round-018 held/production/reset/follow-up boundaries; verifies stages 9--13 are consecutive held states on U_4; rederives every stage-11--13 correction, face error, outside and active-envelope gate value, and tight bank; proves `Psi_9=Psi_10>Psi_11>Psi_12>Psi_7>Psi_13`; checks the exact analytical telescope `R_(9:k)^post`, the positive stage-12 STOP, the positive stage-13 payment slack, and endpoint monotonicity over the coefficient rectangle; and rechecks admissions 1,2,4,8, terminal stage 16, J=4, nu_fin=9, swept volume 114, one terminal return, and the unchanged eleven-vector. It does not treat the reserve as advance state or make a global, online-horizon, multi-admission, asymptotic, logarithmic, alternate-phase, nonpath, or finite-precision claim.
- Round-020 exact causal-ledger audit: `check_round020.py` replays the same actual q=1/5 path with `Fraction` arithmetic. It verifies the consecutive later admissions at stages 4 and 8, their exact restricted-optimum drops, every correction score from stages 3--12, the frozen-candidate pre/post reset scores, and the causal decrease/increase/drop update at every checkpoint. Starting with zero credit at held stage 3, it proves every balance through stage 12 is nonnegative and that stage 9 has the smallest positive post-first-admission balance. Restarting at stage 7, it keeps production, reset, and follow-up separate, proves the exact positive stage-9 deficit, verifies realized fixed-U_4 decreases, and proves stage 11 is the last local STOP and stage 12 the first local GO. It checks the exact cross-state identity in which unused stage-4 credit pays the restarted stage-9 deficit, then rechecks admissions 1,2,4,8, terminal stage 16, J=4, nu_fin=9, swept volume 114, one terminal return, and the unchanged eleven-vector. It makes no uniform-horizon, all-admission, asymptotic, logarithmic, alternate-order, nonpath, or finite-precision claim.
- Round-021 exact nonpath STOP audit: `check_round021.py` uses generic exact-rational graph matrices and dense Gauss--Jordan restricted solves to replay the asymmetric T tree. It verifies every action through stage 17, all positive proximal candidates, the two stage-4 branch gates, the stage-9 gate, both Schur loads/pivots and objective drops, the stage-5 and stage-9 pre/post negative balances, the negative balances through stage 13, the exact positive stage-14 balance, and `(J,T,nu_fin,n_fin,mathfrak V_T)=(5,17,10,6,125)`. Independent review found and repaired one checker-only normalization defect for nonunit-degree enumeration seeds: the normalized source is `alpha*(1_seed/d_seed-rho)`. The named witness has `d_seed=1`, so no theorem value changed. The corrected `--enumerate` audit passes all 3806 connected labeled rooted graphs on 2--5 vertices; an independent atlas sweep also leaves the first witnesses at six vertices. The reviewer separately reproduced every named fraction, confirmed all-history solvency and restarted-only scope, and obtained a clean 54-page build. Finite minimality remains scaffolding, not a theorem or nonpath response-work claim.
- Audit record: two independent reviews rejected promotion for the missing coefficient/anti-cancellation, rapid-front boundary/base, and uniform projection/envelope arguments. The central-binomial sentence was removed rather than used to cover those gaps.
- Owner-side reconciliation checks passed through Round 017 on 2026-08-21: all five exact finite checkers, 44-page direction build with resolved references and visually inspected Round-017 theorem pages, focused Ruff check/format, `make note-audit` (18 notes across 5 tracks), `make note-graph` (18 nodes and 26 acyclic edges), control-character/conflict scans, and scoped diff checks. Independent Round-015 review used a separate dense exact-Fraction replay to verify the chronology, the held `U_4` pair, both bank comparisons, the eleven-vector, and the finite coefficient-one scope; it found no actionable issue, and the result was promoted in the redistributed Round-015 record. Independent Round-016 review used a separate generic normalized-path matrix and exact Gauss--Jordan solve to reproduce both held-pair affine constraints, their disjoint exact thresholds, the chronology, the eleven-vector, and the finite pairwise scope; it found no actionable issue, and the result was promoted in the redistributed Round-016 record. Independent Round-017 review used a separate dense exact-rational replay to reproduce the tight coefficients, held values, frozen-candidate reset, Schur drop, raw-linear STOP, squared-bank slack, reserve identity, rectangle extremality, eleven-vector, and reset-local exclusions; it returned clean without an edit, and the result was promoted in the redistributed Round-017 record.
- Round-018 owner-side checks passed on 2026-08-21: all six exact finite checkers; a fresh 46-page build with resolved references, no overfull horizontal boxes, and visual inspection of pages 35--38; focused Ruff check/format; `make note-audit` (18 notes across 5 tracks); `make note-graph` (18 nodes and 26 acyclic edges); label-uniqueness, control-character, conflict-marker, root-artifact, and scoped-diff checks. Independent Round-018 review used a separate dense exact-rational replay to reproduce the literal chronology, all displayed fractions and signs, the production/reset/follow-up transitions and net change, both coefficient thresholds and the complete held-pair-feasible rectangle conclusion, the eleven-vector, and the finite scope exclusions. It also reran the checker, forced a clean 46-page build, inspected the theorem render, and verified the scoped diff; it returned CLEAN without an audit edit.
- Round-019 owner-side checks passed on 2026-08-21: all seven exact finite checkers; a fresh post-audit 49-page build with resolved references and no overfull horizontal boxes; visual inspection of the Round-019 proposition, proof, and repaired derivative display on pages 37--39; focused Ruff check/format for `check_round019.py`; `make note-audit` (18 notes across 5 tracks); `make note-graph` (18 nodes and 26 acyclic edges); and label-uniqueness, control-character, conflict-marker, bytecode, and scoped-diff checks. Repository-wide Ruff check and format check pass at the reconciled Round-019 baseline. Independent Round-019 audit found the substantive claims clean and one narrow notation defect in the rectangle derivatives: fixed tight-pair bank symbols were replaced by the explicitly variable banks `b_7(c_3)` and `b_13(c_4)` without changing the proof or scope.
- Round-020 owner-side checks passed on 2026-08-22: all eight exact finite checkers; a fresh 51-page direction build with resolved references and no overfull horizontal boxes; focused Ruff check/format for `check_round020.py`; `make note-audit` (18 notes across 5 tracks); `make note-graph` (18 nodes and 26 acyclic edges); and label-uniqueness, control-character, conflict-marker, and scoped-diff checks. Independent Round-020 review used a separate dense exact-rational replay and independent Schur calculations to reproduce the chronology, both drops, every balance, the stage-11/12 ledger STOP/GO, cross-state identity, vector, and scope. It required one wording repair defining nonnegative balance only as scalar-ledger closure and excluding convergence, objective-decrease, and work certificates; the checker, 51-page build, render, and focused diff then returned clean without an audit edit.

## Known gaps

The transported-center recurrence changes the estimate center at admission and therefore does not prove the literal zero-padded conjecture. Existing eleven-vectors are for internally gated exact-real path implementations with R_int=1 and one terminal external certificate/output stage. Intermediate external emissions, finite precision, bit complexity, matching global constant-ratio lower powers, upper-log removal, automatic face-coefficient rules, all-held-segment control, multi-admission squared-bank telescopes, general longer or cross-face phase potentials, and nonpath response growth remain open. Round 017 pays the frozen-candidate reset; Round 018 proves that the same sole-reserve account fails on both adjacent fixed-face steps and at the tight-pair net. Round 019 tests one longer aggregate: the realized post-reset recovery first closes the literal 7--13 block, but is analytical rather than advance credit and supplies no online face-general or repeated-admission horizon. Round 020 gives a face-general causal accounting identity and one finite two-admission cross-state cancellation on the path. Round 021 proves that a restarted negative block need not recover before the next admission on a six-vertex tree, while leaving all-history solvency open. Exact drops may require charged response work, and no nonpath eleven-vector has been proved. A coordinatewise monotone lower-point invariant with explicitly restarted accelerated auxiliary state is not ruled out by any scalar-bank result. The exact PPR core is existential, whereas the RPPR gate is the implementable mechanism.
