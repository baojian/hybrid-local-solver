\section{Scope and claim ledger}
\label{sec:scope}

\input{../../tex/shared/source_aligned_problem}

The shared project model supplies the symmetric Stieltjes matrix $Q$, the
smooth PageRank quadratic, and the RPPR objective.  This note uses the shared
model only as a mathematical reference.  It does not adopt a repository-wide
residual or identify the ACL, objective-gap, proximal, and degree-normalized
accuracy namespaces.

For every exposed vertex set $U$, define the note-scoped charged volume
\begin{equation}
 \operatorname{cvol}(U):=\sum_{u\in U}(1+d_u).
 \label{eq:charged-volume}
\end{equation}
Since the shared graph has no isolated vertices,
$\vol(U)\leq\operatorname{cvol}(U)\leq2\vol(U)$.  This charge includes one
stored or output word per active coordinate in addition to its adjacency
scan; response, sketch, rekey, and factor work is charged separately.

{\small
\begin{longtable}{@{}p{0.19\textwidth}p{0.75\textwidth}@{}}
\toprule
Status & Statement \\
\midrule
\endfirsthead
\toprule
Status & Statement \\
\midrule
\endhead
\statusproved & Iterative and response methods act on the same restricted
Green operator $Q_{SS}^{-1}$.  A bordered active-set expansion has an exact
Schur correction. \\
\statusproved & Successive exact corrections on nested monotone faces are
$Q$-orthogonal.  A primal correction can be dense on the enlarged face, but
its dual image under $Q$ vanishes on the old face and is supported only on the
new batch and current boundary. \\
\statusproved & The entire lifted Schur-frontier spaces are mutually
$Q$-orthogonal.  Arbitrary certified frontier-solve errors therefore add in
quadrature, so the settled face needs no numerical restart when its response
is applied at charged cost. \\
\statusproved & Consecutive exact light transfers may be batched
associatively: their dual signatures add, their endpoint and exterior demands
are unchanged, and sequential Schur elimination equals one aggregate
elimination. \\
\statusproved & An energy-norm error certificate gives a rigorous interval for
every queried boundary KKT demand.  Every response leverage is at most
$\sqrt{(1-\alpha)/2}$, so coordinate-specific leverage estimation is
unnecessary. \\
\statusproved & If a correction is generated by a frontier source batch $B$,
its source-aware squared boundary leverages sum to at most $|B|$.  This gives
an a priori output-volume bound on ambiguous intervals without materializing
the dense primal correction. \\
\statusproved & Given constant-factor aggregate source-aware leverage masses
on a bounded-arity boundary hierarchy, branch and bound locates every
potentially ambiguous row in a number of group probes controlled by $|B|$ and
the correction energy, rather than the ambient boundary size. \\
\statusproved & Every source-aware squared leverage is exactly the decrease of
one terminal Schur diagonal across the batch elimination.  These losses
telescope to at most $(1-\alpha)/2$ per still-exterior label.  A one-sided
group oracle needs only additive accuracy at the current pruning scale, and
can replace the source-response sketches entirely. \\
\statusproved & Schur-diagonal loss and orthogonal correction energy can be
combined multiplicatively.  If a coordinate or hierarchy node is awakened
only when the product interval reaches its current margin, the sum of its
wake-up margins is bounded by the geometric mean of its total diagonal loss
and the global correction-energy budget.  For a unit seed, this produces a
per-coordinate $1/\sqrt\alpha$ rather than $1/\alpha$ scale at margin
$\Theta(\alpha\tau)$. \\
\statusproved & The shared PageRank matrix is diagonally congruent to the
grounded Laplacian of the original graph with conductance
$(1-\alpha)/2$ on each graph edge and conductance $\alpha d_v$ from $v$ to
ground.  Principal Schur complements and normalized exterior responses obey
the same congruence. \\
\statusproved & The response rows, source-aware leverages, and Schur-diagonal
losses for a current face depend only on that face's internal edges, its cut
incidences, and the degrees of the incident boundary labels.  Edges wholly in
the unexposed exterior are irrelevant.  Thus static locator rebuilding can be
charged to geometrically growing exposed volume; dynamic update and query
costs remain open. \\
\statusproved & Chebyshev inverse-square-root probes construct simultaneous
one-sided estimates of every exposed boundary diagonal loss with high
probability in
$\widetilde O(\operatorname{cvol}(S)/\sqrt\alpha)$ work.  Full snapshots at
geometrically growing checkpoints have the same final-volume product cost. \\
\statusproved & At a fixed anchor, any linear sketch of the full normalized
exterior dual signature has an exact transposed harmonic-response formula.
After precomputing one anchor response per sketch row, a frontier solve updates
the sketch from frontier-incidence scans and stored anchor-sketch row reads,
without materializing the dense old-face correction or scanning the boundary.
\\
\statusproved & Two-sided Gaussian sketches estimate every reached hierarchy
group mass through polylogarithmically many scalar transposed harmonic
measurements.  Combined with leverage packing, this avoids both a dense
exterior response and an all-leaf squared range-add. \\
\statusproved & One signed hash-and-bit bank shared by a complete packed
fixed-event query contains every potentially ambiguous response row at margin
$m$ and certified correction-energy radius $E$ with high probability; exact
validation removes false candidates.  Its target dimension and candidate list are
$\widetilde O(1+|F|E^2/m^2)$, independent of the ambient boundary size.
Anchor solves, stored-row reads, source randomness, and exact candidate
validation remain fully charged.
\\
\statusproved & Exact validation permits certified adaptive reuse of the
target hash/sign layer.  A first-failure coupling reuses one bank per doubling
capacity throughout an epoch; the total number of distinct target rows is
$\widetilde O(K_{\max})$, rather than the sum of the per-event dimensions.
Fresh source probes keep a separate summable failure budget.  Harmonic bank
construction and every later application remain charged.
\\
\statusproved & The transposed harmonic product has exact target and source
orientations.  For a coded bank, the source orientation uses no stored target
extension and is formed by one anchor-cut scan after the source solves.  An
online rent-or-buy rule at every capacity uses at most twice the smaller of
the target-row count and the cumulative source width in anchor right-hand
sides.  Repeated anchor scans or stored-response reads remain charged.
\\
\statusproved & For a whole nested checkpoint segment, divide every far
boundary response row by its own initial gate slack.  All far labels that
cross the gate then lie in one heavy-row query, whose target dimension and
candidate degree volume are
$\widetilde O(1+b\mathcal E/m^2)$, where $b$ is total admitted batch size,
$\mathcal E$ is total orthogonal correction energy, and $m$ is the retained
transition-band width.  The realized gate needs only one accumulated
directional column, whose scalar bucket state is causal and append-only.  For
direction-independent row energies, Gaussian compression uses only
logarithmically many source columns and the decoder's bucket norms accumulate
exactly under append-only source blocks.  Neither route stores or replays
source history.  Harmonic application, capacity growth, weight refresh,
transition-band maintenance, and validation remain charged.
\\
\statusrefuted & Fixed-bank append-only bucket norms do not support a free
checkpoint-slack refresh.  In one bucket, two distinct three-label source
histories have identical retained norms under the old weights but require
opposite decoded bits after one positive row reweight.  Three colliding labels
are minimal for this named state obstruction.  The exact no-replay interface
must reject; an explicit micro-replay instead pays six old response-cell reads
and twelve numerical writes.  This is not a lower bound against richer Gram
state, source replay, cut rebuilding, collision-free banks, or other decoders.
\\
\statusproved & For one bucket containing exactly the three codes
$00,01,10$, retaining the four canonical unweighted decoder norms and the
single pivot norm $\|z_0\|_2^2$ determines every exact decoder norm under
arbitrary positive row weights.  The named
\textsc{ThreeLabelPivotGramRefresh} materializes four refreshed norms with one
additional persistent scalar and supports arbitrary repeated reweights.
Incoming source blocks remain append-only only after their three ordinary
bucket measurements are divided back to the canonical unweighted rows.  More
generally, retaining every Gram entry whose two codes share a bit side is
sufficient; among $k$ distinct codes it has
$k+\binom{k}{2}-c=\Theta(k^2)$ cells, where $c$ is the number of complementary
code pairs.  This count is necessary only among linear explicit
Gram-statistic states that reconstruct every side norm on an open set.
\\
\statusrefuted & One pivot norm is not a universal bucket-refresh state.
For codes $00,01,10,11$, two scalar histories have the same four old decoder
norms and the same pivot norm, but positive weights $(1,1,1,3)$ make the first
bit comparison $49<121$ in one history and $169>121$ in the other.  This is a
four-label obstruction to the named five-scalar state, not an unrestricted
cell-information lower bound. \\
\statusproved & If the normalized anchor--boundary cut has rank $s$, one bank
of $s$ harmonic basis vectors turns every hierarchy group norm into an exact
quadratic of additive $s$-dimensional summaries.  Source-only Gaussian probes
then give a complete packed reporter at accelerated frontier cost; no target
harmonic query remains. \\
\statusrefuted & A universal low-rank harmonic bank is impossible: on a
linear-volume comb tree the normalized cut has rank $n$, its harmonic bank is
dense, and every rank-below-$n$ approximation has energy-operator error at
least $(1-\alpha)/(2\sqrt3)$, independent of $n$ for fixed $\alpha<1$.
This is a representation obstruction, not a tree-algorithm lower
bound. \\
\statusrefuted & Squaring the response does not restore an exact
polylogarithmic target dimension.  On a nonequitable ``notched double sun''
with a cyclic anchor, a prescribed linear sequence of singleton face
additions has dense, linearly independent terminal-Schur diagonal-loss
vectors for each fixed size $n$ when $\alpha$ lies in an $n$-dependent
interval $(\alpha_n,1)$.  This is a structural
response-interface trace, not a proved RPPR admission trajectory, and rules
out only an exact fixed low-dimensional linear loss tag; scale-truncated,
nonlinear, adaptive, sparse-recovery, and cyclic separator representations
remain open. \\
\statusproved & The same notched-double-sun trace is easy at its separated
finite band.  For fixed $n$ and $\alpha$ sufficiently close to one, threshold
$((1-\alpha)/2)^5$ retains only the report leaf matched to the admitted petal;
an explicit one-sided additive group oracle follows one hierarchy path and
validates one leaf.  Exact rank is therefore not a margin-scale lower bound.
\\
\statusrefuted & On that structural epoch, the literal adaptive coded-bank
implementation that draws a fresh two-layer block and explicitly materializes
its dense $|A_k|\times p_k$ harmonic table at every event has an
$\Omega(n)$ unsuppressed representation overhead relative to the candidate
ledger.  A summable failure schedule makes every event valid, but table writes
alone violate the desired epoch charge.  This refutes only fresh explicit
dense-table materialization, not implicit, batched, or compressed fresh-block
implementations. \\
\statusproved & The source vectors in those measurements do not require
$K_F^{-1/2}$.  Chebyshev whitening of any constant-factor spectral frontier
prepares them once in $\widetilde O(T_{\rm mv}/\sqrt\alpha)$ work and
preserves every group mass within an explicit constant. \\
\statusproved & A positive geometric quadrature writes $K_F^{-1/2}$, within
an explicit constant, as a logarithmic ladder of shifted Stieltjes resolvents.
Its total shifted work mass and monotone Schur-coordinate settlement count
have the exact $1/\sqrt\alpha$ dependence.  Each rung is a sparse shifted face
system, and every unfinished solve is represented exactly by nonnegative
anchor debt. \\
\statusrefuted & Replacing a shifted Schur-coordinate settlement by literal
exact coordinate pushes on its sparse face system does not preserve the
accelerated ledger.  Already on one edge, the first shifted-resolvent rung
requires $\Omega(1/\alpha)$ alternating sparse pushes for a constant-factor
residual reduction, whereas the scalar Schur system finishes in one push.
This refutes only the literal row-by-row debt realization, not block response,
elimination, over-relaxed transport, or dynamic sparsification. \\
\statusproved & On one exact-real fixed $(A,F)$ face, fixed exposed cut, and
fixed shifted ladder, additive nonnegative debt fragments may be aggregated
and flushed by signed Chebyshev recurrence.  Scalar, $r$-column, and signed
positive/negative-split costs retain every fragment write and all eleven
resource coordinates.  The energy certificate gives a simultaneous
mathematical interval family; the named non-output-sensitive
\textsc{AllBoundaryFlush} additionally pays to scan, materialize, validate,
and emit every current boundary interval.  Geometrically growing fixed-
decision faces have the corresponding fixed-column product bound. \\
\statusrefuted & The literal canonical all-violations boundary gate cannot in
general sleep until multiplicative face growth.  On an ambient-degree,
single-seed path, a family-dependent sufficiently small positive $\rho_n$
makes every prefix expansion a forced singleton, while the charged-volume
ratio of successive faces tends to one.  No positive $n$-uniform lower bound
on $\rho_n$ is asserted.  A cited append-only path response supplies a fully
charged linear comparator, so this is a scheduling obstruction, not a path
running-time lower bound. \\
\statusrefuted & On the frozen high-cut-rank notched-double-sun face, unit
first-rung fragments have dense exact exterior response columns.  For every
fixed $n$ and sufficiently near-one $\alpha$, gate
$g=((1-\alpha)/2)^{5/2}$ and band $g/2$ nevertheless give exactly one new
delta label per event.  The named \textsc{EagerExactSlack} policy, which
stores and rewrites every separately addressed exact unreported-label slack,
therefore pays $\Theta(pn^2)$ response/control/materialization work for
$\Theta(rn)$ output, with $p=r$ for nonnegative and $p=2r$ for signed-split
certification.  This is a fixed-face exact-representation STOP, not an RPPR
chronology or a lower bound on implicit, packed, scale-truncated, or cyclic
partial reporters. \\
\statusproved & On the same frozen face and unit-fragment order, the
scale-aware \textsc{NotchedSunScaleDelta} representation keeps only a verified
template, event counter, ladder, and stream metadata.  For
$\vartheta\leq1/(1296n^2)$, an explicit entrywise Neumann-tail bound certifies
the matching label above $\vartheta^{5/2}$ and every future label below half
that gate.  It therefore returns one delta label and one universal future-safe
certificate per logical column and event with $\Theta(pn)$ fragment/control
work, $\Theta(rn)$ emission, and no response or numerical materialization.
This is a fixed-template exact-real GO, not a generic high-rank reporter. \\
\statusproved & The reporter's pointwise matching and off-matching estimates
are order-blind.  \textsc{NotchedSunPermutationDelta} therefore checks one
common previously unseen petal label across all streams, retains a
one-bit-per-label seen set, and handles every online-verified permutation with
the same linear eleven-coordinate vector.  It does not relax amplitudes,
pairing, face, cut, ladder, scale, or the common cross-stream schedule. \\
\statusproved & Positive declared multiplicities replace that permutation by
any common multiset permutation.  With total length $L$ and last-unseen
horizon $H=L-\min_i\mu_i$, the exact uniform-envelope condition is
$18H\sqrt\vartheta+3\vartheta\leq1$.  One counter per label validates every
repeat; first occurrences emit deltas, repeats do not, and unseen labels keep
one universal certificate.  The complete vector has $R_{\rm int}=L$,
$\mathcal C_{\rm frag}^{\Sigma}=pL$, $C_{\rm emit}=\Theta(rL)$, and zero
response/materialization work.  This is still a fixed-template exact-real
claim, and the scale is sharp only for its displayed scalar envelope. \\
\statusproved & A finite declared common schedule of positive exact-real
logical amplitudes needs no first-occurrence assumption.  For label $i$, the
capacity $\kappa_i=d_{u_i}(1+c_\eta)\sqrt\vartheta$ determines its first
prefix-mass crossing $k_i^*$.  The exact pre-crossing mass horizon $F_i$ and
gap $\gamma_i$ give the band
$m_{\mathcal A}=\min_i\{\lambda_i\gamma_i-\epsilon_\vartheta F_i\}$.
Whenever it is positive, unseen and seen-but-unreported labels remain below
$g-m_{\mathcal A}$, while occurrence $k_i^*$ is the first exact crossing.
The reporter stores the declared prefix schedules and counters, not response
or slack state, and has $R_{\rm int}=L$ and linear fragment/output work. \\
\statusproved & Each logical column may instead declare its own finite
positive occurrence schedule, while an external stream asynchronously
interleaves one column event at a time.  A column--label cell has the same
structural capacity but a column-specific crossing, local mass horizon, and
margin.  Other columns enlarge only its global event horizon.  The signed
$2$-minus-$1$ pair remains atomic within its column.  With total event count
$L_\Sigma$, the complete vector has $R_{\rm int}=L_\Sigma$,
$\mathcal C_{\rm frag}^{\Sigma}=L_\Sigma$ or $2L_\Sigma$, and
$C_{\rm emit}=\Theta(L_\Sigma)$, with zero response and materialization work.
\\
\statusproved & Any nonempty simultaneous batch may contain arbitrary tagged
next consecutive occurrences from several labels and columns.  Atomic
validation rejects gaps, duplicates, replays, amplitude mismatches, and split
signed pairs before state mutation.  At each batch boundary the new-delta set
is exactly $\{(q,i):c_{qi}^-<k_{qi}^*\leq c_{qi}^+\}$, and one universal
certificate is emitted per affected column.  For $B$ batches, affected-column
count $S_\Sigma$, and peak batch size $b_{\max}$, the full vector has
$R_{\rm int}=B$, $M_{\rm tmp}=O(b_{\max})$, and
$C_{\rm emit}=\Theta(S_\Sigma+rn)$ while retaining zero response and
materialization work.  This is batch-boundary semantics only; there is no
ordered subevent claim or cross-column coupling. \\
\statusopen & On a high-cut-rank nonequitable core, implement output-sensitive
partial flushes, or prove that a different support-safe response trace may
bypass the literal singleton schedule.  The fixed-face graph-work bridge,
adaptive target-bank reuse, and anchor-right-hand-side count are no longer
algebraic obstructions. \\
\statusproved & A logarithmic batch of source-normalized Gaussian response
sketches simultaneously supplies constant-factor group masses for every node
of a fixed boundary hierarchy.  Dynamic local maintenance of those sketches
remains open. \\
\statusproved & Fresh per-batch normalized response sketches can be accumulated
causally along an adaptive trace.  Their squared masses remain valid for every
prefix, and a newly exposed boundary label has zero contribution from all past
batches, so no response replay is required. \\
\statusproved & Across a nested exact-face expansion, the squared exterior
demand change is at most the primal energy shock.  Boundary degree volume
moving by normalized margin $\theta$ is therefore at most
$2\Delta/\theta^2$. \\
\statusproved & Along a monotone support-safe exact-face trace, every inactive
normalized boundary demand is nondecreasing and the total degree-weighted
variation is at most $(1-\alpha)/2$.  Exact threshold crossings therefore
occur at most once per boundary coordinate. \\
\statusproved & If anchor volumes grow geometrically, all linear-volume
rebuild costs sum to a constant multiple of the final volume. \\
\statusproved & A converged frontier probe may precede a heavy rebuild without
changing the eventual exact response.  It can omit a terminal rebuild when the
probe already certifies the requested output. \\
\statusconditional & Given a fully charged response interface and
preconditioned repairs on geometrically growing faces, total work is bounded
by response maintenance plus final volume times the square root of the
effective preconditioned condition number, up to logarithms. \\
\statusopen & No arbitrary-graph implementation of the required finite-band
heavy-change reporter is proved.  The measurement dimension is now
output-sensitive; the remaining target-side primitive is to build and apply
the coded harmonic rows implicitly or in shared batches, or maintain the
equivalent incremental squared-response range-add, across an adaptive epoch
without repeated old-face cut scans or stored-response reads.  Adaptive reuse
and online rent-or-buy already control the target dimension and anchor
right-hand-side count.  Literal per-event dense harmonic
tables are already ruled out by
\cref{prop:notched-sun-fresh-dense-coded-table-charge}.
\\
\statusrefuted & The phrase ``warm start the SDD solver'' is not a sufficient
algorithm: explicit old-face correction, complete boundary refresh, or full
vector output after every expansion can recreate quadratic repeated-prefix
work. \\
\bottomrule
\end{longtable}
}

The architectural recommendation is therefore risk-adjusted rather than
absolute.  A graph-uniform output-linear response solver would dominate the
mixed construction asymptotically.  Conversely, a graph-uniform
expanding-subspace first-order theorem could attain the product scale without
nontrivial response state.  The mixed route is useful because it exposes and
combines the partial theorems already available at both endpoints.
