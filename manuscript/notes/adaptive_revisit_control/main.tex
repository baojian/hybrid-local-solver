% Standalone research note for the hybrid-local-solver project.
%
% This note studies the adaptive-revisit direction suggested by the measured
% frontier alternating ladder.  It separates exact ledgers, impossibility
% statements, unconditional safeguards, and the remaining accelerated arm
% theorem.

\documentclass[11pt]{article}

\input{../../tex/shared/research_note_preamble}

\title{Adaptive Revisit Control for Local SOR:\\
What the Signal Certifies and How to Use It Safely}
\author{Baojian Zhou\\
\small Working research note for \texttt{hybrid-local-solver}}
\date{August 2026}

\begin{document}
\maketitle

\begin{abstract}
The measured frontier adaptive ladder stores one number after each solve:
total charged row work divided by the charge of the touched support.  We
analyze this revisit ratio as a possible controller for later local PageRank
instances.  The ratio has an exact interpretation: it is the
charge-weighted mean number of paid visits to a touched coordinate.  A finer
four-way ledger, indexed by the previous and current relaxation modes on each
revisited coordinate, gives more information.  In particular, every revisit
whose previous operation was exact Gauss--Seidel certifies intervening
neighbor-generated backflow, because the exact operation left zero residual
on that coordinate.  On a nested propagate--settle execution, the same
measured ratio factors exactly into a within-epoch work multiplier and the
settled-volume revisit factor.  This yields a causal revisit bank that
enforces a declared work-per-touched-support ceiling online.  On any
support-safe terminal RPPR trace, the realized ratio itself gives the
a posteriori bound $W\leq2\varrho_{\rm rev}/\rho$.  Thus a global bank at
$\widetilde O(1/\sqrt\alpha)$ certifies an accelerated target-scale fast path;
if the bank blocks, the spent prefix is capped before a certified fallback.
This success certificate cannot be promoted to a universal theorem for the
literal fixed-band two-rung arm.  On a one-edge graph, a support-safe handoff
can lie strictly below the spreading threshold while its exact delivery tail
takes $\Omega(1/\alpha)$ work at fixed regularization and accuracy.  Thus
that arm needs forced or quantitatively developed spreading, or an exact
incremental Schur block, to carry an accelerated worst-case theorem.

The scalar ratio is not, however, a direction certificate for relaxation.
Its denominator depends on the policy, it does not order total work, and one
historical observation cannot distinguish future task classes whose best
relaxations are opposite.  The exact coordinate-energy identity gives a
second limitation: at a fixed residual and coordinate, immediate objective
decrease per charge is uniquely maximized by unit relaxation.  Any benefit
from over-relaxation must therefore be a multi-operation transport effect,
not a myopic descent effect.

Two unconditional safeguards survive these negative results.  For the
note-scoped unregularized residual gate, any causal controller may select
relaxations from $[1,2-\delta]$.  If every paid update is live, the resulting
variable-relaxation method terminates from the zero iterate with charged work
at most
\[
 \frac{1+\alpha}
 {\delta(2-\delta)\,\alpha\epsppr^{2}}.
\]
This is a safety bound, not the desired accelerated rate.  More importantly,
a budgeted adaptive prefix can switch to exact Gauss--Seidel on the same
irreversible state with an explicit near-baseline work ceiling.  For RPPR,
exact restricted settlement supplies an even stronger canonical checkpoint:
any history-dependent policy may switch without reset, and any nonempty
subset of exact boundary violations can be admitted while preserving true
support containment and finite termination.  Combining this absorption law
with the revisit bank gives a one-state, work-capped switching theorem.
Absorption alone is not enough for best-arm work.  Already on a finite
unweighted tree, the exact single-seed RPPR boundary gate has two safe batch
orders with the same terminal point whose settled-volume revisit differs by
an arbitrarily large factor.  We isolate the missing property as a mergeable
countdown.  If every arm has a work-valued countdown decreased by its own
blocks and never increased by other arms, a work-fair scheduler on one shared
state is $K\kappa$-competitive whenever those countdowns are
$\kappa$-tight.  Irreversible activation tokens instantiate this condition:
on endpoint paths, common forward Schur records plus one reverse recovery
give a $\kappa=1$ countdown and $O(C(S^\star))\leq O(1/\rho)$ work.  The same
conclusion extends to the smallest genuinely branching topology, a
center-seeded three-arm spider, by storing one affine transfer product per
actual common arm prefix and two scalar root aggregates.  Every legal exact
boundary-batch order updates only committed common records; no arm-private
trajectory or eager rekey of untouched tips is used.  On the double-Y seeded
at one branch vertex, $s=e_o$, with two adjacent degree-three branch vertices,
four pendant products couple through a
fixed rank-two Schur core once the second branch is reached and retain the same
$\kappa=1$ token tightness with a fully separated linear ledger.  A two-vertex
activation breakpoint proves that no jointly continuous
state-energy countdown can pay a fixed activation charge uniformly.
For each fixed $m$ on the branch-seeded growing caterpillar, a legal
family-dependent small-$\rho$ positive-subset singleton order first reaches an
$m$-vertex tridiagonal branch core, leaves $m+1$ positive pendant tips
deferred, and then extends one long arm $m$ times.
Every extension changes every deferred exact tip demand.  This forces
$\Theta(m^2)$ old-key writes in the named EagerTipKey representation and
breaks its linear activation-token template.  It is not a lower bound for
implicit states: a balanced affine-transfer tree still supports one named
tip query in logarithmic exact work.  For the explicit backbone-first
exact-KKT positive-subset policy, the named exact-real
\emph{CaterpillarKineticDelta}
reporter now combines a separable last Green column with a strict crossing-key
heap.  Its delta-label interface retains the exact all-positive live-tip set
and has exhaustive work $O(m\log(2+m))$.  On the smaller explicit range
$0<\rho<\rho_{\rm can}(m,\alpha)$, the actual canonical all-violations trace
instead consists of exactly $m$ three-label distance layers.  Early side-leaf
and long-arm admissions alter only constant many tridiagonal transfer cells
per batch, and the named \emph{CaterpillarCanonicalLayerDelta} reporter has a
fully charged $O(m\log(2+m))$ exact-real ledger.  Arbitrary pre-backbone
positive-subset interleavings, the remaining range up to $\rho_{\rm cat}$,
finite precision, and a uniform-in-$m$ parameter range remain open.

In addition, a branchable weighted portfolio of $K$ local policies satisfies
the exact oracle inequality
\[
 \operatorname{Work}_{\rm port}
 \leq \min_j \frac{\operatorname{Work}_j}{\nu_j},
\]
up to one scheduling chunk, where $\nu_j$ is policy $j$'s work share.
Uniform shares are $K$-competitive with the best arm on every instance.  A
learned predictor may use the causal revisit ledger to set the shares; mixing
it with an exploration floor preserves the worst-case guarantee without any
stationarity, graph-transfer, or identifiability assumption.  Consequently a
finite portfolio automatically inherits a
$\widetilde O(\mathsf V_{\rm loc}/\sqrt\alpha)$ local-work theorem whenever one of its arms has
one.  Simultaneous branches are not essential: a geometric restart portfolio
uses one live state and is less than $4K$-competitive up to its base work
quantum.  The unresolved mathematical task is now clean: prove an accelerated
bank for a modified arm with a genuine transport mechanism, or extend the
common transfer countdown beyond a fixed adjacent two-branch core.  Revisit
feedback can safely select among theories; by itself it does not create the
missing accelerated theory.
\end{abstract}

\tableofcontents

\section{Scope and claim ledger}
\label{sec:scope}

\input{../../tex/shared/source_aligned_problem}

The repository-wide residual convention remains open.  This note uses only
the document-scoped unregularized residual
\begin{equation}
 r(\widehat x):=b-Q\widehat x,
 \qquad
 \max_{u\in V}\frac{\abs{r_u(\widehat x)}}{\sqrt{d_u}}
 \leq g,
 \qquad g:=\alpha\epsppr.
 \label{eq:note-gate}
\end{equation}
It is the verifier gate of the measured ladder notes, not an adopted
repository stopping rule.  No conversion from $\epsppr$ to $\rho$,
$\epsappr$, $\epsobj$, or $\epspg$ is asserted.

The measured row-operation charge is
\begin{equation}
 c(u):=1+d_u.
 \label{eq:charge}
\end{equation}
This differs from the manuscript's pure adjacency-scan charge $d_u$ by at
most a factor two because the shared model has $d_u\geq1$.  Theorems below
state which convention they use.  Frontier enumeration, ranking, state
copying, and scheduler arithmetic are not hidden inside \eqref{eq:charge};
where they matter, they are reported separately.
When discussing a future accelerated arm, $\mathsf V_{\rm loc}(I)$ denotes
its explicitly defined local degree-volume ledger on instance $I$; it is not
the reserved vertex set $V$.

\begin{center}
\scriptsize
\begin{tabularx}{\textwidth}{@{}p{0.21\textwidth}X@{}}
\toprule
Status & Statement \\
\midrule
\statusobservation & The sibling note
\texttt{frontier\_adaptive\_ladder} records the measured scalar revisit
memory and its fixed update map.  The fixed two-rung schedule beats that
complete adaptive artifact by $12\%$ on the available 24-cell campaign, so
the memory has no demonstrated empirical gain on that schedule. \\
\statussource & Adaptive restart uses observable oscillation to reset global
momentum \citep{odonoghue2015adaptive}.  That precedent does not prove that a
local revisit count identifies a good SOR relaxation. \\
\statusproved & The scalar revisit ratio is exactly a charge-weighted mean
multiplicity.  A four-way previous-mode/current-mode decomposition is exact,
and every revisit following an exact operation certifies intervening
off-coordinate backflow. \\
\statusproved & On nested propagate--settle traces, the empirical ratio
factors exactly into within-epoch multiplicity and settled-volume revisit.
An online revisit bank enforces the former work cap rather than assuming it. \\
\statusproved & Every terminal support-safe RPPR trace has the a posteriori
certificate $W\leq2\varrho_{\rm rev}/\rho$.  A global bank at the accelerated
revisit scale therefore gives a certified fast path, while its failure caps
the prefix before fallback. \\
\statusrefuted & The literal fixed-band two-rung continuation is not a
graph-uniform accelerated arm.  A support-safe one-edge handoff can skip its
spreading rung and require $\Omega(1/\alpha)$ exact-delivery work at fixed
regularization and accuracy. \\
\statusrefuted & The scalar ratio alone neither orders total work nor
determines whether the next relaxation should rise or fall over unrestricted
task sequences.  Immediate energy decrease per charge also cannot justify
over-relaxation. \\
\statusproved & Any causal variable-relaxation controller clamped to
$[1,2-\delta]$ terminates under the live gate \eqref{eq:note-gate}; a
condition-free but nonaccelerated work bound is explicit.  A budgeted
adaptive prefix followed by exact fallback retains a quantified one-state
work ceiling without reset. \\
\statusproved & A branchable weighted policy portfolio has a deterministic
best-arm oracle inequality on every instance.  A geometric restart portfolio
obtains a constant-factor best-arm guarantee with only one live state.  An
arbitrary learned allocation remains safe after mixing with a positive
exploration floor. \\
\statusproved & For RPPR, exact settlement is an absorption-safe canonical
checkpoint.  Arbitrary causal policy changes and partial exact-certified
boundary batches preserve support containment and terminate correctly on one
irreversible state; the revisit bank caps inter-checkpoint work. \\
\statusrefuted & Absorption, nested support, and persistence of unadmitted
violations do not imply best-order work.  A finite unweighted single-seed
RPPR tree has an unbounded settled-revisit gap between two legal exact
boundary-batch orders with the same terminal point. \\
\statusconditional & Interference-monotone, $\kappa$-tight work countdowns
give a $K\kappa$-competitive shared-state portfolio without solution branches
or reset.  The common energy countdown instantiates safety but not
acceleration. \\
\statusproved & Irreversible activation tokens instantiate the mergeable
condition.  On endpoint paths, a common forward Schur record and one reverse
recovery give $\kappa=1$ and work at most $4/\rho$.  On a center-seeded
three-arm spider, exact affine transfer products give a $\kappa=1$ common-state
countdown with separately charged linear response, exposure, control, state,
recovery, and output work for every legal certified batch order.  The same
token tightness holds on the double-Y seeded at branch vertex $o$, $s=e_o$:
its common response rank is one before the second branch vertex is admitted
and two afterward, and its ledger also
separates validation and transient memory.  Continuous
numerical countdowns cannot pay activation uniformly near a two-vertex support
breakpoint. \\
\statusrefuted & One legal small-$\rho$ positive-subset caterpillar order
forces $\Theta(m^2)$ literal EagerTipKey rewrites, but not implicit/lazy
reporter work. \\
\statusproved & For fixed $m$, the branch seed, family-$\rho$, and the
backbone-first positive-subset policy, CaterpillarKineticDelta is an exact-real
$O(m\log(2+m))$ delta reporter; repeated full lists are excluded. \\
\statusproved & For fixed $m$ and $0<\rho<\rho_{\rm can}(m,\alpha)$, canonical
all-violations consists of $m$ three-label layers.  The exact-real
CaterpillarCanonicalLayerDelta has a complete $O(m\log(2+m))$ delta/output
ledger. \\
\statusconditional & If one arm has
$\widetilde O(\mathsf V_{\rm loc}/\sqrt\alpha)$ work on an instance class, the finite
portfolio inherits that scale.  No such graph-uniform theorem is claimed
here for the literal frontier ladder or the fixed two-rung arm. \\
\statusopen & Extend charged implicit reporting to the remaining canonical
range $\rho_{\rm can}\leq\rho<\rho_{\rm cat}$, arbitrary pre-backbone
positive-subset orders, or cycles; prove a
forced-spreading arm beyond its current structural classes, or reduce the
continuous $\omega$ family to a finite safe menu. \\
\bottomrule
\end{tabularx}
\end{center}

\section{The literal feedback signal}
\label{sec:literal}

Consider one sequential run with paid row operations
$t=1,\ldots,T$.  Operation $t$ acts on $u_t$, uses relaxation
$\omega_t$, and costs $c(u_t)$.  Guarded skips have zero charge and are not
included.  Let
\begin{equation}
 S:=\{u_t:1\leq t\leq T\},
 \qquad
 C(S):=\sum_{u\in S}c(u),
 \qquad
 m_u:=\card{\{t:u_t=u\}},
 \label{eq:touched}
\end{equation}
and define
\begin{equation}
 W:=\sum_{t=1}^T c(u_t),
 \qquad
 \varrho_{\rm rev}:=\frac{W}{C(S)}.
 \label{eq:revisit}
\end{equation}
This is the mathematical version of the frontier artifact's stored ratio.

\begin{lemma}[Exact multiplicity identity]
\label{lem:multiplicity}
For every nonempty paid run,
\begin{equation}
 \varrho_{\rm rev}
 =\frac{\sum_{u\in S}c(u)m_u}{\sum_{u\in S}c(u)}
 =1+\frac{\sum_{u\in S}c(u)(m_u-1)}{C(S)}.
 \label{eq:multiplicity}
\end{equation}
Hence $\varrho_{\rm rev}\geq1$, with equality exactly when every touched
coordinate is paid for once.
\end{lemma}

\begin{proof}
Group the terms in $W$ by their operated coordinate.  The second equality
subtracts the first paid visit to every $u\in S$.
\end{proof}

Thus the numerator of $\varrho_{\rm rev}-1$ is an exact redundant-work
ledger relative to the realized touched set.  This is useful, but it is not
yet causal: a planned spread-then-settle revisit, a same-mode repeated sweep,
and a neighbor-reactivated coordinate all contribute identically.

\subsection{A causal four-way refinement}

Label a paid operation \emph{exact} if $\omega_t=1$ and \emph{spreading} if
$\omega_t>1$.  For every revisit of $u$, inspect the most recent earlier paid
operation on the same $u$.  For $a,b\in\{\mathrm e,\mathrm s\}$, let
$\mathsf{Rev}_{a\to b}$ be the total charge of revisits whose previous same-coordinate
mode was $a$ and whose current mode is $b$.  This ledger requires only two
bits of state per touched coordinate: whether it has been visited and the
mode of its most recent paid visit.

\begin{proposition}[Causal revisit ledger]
\label{prop:causal-ledger}
Every paid run satisfies
\begin{equation}
 W=C(S)+\mathsf{Rev}_{\mathrm e\to\mathrm e}
       +\mathsf{Rev}_{\mathrm e\to\mathrm s}
       +\mathsf{Rev}_{\mathrm s\to\mathrm e}
       +\mathsf{Rev}_{\mathrm s\to\mathrm s},
 \label{eq:causal-ledger}
\end{equation}
and therefore
\begin{equation}
 \varrho_{\rm rev}
 =1+\frac{\mathsf{Rev}_{\mathrm e\to\mathrm e}
       +\mathsf{Rev}_{\mathrm e\to\mathrm s}
       +\mathsf{Rev}_{\mathrm s\to\mathrm e}
       +\mathsf{Rev}_{\mathrm s\to\mathrm s}}{C(S)}.
 \label{eq:causal-ratio}
\end{equation}
Moreover, in exact sequential arithmetic, every charged revisit counted in
$\mathsf{Rev}_{\mathrm e\to\mathrm e}
 +\mathsf{Rev}_{\mathrm e\to\mathrm s}$ certifies that at
least one off-coordinate operation altered that coordinate's residual after
its previous exact operation.  In the shared graph model, such an operation
must occur at a neighbor.
\end{proposition}

\begin{proof}
Every paid operation is either the first paid visit to its coordinate or is
in exactly one of the four previous/current-mode classes.  The first visits
sum to $C(S)$, proving \eqref{eq:causal-ledger}.

For the causal statement, write $r=b-Q\widehat x$.  A row operation on $u$
has the form
\begin{equation}
 \widehat x^+=\widehat x+\omega\frac{r_u}{Q_{uu}}e_u,
 \qquad
 r^+=r-\omega\frac{r_u}{Q_{uu}}Qe_u.
 \label{eq:row-update}
\end{equation}
If $\omega=1$, then $r_u^+=0$.  Before a later paid revisit, $r_u$ must
again cross the live guard.  Operations on a coordinate $v$ change $r_u$
only through $Q_{uv}$.  In the shared PageRank matrix, $Q_{uv}\neq0$ for
$u\neq v$ exactly when $u$ and $v$ are adjacent.  Thus some intervening
neighbor operation generated the new residual.
\end{proof}

The quantity
\begin{equation}
 B_{\rm nbr}:=
 \frac{\mathsf{Rev}_{\mathrm e\to\mathrm e}
       +\mathsf{Rev}_{\mathrm e\to\mathrm s}}{C(S)}
 \label{eq:backflow-share}
\end{equation}
is therefore a certified neighbor-backflow revisit share.  By contrast,
$\mathsf{Rev}_{\mathrm s\to\mathrm e}
 +\mathsf{Rev}_{\mathrm s\to\mathrm s}$ mixes neighbor
backflow with the deterministic self-reflection left by over-relaxation.  The
four-way ledger does not prove which $\omega$ is optimal, but it separates a
real causal event that the scalar ratio erases.

\section{What the scalar cannot certify}
\label{sec:limits}

\subsection{The denominator is policy dependent}

The touched set $S$ is produced by the policy.  Consequently
$\varrho_{\rm rev}$ is not a normalization by an instance-only quantity.

\begin{proposition}[Revisit ratio does not order work]
\label{prop:not-order}
There are valid charged-event ledgers $A$ and $B$ for which
$\varrho_{\rm rev}(A)<\varrho_{\rm rev}(B)$ but $W(A)>W(B)$, with an
arbitrarily large work ratio.
\end{proposition}

\begin{proof}
Use coordinates of charge two.  Ledger $A$ touches $M$ coordinates once, so
$C(S_A)=W(A)=2M$ and $\varrho_{\rm rev}(A)=1$.  Ledger $B$ touches one
coordinate twice, so $C(S_B)=2$, $W(B)=4$, and
$\varrho_{\rm rev}(B)=2$.  For $M\geq3$, the ratio ordering is opposite the
work ordering, and $W(A)/W(B)=M/2$ is unbounded.  This is a ledger-level
counterexample; it makes no claim that every such event stream is generated
by the same SOR instance.
\end{proof}

This proposition rules out comparing policies by the scalar ratio alone.  A
repeated-task learner needs an arm-independent loss normalization, such as a
declared work cap, or it must learn total charged work directly.

\subsection{One historical scalar gives no direction}

Even if the denominator problem is repaired, one chosen policy reveals no
counterfactual cost for another policy.

\begin{proposition}[One-signal nonidentifiability]
\label{prop:nonidentify}
Fix two candidate future policies.  Over unrestricted repeated-task cost
sequences, no deterministic rule that sees the same historical revisit
record in two environments can choose a bounded-competitive future policy in
both.  For a randomized rule and every $M>1$, one of two indistinguishable
environments has expected competitive ratio at least $(M+1)/2$.
\end{proposition}

\begin{proof}
Let the complete history, including $\varrho_{\rm rev}$, be identical.  In
environment one, assign future policy costs $(1,M)$; in environment two,
assign $(M,1)$.  A deterministic rule makes the same choice in both and pays
$M$ in one of them while the best policy costs one.  If it chooses the first
policy with probability $p$, its expected costs are
$p+(1-p)M$ and $pM+(1-p)$.  Their maximum is at least their average,
$(M+1)/2$.
\end{proof}

This is a decision-theoretic result, not an SOR graph construction.  Its role
is precise: a guarantee from the historical scalar requires an additional
assumption such as stationary tasks, a relation between contexts and costs,
counterfactual probes, or a safe portfolio that runs more than one policy.
Keying memory only by graph size $n$ supplies none of these assumptions;
nonisomorphic graphs, different seeds, and different gates can share $n$.

\section{Turning revisit into an online work certificate}
\label{sec:revisit-certificate}

The scalar is not a reliable predictor of the best relaxation, but it is an
exact certificate of work already spent.  It also factors cleanly into the
two revisit mechanisms isolated by the propagate--settle framework:
repetition inside one epoch and repeated settlement of a growing region.

\subsection{Two-level revisit factorization}

Let
\[
 U_0\subseteq U_1\subseteq\cdots\subseteq U_K
\]
be nonempty nested regions.  Suppose epoch $k$ incurs charged work $W_k$,
the union of all coordinates touched by the full run is $U_K$, and write
\begin{equation}
 \mu_k:=\frac{W_k}{C(U_k)},
 \qquad
 \mathfrak R_{\rm set}
 :=\frac{\sum_{k=0}^K C(U_k)}{C(U_K)}.
 \label{eq:two-level-definitions}
\end{equation}
The first quantity is the within-epoch work multiplier.  The second is the
settled-volume revisit factor proved useful in the sibling
\texttt{propagate\_settle\_framework} note.

\begin{theorem}[Exact two-level revisit factorization]
\label{thm:two-level-factorization}
Define the settled-volume-weighted mean
\begin{equation}
 \overline\mu:=
 \frac{\sum_{k=0}^K\mu_kC(U_k)}
      {\sum_{k=0}^K C(U_k)}.
 \label{eq:mean-local-multiplier}
\end{equation}
Then the final operation-level revisit ratio satisfies
\begin{equation}
 \varrho_{\rm rev}
 =\overline\mu\,\mathfrak R_{\rm set}.
 \label{eq:two-level-factorization}
\end{equation}
Consequently, if $m\leq\mu_k\leq M$ for every epoch, then
\begin{equation}
 m\,\mathfrak R_{\rm set}
 \leq\varrho_{\rm rev}
 \leq M\,\mathfrak R_{\rm set}.
 \label{eq:factorization-sandwich}
\end{equation}
If every epoch scans its whole settled region at least once, one may take
$m=1$.
\end{theorem}

\begin{proof}
The total charged work is
\[
 W=\sum_{k=0}^K W_k
  =\sum_{k=0}^K\mu_kC(U_k)
  =\overline\mu\sum_{k=0}^K C(U_k).
\]
Divide by $C(U_K)$ and use
\cref{eq:revisit,eq:two-level-definitions}.  The sandwich follows because a
weighted mean lies between its smallest and largest entries.
\end{proof}

This identity bridges the empirical and theoretical statistics.  A large
$\varrho_{\rm rev}$ can arise from large within-epoch multiplicity, large
settled-volume revisit, or both.  The causal ledger diagnoses the first
mechanism; incremental Schur state or a different expansion schedule attacks
the second.  The identity applies to nested propagate--settle executions, not
automatically to the literal frontier artifact's noncanonical SOR phases.

\subsection{The revisit bank}

The ratio admits a causal token interpretation requiring no model of the
graph dynamics.  After $t$ paid events, let $S_t$ be the touched union and
$W_t$ the charged work.  For a declared budget factor $\Gamma\geq1$, define
\begin{equation}
 \mathsf{Bank}_t(\Gamma):=\Gamma C(S_t)-W_t.
 \label{eq:revisit-bank}
\end{equation}

\begin{theorem}[Exact revisit-bank updates]
\label{thm:revisit-bank}
Suppose event $t$ operates coordinate $u$ at charge $c(u)$.  If this is its
first paid visit, then
\begin{equation}
 \mathsf{Bank}_t(\Gamma)-\mathsf{Bank}_{t-1}(\Gamma)
 =(\Gamma-1)c(u).
 \label{eq:bank-first}
\end{equation}
If it is a revisit, then
\begin{equation}
 \mathsf{Bank}_t(\Gamma)-\mathsf{Bank}_{t-1}(\Gamma)
 =-c(u).
 \label{eq:bank-revisit}
\end{equation}
Therefore a controller that admits every desired first visit but admits a
revisit only when
\begin{equation}
 c(u)\leq\mathsf{Bank}_{t-1}(\Gamma)
 \label{eq:bank-admission}
\end{equation}
maintains, at every nonempty prefix,
\begin{equation}
 W_t\leq\Gamma C(S_t),
 \qquad
 \varrho_{{\rm rev},t}\leq\Gamma.
 \label{eq:bank-invariant}
\end{equation}
\end{theorem}

\begin{proof}
A first visit increases both $C(S_t)$ and $W_t$ by $c(u)$, proving
\cref{eq:bank-first}.  A revisit increases only $W_t$, proving
\cref{eq:bank-revisit}.  The bank starts at zero.  First visits preserve its
nonnegativity for $\Gamma\geq1$, and
\cref{eq:bank-admission} preserves it on revisits.  Rearranging a nonnegative
bank gives \cref{eq:bank-invariant}.
\end{proof}

The bank is stronger than checking the ratio after a solve: it makes a
work-per-touched-support cap enforceable online.  It is not a convergence
proof.  When a useful revisit would overdraw the bank, the controller must
settle, switch policy, increase its declared budget, or invoke a certified
fallback.  Silently dropping the operation could destroy progress.

The same identity gives a useful guarantee even before one proves that the
bank never blocks.

\begin{corollary}[A posteriori accelerated RPPR fast path]
\label{cor:rppr-fast-path}
Consider any terminal RPPR execution whose charged touched union $S$ is
contained in $S^\star(\rho)$.  No nesting or fixed relaxation schedule is
required.  Its realized charged work satisfies
\begin{equation}
 W=\varrho_{\rm rev}C(S)
 \leq \frac{2\varrho_{\rm rev}}{\rho}.
 \label{eq:rppr-fast-path}
\end{equation}
Consequently, if a global revisit bank with factor $\Gamma$ remains
nonnegative until termination, then
\begin{equation}
 W\leq\frac{2\Gamma}{\rho}.
 \label{eq:banked-rppr-fast-path}
\end{equation}
Taking $\Gamma=\widetilde O(1/\sqrt\alpha)$ certifies the intended
$\widetilde O(1/(\rho\sqrt\alpha))$ work scale on that execution.
\end{corollary}

\begin{proof}
The first equality is the definition of the measured revisit ratio.  Support
containment and the source volume bound give
\[
 C(S)\leq C(S^\star(\rho))
 \leq2\vol(S^\star(\rho))\leq\frac2\rho.
\]
If the bank remains nonnegative, \eqref{eq:bank-invariant} supplies
$\varrho_{\rm rev}\leq\Gamma$.
\end{proof}

This is an a posteriori certificate with no graph-structural hypothesis
beyond support safety, not a graph-uniform success theorem.  A safe service
can declare an
accelerated cap $\Gamma_{\rm acc}$, run any support-safe mixture of the new
policies, and switch at the first proposed event that would overdraw the
global bank.  The prefix then costs at most $2\Gamma_{\rm acc}/\rho$.
Canonicalize the current certified region and include that cost in any
fallback continuation bound $C_{\rm fall}$; the complete service obeys
\begin{equation}
 W_{\rm total}
 \leq \frac{2\Gamma_{\rm acc}}{\rho}+C_{\rm fall}.
 \label{eq:accelerated-or-fallback}
\end{equation}
Thus every instance either carries an a posteriori target-scale
certificate or enters a separately certified fallback after a capped local
prefix.  Proving that fallback is never invoked remains the universal
acceleration problem.

The literal measured two-rung arm does not solve that problem.  The next
theorem keeps the note's unregularized residual gate and uses RPPR only as
the certified support envelope, exactly as in
\Cref{cor:rppr-fast-path}.

\begin{theorem}[A support-safe one-edge obstruction to fixed-band two-rung]
\label{thm:two-vertex-two-rung-obstruction}
Fix a band factor $B>1$ and put
\[
 \vartheta_B:=\frac{B+1}{2}\in(1,B),
 \qquad
 \alpha_B:=\min\left\{\frac14,
                   \frac{3\log\vartheta_B}{16}\right\}.
\]
For every $0<\alpha\leq\alpha_B$, there is a one-edge, single-seed
support-safe handoff at fixed RPPR regularization $\rho=1/4$ for which the
literal two-rung schedule
\[
 [(Bg,\omega_\star),(g,1)]
\]
executes no spreading operation and needs exactly
\begin{equation}
 N_B(\alpha)
 :=\left\lceil
   \frac{\log\vartheta_B}
        {\log((1+\alpha)/(1-\alpha))}
  \right\rceil
 \label{eq:two-vertex-exact-tail}
\end{equation}
exact-delivery pushes.  Its realized revisit factor satisfies
\begin{equation}
 \varrho_{\rm rev}
 \geq\frac{N_B(\alpha)}2
 \geq\frac{3\log\vartheta_B}{16\alpha}.
 \label{eq:two-vertex-bank-lower}
\end{equation}
Here $\rho$ is fixed and
$\epsppr=(1-\alpha)/[(1+\alpha)\vartheta_B]$ stays bounded away from zero.
Consequently the literal fixed-band arm cannot guarantee an
$\widetilde O(1/\sqrt\alpha)$ global bank, even on one edge.
\end{theorem}

\begin{proof}
Let the graph be $o-v$, with seed $o$.  Both degrees equal one, and write
\[
 Q=\begin{pmatrix}a&-h\\-h&a\end{pmatrix},
 \qquad
 a:=\frac{1+\alpha}{2},
 \qquad
 h:=\frac{1-\alpha}{2},
 \qquad
 \zeta:=\frac{h}{a}=\frac{1-\alpha}{1+\alpha}.
\]
At $\rho=1/4$, the shifted RPPR load is
$\ell_\rho=(3\alpha/4,-\alpha/4)$.  Its full solution has
\[
 (Q^{-1}\ell_\rho)_v
 =\frac{\alpha(3h-a)}{4(a^2-h^2)}>0
\]
because $\alpha<1/2$.  Thus $S^\star(1/4)=\{o,v\}$.  More explicitly, the
canonical root point $y=(3\alpha/(4a),0)$ has shifted boundary violation
\[
 (\ell_\rho-Qy)_v=\frac{\alpha(3\zeta-1)}4>0,
\]
so the exact boundary gate certifies $v$ before any operation on it.

Do not settle the enlarged two-coordinate RPPR system.  Instead switch on
the same irreversible state to the note's unregularized residual
$r=b-Q\widehat x$.  One exact root update moves $y_o$ to $\alpha/a$, the
unregularized root checkpoint, and leaves
\begin{equation}
 r=(0,\alpha\zeta).
 \label{eq:two-vertex-handoff}
\end{equation}
This update and every later one touch only the certified set
$S^\star(1/4)$.

Choose
\[
 g:=\frac{\alpha\zeta}{\vartheta_B},
 \qquad
 \epsppr:=\frac g\alpha=\frac{\zeta}{\vartheta_B}.
\]
At the handoff, $\max_u|r_u|=\alpha\zeta<Bg$, so the strict live guard on
the spreading rung is empty with a nonzero margin.  In the exact rung only
one residual coordinate is nonzero at a time.  A unit row push zeros it and
transfers the factor $h/a=\zeta$ to the other endpoint.  After $t$ pushes,
the maximum residual is therefore $\alpha\zeta^{t+1}$.  The first $t$ for
which this is at most $g$ is exactly \eqref{eq:two-vertex-exact-tail}.

Each tail push has charge two and $C(S^\star)=4$, so the tail alone gives
$\varrho_{\rm rev}\geq N_B/2$.  For $\alpha\leq1/2$,
\[
 \log\frac{1+\alpha}{1-\alpha}
 =2\operatorname{arctanh}(\alpha)
 \leq\frac{2\alpha}{1-\alpha^2}
 \leq\frac{8\alpha}{3}.
\]
Substitution proves \eqref{eq:two-vertex-bank-lower}.  The choice of
$\alpha_B$ also makes $N_B\geq2$; all prior gate and handoff work can only
increase the ratio.
\end{proof}

Thus the false lemma is now precise: support containment, current residual
band, and an exact delivery rung do not imply accelerated cleanup.  This
does not refute a forced or quantitatively developed $\omega_\star$ burn-in,
nor exact Schur settlement; either mechanism prevents the displayed dead
spreading phase.  In the causal ledger, the tail is entirely
exact-after-exact neighbor backflow, so merely labeling that debt also does
not make it an accelerated countdown.

For the next corollary, every paid row update, ranking scan, or boundary scan
is represented as a charged coordinate-touch event in this same ledger.  A
procedure may use a more detailed cost model, but it may not omit these
charges and still invoke the bank guarantee.

\begin{corollary}[Banked propagate--settle work]
\label{cor:banked-propagate-settle}
Consider nested epochs $U_0,\ldots,U_K$.  In epoch $k$, cap all propagation,
ranking, and boundary-inspection work by running a revisit bank with factor
$\Gamma_{\rm p}$ and touched union $U_k$.  Suppose exact settlement and its
boundary update cost at most
$C_{\rm s}(w+1)^2C(U_k)$.  Then
\begin{equation}
 \operatorname{Work}_{0:K}
 \leq
 \bigl(\Gamma_{\rm p}+C_{\rm s}(w+1)^2\bigr)
 \mathfrak R_{\rm set}\,C(U_K).
 \label{eq:banked-propagate-work}
\end{equation}
For RPPR, if $U_K=S^\star(\rho)$ and
$\mathfrak R_{\rm set}=\widetilde O(1/\sqrt\alpha)$, this becomes
\begin{equation}
 \operatorname{Work}_{0:K}
 =\widetilde O\!\left(
 \frac{\Gamma_{\rm p}+(w+1)^2}{\rho\sqrt\alpha}
 \right).
 \label{eq:banked-rppr-product}
\end{equation}
\end{corollary}

\begin{proof}
The bank gives propagation-side work at most
$\Gamma_{\rm p}C(U_k)$ in each epoch.  Add the settlement bound and sum.
Definition \eqref{eq:two-level-definitions} proves
\eqref{eq:banked-propagate-work}.  In the shared graph model,
$C(U)\leq2\vol(U)$, while
$\vol(S^\star(\rho))\leq1/\rho$.  Substitute these inequalities and the
stated revisit target.
\end{proof}

This corollary removes one assumption from the abstract propagate--settle
ledger: bounded propagation multiplicity can be enforced rather than merely
postulated.  The remaining product-scale obligation is the cross-epoch
factor $\mathfrak R_{\rm set}$, or an incremental state representation that
avoids paying fresh settlement volume at every epoch.

\section{Absorption-safe causal switching}
\label{sec:absorption-switching}

Exact settlement supplies a canonical checkpoint at which changing policies
requires neither cloning nor restart.  The cleanest theorem is for RPPR,
whose support gate is already compatible with the shared source model.
Define the shifted load
\begin{equation}
 \ell_\rho:=b-\alpha\rho D^{1/2}\one.
 \label{eq:adaptive-shifted-load}
\end{equation}
For a nonempty region $U$, its canonical restricted point is
\begin{equation}
 x[U]_U:=Q_{UU}^{-1}(\ell_\rho)_U,
 \qquad
 x[U]_{U^c}:=0.
 \label{eq:adaptive-canonical-point}
\end{equation}
At this checkpoint, define the boundary-violation set
\begin{equation}
 \mathcal B(U):=
 \left\{v\in\partial U:
  (\ell_\rho-Qx[U])_v>0
 \right\}.
 \label{eq:adaptive-boundary-violations}
\end{equation}

\begin{theorem}[Absorption-safe adaptive boundary gate]
\label{thm:absorption-safe-switch}
Assume a single seed $o$ and $(\ell_\rho)_o>0$.  Start with $U_0=\{o\}$.
At checkpoint $k$, a causal controller may use the entire work and revisit
history to choose any nonempty subset
\begin{equation}
 \varnothing\neq B_k\subseteq\mathcal B(U_k)
 \label{eq:adaptive-subset}
\end{equation}
when $\mathcal B(U_k)$ is nonempty, and set
$U_{k+1}:=U_k\cup B_k$.  It may change its ranking, relaxation, batching, or
proposal policy arbitrarily between checkpoints.  After every expansion,
replace the current iterate by the canonical point $x[U_{k+1}]$.

Then:
\begin{enumerate}[label=(\roman*),leftmargin=*]
 \item every $U_k$ lies in the true support $S^\star(\rho)$;
 \item every newly added coordinate is positive at the next checkpoint;
 \item the checkpoint $x[U_k]$ depends only on $U_k$, so every earlier
 propagation and policy choice is absorbed exactly;
 \item the process terminates after at most
 $|S^\star(\rho)|-|U_0|$ expansions; and
 \item at termination, $x[U_K]=x^\star(\rho)$.
\end{enumerate}
Thus policy switching at exact checkpoints is correct on one irreversible
state and needs no cross-policy state-transfer assumption.
\end{theorem}

\begin{proof}
The matrix $Q$ is a symmetric positive-definite Stieltjes matrix, so every
principal inverse and every principal Schur-complement inverse is
entrywise nonnegative.  At $U_0$, the positive seed load gives
$x[U_0]_o>0$.  It also forces $o\in S^\star(\rho)$: otherwise the inactive
KKT inequality would contradict $(\ell_\rho)_o>0$.  Suppose
$x[U_k]_{U_k}>0$ and choose
$B_k$ as in \eqref{eq:adaptive-subset}.  Block elimination gives
\begin{equation}
 x[U_{k+1}]_{B_k}
 =
 \left(
  Q_{B_kB_k}
  -Q_{B_kU_k}Q_{U_kU_k}^{-1}Q_{U_kB_k}
 \right)^{-1}
 (\ell_\rho-Qx[U_k])_{B_k}>0.
 \label{eq:adaptive-schur-positive}
\end{equation}
The old coordinates receive
$-Q_{U_kU_k}^{-1}Q_{U_kB_k}x[U_{k+1}]_{B_k}\geq0$, so positivity persists.

For support containment, assume inductively $U_k\subseteq S^\star(\rho)$.
Order comparison gives
$x[U_k]_{U_k}\leq x^\star_{U_k}$.  If
$v\in\mathcal B(U_k)$ but $x_v^\star=0$, then the Stieltjes off-diagonal
signs and $x^\star\geq0$ imply
\[
 (\ell_\rho-Qx^\star)_v
 \geq(\ell_\rho-Qx[U_k])_v>0,
\]
contradicting the inactive RPPR KKT inequality.  Hence every boundary
violation lies in $S^\star(\rho)$.

The formula \eqref{eq:adaptive-canonical-point} is unique, proving absorption
and policy independence at a fixed $U_k$.  Each nonterminal checkpoint adds
at least one new true-support vertex, so the number of expansions is finite.
If $\mathcal B(U_K)$ is empty, the restricted equations give the active KKT
equalities.  On a boundary coordinate, the stopping test gives
$\ell_\rho-Qx[U_K]\leq0$; also $b_v=0$ and the Stieltjes signs give
$b_v-(Qx[U_K])_v\geq0$.  These are exactly the lower and upper inactive RPPR
KKT inequalities.  A nonseed coordinate outside
$U_K\cup\partial U_K$ has no edge to $U_K$, so $Qx[U_K]=b_v=0$ there and its
inactive KKT interval holds as well.  The seed already lies in $U_K$.
The global KKT conditions and uniqueness imply
$x[U_K]=x^\star(\rho)$.
\end{proof}

\begin{corollary}[Revisit-controlled no-reset switching]
\label{cor:no-reset-switching}
In \Cref{thm:absorption-safe-switch}, let any number of proposal or
propagation policies run between checkpoints, but charge all of their work
to the bank of \Cref{thm:revisit-bank}.  Require charged row operations to
remain inside the current certified region together with coordinates already
admitted by the exact boundary certificate.  If an operation would overdraw
the bank, settle that certified region and switch policy at the canonical
checkpoint.  At every nonterminal checkpoint, still admit a nonempty subset
of the exact boundary violations as in \eqref{eq:adaptive-subset}.
Correctness is independent of the switch sequence, and the work obeys
\eqref{eq:banked-propagate-work} whenever the stated settlement-cost bound
holds.
\end{corollary}

\begin{proof}
The bank supplies the per-epoch propagation bound.  Exact settlement absorbs
the policy history, while \Cref{thm:absorption-safe-switch} supplies support
containment and correctness.  Apply
\Cref{cor:banked-propagate-settle}.
\end{proof}

This is a genuine no-reset switch theorem, but not yet a best-arm competitive
one.  It proves that revisit-triggered switching is state-compatible and
work-capped.  Matching the fastest arm still requires bounding
$\mathfrak R_{\rm set}$, maintaining incremental Schur state, or proving that
pre-switch work is useful to the arm chosen next.

\subsection{Why absorption alone cannot give a work oracle}

The obstruction occurs inside the exact RPPR gate itself.  It is therefore
unsafe to infer a competitive ratio merely from canonical settlement,
monotone support growth, or persistence of unadmitted violations.

\begin{theorem}[RPPR heavy-early obstruction on a tree]
\label{prop:batch-order-obstruction}
Fix $\alpha\in(0,1)$.  For every integer $L\geq1$, there are a finite simple
unweighted tree, a single seed $o$, and $\rho>0$ for which the exact RPPR
boundary gate of \Cref{thm:absorption-safe-switch} has two legal batch traces
with the same terminal region and terminal canonical point, but
\begin{equation}
 \frac{\mathfrak R_{\rm set}^{\rm early}}
      {\mathfrak R_{\rm set}^{\rm late}}
 \geq \frac{L+1}{5}.
 \label{eq:rppr-heavy-early-ratio}
\end{equation}
Every batch before a possible common final batch is a singleton.  In
particular, exact RPPR absorption, true-support containment, and persistent
boundary violations do not imply a constant best-order settled-work bound,
even on trees.
\end{theorem}

\begin{proof}
Put
\[
 a:=\frac{1+\alpha}{2},
 \qquad
 \beta:=\frac{1-\alpha}{2},
 \qquad
 t:=\frac{\beta}{a}=\frac{1-\alpha}{1+\alpha}.
\]
For an integer $M$, form a tree from the path
$o-z_1-\cdots-z_L$, a vertex $h$ adjacent to $o$, and $M-1$ pendant leaves
$y_1,\ldots,y_{M-1}$ adjacent to $h$.  Thus
\[
 d_o=2,
 \quad d_h=M,
 \quad d_{z_i}=2\ (i<L),
 \quad d_{z_L}=d_{y_j}=1.
\]
Write $P_k:=\{o,z_1,\ldots,z_k\}$, with $P_0=\{o\}$.

We first make every next path vertex a strict violation at its path-only
prefix.  For $0\leq k<L$, define
\[
 r_k(\rho):=(\ell_\rho-Qx[P_k])_{z_{k+1}}.
\]
The principal system on $P_k$ is independent of $M$.  At $\rho=0$ it is an
irreducible Stieltjes system with positive load at $o$, so
$x[P_k]_{z_k}>0$ and
\[
 r_k(0)=-Q_{z_{k+1},z_k}x[P_k]_{z_k}>0.
\]
Moreover, $r_k$ is affine in $\rho$:
\begin{equation}
 r_k(\rho)=r_k(0)-\rho\gamma_k,
 \qquad
 \gamma_k:=\alpha\left(
  \sqrt{d_{z_{k+1}}}
  -Q_{z_{k+1},P_k}Q_{P_kP_k}^{-1}
       D_{P_k}^{1/2}\one
 \right)>0.
 \label{eq:path-prefix-margin}
\end{equation}
The last inequality follows from the Stieltjes signs.  Hence
\begin{equation}
 \eta_L:=\frac12\min_{0\leq k<L}\frac{r_k(0)}{\gamma_k}>0
 \label{eq:path-prefix-eta}
\end{equation}
and $r_k(\rho)>0$ for every $k<L$ whenever $0<\rho<\eta_L$.

Now set
\begin{equation}
 \rho=\rho_M:=\frac{t}{4(M+t)}.
 \label{eq:heavy-rho}
\end{equation}
At the initial checkpoint $P_0$,
\[
 x_o[P_0]=\frac{\alpha(1-2\rho)}{a\sqrt2},
\]
and the violation residual at $h$ is
\begin{equation}
 (\ell_\rho-Qx[P_0])_h
 =\frac{\alpha}{\sqrt M}
  \left(\frac{t(1-2\rho)}2-\rho M\right).
 \label{eq:heavy-initial-margin}
\end{equation}
It is positive exactly when $\rho<t/[2(M+t)]$, hence at $\rho_M$.
Because $\rho_M\to0$, choose $M$ large enough that
\begin{equation}
 \rho_M<\eta_L,
 \qquad
 M\geq\frac32L^2+\frac{21}{2}L+6.
 \label{eq:heavy-M-choice}
\end{equation}

We shall use persistence inside the exact gate.  Suppose
$U'=U\cup B$ and every coordinate of $B$ violates at $x[U]$.  The Schur
formula \eqref{eq:adaptive-schur-positive} gives
$x[U']_B>0$ and $x[U']_U-x[U]_U\geq0$.  For every $v\notin U'$,
\begin{align}
 &(\ell_\rho-Qx[U'])_v-(\ell_\rho-Qx[U])_v \notag\\
 &\qquad={}-Q_{vU}(x[U']_U-x[U]_U)-Q_{vB}x[U']_B\geq0.
 \label{eq:rppr-violation-persistence}
\end{align}
Thus every unadmitted violation persists under every legal canonical
expansion.

Two traces are consequently legal.  The \emph{late} trace admits
$z_1,\ldots,z_L$ and then $h$: each $z_{k+1}$ violates on $P_k$ by
\eqref{eq:path-prefix-eta}, while $h$ persists.  For the \emph{early} trace,
first admit $h$.  At every later region $P_k\cup\{h\}$, comparison with the
legal expansion from $P_k$ gives
$x[P_k\cup\{h\}]_{P_k}\geq x[P_k]_{P_k}$.  Since $h$ is not adjacent to
$z_{k+1}$, the Stieltjes signs then give
\[
 (\ell_\rho-Qx[P_k\cup\{h\}])_{z_{k+1}}
 \geq r_k(\rho)>0.
\]
Thus the early trace may admit $h,z_1,\ldots,z_L$.  Both traces reach
\[
 U^\dagger:=P_L\cup\{h\},
\]
where absorption makes their canonical points identical.

Only the leaves $y_j$ remain outside $U^\dagger$.  They have equal degree,
equal shifted load, and the same sole active neighbor $h$, so their violation
residuals are tied.  If the common residual is nonpositive, both traces stop
at $U^\dagger$.  If it is positive, both admit all leaves in one common
final batch, after which the full graph is terminal.  Thus their suffix,
terminal point, and denominator $C(U_K)$ are identical.

It remains to compare the settled-charge numerators.  Here
\[
 C(P_k)=3+3k\quad(k<L),
 \qquad
 C(P_L)=3L+2,
 \qquad
 c(h)=M+1.
\]
Let $S$ denote the common optional final settlement charge.  Direct summation
gives
\begin{align}
 A_{\rm early}
 &=3+\sum_{k=0}^L\bigl(C(P_k)+M+1\bigr)+S \notag\\
 &=(L+1)M+\frac32L^2+\frac{11}{2}L+6+S,
 \label{eq:heavy-early}\\
 A_{\rm late}
 &=3+\sum_{k=1}^LC(P_k)+\bigl(C(P_L)+M+1\bigr)+S \notag\\
 &=M+\frac32L^2+\frac{15}{2}L+5+S.
 \label{eq:heavy-late}
\end{align}
The full graph has charge $3M+3L+1$, so $S$ is either zero or at most this
quantity.  Therefore
\[
 A_{\rm early}\geq(L+1)M,
 \qquad
 A_{\rm late}
 \leq4M+\frac32L^2+\frac{21}{2}L+6
 \leq5M,
\]
where the last inequality is \eqref{eq:heavy-M-choice}.  The common terminal
denominator cancels, proving \eqref{eq:rppr-heavy-early-ratio}.
\end{proof}

The expensive branch can therefore be admitted early and paid throughout a
long independent chain even though every addition is KKT-certified and both
orders end at the same exact solution.  A positive one-state oracle theorem
must use more than correctness and monotonicity: for example an
interference-monotone countdown, an explicit order rule whose charge can be
amortized, or incremental settlement state that does not repay the whole
region.

\begin{corollary}[No policy-blind accelerated-bank success theorem]
\label{cor:no-policy-blind-bank}
Fix $\alpha\in(0,1)$ and any finite bank factor $\Gamma$.  There is a legal
support-safe exact RPPR trace on a finite unweighted tree whose settlement
touches alone violate $W\leq\Gamma C(S)$ at some prefix.  Consequently
support safety, exact checkpoint absorption, and the RPPR KKT gate cannot by
themselves prove that a global bank of scale
$\widetilde O(1/\sqrt\alpha)$ never blocks.
\end{corollary}

\begin{proof}
Choose $L$ with $(L+1)/4>\Gamma$ in
\Cref{prop:batch-order-obstruction} and use the early trace.  Its cumulative
settlement work is at least $(L+1)M$.  Its terminal region is contained in
the full graph, whose charge is $3M+3L+1\leq4M$ under
\eqref{eq:heavy-M-choice}.  Hence its terminal realized ratio is greater
than $\Gamma$.  The bank invariant must therefore fail at or before that
checkpoint.
\end{proof}

\subsection{A mergeable countdown certificate}

There is, however, a sharp positive condition under which all policy work can
be committed to one state without branching or reset.  Let $\mathcal Z$ be a
space of certified states with a terminal subset.  A paid policy block maps
one certified state to another and has a declared positive charge.  In the
RPPR application, these may be the canonical checkpoints of
\Cref{thm:absorption-safe-switch}.

\begin{theorem}[Shared-state portfolio from mergeable countdowns]
\label{thm:mergeable-countdown}
Let $P_1,\ldots,P_K$ be policies acting on the same certified state.  Suppose
that for every $j$ there is a work-valued function
$\Phi_j:\mathcal Z\to\mathbb R_+$ with the following properties at every
nonterminal state $z$:
\begin{enumerate}[label=(\roman*),leftmargin=*]
 \item \emph{interference monotonicity:} every legal block of every policy,
 $z\mapsto z^+$, satisfies $\Phi_j(z^+)\leq\Phi_j(z)$;
 \item \emph{own-service progress:} every block of $P_j$ with charge $a$,
 including a block that reaches the terminal state, satisfies
 $\Phi_j(z^+)\leq\Phi_j(z)-a$; and
 \item $P_j$ always has a legal next block unless the common terminal
 certificate already holds.
\end{enumerate}
Run a work-fair scheduler with fixed positive shares
$\nu_1,\ldots,\nu_K$, $\sum_j\nu_j=1$, committing every scheduled block to
the same state.  Assume that at every prefix of total charge $C$, policy $j$
has received at least $\nu_j(C-\Delta)$ service.  Then it terminates with
\begin{equation}
 C_{\rm shared}
 \leq
 \min_{1\leq j\leq K}
 \frac{\Phi_j(z_0)}{\nu_j}+\Delta.
 \label{eq:mergeable-countdown-bound}
\end{equation}
In particular, uniform shares give
\begin{equation}
 C_{\rm shared}
 \leq K\min_j\Phi_j(z_0)+\Delta.
 \label{eq:mergeable-uniform}
\end{equation}
If $C_j$ is the standalone work of $P_j$ and the countdown is
$\kappa$-tight, meaning $\Phi_j(z_0)\leq\kappa C_j$ for every $j$, then the
shared irreversible run is $K\kappa$-competitive with the best arm, up to
$\Delta$.
\end{theorem}

\begin{proof}
Fix $j$.  Blocks of other policies never increase $\Phi_j$.  Until
and including termination, every unit of service given to $P_j$ decreases
$\Phi_j$ by at least one unit.  Because $\Phi_j$ is nonnegative, the common
certificate must therefore hold by the time $P_j$ receives
$\Phi_j(z_0)$ service.  Work
fairness gives that amount of service by total charge at most
$\Phi_j(z_0)/\nu_j+\Delta$.  Minimize over $j$.  The last two conclusions are
immediate.
\end{proof}

Unlike the branchable portfolio, this theorem has only one evolving solution
state: every block from every arm is retained.  Unlike a restart portfolio,
no completed block is discarded.  The price is precisely
interference monotonicity.  The batch-order obstruction shows why that
property cannot be omitted from a settlement-volume countdown.

\subsection{Activation tokens: a tight mergeable countdown}

The abstract condition has a concrete realization when transport work is
represented by irreversible common-state events rather than by a smooth
numerical error.

\begin{lemma}[Irreversible activation-token countdown]
\label{lem:activation-token-countdown}
For policy $P_j$, let $T_j$ be a finite token set with positive weights
$w_j(t)$.  Suppose every certified shared state $z$ records a consumed set
$H_j(z)\subseteq T_j$ such that:
\begin{enumerate}[label=(\roman*),leftmargin=*]
 \item every legal block $z\mapsto z^+$ satisfies
 $H_j(z)\subseteq H_j(z^+)$;
 \item every nonterminal $P_j$ block of charge $a$ consumes new tokens
 $N=H_j(z^+)\setminus H_j(z)$ with
 $a\leq\sum_{t\in N}w_j(t)$;
 \item $P_j$ has a legal next block until the common terminal certificate;
 and
 \item
 $\sum_{t\in T_j\setminus H_j(z_0)}w_j(t)\leq\kappa_j C_j$,
 where $C_j$ is its standalone work from $z_0$.
\end{enumerate}
Then
\begin{equation}
 \Phi_j(z):=\sum_{t\in T_j\setminus H_j(z)}w_j(t)
 \label{eq:activation-token-potential}
\end{equation}
is an interference-monotone, $\kappa_j$-tight mergeable countdown for
\Cref{thm:mergeable-countdown}.
\end{lemma}

\begin{proof}
Monotonicity of $H_j$ makes $\Phi_j$ nonincreasing under every legal block.
For an own block,
\[
 \Phi_j(z)-\Phi_j(z^+)
 =\sum_{t\in H_j(z^+)\setminus H_j(z)}w_j(t)\geq a.
\]
The last hypothesis is exactly $\kappa_j$-tightness.
\end{proof}

The important restriction is that these are events in the common certified
state.  Tokens for an arm-private simulated trajectory would merely hide a
branch and would not establish interference monotonicity.

\begin{theorem}[A $\kappa=1$ shared-state countdown on endpoint paths]
\label{thm:path-token-countdown}
Consider single-seed RPPR on an endpoint-seeded path
$P_N=(v_0,\ldots,v_N)$, and suppose
\[
 S^\star(\rho)=U_J:=\{v_0,\ldots,v_J\}.
\]
Require the common checkpoint representation to store, for every admitted
prefix $U_k$, the exact forward Schur records described below.  Every legal
support expansion, regardless of which arm requests it, appends the unique
new record; support-preserving blocks leave the records unchanged.  Once the
terminal prefix is certified, the common verifier enters a terminalization
phase: reverse-recovery blocks are legal, while numerical policy updates that
could overwrite recovered output are not.

Then the exact path-Schur arm has an irreversible activation-token countdown
with $\kappa=1$.  Its declared standalone work and initial countdown are
\begin{equation}
 C_{\rm path}=\Phi_{\rm path}(z_0)\leq2C(S^\star(\rho)),
 \label{eq:path-token-work}
\end{equation}
up to deletion of any seed-initialization token already paid at $z_0$.
Consequently
\begin{equation}
 C_{\rm path}\leq\frac4\rho,
 \label{eq:path-token-rppr}
\end{equation}
and a uniform work-fair shared-state portfolio containing this arm terminates
on the promised path class in at most
$2K C(S^\star)+\Delta\leq4K/\rho+\Delta$ declared work.  No reset or
arm-private numerical iterate is required.
\end{theorem}

\begin{proof}
Write
\[
 a_i:=Q_{v_i v_i},
 \qquad
 \beta_i:=-Q_{v_{i-1}v_i}>0\quad(i\geq1),
 \qquad
 \lambda_i:=(\ell_\rho)_{v_i}.
\]
The common representation stores the forward elimination records
\begin{align}
 \delta_0&=a_0,& \eta_0&=\lambda_0,\notag\\
 \delta_i&=a_i-\frac{\beta_i^2}{\delta_{i-1}},&
 \eta_i&=\lambda_i+\frac{\beta_i\eta_{i-1}}{\delta_{i-1}}.
 \label{eq:path-common-schur-record}
\end{align}
Positive definiteness gives $\delta_i>0$.  At prefix $U_k$, the unique
boundary residual has the sign of
$\eta_{k+1}=\lambda_{k+1}+\beta_{k+1}\eta_k/\delta_k$.
Thus it is positive for $k<J$ and nonpositive at $k=J$ unless $J=N$.
The next forward block is therefore always legal until the exact terminal
prefix is found.  The terminal solution is then recovered once in reverse:
\begin{equation}
 x_{v_J}=\frac{\eta_J}{\delta_J},
 \qquad
 x_{v_i}=\frac{\eta_i+\beta_{i+1}x_{v_{i+1}}}{\delta_i}
 \quad(i=J-1,\ldots,0).
 \label{eq:path-common-backsolve}
\end{equation}

Let $w_i:=c(v_i)=1+d_{v_i}$.  Give every $v_i\in S^\star$ one forward
token $f_i$ and one reverse token $b_i$, both of weight $w_i$.  The forward
token is consumed when $v_i$ is admitted or initialized and its record is
appended; the reverse token is consumed when
\eqref{eq:path-common-backsolve} recovers that coordinate.  If the current
prefix is $U_k$ and $H$ is the reverse-recovered set, the remaining-token
countdown is
\begin{equation}
 \Phi_{\rm path}(U_k,H)
 =\sum_{i=k+1}^{J}w_i
  +\sum_{v_i\in S^\star\setminus H}w_i,
 \label{eq:path-token-potential}
\end{equation}
with the already initialized seed token handled identically on both sides.
Any compatible block either leaves this quantity unchanged, advances the
prefix, or recovers output.  A path-arm forward or reverse block has declared
charge exactly the token it consumes, so
\Cref{lem:activation-token-countdown} applies with $\kappa=1$ and initial
value at most $2C(S^\star)$.

The recurrence and backsolve use constant arithmetic per path vertex; the
declared charge dominates this work and its adjacency access by an absolute
factor.  Finally,
$C(S^\star)\leq2\vol(S^\star)\leq2/\rho$, proving
\eqref{eq:path-token-rppr}.  Apply
\Cref{thm:mergeable-countdown} with uniform shares for the portfolio bound.
\end{proof}

This theorem is the clean positive answer for serializable fronts: preserve
an activation-once transport representation in the common state, and charge
forward discovery and terminal recovery exactly once.  The next result shows
that the first genuine branch is still serializable if the common state stores
an implicit affine response rather than eagerly materializing all current
coordinate values.

\begin{theorem}[Three-arm common-state countdown]
\label{thm:three-arm-spider-token-countdown}
Let $Y(N_1,N_2,N_3)$ be the unweighted tree with center $o$ and three
internally disjoint arms
\begin{equation*}
 o-v_{a,1}-\cdots-v_{a,N_a},
 \qquad a\in\{1,2,3\},\qquad N_a\geq1.
\end{equation*}
Use the single seed $s=e_o$, fix $\alpha\in(0,1)$ and
$\rho\in(0,1/3)$, and run the exact canonical RPPR boundary gate of
\Cref{thm:absorption-safe-switch}.  A legal certified block may admit any
nonempty subset of the currently positive boundary tips.  The common state
stores the actual three admitted prefixes, the affine transfer records below,
and a terminal output phase; it stores no arm-private support or numerical
trajectory.

In exact real-cell arithmetic, there is an exact response arm such that every
legal certified block order, including orders committed by other policies,
preserves its irreversible activation-token countdown with $\kappa=1$.
Writing $S^\star:=S^\star(\rho)$ and
$C(S^\star)=\sum_{v\in S^\star}(1+d_v)$, its resource coordinates obey
\begin{align}
 \mathcal C_{\rm adj}
 &\leq \vol(S^\star)+6,
 &
 \mathcal C_{\rm resp}
 &=O(\card{S^\star}),
 \notag\\
 \mathcal C_{\rm set/rec}
 &=O(\card{S^\star}),
 &
 \mathcal C_{\rm ctl}
 &=O(\card{S^\star}),
 \label{eq:spider-resource-ledger}\\
 \mathcal C_{\rm state\text{-}write}
 &=O(\card{S^\star}),
 &
 \mathcal M_{\rm state}
 &=O(\card{S^\star}),
 \qquad
 \mathcal C_{\rm out}=\card{S^\star}.
 \notag
\end{align}
Here response work includes every forward record and exact checkpoint update;
settlement/recovery is the one terminal materialization pass, not an
intermediate full-vector solve; control includes all three tip tests after
each committed expansion; state writes and peak resident cells are separate;
and output counts exact listed coordinates.  There is no ambient-graph
preprocessing.  For a fixed cell-operation convention, the arm's declared
standalone work $C_Y$ and initial token mass are equal and satisfy
\begin{equation}
 \Phi_Y(z_0)=C_Y=O(C(S^\star))=O(1/\rho),
 \label{eq:spider-token-work}
\end{equation}
after deleting initialization tokens already paid at $z_0$.  Therefore a
uniform work-fair common-state portfolio containing this arm finishes on this
three-arm family in at most $K C_Y+\Delta$ declared work.

The statement is structural.  It does not cover two branching vertices,
arbitrary trees, cycles, finite precision, or bit complexity, and it does not
assert a conversion from $\rho$ to $\epsppr$.
\end{theorem}

\begin{proof}
Put
\begin{equation*}
 \lambda_v:=(\ell_\rho)_v,
 \qquad
 \sigma_v:=Q_{vv},
 \qquad
 \beta_{a,i}:=-Q_{v_{a,i-1},v_{a,i}}>0,
 \qquad v_{a,0}:=o.
\end{equation*}
The condition $\rho<1/3$ is exactly
$\lambda_o=\alpha(1-3\rho)/\sqrt3>0$, so the seed initialization required by
\Cref{thm:absorption-safe-switch} is valid.  Every region reached from $o$ is
the union of three arm prefixes.  Since the exact gate terminates at the true
support, write
\begin{equation}
 S^\star
 =\{o\}\cup
  \bigcup_{a=1}^3\{v_{a,1},\ldots,v_{a,J_a}\},
 \label{eq:spider-support-prefixes}
\end{equation}
for some $0\leq J_a\leq N_a$.

We first give the common exact response.  Introduce a harmless positive ghost
coupling
\begin{equation*}
 \widehat\beta_{a,i+1}
 :=\begin{cases}
     \beta_{a,i+1},&i<N_a,\\
     1,&i=N_a,
   \end{cases}
\end{equation*}
and the affine transfer cell
\begin{equation}
 M_{a,i}
 :=
 \begin{pmatrix}
  \sigma_{v_{a,i}}/\widehat\beta_{a,i+1}
    &-\beta_{a,i}/\widehat\beta_{a,i+1}
    &-\lambda_{v_{a,i}}/\widehat\beta_{a,i+1}\\
  1&0&0\\
  0&0&1
 \end{pmatrix}.
 \label{eq:spider-transfer-cell}
\end{equation}
For an admitted prefix of length $k\geq1$, set
\begin{equation}
 P_{a,k}:=M_{a,k}\cdots M_{a,1}
 =
 \begin{pmatrix}
  A_{a,k}&B_{a,k}&C_{a,k}\\
  D_{a,k}&E_{a,k}&F_{a,k}\\
  0&0&1
 \end{pmatrix}.
 \label{eq:spider-transfer-product}
\end{equation}
The arm equations say exactly that
\begin{equation*}
 \begin{pmatrix}x_{v_{a,k+1}}\\x_{v_{a,k}}\\1\end{pmatrix}
 =P_{a,k}
  \begin{pmatrix}x_{v_{a,1}}\\x_o\\1\end{pmatrix},
\end{equation*}
where the current restricted endpoint condition is the ghost value
$x_{v_{a,k+1}}=0$.  The coefficient $A_{a,k}$ is the positive determinant of
the arm-prefix principal matrix divided by
$\prod_{i=1}^k\widehat\beta_{a,i+1}$, so it is nonzero.  Consequently
\begin{align}
 p_{a,k}&:=-B_{a,k}/A_{a,k},
 &q_{a,k}&:=-C_{a,k}/A_{a,k},\notag\\
 r_{a,k}&:=D_{a,k}p_{a,k}+E_{a,k},
 &s_{a,k}&:=D_{a,k}q_{a,k}+F_{a,k}
 \label{eq:spider-affine-arm-response}
\end{align}
give the exact affine readouts
\begin{equation*}
 x_{v_{a,1}}=p_{a,k}x_o+q_{a,k},
 \qquad
 x_{v_{a,k}}=r_{a,k}x_o+s_{a,k}.
\end{equation*}
For $k=0$, use $p_{a,0}=q_{a,0}=0$ and regard the arm endpoint as $o$, so
$r_{a,0}=1$ and $s_{a,0}=0$.

At the common prefix vector $k=(k_1,k_2,k_3)$, store only the two root
aggregates
\begin{align}
 \Delta(k)
 &:=Q_{oo}-\sum_{a:k_a>0}\beta_{a,1}p_{a,k_a},\notag\\
 \eta(k)
 &:=\lambda_o+\sum_{a:k_a>0}\beta_{a,1}q_{a,k_a}.
 \label{eq:spider-root-aggregates}
\end{align}
Here $\Delta(k)>0$ is the root Schur complement, and the canonical root value
is exactly
\begin{equation}
 x[U(k)]_o=\eta(k)/\Delta(k).
 \label{eq:spider-root-readout}
\end{equation}
If $k_a<N_a$, the exact KKT violation at the next tip is
\begin{equation}
 g_a(k)
 :=(\ell_\rho-Qx[U(k)])_{v_{a,k_a+1}}
 =\lambda_{v_{a,k_a+1}}
  +\beta_{a,k_a+1}
    \bigl(r_{a,k_a}x[U(k)]_o+s_{a,k_a}\bigr).
 \label{eq:spider-tip-readout}
\end{equation}
Thus all current boundary certificates are obtained by three constant-size
readouts.  No stored value on an untouched arm is rekeyed when the root value
changes.

Appending $v_{a,k_a+1}$ left-multiplies one product,
$P_{a,k_a+1}=M_{a,k_a+1}P_{a,k_a}$, replaces one pair $(p,q)$ in
\cref{eq:spider-root-aggregates}, and reevaluates
\cref{eq:spider-root-readout,eq:spider-tip-readout}.  This is constant exact
arithmetic and a constant number of cell writes.  A legal batch contains at
most one tip from each arm.  It may be processed in any order: every tip was
positive before the batch, and violation persistence
\eqref{eq:rppr-violation-persistence} keeps the unprocessed tips positive.
The final products and the two root sums are independent of that
sequentialization.  Hence the representation follows the actual committed
common prefixes under every legal certified batch order.

Once all three readouts are nonpositive or their arms are exhausted, the
exact KKT gate has certified the terminal support.  The verifier then forbids
support-changing numerical blocks and enters an irreversible output phase.
Compute the root once; on each nonempty arm compute
$x_{v_{a,1}}=p_{a,J_a}x_o+q_{a,J_a}$ and apply the stored cells
\cref{eq:spider-transfer-cell} outward.  This recovers and writes each exact
coordinate once.  The three-tip check that discovers terminality is always
part of the preceding initialization or append block; that block also writes
the terminal-phase bit.  There is therefore no paid, tokenless terminal-check
block.

We now audit resources.  Every admitted vertex is scanned once.  In addition,
at most one terminal exterior tip per arm is exposed, and each has degree at
most two.  This proves the adjacency bound in
\cref{eq:spider-resource-ledger}.  Every append performs one constant-size
product update, one root-sum update, and at most three tip tests.  Every local
transfer cell is written once and retained until recovery, and recovery and
output touch each support vertex once.  These facts prove all remaining
coordinates in \cref{eq:spider-resource-ledger}.  In particular, the ledger
does not hide an intermediate materialized canonical vector.  This is an
exact-cell count only; coefficient bit lengths and numerical stability are
outside the claim.

For completeness, define the tokens explicitly.  Let
\begin{equation*}
 \mathcal X_Y
 :=S^\star\cup\bigl(\partial S^\star\cap
       \{v_{a,i}:a\in\{1,2,3\},\ i\geq1\}\bigr).
\end{equation*}
The token set contains an exposure token $e_v$ for every $v\in\mathcal X_Y$,
a forward response token $f_v$ for every $v\in S^\star$, and a terminal
recovery/output token $b_v$ for every $v\in S^\star$.  Fix the implementation's
cell-operation templates: an exposure executes its full adjacency scan and
boundary-record write, a forward append executes the fixed $3$-by-$3$
product/root template and tests all three tip slots (including an exhaustion
flag), and a recovery executes its fixed state/output template.  Give
$e_v,f_v,b_v$ exactly the charges of those respective templates.  Thus these
are the arm's actual declared operations, not padded analytical credits.  Call
the resulting token set $T_Y$; its remaining total from $z_0$ is exactly the
standalone declared charge $C_Y$.

The consumed set $H_Y(z)$ is defined by events in the common state: first
exposure of a label, committed admission and response append, and irreversible
terminal recovery/output.  A support-preserving block leaves it unchanged;
every certified expansion or terminal block only adds events.  An own forward
block chooses a positive tip from \cref{eq:spider-tip-readout}, consumes its
new $f_v$ and any newly exposed $e_v$ tokens, and charges no more than their
weight.  An own terminal block consumes a new $b_v$ and likewise charges no
more than its weight.  The arm always has one of these legal blocks until the
full exact output certificate holds.  Tokens consumed by another policy are
consumed in the same committed prefixes and can only help; no simulated
private order appears in $H_Y$.

Therefore
\begin{equation*}
 \Phi_Y(z):=\sum_{t\in T_Y\setminus H_Y(z)}w(t)
\end{equation*}
satisfies \Cref{lem:activation-token-countdown}.  By the definition of the
declared cell charges, its initial mass equals the standalone declared work,
so $\kappa=1$.  Finally $\card{\mathcal X_Y}\leq\card{S^\star}+3$,
$C(S^\star)\leq2\vol(S^\star)\leq2/\rho$, and $\rho<1/3$ absorbs the constant
exterior-tip term.  This proves \cref{eq:spider-token-work}; the portfolio
bound is \Cref{thm:mergeable-countdown}.
\end{proof}

The minimal degree-three branch therefore does not furnish a legal-order
counterexample.  Its three response directions meet only through one scalar
root value, so one fixed-size affine product per arm is enough.  The next
theorem tests the first larger interface rather than extrapolating this scalar
argument: two adjacent branching vertices require a rank-two core.

\begin{theorem}[Double-Y rank-two common-state countdown]
\label{thm:double-y-two-core-token-countdown}
Let $\mathsf{DY}(N_{o,1},N_{o,2};N_{h,1},N_{h,2})$ be the unweighted tree
with adjacent degree-three vertices $o$ and $h$ and four pendant arms
\begin{equation*}
 u-v_{u,a,1}-\cdots-v_{u,a,N_{u,a}},
 \qquad u\in\{o,h\},\quad a\in\{1,2\},\quad N_{u,a}\geq1.
\end{equation*}
Use the single seed $s=e_o$, fix $\alpha\in(0,1)$ and
$\rho\in(0,1/3)$, and run the exact canonical RPPR gate of
\Cref{thm:absorption-safe-switch}.  Require every committed support expansion,
including one requested by another policy, to maintain the common response
state below and to include that update in its declared charge.  Support-
preserving blocks may not overwrite these records, and the terminal phase may
not overwrite recovered coordinates.

In exact real-cell arithmetic, one affine transfer product per actual pendant
prefix and a core Schur interface of dimension at most two give an exact
response arm for every legal certified boundary-batch order.  Its irreversible
activation-token countdown remains $\kappa=1$.  Before $h$ is admitted the
interface dimension is one; after $h$ is admitted its core matrix is positive
definite of rank two.  In particular, writing
\begin{equation}
 \zeta:=\frac{1-\alpha}{1+\alpha},
 \qquad
 \rho_{\rm link}(\alpha):=\frac{\zeta}{3(3+\zeta)},
 \label{eq:double-y-link-threshold}
\end{equation}
every legal order enters the rank-two phase whenever
$0<\rho<\rho_{\rm link}(\alpha)$.

For arbitrary $0<\rho<1/3$, with $S^\star:=S^\star(\rho)$ and
$C(S^\star)=\sum_{v\in S^\star}(1+d_v)$, the implementation has the separately
charged resource coordinates
\begin{align}
 \mathcal C_{\rm adj}
 &\leq \vol(S^\star)+8,
 &
 \mathcal C_{\rm gate\text{-}ctl}
 &=O(\card{S^\star}),
 \notag\\
 \mathcal C_{\rm resp\text{-}build}
 &=O(\card{S^\star}),
 &
 \mathcal C_{\rm resp\text{-}upd}
 &=O(\card{S^\star}),
 \notag\\
 \mathcal C_{\rm resp\text{-}qry}
 &=O(\card{S^\star}),
 &
 \mathcal C_{\rm state\text{-}write}
 &=O(\card{S^\star}),
 \label{eq:double-y-resource-ledger}\\
 \mathcal M_{\rm persistent}
 &=O(\card{S^\star}),
 &
 \mathcal M_{\rm transient}
 &=O(1),
 \notag\\
 \mathcal C_{\rm rec}
 &=O(\card{S^\star}),
 &
 \mathcal C_{\rm val}
 &=O(\card{S^\star}),
 \qquad
 \mathcal C_{\rm out}=\card{S^\star}.
 \notag
\end{align}
There is no ambient preprocessing.  Adjacency includes closure discovery from
the seed, gate/control includes every fixed-slot membership and positivity
test, response construction/update/query are distinct coordinates, validation
reads the stored rows after recovery, and output counts exact listed
coordinates.  Reading and initializing the single seed costs $O(1)$ and is
included in the paid initial state.  If $C_{\rm DY}$ is the sum of these declared cell-operation
templates, then, after removing initialization events already paid at the
initial common state $z_0$,
\begin{equation}
 \Phi_{\rm DY}(z_0)=C_{\rm DY}
 =O(C(S^\star))=O(1/\rho).
 \label{eq:double-y-token-work}
\end{equation}
Thus a uniform work-fair portfolio containing this arm terminates on the
double-Y family within $K C_{\rm DY}+\Delta$ declared work.

The theorem is an exact RPPR result on this promised tree family.  It does not
cover a longer backbone of branching vertices, arbitrary trees, cycles,
finite precision, bit complexity, or an RPPR-to-PPR accuracy conversion.
\end{theorem}

\begin{proof}
Put $\lambda_v:=(\ell_\rho)_v$ and $\beta_0:=-Q_{oh}>0$.  Since
$d_o=d_h=3$,
\begin{equation*}
 \lambda_o=\frac{\alpha(1-3\rho)}{\sqrt3}>0,
 \qquad
 \lambda_h=-\alpha\rho\sqrt3,
 \qquad
 \beta_0=\frac{1-\alpha}{6}.
\end{equation*}
Thus the seed initialization satisfies
\Cref{thm:absorption-safe-switch}.  Every reached region is connected.  Before
$h$ is admitted it consists of $o$ and two prefixes on the $o$-arms; afterward
it consists of $o,h$ and four pendant prefixes.  The true support has the same
form because the exact gate terminates at $S^\star$.

For each admitted pendant prefix, use the ghost-coupled transfer cells and
products of
\cref{eq:spider-transfer-cell,eq:spider-transfer-product}, with the relevant
branch vertex $u\in\{o,h\}$ in place of the spider root.  Denote the resulting
affine readouts by
\begin{equation*}
 x_{v_{u,a,1}}=p_{u,a,k}x_u+q_{u,a,k},
 \qquad
 x_{v_{u,a,k}}=r_{u,a,k}x_u+s_{u,a,k},
\end{equation*}
and use $p=q=0$, $r=1$, $s=0$ at an empty prefix.  Positive definiteness of
every arm-prefix principal matrix makes each division in these readouts valid.
For the two branch vertices store
\begin{align}
 \Delta_u(k)
 &:=Q_{uu}-\sum_{a:k_{u,a}>0}
      \beta_{u,a,1}p_{u,a,k_{u,a}},
 &
 \eta_u(k)
 &:=\lambda_u+\sum_{a:k_{u,a}>0}
      \beta_{u,a,1}q_{u,a,k_{u,a}},
 \label{eq:double-y-branch-aggregates}
\end{align}
where $\beta_{u,a,i}:=-Q_{v_{u,a,i-1},v_{u,a,i}}$ and
$v_{u,a,0}:=u$.

While $h$ is inactive, the exact core and central boundary readouts are
\begin{equation}
 x_o=\frac{\eta_o}{\Delta_o},
 \qquad
 g_h:=\lambda_h+\beta_0x_o.
 \label{eq:double-y-scalar-core}
\end{equation}
Once $h$ is admitted, eliminating the four current pendant prefixes leaves
\begin{equation}
 \mathcal K(k):=
 \begin{pmatrix}
  \Delta_o(k)&-\beta_0\\
  -\beta_0&\Delta_h(k)
 \end{pmatrix},
 \qquad
 \mathcal K(k)\binom{x_o}{x_h}=\binom{\eta_o(k)}{\eta_h(k)}.
 \label{eq:double-y-rank-two-core}
\end{equation}
This is a principal Schur complement and hence is positive definite and rank
two.  With
$\Theta(k):=\Delta_o(k)\Delta_h(k)-\beta_0^2>0$, its exact readout is
\begin{equation}
 x_o=\frac{\Delta_h\eta_o+\beta_0\eta_h}{\Theta},
 \qquad
 x_h=\frac{\beta_0\eta_o+\Delta_o\eta_h}{\Theta}.
 \label{eq:double-y-core-readout}
\end{equation}
For every active branch vertex $u$, the next pendant-tip violation is
\begin{equation}
 g_{u,a}(k)
 =\lambda_{v_{u,a,k_{u,a}+1}}
  +\beta_{u,a,k_{u,a}+1}
   \bigl(r_{u,a,k_{u,a}}x_u+s_{u,a,k_{u,a}}\bigr).
 \label{eq:double-y-tip-readout}
\end{equation}
There are three live slots before $h$ is admitted---two $o$-tips and $h$---and
at most four afterward.  The implementation computes these constant-size
readouts when needed; it stores no materialized boundary keys.

The displayed threshold genuinely reaches both branch vertices.  At the
initial region $\{o\}$, $Q_{oo}=(1+\alpha)/2$.  Equation
\eqref{eq:double-y-scalar-core} and $\beta_0=(1-\alpha)/6$ give
\begin{equation*}
 g_h(\{o\})
 =\frac{\alpha}{\sqrt3}
   \left[-3\rho+\frac{\zeta}{3}(1-3\rho)\right]>0
 \quad\Longleftrightarrow\quad
 \rho<\frac{\zeta}{3(3+\zeta)}.
\end{equation*}
Hence $h\in S^\star$ in this range.  If another positive tip is selected
first, violation persistence
\eqref{eq:rppr-violation-persistence} keeps $h$ positive; finiteness of the
tree forces its eventual admission under every legal order.  For larger
$\rho<1/3$, the same representation remains valid whether or not $h$ belongs
to $S^\star$; if it does not, the core stays scalar.

Appending a pendant tip left-multiplies only its own transfer product, changes
one pair in \eqref{eq:double-y-branch-aggregates}, and reevaluates the scalar or
rank-two core.  Admitting $h$ changes the active-core bit, initializes
$(\Delta_h,\eta_h)=(Q_{hh},\lambda_h)$, and exposes its two pendant-tip slots.
Every operation uses constant exact arithmetic and constant state writes.  A
legal batch contains at most one current tip from each active arm and may also
contain $h$ before the rank-two phase.  Sequentialize only those actually
committed vertices.  Every member was positive before the batch and remains
positive by \eqref{eq:rppr-violation-persistence}; the final prefix products,
branch sums, and core bit depend only on the committed support.  This is not a
private replay of a different order.

At most four exterior boundary rows are ever live at termination.  If $h$ is
active they are four pendant tips, each of degree at most two; if $h$ is
inactive they are at most two such tips and the degree-three vertex $h$.
Scanning each admitted or boundary row once therefore costs at most
$\vol(S^\star)+8$.  Every admission constructs one local transfer cell,
updates one product and one constant-size core, queries at most four slots, and
writes only permanent prefix/core records.  This proves all forward coordinates
of \eqref{eq:double-y-resource-ledger} and gives linear persistent state and
constant transient workspace.

The fixed-slot check in the initialization or last append that finds no
positive violation also writes the irreversible terminal-phase bit.  Recover
the one- or two-vertex core once and traverse each stored pendant product
outward once.  Then make one charged pass over the stored bounded-degree rows
to validate active positivity and equations and the terminal boundary
inequalities, and make one separate exact output pass.  Nonboundary inactive
vertices have no active neighbor and satisfy their KKT interval automatically.
These operations prove the recovery, validation, and output coordinates in
\eqref{eq:double-y-resource-ledger}; no row is rediscovered and no intermediate
canonical vector is materialized during the forward phase.

It remains to verify the countdown without analytical padding.  Let
\begin{equation*}
 \mathcal X_{\rm DY}:=S^\star\cup\partial S^\star.
\end{equation*}
Give every $v\in\mathcal X_{\rm DY}$ one exposure token with weight equal to
its full adjacency/row-record template.  Give every $v\in S^\star$ separate
forward-response, recovery, validation, and output tokens whose weights are
exactly the corresponding fixed cell-operation templates, including gate,
control, state-write, and core-query work where it occurs.  The common consumed
set records first row exposure, committed admission and response update,
irreversible recovery, completed validation, and exact output.  Events already
paid during initialization are removed from both the token set and
$C_{\rm DY}$.

Every compatible block only adds common events.  An own forward or terminal
block charges no more than the exact weights of the new tokens it consumes and
one is always legal until the full validated output certificate holds.  An
expansion performed by another policy invokes the same mandatory common
updater and consumes the same permanent events, so it can only decrease the
remaining mass; there is no catch-up replay.  Thus
\Cref{lem:activation-token-countdown} applies.  By construction the initial
mass is exactly the standalone declared work, proving $\kappa=1$.  Finally
$\card{\mathcal X_{\rm DY}}\leq\card{S^\star}+4$, all degrees are at most three,
and
$C(S^\star)\leq2\vol(S^\star)\leq2/\rho$.  This proves
\eqref{eq:double-y-token-work} and the portfolio bound.
\end{proof}

The adjacent second branch therefore increases the exact response interface
from one scalar to one fixed rank-two Schur core, but does not break the
activation-once countdown.  The next unresolved tree has a growing branch-core
backbone rather than this constant two-vertex interface.

\begin{proposition}[A growing branch core forces global rekeys in an eager tip array]
\label{prop:branch-caterpillar-eager-rekey-obstruction}
Fix $\alpha\in(0,1)$ and an integer $m\geq2$.  Let
$\mathsf{BC}_m$ be the unweighted branch caterpillar with branch path
$b_1-\cdots-b_m$, single seed $s=e_{b_1}$, and the following pendant arms:
$b_1-a_1-\cdots-a_m$ and $b_1-r_1$, one leaf $r_i$ at every
$b_i$ for $2\leq i<m$, and two leaves $r_m,r_{m+1}$ at $b_m$.
Thus every $b_i$ has degree three, both endpoint branches have two nonempty
pendant arms, and
\begin{equation}
 |V(\mathsf{BC}_m)|=3m+1,
 \qquad C(V(\mathsf{BC}_m))=9m+1.
 \label{eq:branch-caterpillar-size}
\end{equation}
For each fixed $m$ there is an explicit number
$\rho_{\rm cat}(m,\alpha)>0$, defined in
\eqref{eq:branch-caterpillar-reach-threshold} below.  For every
$0<\rho<\rho_{\rm cat}(m,\alpha)$, the exact canonical RPPR KKT gate with the
positive-subset policy permitted by \Cref{thm:absorption-safe-switch} may
follow the legal singleton order
\begin{equation}
 b_2,b_3,\ldots,b_m;\qquad
 a_1,a_2,\ldots,a_m;\qquad
 r_1,r_2,\ldots,r_{m+1}.
 \label{eq:branch-caterpillar-legal-order}
\end{equation}
In particular, the response core genuinely reaches dimension $m$ and the
true support is the whole tree.  This is not the canonical all-violations
batch rule: that rule may co-admit the deliberately deferred positive leaves.
No positive lower bound on $\rho_{\rm cat}(m,\alpha)$ uniform in $m$ is
claimed.  The threshold is an analysis-side reach condition, not a table
constructed or queried by the online execution.

Consider the named representation \emph{EagerTipKey}: at every canonical
checkpoint $U$ it retains a separately addressed exact-real cell
\begin{equation*}
 K_v=(\ell_\rho-Qx[U])_v
 \qquad(v\in\partial U),
\end{equation*}
and, before the next gate reply, it overwrites every such cell whose exact
value changed.  Shared lazy tags, affine formulas, sign-only flags, and
on-demand recomputation are not EagerTipKey states.  Along
\eqref{eq:branch-caterpillar-legal-order}, each of the $m$ long-arm admissions
changes all $m+1$ deferred leaf keys.  Hence EagerTipKey incurs at least
$m(m+1)$ old-key writes and cannot have an $O(C(S^\star))$ activation-once
countdown.

A literal sequential implementation that scans each row once, solves the
current tridiagonal branch response by a linear exact sweep at each
checkpoint, refreshes the eager array internally, emits one constant-size
singleton certificate per expansion and one terminal certificate, then
materializes, validates, and emits the exact solution once has the exhaustive
resource vector
\begin{equation}
 \begin{aligned}
 \mathcal C_{11}(\mathcal E_m^{\rm eager})
 :=\bigl(&C_{\rm adj},R_{\rm adj},R_{\rm int},C_{\rm pre},C_{\rm ctl},
 C_{\rm rec},C_{\rm resp},M_{\rm pers},M_{\rm tmp},C_{\rm mat},C_{\rm emit}
 \bigr)\\
 =\bigl(&\Theta(m),\Theta(m),3m+1,0,\Theta(m^2),0,
 \Theta(m^2),\Theta(m),O(m),\Theta(m),\Theta(m)\bigr).
 \end{aligned}
 \label{eq:branch-caterpillar-eager-eleven-vector}
\end{equation}
The $\Omega(m^2)$ eager-key work in $C_{\rm ctl}$ persists even if exact core
coordinates are supplied for free; the displayed $C_{\rm resp}$ is the
separate cost of the chosen repeated linear tridiagonal sweeps.  If the
external interface instead requires the complete positive-tip label list at
every checkpoint, then its repeated list itself adds
$C_{\rm emit}=\Theta(m^2)$.
Here the cached boundary-key array is a control data structure: indexing,
testing, and persistent key-cell overwrites belong to $C_{\rm ctl}$, whereas
the response arithmetic that computes the exact values belongs to
$C_{\rm resp}$.

This is only a representation-specific exact-cell obstruction.  It is not a
lower bound for implicit or lazy response states, kinetic/group reporters,
sign-only persistence records, arbitrary RPPR algorithms, or finite-precision
implementations.
\end{proposition}

\begin{proof}
Put $B_i:=\{b_1,\ldots,b_i\}$,
$A_t:=\{a_1,\ldots,a_t\}$, and $A_0:=\varnothing$.  For a connected
$U\ni b_1$ and a nonseed boundary vertex $v$, define
\begin{align}
 \Gamma_{U,v}
 &:=-Q_{vU}Q_{UU}^{-1}b_U,
 &
 H_{U,v}
 &:=\sqrt{d_v}-Q_{vU}Q_{UU}^{-1}D_U^{1/2}\one.
 \label{eq:branch-caterpillar-affine-demand}
\end{align}
The principal matrix $Q_{UU}$ is an irreducible Stieltjes matrix.  Its inverse
is strictly positive, $b_U$ is positive at $b_1$, and $v$ has an edge into
$U$.  Therefore $\Gamma_{U,v}>0$ and $H_{U,v}>0$.  Direct substitution of
$\ell_\rho=b-\alpha\rho D^{1/2}\one$ gives the exact affine demand
\begin{equation}
 (\ell_\rho-Qx[U])_v
 =\Gamma_{U,v}-\alpha\rho H_{U,v}.
 \label{eq:branch-caterpillar-demand-line}
\end{equation}

Let
\begin{equation*}
 \mathcal P_m:=
 \{(B_i,b_{i+1}):1\leq i<m\}
 \cup\{(B_m\cup A_t,a_{t+1}):0\leq t<m\}
 \cup\{(B_m,r_i):1\leq i\leq m+1\}
\end{equation*}
and set
\begin{equation}
 \rho_{\rm cat}(m,\alpha)
 :=\min\left\{
  \frac13,
  \min_{(U,v)\in\mathcal P_m}
  \frac{\Gamma_{U,v}}{\alpha H_{U,v}}
 \right\}>0.
 \label{eq:branch-caterpillar-reach-threshold}
\end{equation}
For $0<\rho<\rho_{\rm cat}(m,\alpha)$,
\eqref{eq:branch-caterpillar-demand-line} is strictly positive for every pair
in $\mathcal P_m$, while $\rho<1/3$ makes the degree-three seed load positive.
Consequently the backbone and then the long arm in
\eqref{eq:branch-caterpillar-legal-order} are legal singleton admissions.
All $r_i$ are already positive at $B_m$ and remain positive under every later
admission by violation persistence
\eqref{eq:rppr-violation-persistence}.  They may therefore be admitted in the
displayed singleton order.  The absorption theorem puts every admitted vertex
in $S^\star$; since the order admits every graph vertex,
$S^\star=V(\mathsf{BC}_m)$.  Eliminating the current pendant prefixes leaves
an SPD tridiagonal system on $b_1,\ldots,b_m$, so the active branch-core
dimension is exactly $m$ after the first phase.  The vertex and charge counts
in \eqref{eq:branch-caterpillar-size} follow from
$|E|=3m$ and $C(V)=|V|+2|E|$.

Fix $1\leq t\leq m$, let $U=B_m\cup A_{t-1}$, and append $v=a_t$.
Block elimination gives
\begin{equation}
 x[U\cup\{v\}]_U-x[U]_U
 =-Q_{UU}^{-1}Q_{Uv}\,x[U\cup\{v\}]_v.
 \label{eq:branch-caterpillar-dense-shift}
\end{equation}
The new coordinate is strictly positive.  The set $U$ is connected, so
$Q_{UU}^{-1}$ is strictly positive; moreover $-Q_{Uv}$ has one positive
entry at the parent of $v$.  Hence the vector in
\eqref{eq:branch-caterpillar-dense-shift} is strictly positive in every old
coordinate.  In particular, every branch value $x_{b_i}$ increases strictly.
For the deferred leaf attached to $b_i$,
\begin{equation*}
 K_{r_i}=(\ell_\rho)_{r_i}-Q_{r_i b_i}x_{b_i}
          =(\ell_\rho)_{r_i}+(-Q_{r_i b_i})x_{b_i},
\end{equation*}
with the same formula for $r_{m+1}$ at $b_m$.  Thus all $m+1$ leaf keys
change strictly after each of the $m$ long-arm admissions.  A literal eager
array has no shared indirection by definition, so it must perform at least
$m(m+1)$ distinct persistent-cell overwrites.

For the displayed implementation, first-exposure bounded-degree scans and
their sequential batches cost $\Theta(m)$.  There are $3m$ singleton
expansions after the initialized seed and one terminal interaction.  Eager
key testing, indexing, and overwriting cost $\Theta(m^2)$; the lower bound
above is matched by scanning the live array.  Re-solving a tridiagonal core of
length at most $m$ at each checkpoint costs $\Theta(m^2)$ response arithmetic,
separately from those writes.  Stored rows, core records, and keys use
$\Theta(m)$ persistent cells, and a linear sweep uses $O(m)$ scratch.  There
is no preprocessing or recurrence direction.  One terminal recovery,
stored-row validation, final vector materialization, all singleton replies,
and the exact $3m+1$-cell output are linear.  These charges prove
\eqref{eq:branch-caterpillar-eager-eleven-vector}.

Finally, the ordinary bounded-degree activation and exposure tokens consumed
by the first long-arm append have $O(1)$ weight, whereas that block already
forces $m+1$ old-key writes.  Pairwise append--key tokens can pay the block,
but the $m(m+1)$ such tokens have quadratic initial mass.  Thus the first
long-arm append is an explicit failure point for the linear activation-token
template, and any own-service countdown paying the entire eager trace has
quadratic initial mass by telescoping.
\end{proof}

\begin{remark}[The implicit transfer route remains open]
\label{rem:branch-caterpillar-implicit-transfer-comparator}
Eliminating each actual pendant prefix changes only the diagonal and load at
its attachment branch.  The remaining branch system is tridiagonal.  A
current core of length $t$ has equations
\begin{equation*}
 \Delta_i x_i-\beta_{i-1}x_{i-1}-\beta_i x_{i+1}=\eta_i,
 \qquad x_0=x_{t+1}=0,
\end{equation*}
where $\beta_0$ and the last $\beta_t$ may be any positive ghost couplings.
Thus the
constant-size cells
\begin{equation*}
 T_i=
 \begin{pmatrix}
  \Delta_i/\beta_i&-\beta_{i-1}/\beta_i&-\eta_i/\beta_i\\
  1&0&0\\0&0&1
 \end{pmatrix},
 \qquad
 \begin{pmatrix}x_{i+1}\\x_i\\1\end{pmatrix}
 =T_i\begin{pmatrix}x_i\\x_{i-1}\\1\end{pmatrix},
\end{equation*}
encode the exact response.  Positive definiteness makes the endpoint
coefficient used to solve $x_1$ from $x_{t+1}=0$ nonzero.  A balanced product
tree over the $T_i$ supports one attachment-cell update in
$O(\log(2+m))$ exact operations.  A branch append replaces the old ghost cell
and appends one new ghost cell, again in $O(\log(2+m))$ work.  The total
product determines $x_1$, and a prefix-product query then determines any named
$x_i$ and its attached tip demand in $O(\log(2+m))$ work.  The tree retains
$O(m)$ cells.

This represents the dense shift
\eqref{eq:branch-caterpillar-dense-shift} without rewriting every coordinate
or key.  With $q$ internally requested exact tip values, its response work is
$O((m+q)\log(2+m))$ plus $O(m)$ terminal recovery; every requested value must
still be charged, and an externally emitted scalar reply adds one output
cell.  Querying every live tip at every checkpoint of the witness has
$q=\Theta(m^2)$, and emitting every complete positive-tip list has quadratic
output by itself.

The point-query comparator alone is not a dynamic all-positive-tip reporter.
In particular, the proposition does not rule out lazy affine keys,
persistence flags for already positive tips, or a kinetic/group minimum
structure.  It only closes the literal eager exact-key route and keeps the
append-only tridiagonal response distinct from repeated core solves.  The
next two theorems give charged delta reporters for one explicit
backbone-first positive-subset policy and for the actual canonical
all-violations trace on a stricter fixed-family parameter range.  Arbitrary
pre-backbone positive-subset interleavings and the remaining canonical range
stay open.  Transfer-product conditioning and finite-precision stability are
not claimed.
\end{remark}

\begin{theorem}[A kinetic delta reporter for the backbone-first caterpillar]
\label{thm:branch-caterpillar-kinetic-delta-reporter}
Fix $\alpha\in(0,1)$ and $m\geq2$, use the branch seed $s=e_{b_1}$ on
$\mathsf{BC}_m$, and assume
\begin{equation*}
 0<\rho<\rho_{\rm cat}(m,\alpha)\leq\frac13.
\end{equation*}
Consider the following exact KKT positive-subset policy class
$\mathfrak P_m^{\rm bf}$.  Starting from $B_1=\{b_1\}$, its first $m-1$
committed batches are the singletons
$\{b_2\},\ldots,\{b_m\}$.  Once $B_m$ has been reached, every committed batch
may be an arbitrary nonempty subset of the currently known strict positive
boundary tips.  In particular, the side leaves may be interleaved arbitrarily
with the sequentially exposed long-arm frontier.  This is not the canonical
all-violations policy: during the backbone phase it deliberately defers any
positive off-backbone tip.

There is a named exact-real reporter, \emph{CaterpillarKineticDelta}, with the
following interface at every exact restricted checkpoint $U$ of every order in
$\mathfrak P_m^{\rm bf}$:
\begin{enumerate}[label=(\roman*),leftmargin=*]
 \item it retains the exact set
 \begin{equation*}
  \mathcal K(U):=
  \{v\in\partial U:(\ell_\rho-Qx[U])_v>0\};
 \end{equation*}
 \item it returns only the delta labels that became strict positive since the
 preceding checkpoint, and supports an exact membership query for every live
 tip; and
 \item it accepts a proposed batch only after checking that every proposed
 label is a live member of $\mathcal K(U)$, then returns one charged
 certificate reply for that committed batch.
\end{enumerate}
The reporter never re-emits a still-live positive label merely because its
exact demand changed.  Violation persistence is the proof carried by its
positive flag.  At initialization its delta is $\mathcal K(B_1)$.  The
displayed interface tests membership once for every label in the next
proposed committed batch.  If a caller requests $q_{\rm spec}$ additional or
repeated speculative membership tests, they add $q_{\rm spec}$ to
$C_{\rm ctl}$ and add the corresponding separately requested interaction and
reply cells to $R_{\rm int}$ and $C_{\rm emit}$; none is free in the vector
below.

It uses the balanced tridiagonal transfer state from
\Cref{rem:branch-caterpillar-implicit-transfer-comparator}, one exact kinetic
minimum heap, and $O(m)$ retained cells.  If $J$ is the number of committed
batches after seed initialization, then
\begin{equation*}
 2m-1\leq J\leq3m.
\end{equation*}
With one external delta/certificate interaction per committed batch and one
terminal return, its exhaustive resource vector is
\begin{equation}
 \begin{aligned}
 \mathcal C_{11}(\mathcal E_m^{\rm kin})
 :=\bigl(&C_{\rm adj},R_{\rm adj},R_{\rm int},C_{\rm pre},C_{\rm ctl},
 C_{\rm rec},C_{\rm resp},M_{\rm pers},M_{\rm tmp},C_{\rm mat},C_{\rm emit}
 \bigr)\\
 =\bigl(&\Theta(m),\Theta(m),J+1,0,
 O(m\log(2+m)),0,O(m\log(2+m)),\\
 &\Theta(m),O(m),\Theta(m),\Theta(m)\bigr).
 \end{aligned}
 \label{eq:branch-caterpillar-kinetic-eleven-vector}
\end{equation}
Consequently its additive exact-cell work is
\begin{equation}
 O(m\log(2+m))
 =O\!\left(C(S^\star)\log(2+C(S^\star))\right),
 \qquad C(S^\star)=9m+1.
 \label{eq:branch-caterpillar-kinetic-total}
\end{equation}

This is an output-delta theorem.  If the external task instead requests the
complete current positive-tip list at every checkpoint, then the prescribed
witness can require $\Theta(m^2)$ emitted labels, independently of the
reporter's internal work.  The theorem does not cover the canonical
all-violations policy, an arbitrary interleaving before $B_m$, another seed,
an $m$-uniform positive range of $\rho$, finite precision, bit complexity,
or transfer-product conditioning.  It supplies neither a $\kappa=1$ linear
activation countdown nor a graph-uniform accelerated arm.
\end{theorem}

\begin{proof}
Put $a=(1+\alpha)/2$, $h=(1-\alpha)/2$, and
$\beta=h/3$.  During the backbone phase the active set is exactly $B_i$.
Every branch vertex has ambient degree three, so $Q_{B_iB_i}$ is the leading
$i$-by-$i$ tridiagonal matrix with diagonal $a$ and off-diagonal $-\beta$.
Define its leading continuants by
\begin{equation*}
 \mathcal D_0=1,\qquad \mathcal D_1=a,\qquad
 \mathcal D_i=a\mathcal D_{i-1}-\beta^2\mathcal D_{i-2}.
\end{equation*}
Every $\mathcal D_i$ is positive because $Q_{B_iB_i}$ is SPD.  The last
Green column has the exact separable form
\begin{equation}
 (Q_{B_iB_i}^{-1})_{b_jb_i}=u_j\upsilon_i,qquad
 u_j:=\frac{\mathcal D_{j-1}}{\beta^{j-1}},qquad
 \upsilon_i:=\frac{\beta^{i-1}}{\mathcal D_i}
 \quad(1\leq j\leq i).
 \label{eq:branch-prefix-green-factorization}
\end{equation}
This follows directly by expanding the corresponding tridiagonal cofactor.

Write $x^{(i)}:=x[B_i]$.  Appending the strict positive backbone tip
$b_{i+1}$ and using block elimination gives
\begin{equation}
 x^{(i+1)}_{B_i}-x^{(i)}_{B_i}
 =\beta x^{(i+1)}_{b_{i+1}}Q_{B_iB_i}^{-1}e_{b_i}
 =\Delta\theta_i\,(u_1,\ldots,u_i)^\top,
 \qquad
 \Delta\theta_i:=\beta\upsilon_i x^{(i+1)}_{b_{i+1}}>0.
 \label{eq:branch-prefix-one-parameter-update}
\end{equation}
Let $\theta_1=0$ and
$\theta_{i+1}=\theta_i+\Delta\theta_i$.  For every $j\leq i$,
\begin{equation}
 x^{(i)}_{b_j}=\xi_j+u_j\theta_i,
 \qquad
 \xi_j:=x^{(j)}_{b_j}-u_j\theta_j.
 \label{eq:branch-prefix-affine-coordinate}
\end{equation}

Every boundary tip $v$ exposed during this phase has one active parent
$b_j$.  Put $\gamma_v:=-Q_{vb_j}>0$ and store the exact crossing key
\begin{equation}
 \theta_v^\times
 :=\frac{-(\ell_\rho)_v/\gamma_v-\xi_j}{u_j}.
 \label{eq:branch-tip-crossing-key}
\end{equation}
For every later backbone prefix at which $v$ is still outside,
\begin{equation}
 (\ell_\rho-Qx[B_i])_v
 =\gamma_vu_j(\theta_i-\theta_v^\times).
 \label{eq:branch-tip-kinetic-demand}
\end{equation}
Thus $v$ is a strict violation exactly when
$\theta_v^\times<\theta_i$.  CaterpillarKineticDelta inserts the pair
$(\theta_v^\times,v)$ when $v$ is first exposed.  At $B_1$ it performs the
same strict minimum drain at $\theta_1$, producing the declared initialization
delta.  After each backbone append it updates $\theta_i$ and repeatedly
removes the minimum key while
that strict inequality holds, inserting each removed label into the
persistent positive set.  Equality is retained in the heap because the gate
requires a strict positive demand.  Hence the heap reports every and only
new strict positive tip during the entire backbone phase.  The certified
backbone tip is among those labels and is removed from the positive set when
its singleton is committed.

At $B_m$, the definition of $\rho_{\rm cat}(m,\alpha)$ includes the pairs
$(B_m,a_1)$ and $(B_m,r_i)$ for every $1\leq i\leq m+1$.  Therefore every
remaining boundary tip is already in the persistent positive set.  After
this checkpoint a committed side leaf has no child, while committing $a_t$
exposes only $a_{t+1}$.  Its demand at the base face
$B_m\cup A_t$ is strict positive by the same definition of
$\rho_{\rm cat}$.  Any side leaves already committed were themselves strict
positive expansions, so the Schur positivity and violation-persistence
identity \eqref{eq:rppr-violation-persistence} can only increase the new arm
tip's demand.  If $a_t$ and side leaves occur in one allowed batch, compute the
same final restricted point by sequentializing $a_t$ first and those
already-positive leaves afterward; this is an equality of the final support,
not a replay used by the online reporter.  One exact named transfer query
therefore certifies and flags
$a_{t+1}$.  Every older positive flag remains valid until its vertex is
committed.  This proves that the retained set is exactly $\mathcal K(U)$ for
every allowed suffix order, and it also proves legality and finite
termination.  The long arm forces at least $m$ suffix batches, while at most
all $3m$ nonseed vertices are committed separately, giving the claimed range
for $J$.

It remains to account for the implementation.  Every bounded-degree row is
exposed and retained once; recognizing its branch, arm, or leaf role is
charged with that scan and with control membership, not preprocessing.  The
backbone and long-arm fronts force $\Theta(m)$ adaptive adjacency rounds.
There are $O(m)$ transfer-cell appends or attachment updates and $O(m)$ named
tip/endpoint queries.  Each touches $O(\log(2+m))$ stored product-tree cells
and is charged to $C_{\rm resp}$.  A multi-vertex batch applies these updates
only to its actual committed members; it neither replays an unchosen order nor
materializes an intermediate full response.  There is no hidden bulk subtree
query.
The kinetic heap inserts and removes each tip at most once; every root test,
child comparison, exact crossing comparison, membership lookup, committed
batch check, and final stored-row validation is charged to $C_{\rm ctl}$.
These operations total $O(m\log(2+m))$.

The retained graph records, transfer products, continuants, heap, positive
flags, and batch membership use $\Theta(m)$ cells.  Heap paths need only
$O(\log(2+m))$ scratch, while one terminal response recovery and validation
buffer is allowed $O(m)$ scratch.  The terminal tridiagonal/pendant recovery
arithmetic is included in $C_{\rm resp}$; writing its exact candidate is
charged separately as $C_{\rm mat}=\Theta(m)$.  Each nonseed label is emitted
at most once as a new-positive delta and once in a committed-batch
certificate; the $J+1$ constant-size reply headers and the final
$3m+1$-coordinate exact output are all charged to
$C_{\rm emit}=\Theta(m)$.  No iterative recurrence is used.  These charges
prove \eqref{eq:branch-caterpillar-kinetic-eleven-vector} and
\eqref{eq:branch-caterpillar-kinetic-total}.
\end{proof}

The canonical all-violations policy does not preserve the backbone-only
prefixes used in the preceding theorem: sufficiently light side tips enter
as soon as their parents are reached.  On an explicit fixed-family parameter
range, however, that interleaving is simpler rather than harder.  Every live
boundary is the next graph-distance layer, so no negative key survives from
one checkpoint to the next.

For $0\leq k<m$, put
\begin{equation}
 \begin{split}
 R_k&:=\{r_1,\ldots,r_k\},\qquad R_0:=\varnothing,\\
 U_k^{\rm can}&:=B_{k+1}\cup A_k\cup R_k,\\
 \mathcal F_k^{\rm can}
 &:={}
 \begin{cases}
  \{b_{k+2},a_{k+1},r_{k+1}\},&0\leq k<m-1,\\
  \{a_m,r_m,r_{m+1}\},&k=m-1.
 \end{cases}
 \end{split}
 \label{eq:branch-caterpillar-canonical-layers}
\end{equation}
Thus $U_0^{\rm can}=\{b_1\}$,
$\partial U_k^{\rm can}=\mathcal F_k^{\rm can}$, and
$U_k^{\rm can}\cup\mathcal F_k^{\rm can}=U_{k+1}^{\rm can}$ for
$k<m-1$, while the last union is $V(\mathsf{BC}_m)$.  Let
\begin{equation}
 \mathcal P_m^{\rm can}:=
 \{(U_k^{\rm can},v):0\leq k<m,\ v\in\mathcal F_k^{\rm can}\}
 \label{eq:branch-caterpillar-canonical-pairs}
\end{equation}
and, using \eqref{eq:branch-caterpillar-affine-demand}, define the
analysis-side threshold
\begin{equation}
 \rho_{\rm can}(m,\alpha)
 :=\min\left\{
  \rho_{\rm cat}(m,\alpha),
  \min_{(U,v)\in\mathcal P_m^{\rm can}}
  \frac{\Gamma_{U,v}}{\alpha H_{U,v}}
 \right\}>0.
 \label{eq:branch-caterpillar-canonical-threshold}
\end{equation}
This finite threshold is a reach condition, not a table built or queried by
the online execution.  It may shrink with $m$.

\begin{theorem}[Canonical layer-delta reporting on the branch caterpillar]
\label{thm:branch-caterpillar-canonical-layer-reporter}
Fix $m\geq2$, $\alpha\in(0,1)$, branch seed $s=e_{b_1}$, and
\begin{equation*}
 0<\rho<\rho_{\rm can}(m,\alpha)
 \leq\rho_{\rm cat}(m,\alpha)\leq\frac13.
\end{equation*}
The exact canonical all-violations RPPR gate on $\mathsf{BC}_m$ has precisely
the faces $U_0^{\rm can},\ldots,U_{m-1}^{\rm can}$ and the $m$ committed
batches $\mathcal F_0^{\rm can},\ldots,\mathcal F_{m-1}^{\rm can}$ in
\eqref{eq:branch-caterpillar-canonical-layers}, followed by the full support.

There is a named exact-real reporter,
\emph{CaterpillarCanonicalLayerDelta}, that at every canonical checkpoint
enumerates the entire current boundary, computes and strictly tests every
exact demand, emits every and only newly strict-positive label, and returns
one charged certificate reply for the canonical batch.  Positive ties are
all emitted in the same batch; equality is nonpositive and remains outside.
In the stated strict parameter range equality never occurs on the displayed
trace.  Every emitted label is consumed immediately, so no old boundary key
or positive label survives a checkpoint.

With one external delta/certificate interaction per committed batch and one
terminal return, its exhaustive resource vector in the shared order is
\begin{equation}
 \begin{aligned}
 \mathcal C_{11}(\mathcal E_m^{\rm can})
 :=\bigl(&C_{\rm adj},R_{\rm adj},R_{\rm int},C_{\rm pre},C_{\rm ctl},
 C_{\rm rec},C_{\rm resp},M_{\rm pers},M_{\rm tmp},C_{\rm mat},C_{\rm emit}
 \bigr)\\
 =\bigl(&\Theta(m),\Theta(m),m+1,0,\Theta(m),0,
 O(m\log(2+m)),\\
 &\Theta(m),O(m),\Theta(m),\Theta(m)\bigr).
 \end{aligned}
 \label{eq:branch-caterpillar-canonical-eleven-vector}
\end{equation}
Consequently its additive exact-cell work is
\begin{equation}
 O(m\log(2+m))
 =O\!\left(C(S^\star)\log(2+C(S^\star))\right),
 \qquad C(S^\star)=9m+1.
 \label{eq:branch-caterpillar-canonical-total}
\end{equation}

This is a fixed-$m$, branch-seeded, family-$\rho$, exact-real canonical
all-violations and delta-interface theorem.  It makes no claim for
$\rho_{\rm can}\leq\rho<\rho_{\rm cat}$, another seed, arbitrary
positive-subset or pre-backbone orders, a positive $m$-uniform range of
$\rho$, finite precision, bit complexity, transfer-product conditioning,
$\kappa=1$, RPPR-to-PPR conversion, or product-scale or graph-uniform
accelerated work.
\end{theorem}

\begin{proof}
For every $(U,v)\in\mathcal P_m^{\rm can}$, the set $U$ is connected,
contains the seed, and $v$ has an edge into $U$.  The Stieltjes argument in
\eqref{eq:branch-caterpillar-affine-demand} therefore gives
$\Gamma_{U,v}>0$ and $H_{U,v}>0$.  By
\cref{eq:branch-caterpillar-demand-line,eq:branch-caterpillar-canonical-threshold},
every $v\in\mathcal F_k^{\rm can}$ is a strict boundary violation at
$x[U_k^{\rm can}]$.

Now induct on $k$.  The seed load is positive because
$\rho<\rho_{\rm can}\leq1/3$, so the initialized canonical face is
$U_0^{\rm can}=\{b_1\}$.  The tree boundary of this face is exactly
$\mathcal F_0^{\rm can}$, and every member is strict positive, so the
canonical all-violations gate admits the whole set.  If the current face is
$U_k^{\rm can}$, its full boundary is exactly
$\mathcal F_k^{\rm can}$ and the same threshold argument makes every member
strict positive.  The next canonical batch is therefore that entire set.
Equation \eqref{eq:branch-caterpillar-canonical-layers} gives the next face,
and the final batch gives the whole graph.  This proves the exact chronology,
$S^\star=V(\mathsf{BC}_m)$, and the assertion that every boundary vertex is
newly live at its checkpoint.  Its label may have been revealed by the
preceding layer's row scan, but its own boundary row is first scanned and
charged only when it becomes live.  Strict comparison handles the zero
convention, while the all-violations rule includes every positive tie.

It remains to implement the exact tests and response without dense rewrites.
At $U_k^{\rm can}$, eliminate the active long-arm prefix $A_k$ into one
diagonal/load absorption pair at $b_1$, and eliminate each closed side leaf
$r_i$, $i\leq k$, into the cell at $b_i$.  The surviving system on
$B_{k+1}$ is SPD and tridiagonal.  Store its affine transfer cells in the
balanced product tree of
\Cref{rem:branch-caterpillar-implicit-transfer-comparator}, together with one
affine product for the active long-arm prefix.

Every vertex of $\mathcal F_k^{\rm can}$ has one active parent.  One named
branch query gives the last branch value used by the next branch and its side
leaf, and one named arm query gives the active arm-tip value; at $k=0$ both
reduce to the seed value.  The branch endpoint solve also recovers $x_{b_1}$,
which is the input to the arm-product readout, so no third core query is
hidden.  Hence at most two charged transfer queries compute
all three exact demands at a checkpoint.  The reporter scans the at-most-three
charged boundary rows, retains each row after its first exposure, performs
strict sign comparisons, and emits the three positive labels.

After commitment, the side leaf changes only the diagonal/load cell at its
branch, the long-arm vertex appends one arm-transfer cell and changes only the
induced branch-core absorption cell at $b_1$, and,
except in the last batch, the new branch vertex appends one tridiagonal cell.
The final batch instead contributes two leaf absorptions at $b_m$.  Thus a
batch performs only $O(1)$ product-tree appends or point updates, each costing
$O(\log(2+m))$ exact response operations.  Applying these constant-size
updates in a fixed internal order is Schur-complement associativity for the
one committed support; it neither replays another gate order nor emits an
intermediate face.  The side-leaf absorption changes the fixed-prefix
continuants and Green factors used in
\eqref{eq:branch-prefix-green-factorization}, but the balanced product tree
localizes that change to one point update.  Since the complete boundary is
consumed, the kinetic heap
from \Cref{thm:branch-caterpillar-kinetic-delta-reporter} is empty after every
checkpoint and requires no repair.

Every bounded-degree row is exposed and retained once, over $\Theta(m)$
adaptive adjacency work and rounds.  There are exactly $m$ committed batches,
so the specified external interface has $R_{\rm int}=m+1$.  Boundary
enumeration, membership, strict comparisons, tie handling, batch checks, and
the final stored-row validation use $\Theta(m)$ control operations.  The
$O(1)$ transfer updates and named queries per batch give
$O(m\log(2+m))$ response work, including $O(m)$ terminal recovery.  No
preprocessing or iterative recurrence is used.

Stored rows, arm/core products, absorption cells, and membership records use
$\Theta(m)$ persistent cells.  Forward queries need logarithmic scratch; one
terminal recovery and validation buffer gives the reported $O(m)$ peak.
Writing the terminal candidate costs $C_{\rm mat}=\Theta(m)$.  The $3m$
delta labels, $m$ constant-size certificate replies, one terminal header, and
the exact $(3m+1)$-coordinate output give
$C_{\rm emit}=\Theta(m)$.  These charges prove
\eqref{eq:branch-caterpillar-canonical-eleven-vector} and
\eqref{eq:branch-caterpillar-canonical-total}.
\end{proof}

\begin{proposition}[Continuous numerical countdowns cannot pay activation]
\label{prop:no-continuous-activation-countdown}
Fix $\alpha\in(0,1)$.  On the one-edge graph there are legal exact RPPR
activations of fixed charge two whose before-and-after canonical states
converge to one another.  Hence no countdown jointly continuous in
$(\rho,x)$ can decrease by the full activation charge uniformly over RPPR
instances.  In particular, an exact obstacle-energy countdown needs an
unbounded multiplier near the activation breakpoint.
\end{proposition}

\begin{proof}
Retain $a=(1+\alpha)/2$ and $h=(1-\alpha)/2$.  For $0<\rho<h$, the shifted
load is $\ell_\rho=(\alpha(1-\rho),-\alpha\rho)$ and the root checkpoint is
\[
 x^-=(\alpha(1-\rho)/a,0).
\]
Its boundary demand at $v$ is
\[
 g_v=(\ell_\rho-Qx^-)_v=\frac{\alpha(h-\rho)}a>0.
\]
The Schur complement is $a-h^2/a=\alpha/a$, so the legal exact expansion
satisfies
\begin{equation}
 x_v^+=h-\rho,
 \qquad
 x_o^+-x_o^-=\frac{h(h-\rho)}a.
 \label{eq:vanishing-activation-jump}
\end{equation}
Thus $\|x^+-x^-\|\to0$ as $\rho\uparrow h$, although scanning and activating
$v$ always costs $c(v)=2$.  Joint continuity would force
$\Phi(\rho,x^-)-\Phi(\rho,x^+)\to0$, contradicting a uniform own-service
decrease of two.  The exact obstacle-energy drop is
\[
 \frac12\frac{g_v^2}{\alpha/a}
 =\frac{\alpha(h-\rho)^2}{2a}.
\]
An energy scaling that pays charge two must therefore be at least
$4a/[\alpha(h-\rho)^2]$, which diverges.
\end{proof}

The discontinuous support tokens in
\Cref{thm:path-token-countdown,thm:three-arm-spider-token-countdown} are not a
technical convenience; the proposition shows why a support jump must be
recorded explicitly.  Raw causal debts do not repair this issue either: an
exact neighbor push can create new exact-after-exact backflow from zero, and
a fresh over-relaxed push can create delayed-reflection magnitude from zero.
Neither raw magnitude is interference monotone without additional transport
aggregation.

\section{Energy facts for adaptive relaxation}
\label{sec:energy}

The next statements apply to the unregularized quadratic $f$ in the shared
model.  They do not assume monotone residual signs.
Define the error energy
\begin{equation}
 \mathcal E(\widehat x)
 :=f(\widehat x)-f(x^0)
 =\frac12(\widehat x-x^0)^\mathsf TQ(\widehat x-x^0).
 \label{eq:error-energy}
\end{equation}

\begin{lemma}[Exact coordinate-energy ledger]
\label{lem:energy}
Let $\widehat x$ be any iterate, $r=b-Q\widehat x$, and let
$\widehat x^+$ be the row update \eqref{eq:row-update} on $u$.  For every
$0<\omega<2$,
\begin{equation}
 f(\widehat x)-f(\widehat x^+)
 =\frac{\omega(2-\omega)}{2Q_{uu}}r_u^2
 =\frac{\omega(2-\omega)}{1+\alpha}r_u^2.
 \label{eq:energy-drop}
\end{equation}
Consequently, along any finite adaptive run,
\begin{equation}
 f(\widehat x_0)-f(\widehat x_T)
 =\sum_{t=0}^{T-1}
 \frac{\omega_t(2-\omega_t)}{1+\alpha}r_{t,u_t}^2.
 \label{eq:telescope}
\end{equation}
The right-hand side is computable from the live local residuals and does not
require knowing $x^0$.
\end{lemma}

\begin{proof}
Set $z=\omega r_u/Q_{uu}$.  The exact quadratic expansion gives
\[
 f(\widehat x+ze_u)-f(\widehat x)
 =z\nabla_u f(\widehat x)+\tfrac12Q_{uu}z^2
 =-\frac{\omega(2-\omega)}{2Q_{uu}}r_u^2.
\]
The shared matrix has $Q_{uu}=(1+\alpha)/2$.  Summing proves the telescoping
identity.
\end{proof}

\begin{corollary}[Myopic descent selects the exact rung]
\label{cor:myopic}
At a fixed iterate and coordinate, immediate objective decrease per charge
is uniquely maximized over $0<\omega<2$ by $\omega=1$.
\end{corollary}

\begin{proof}
The residual and charge are fixed, while
$\omega(2-\omega)=1-(\omega-1)^2$ is uniquely maximized at one.
\end{proof}

Thus no controller based only on the current update's energy efficiency can
explain the measured advantage of over-relaxation.  The benefit must appear
over a block: over-relaxation changes which coordinates become live, how far
the frontier travels, and which expensive rows are revisited.  Equation
\eqref{eq:telescope} is still useful as an auditable block-progress
denominator, but comparing modes from the same starting state requires a
probe, a branch, or a structural theorem.

\subsection{A condition-free safety theorem}

The energy ledger proves that adaptive relaxation itself does not endanger
termination if it is clamped away from two.

\begin{theorem}[Safe causal variable relaxation]
\label{thm:safe-adaptive}
Fix $\delta\in(0,1]$.  Start from $\widehat x_0=0$.  At every paid step,
choose any coordinate satisfying the live guard
\begin{equation}
 \frac{\abs{r_{t,u_t}}}{\sqrt{d_{u_t}}}>g
 \label{eq:live}
\end{equation}
and choose $\omega_t\in[1,2-\delta]$ by any causal function of the entire
past, including the causal revisit ledger.  Require the controller to execute
such a live coordinate whenever \eqref{eq:note-gate} fails.  Then only
finitely much charged row work can occur before \eqref{eq:note-gate} holds,
and
\begin{equation}
 W\leq
 \frac{1+\alpha}
 {\delta(2-\delta)\,\alpha\epsppr^{2}}.
 \label{eq:safety-work}
\end{equation}
The statement is ordering independent and allows signed residuals.
\end{theorem}

\begin{proof}
Let $m_\delta:=\delta(2-\delta)$.  On $[1,2-\delta]$,
$\omega(2-\omega)\geq m_\delta$.  A paid live operation satisfies
$r_{t,u_t}^2>g^2d_{u_t}$.  Since $d_u\geq1$,
$c(u)=1+d_u\leq2d_u$.  By \Cref{lem:energy}, its objective decrease is at
least
\begin{equation}
 \frac{m_\delta g^2}{2(1+\alpha)}c(u_t).
 \label{eq:progress-charge}
\end{equation}

The minimum objective is $f(x^0)$, and the zero-start energy is
\[
 f(0)-f(x^0)=\tfrac12 b^\mathsf TQ^{-1}b
 \leq\frac{\norm{b}_2^2}{2\alpha}
 \leq\frac{\alpha}{2}.
\]
The last inequality follows from
$b_i=\alpha s_i/\sqrt{d_i}$, $d_i\geq1$, $s\geq0$, and
$\sum_i s_i=1$, which give $\norm{b}_2^2\leq\alpha^2$.  Sum
\eqref{eq:progress-charge}, use $g=\alpha\epsppr$, and solve for $W$ to
obtain \eqref{eq:safety-work}.  Infinite charged work would force more
objective decrease than the zero-start energy, so the paid process is finite.
If a live coordinate remained, the rule could make another paid step;
therefore exhaustion is exactly the note-scoped gate.
\end{proof}

The theorem permits the literal clipping interval $[1.10,1.68]$ used by the
frontier artifact, as well as a controller that raises or lowers $\omega$
after every operation.  It does not prove acceleration: the
$\epsppr^{-2}$ and $\alpha^{-1}$ scale is weaker than the intended
$\widetilde O(\mathsf V_{\rm loc}/\sqrt\alpha)$ target.  Its value is modular.  A learned rule
may be experimentally aggressive without carrying the correctness proof; the
clamp and live guard carry it.

This energy argument is also one unconditional but nonaccelerated instance
of \Cref{thm:mergeable-countdown}.  With
$m_\delta:=\delta(2-\delta)$, define
\begin{equation}
 \Phi_{\rm en}(\widehat x)
 :=\frac{2(1+\alpha)}{m_\delta g^2}\mathcal E(\widehat x).
 \label{eq:energy-countdown}
\end{equation}
Every live update with relaxation in $[1,2-\delta]$ decreases this same
countdown by at least its charge, regardless of which policy requested the
update.  Hence it is interference monotone and recovers the safety theorem on
a genuinely shared state.  It does not distinguish the arms and has the
nonaccelerated scale.  The exact missing object for a lower-bound-matching
adaptive theorem is a $\kappa=\widetilde O(1)$ transport-block countdown for
at least one accelerated arm that other support-safe blocks cannot increase.
The delayed-reflection debt and the exact-after-exact backflow ledger are
concrete candidates for constructing such a countdown.

\subsection{A no-reset experimental prefix with exact fallback}

Even on one irreversible state, an adaptive policy can be tried for a
controlled budget and then handed to exact Gauss--Seidel.  The prefix's
energy decrease is not discarded.

\begin{theorem}[Budgeted adaptive prefix]
\label{thm:budgeted-prefix}
Under the hypotheses of \Cref{thm:safe-adaptive}, run an arbitrary causal
prefix of charged work $W_{\rm pre}$ with
$\omega_t\in[1,2-\delta]$.  From the resulting iterate, switch irreversibly
to live unit-relaxation updates until \eqref{eq:note-gate} holds.  Define
\begin{equation}
 H_0:=\frac{2(1+\alpha)\mathcal E(0)}{g^2}
 \leq\frac{1+\alpha}{\alpha\epsppr^2},
 \qquad m_\delta:=\delta(2-\delta).
 \label{eq:fallback-budget}
\end{equation}
Then
\begin{equation}
 W_{\rm total}
 \leq H_0+(1-m_\delta)W_{\rm pre}.
 \label{eq:fallback-total}
\end{equation}
In particular, imposing the hard cap $W_{\rm pre}\leq\theta H_0$ gives
\begin{equation}
 W_{\rm total}
 \leq[1+(1-m_\delta)\theta]H_0
 \leq(1+\theta)H_0.
 \label{eq:fallback-cap}
\end{equation}
\end{theorem}

\begin{proof}
By \eqref{eq:progress-charge}, the prefix decreases $\mathcal E$ by at
least $m_\delta g^2W_{\rm pre}/(2(1+\alpha))$.  If
$\mathcal E_{\rm hand}$ is the energy at handoff, the same live-update
argument with $\omega=1$ bounds the exact tail by
\[
 W_{\rm tail}
 \leq\frac{2(1+\alpha)\mathcal E_{\rm hand}}{g^2}
 \leq H_0-m_\delta W_{\rm pre}.
\]
Add $W_{\rm pre}$ and apply its cap.  The upper bound on $H_0$ is the
zero-start calculation in \Cref{thm:safe-adaptive}.
\end{proof}

The theorem allows a revisit-based switch trigger on the actual single
state, with a work cap as its independent safety certificate.  It is not a
best-arm oracle inequality: the fallback ceiling is still the weak
energy-based exact-Gauss--Seidel scale.  A lower-bound-matching result would
need to replace $H_0$ by an accelerated local tail or show that a losing
prefix is chargeable to such a tail.

\section{A condition-free best-of-policy wrapper}
\label{sec:portfolio}

The nonidentifiability result concerns a controller forced to choose one
future policy.  Branching removes that information bottleneck.  Let
$P_1,\ldots,P_K$ be local solvers for the same instance and terminal
certificate.  They may include, for example, exact Gauss--Seidel, fixed
two-rung schedules at several band widths, an alpha-scaled ladder, and the
frontier adaptive ladder.  Each policy receives an independent sparse state
initialized from the same input.

For instance $I$, let $C_j(I)$ be the charged edge or row work policy $P_j$
would need if run alone until the verifier accepts it.  Fix positive service
shares $\nu_j$ with $\sum_j\nu_j=1$.  A fractional work scheduler gives arm
$j$ exactly a $\nu_j$ fraction of aggregate charged work and stops all arms
as soon as one certifies.

\begin{theorem}[Weighted portfolio oracle inequality]
\label{thm:portfolio}
Under fractional charged-work scheduling,
\begin{equation}
 C_{\rm port}(I)
 =\min_{1\leq j\leq K}\frac{C_j(I)}{\nu_j}.
 \label{eq:portfolio-exact}
\end{equation}
In particular, uniform service satisfies
\begin{equation}
 C_{\rm port}(I)=K\min_j C_j(I).
 \label{eq:uniform-portfolio}
\end{equation}

If work is scheduled in indivisible chunks and a fair scheduler maintains
received work $w_j(W)\geq\nu_jW-\Delta$ for every arm after aggregate work
$W$, then
\begin{equation}
 C_{\rm port}(I)
 \leq\min_j\frac{C_j(I)+\Delta}{\nu_j}.
 \label{eq:portfolio-discrete}
\end{equation}
\end{theorem}

\begin{proof}
After aggregate fractional work $W$, arm $j$ has received $\nu_jW$.  It
finishes exactly when $W=C_j(I)/\nu_j$; the first finishing arm proves
\eqref{eq:portfolio-exact}.  Uniform shares give
\eqref{eq:uniform-portfolio}.  Under the discrete invariant, aggregate work
$W=(C_j(I)+\Delta)/\nu_j$ guarantees
$w_j(W)\geq C_j(I)$, so arm $j$ has finished.  Minimize over $j$.
\end{proof}

The fractional model is natural for the manuscript's adjacency-edge work:
an adjacency scan can be paused and resumed after buffering its row update.
With atomic service operations, $\Delta$ can instead be taken as the largest
scheduling chunk.  The wrapper uses $K$ sparse solver states and may explore
the union of their frontiers; that storage and scalar-copy overhead must be
reported even though \eqref{eq:portfolio-exact} counts graph work exactly.
The benchmark service used by the measured artifact exposes only one mutable
state, so literal implementation there would require protocol support for
checkpoints or independent instances.

\subsection{Removing simultaneous branches by geometric restart}

If independent branch states are unavailable but the instance can be reset
to its common initial state, a universal geometric schedule retains a
constant-factor guarantee.  It uses one live solver state at a time.

\begin{algorithm}[H]
\caption{Single-live-state restart portfolio}
\begin{algorithmic}[1]
\REQUIRE policies $P_1,\ldots,P_K$; base charged-work quantum $B_0>0$
\STATE test the terminal certificate at the common initial state
\FOR{$k=0,1,2,\ldots$}
  \FOR{$j=1,\ldots,K$}
    \STATE reset to the common initial state
    \STATE run $P_j$ for charged-work budget $2^kB_0$
    \IF{$P_j$ certifies within the budget}
      \RETURN its certified iterate
    \ENDIF
  \ENDFOR
\ENDFOR
\end{algorithmic}
\end{algorithm}

\begin{theorem}[Best-of-policy guarantee with one live state]
\label{thm:restart-portfolio}
Assume charged work can be paused at the declared budget and every policy is
deterministic from the common initial state.  Let
$C_\star:=\min_j C_j(I)$.  The restart portfolio returns a certified solution
using one live policy state and total work
\begin{equation}
 C_{\rm restart}(I)<4K\max\{C_\star,B_0\}
 \leq4K(C_\star+B_0).
 \label{eq:restart-bound}
\end{equation}
With indivisible chunks, the same proof holds after adding the maximum
per-trial budget overshoot to $B_0$.
\end{theorem}

\begin{proof}
Let $k_\star$ be the first epoch with
$2^{k_\star}B_0\geq C_\star$.
A best policy certifies during that epoch.  Bounding every attempted policy
in every epoch by its full budget gives
\[
 C_{\rm restart}(I)
 \leq K\sum_{k=0}^{k_\star}2^kB_0
 <2K\,2^{k_\star}B_0.
\]
If $k_\star=0$, the last quantity is $2KB_0$.  Otherwise minimality gives
$2^{k_\star}B_0<2C_\star$.  Both cases imply
\eqref{eq:restart-bound}.
\end{proof}

This theorem removes simultaneous branching and all distributional
assumptions, at the price of recomputing prefixes.  It does require a true
reset.  A service exposing one irreversible mutable state, with neither
checkpointing nor reset, still needs a switch amortization proving that early
work under a losing mode is useful to the eventual winner.

\subsection{Safe learned allocation}

Let an arbitrary predictor output shares
$\widehat\nu_{t,1},\ldots,\widehat\nu_{t,K}$ for repeated instance $I_t$.
Its context may include graph identity, seed features, $\alpha$, gate
hardness, phase-wise touched charge, and the four-way causal revisit ledger.
For $\gamma\in(0,1]$, use
\begin{equation}
 \nu_{t,j}:=(1-\gamma)\widehat\nu_{t,j}+\frac{\gamma}{K}.
 \label{eq:floor}
\end{equation}

\begin{corollary}[Distribution-free safety of learned shares]
\label{cor:learned-safe}
For every sequence of instances and every predictor, the discrete portfolio
satisfies, separately on every $I_t$,
\begin{equation}
 C_{{\rm port},t}
 \leq\frac{K}{\gamma}
       \left(\min_j C_{t,j}+\Delta_t\right).
 \label{eq:floor-bound}
\end{equation}
If a best arm $j_t^\star$ receives predicted mass
$\widehat\nu_{t,j_t^\star}=\upsilon_t$, the sharper bound is
\begin{equation}
 C_{{\rm port},t}
 \leq
 \frac{C_{t,j_t^\star}+\Delta_t}
 {(1-\gamma)\upsilon_t+\gamma/K}.
 \label{eq:prediction-bound}
\end{equation}
No stationarity or transfer assumption is required.
\end{corollary}

\begin{proof}
Equation \eqref{eq:floor} gives $\nu_{t,j}\geq\gamma/K$ for every arm.
Apply \eqref{eq:portfolio-discrete} to a best arm for
\eqref{eq:floor-bound}, and retain its actual share for
\eqref{eq:prediction-bound}.
\end{proof}

This is the clean theoretical role for revisit memory.  It may improve the
constant by predicting a useful service allocation, but a wrong prediction
cannot remove any arm or invalidate the certificate.  The exploration floor,
not the empirical map from $\varrho_{\rm rev}$ to $\omega$, supplies the
worst-case theorem.

\subsection{Inheritance of accelerated work}

\begin{corollary}[Finite-menu inheritance]
\label{cor:inherit}
Let $\mathcal I$ be an instance class.  Suppose that for every
$I\in\mathcal I$ at least one policy in a fixed menu of size $K$ satisfies
\begin{equation}
 C_j(I)\leq A(I).
 \label{eq:arm-bound}
\end{equation}
Then the uniform fractional portfolio satisfies
$C_{\rm port}(I)\leq K A(I)$.  In particular, if
$A(I)=\widetilde O(\mathsf V_{\rm loc}(I)/\sqrt\alpha)$ and $K$ is constant, the portfolio has
the same asymptotic scale.
\end{corollary}

\begin{proof}
Substitute \eqref{eq:arm-bound} into \eqref{eq:uniform-portfolio}.
\end{proof}

The quantifiers matter.  The good arm may vary with the graph, seed,
accuracy, or local geometry; the portfolio does not need to identify it in
advance.  But the arm must already possess the asserted bound.  This wrapper
therefore removes the \emph{selection condition}, not the structural work
condition needed to prove an accelerated local arm.  It also does not turn an
algorithm-specific lower bound into a universal lower bound for the richer
branching access model.

\section{Recommended adaptive architecture}
\label{sec:architecture}

The proofs suggest three layers with distinct responsibilities.

\paragraph{Layer 1: a causal diagnostic, not a scalar verdict.}
Record $C(S)$, total work, all four $\mathsf{Rev}_{a\to b}$ entries,
phase-wise work,
and the block energy decrease from \eqref{eq:telescope}.  The pair
$\mathsf{Rev}_{\mathrm e\to\mathrm e}
 +\mathsf{Rev}_{\mathrm e\to\mathrm s}$ is certified
neighbor backflow.  A large scalar $\varrho_{\rm rev}$ without this
decomposition is ambiguous.  Also record full graph identity or a stable
fingerprint; graph size alone is not a theoretical context.
For support-safe RPPR traces, report the global fast-path certificate
$2\varrho_{\rm rev}/\rho$ from \Cref{cor:rppr-fast-path}, not merely the
dimensionless ratio.

\paragraph{Layer 2: a safety mechanism independent of prediction.}
For a single state, clamp every adaptive choice to $[1,2-\delta]$ and retain
the live guard, invoking \Cref{thm:safe-adaptive}; independently cap an
experimental prefix and invoke \Cref{thm:budgeted-prefix} before exact
fallback.  On nested RPPR regions, enforce \Cref{thm:revisit-bank} between
canonical settlements and switch only through the exact boundary gate of
\Cref{thm:absorption-safe-switch}; this preserves the irreversible state
without requiring cross-policy state transfer.  Also enforce a global
accelerated bank: successful runs receive \Cref{cor:rppr-fast-path}, and an
overdraw triggers canonicalization plus fallback under
\eqref{eq:accelerated-or-fallback}.  If mergeable countdowns are available,
\Cref{thm:mergeable-countdown} permits a work-fair shared-state portfolio.
With branchable state,
keep exact Gauss--Seidel and at least one conservative two-rung policy in the
portfolio, and enforce \eqref{eq:floor}.  Every returned answer is checked by
the same verifier-owned terminal gate.  If branches are unavailable but a
clean reset is allowed, use \Cref{thm:restart-portfolio}.

\paragraph{Layer 3: learning only changes allocation.}
Use the causal ledger to predict service shares, not to delete arms or alter
the terminal certificate.  One plausible qualitative prior is to distinguish
large exact-after-exact backflow from large spread-to-exact delivery work;
the former is direct evidence of reactivation after settlement, whereas the
latter can be planned debt cleanup.  Whether this distinction predicts the
best arm is empirical and must be evaluated out of sample.

For the presently measured family, a minimal menu is
\begin{equation}
 \left\{
  P_{\rm exact},\;
  P_{\rm 2rung}(\chi_{\rm band}=2.5,\omega_\star),\;
  P_{\rm scaled},\;
  P_{\rm adaptive}
 \right\},
 \label{eq:menu}
\end{equation}
where $P_{\rm scaled}$ uses an alpha-dependent band or relaxation motivated
by the delayed-reflection note, and $\chi_{\rm band}$ is the note-scoped
threshold multiplier rather than the reserved PageRank right-hand side $b$.
The value $2.5$ is only the best of six
tested band widths on the existing campaign; it is not a universal optimum.

\section{What remains for a matching bound}
\label{sec:remaining}

The third direction now has a sharp theoretical boundary.

\begin{enumerate}[leftmargin=*]
 \item \textbf{Selection is solved for a finite branchable menu.}
 The portfolio chooses the fastest certifying arm up to a constant factor on
 every instance, without assuming that revisit statistics transfer between
 instances.

 \item \textbf{Arbitrary causal relaxation is safe.}
 The energy ledger proves termination and an explicit work ceiling, but that
 ceiling has the classical $1/\alpha$ rather than accelerated
 $1/\sqrt\alpha$ dependence and a quadratic tolerance cost.

 \item \textbf{Within-epoch multiplicity is enforceable.}
 The exact factorization \eqref{eq:two-level-factorization} connects the
 empirical ratio to the settled-volume ledger, and the revisit bank caps its
 within-epoch factor online.  A global bank also certifies the complete fast
 path whenever it succeeds.  For a theorem guaranteeing success, the
 remaining structural term is $\mathfrak R_{\rm set}$, unless incremental
 Schur state avoids repeated full settlement.

 \item \textbf{The accelerated fast path is certifiable but not universal.}
 Every support-safe terminal trace with
 $\varrho_{\rm rev}=\widetilde O(1/\sqrt\alpha)$ already performs
 $\widetilde O(1/(\rho\sqrt\alpha))$ charged work.  The heavy-early RPPR
 tree rules out policy-blind bank success, and the one-edge theorem proves
 that the literal fixed-band two-rung arm can take $\Omega(1/\alpha)$ even at
 fixed regularization and accuracy.  A universal positive result therefore
 needs a modified forced-spreading or incremental-transport arm in a fixed
 menu that performs
 $\widetilde O(\mathsf V_{\rm loc}/\sqrt\alpha)$ charged graph work under a clearly stated
 local-volume ledger $\mathsf V_{\rm loc}$.

 \item \textbf{The exact one-state competition condition is isolated.}
 A capped experimental prefix plus exact fallback is safe without reset, and
 the RPPR absorption checkpoint permits arbitrary causal policy switches
 while preserving support containment and correctness.  Separately,
 the geometric restart portfolio is constant-competitive without retaining
 parallel states when reset is available.  On a truly shared irreversible
 state, mergeable $\kappa$-tight countdowns are sufficient for a
 $K\kappa$-competitive theorem.  The heavy-early gate proves that absorption
 alone cannot supply them.  Activation tokens supply the missing countdown
 with $\kappa=1$ on endpoint paths, giving work at most $4/\rho$ for the exact
 Schur arm.  Exact affine arm products now give the same $\kappa=1$ conclusion,
 with a fully separated linear ledger, on the center-seeded three-arm spider
 under every legal certified batch order.  Four such products coupled through
 a fixed rank-two Schur core give the same conclusion on the double-Y seeded at
 one branch vertex, $s=e_o$, with two adjacent degree-three branch vertices,
 including explicit recovery,
 validation, persistent-state, transient-workspace, and output charges.  The
 two-vertex continuity
 obstruction explains why energy or raw debt magnitude cannot replace those
 discrete tokens.  On the growing branch caterpillar, the exact response is
 still an implicit tridiagonal transfer object, but a literal EagerTipKey
 array is not mergeable at linear cost: one legal order forces
 $m(m+1)$ old-key rewrites and has the full resource vector
 \eqref{eq:branch-caterpillar-eager-eleven-vector}.  At the first long-arm
 append, one new activation cannot pay $m+1$ old-key rewrites, explicitly
 violating the ordinary linear activation-token template.
 This obstruction is deliberately representation-specific.
 \Cref{thm:branch-caterpillar-kinetic-delta-reporter} closes the complementary
 backbone-first positive-subset case: a one-parameter Green-column update and
 exact crossing-key heap retain the all-positive live-tip set with the full
 vector \eqref{eq:branch-caterpillar-kinetic-eleven-vector} and
 $O(m\log(2+m))$ work.  It is an output-delta theorem, not a $\kappa=1$
 countdown.  On the stricter threshold
 \eqref{eq:branch-caterpillar-canonical-threshold},
 \Cref{thm:branch-caterpillar-canonical-layer-reporter} closes the actual
 canonical all-violations trace: every current boundary is the next
 three-vertex layer, early pendant admissions update only constant many
 transfer cells, and the full vector
 \eqref{eq:branch-caterpillar-canonical-eleven-vector} remains
 $O(m\log(2+m))$.  The remaining amortization problem is the canonical range
 $\rho_{\rm can}\leq\rho<\rho_{\rm cat}$, arbitrary pre-backbone
 interleavings, or a cyclic front.

 \item \textbf{A continuous $\omega$ family needs regularity.}
 A finite grid inherits the best grid arm.  Approximating the best continuous
 relaxation requires a theorem controlling how thresholded SOR work changes
 with $\omega$; live-set discontinuities make a naive Lipschitz assumption
 unsafe.
\end{enumerate}

Thus this direction can remove a major condition---knowing in advance which
local solver is appropriate---using multiple states or restart, and it now
removes the separate state-compatibility obstacle for irreversible RPPR
switching.  It also gives an a posteriori accelerated certificate without a
graph-structural hypothesis and a precise sufficient condition for a
shared-state best-arm ratio.  It cannot yet prove that the accelerated bank
always succeeds on general graphs.  It does construct tight common-state
transport countdowns on serializable endpoint paths, on the minimal
degree-three spider, and on the adjacent-branch double-Y with branch seed
$s=e_o$ and its fixed rank-two core.  It also proves why the literal two-rung,
 policy-blind full settlement, continuous state-energy, and eager exact-tip-key
 routes cannot supply a universal theorem.  The backbone-first caterpillar
 delta interface has a charged kinetic reporter; general-policy and cyclic
 lazy/kinetic routes remain open.  This boundary prevents an adaptive wrapper from being
mistaken for a graph-uniform proof of the desired lower-bound-matching rate.

\section{Decisive experiments}
\label{sec:experiments}

The next empirical campaign should test the claims at their actual boundary.

\begin{enumerate}[leftmargin=*]
 \item Log $C(S)$ and all four causal revisit charges, separately by rung.
 Verify that a paid revisit after an exact operation is preceded by a
 neighbor update in the trace.
 \item Run a global bank with
 $\Gamma_{\rm acc}=c\log^p(1/(\alpha\rho))/\sqrt\alpha$.  Report the fraction
 of instances certifying before overdraw, the overdraw checkpoint, and the
 exact fallback charge.  This directly tests \Cref{cor:rppr-fast-path}.
 \item Compare total work, not just $\varrho_{\rm rev}$, across policies.
 Report objective decrease from \eqref{eq:telescope} per charged work over
 complete rungs, since per-operation energy necessarily favors $\omega=1$.
 \item Replace the size-only memory key by graph fingerprint, seed class,
 $\alpha$, and hardness.  Evaluate transfer only from earlier to later
 instances; never leak a later arm's cost into the predictor.
 \item Simulate the weighted portfolio offline from per-arm work traces.  The
 measured total should obey \eqref{eq:portfolio-discrete}; report the chunk
 overhead $\Delta$ and peak union support.
 \item Stress distribution shift: alternate path-like, star-like, and
 thick-core instances at equal $n$.  The exploration floor should preserve
 the oracle bound even when the predictor is systematically wrong.
 \item Sweep smaller $\alpha$ and fit work exponents.  A better constant at
 $\alpha\in\{0.025,0.04\}$ does not establish the desired
 $\alpha^{-1/2}$ scale.
\end{enumerate}

\section{Conclusion}

The revisit idea is promising, but its strongest theoretical form is not the
literal scalar-to-$\omega$ formula.  The exact scalar is a multiplicity
statistic.  Its causal refinement exposes neighbor backflow, while the energy
identity shows why over-relaxation must be judged over a transport block.
These facts explain both the signal's value and its ambiguity.

Several guarantees are already complete.  Causal variable relaxation is safe
under a compact clamp and live residual guard.  The empirical revisit ratio
factors exactly across nested epochs, and its within-epoch contribution can
be capped online.  On support-safe RPPR traces the global ratio is also a
direct a posteriori accelerated-work certificate.  A capped adaptive prefix
can fall back exactly on the same irreversible state with a quantified
ceiling.  Exact RPPR settlements form absorption-safe checkpoints for
history-dependent no-reset switching.  A
branchable finite portfolio is condition-free relative to its best arm and
can accept arbitrary
revisit-based predictions without sacrificing a per-instance oracle
inequality.  A geometric restart portfolio retains a constant-factor
best-arm theorem with one live state.  Together these supply a broader
framework for combining exact, two-rung, alpha-scaled, and adaptive local
solvers.

The actual RPPR batch-order obstruction shows why checkpoint safety is not a
work oracle, and the one-edge exact tail rules out the literal fixed-band
two-rung arm as a universal accelerated fallback.  The mergeable-countdown
theorem identifies the positive replacement.  It is fully instantiated on
endpoint paths by common activation-once Schur tokens, with $\kappa=1$ and
$O(C(S^\star))$ work, and on the center-seeded three-arm spider by exact
common affine transfer records with the same token tightness.  The double-Y,
seeded at one branch vertex with $s=e_o$, extends that representation to a
fixed rank-two Schur core without eager
boundary rekeys.  On a growing branch caterpillar, a legal exact small-$\rho$
order proves that a literal eager exact-key array performs quadratic rekeys;
the complete eleven-coordinate ledger separates those writes from repeated
tridiagonal response sweeps, materialization, validation, and output.  A
balanced transfer tree still gives logarithmic named tip queries and linear
state, so this is not a lower bound against implicit reporters.  For the
fixed-$m$, branch-seeded, family-dependent small-$\rho$, backbone-first exact-KKT
positive-subset policy, the separable Green update and a kinetic crossing-key
heap give a fully charged $O(m\log(2+m))$
all-positive delta reporter.  On the smaller explicit range
$0<\rho<\rho_{\rm can}(m,\alpha)$, the canonical all-violations chronology is
exactly $m$ three-label layers; constant-many local absorption updates and
balanced transfer queries give the same charged scale.  The rest of the
canonical $\rho_{\rm cat}$ range, arbitrary pre-backbone interleavings, and
cyclic fronts remain outside these theorems.
The remaining graph-uniform target is a policy-robust implicit reporter or a
forced-spreading arm with its own tight transport proof.  Revisit
feedback can select such theories safely; it is a controller, certificate,
and diagnostic, not by itself the source of the accelerated rate.

\bibliographystyle{plainnat}
\bibliography{../../references}

\end{document}
