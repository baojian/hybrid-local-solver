# Overnight campaign scoreboard — hybrid-local-solver

Protocol: per iteration, k parallel directions with falsifiable targets; merge;
decide CONTINUE / KILL / MERGE / SPAWN. Evidence vocabulary: Proved-draft
(unaudited), Measured, Refuted-draft, Open. Nothing here is audited to the
repository's promotion standard. All code/results under
/home/claude/work/overnight/ (findings/*.md per direction).

---

## Iteration 1 (8 directions) — COMPLETE

| Dir | Question | Verdict | Headline |
|---|---|---|---|
| W1 monotone LB | Do all monotone one-hop push methods pay Ω(1/(αε))? | POSITIVE | Class theorem + 630 runs; W ≥ 3(1+α)²/(256αε). Signed SOR(ω_opt) breaks it (c ∝ √α) — monotonicity is the anti-acceleration axiom. |
| W2 spider info LB | Is 1/(√α·ε) information-forced? | REFUTED (product) | Information is Θ~(1/ε), NOT the product. The open problem is purely COMPUTATIONAL. |
| W3 randomized APCG | Expected work vol(S)/√α, immune to K₂/K₈? | POSITIVE | Exponents 0.45–0.51 vs 0.87–1.0 baselines; deterministic blockers do not bite (q^-1.0 in mean AND max). |
| W4 NB-push / EES | Does edge/elimination state break the spider bound? | POSITIVE | NB weight = impedance ratio; NB forwarding IS elimination. Spider s=0.502 (product scale) vs SOR 0.94. Grid fails 10⁵×. |
| W5 trunc. Chebyshev | Where does certified truncation reach product scale? | MIXED | Honest law W = Θ(K·vol(supp_τ)). Star (0.51,1.00) beats push 43×; loses where ball volume is superlinear. |
| W6 incremental active-set | Amortize nested solves to final volume? | MIXED-SHARP | INC-LDL α-free output-linear on path/cat/spider/theta (10.5–20.5·vol); quadratic on btree. Ordering incompatibility = conj:aggregate made concrete. |
| W7 windowed Lyapunov | Does a windowed exponent survive K₂/K₈? | POSITIVE | Reconstruction validated 5/5 exact; windowed slopes ≤0.145 everywhere. Route B likely TRUE; obstacle is proof technique. |
| W8 regime map | Where is the wedge and the prize? | MIXED (sobering) | WY-style is ~1/ε on trees; push/WY boundary is horizontal in α, not ε=√α. Classic-wedge prize <10× in the whole sweep. |

---

## Iteration 2 (6 directions) — COMPLETE

| Dir | Verdict | Headline result | Decision |
|---|---|---|---|
| **I2-A** carry-mode attack | **MIXED — broken on volume, survives on exponent** | Carry mode holds the α-exponent under every stress (staircase 0.508 with 192 admissions, lollipop 0.510, embedded-K₈ 0.451 — no drift toward 1), but the MARGINAL-HUB RING breaks it: spurious volume grows linearly in hub degree (7.5→191.8× vol(S\*)) = ω(vol(S\*)), the kill clause. Mechanism pinned: momentum inflates the effective gate by a *constant* ≈2.9 (τ=0.344, identical across α) — the constant breaks, not the exponent. Hysteresis 4× tames benign families for free but does not remove the ω. **Third, momentum-independent break: with a perfect gate, 87% of ring work is the BOUNDARY SCAN** (vol(∂S\*)=16×vol(S\*), rescanned every n iterations). Damage bound (Proved-draft, graph-uniform): √Λ⁺ ≤ √Λ⁻ + √(2D_t); ΣD_t telescopes ⇒ carried insertion costs only an *additive* log T. | CONTINUE → **I3-B**: the boundary scan is the real binding cost — build a lazy incremental gate. Separately settle whether vol(shell_τ)/vol(S\*) can grow in α⁻¹. |
| **I2-B** dynamic tree LDL | **MIXED → repaired** | Comb defeats ALL inherited backends incl. the surviving SEG-LDL (whose carving is admission-order dependent; teeth capture the backbone — silent degradation, trip-wire never fires). New **HSEG-LDL** (online 2-approximate heavy-path decomposition, subtree sizes as a third segment-tree aggregate): **q = 0.92–1.11 on all five scaling series including the comb**, W/(vol·log²vol) ∈ [1.59,5.57] over 149 configs, **α-free**, 149/149 certificates. Proved-draft: chain length ≤ log_1.5|S| ⇒ O(log²) per admission. Off-tree analysis: separator size k ⇒ k×k Schur ⇒ 2k×2k transfers at k³ — **width, not cyclicity, is the obstruction** (theta graph is output-linear). | CONTINUE → **I3-F**: `thrash(k)` adversary to falsify the measured re-carve bound (moved/\|S\| ≤ 0.44 and falling); LCT/splay version if it falls. |
| **I2-C** windowed proof | **THEOREM DRAFTED (stronger than targeted)** | C1–C8 were the lemma chain, extended to **C1–C17, verified on 140 instances** (n∈{2,4,8,16,32} × q∈{1/10,1/100,1/400} × 5 seeds × 2ρ): **17/17 predicates, 0 failures**, 7130 stage-checks each. K_n does not merely satisfy a windowed bound — it **ABSORBS**: corrections stop (t_abs ≤ 12), so J_T^fin ≤ B ≤ 0.369 < log 2 uniformly, i.e. **Route-B packing holds on K_n with c = 1**. Positive-part coupling RESOLVED on K_n: D=(n−1)I ⇒ P_h is mean-centering ⇒ coordinatewise 1-Lipschitz maps contract variance ⇒ corrections never pump low→high. Low mode is critically damped at (z−(1−q))² — *that* is the K₂ killer, now explained. One Open: C9 V-contraction at genuinely partial corrections (1 occurrence in 140 instances; certified bound passes with 2.1% margin). | CONTINUE → **I3-C**: the general-graph crux is now a single named inequality — prove ‖P min(u,Δv)‖ ≤ ‖P u‖ for the Perron vector v on non-regular graphs. Absorption is K_n-specific (tuned paths correct forever), so a real telescope with c<1 is still needed there. |
| **I2-D** separation package | **T1–T4 all Proved-draft; MANUSCRIPT-READY** | T1: the requested strengthening is **FALSE**, and the truth is sharper — a transport characterization (Gale/Hall) shows radius-1 instances collapse to a scalar B, with **threshold B = 1−ε·vol** attained at W=0 and matched by M=B·I at W=m; Ω(1/(αε)) holds for all B < 1−ε·vol; on radius ≥2, B=1 costs Ω(vol) transport. T2: **signed-η gap CLOSED** via Ψ(r) := pr(r)_c/π_c, which moves *only* at seed ops for either sign of η (verified 3.6e-15 on 10 graphs) — constant improves 3.7× to W·αε ≥ 0.0433; retires three lemmas and most of G3. T3: star SOR upper bound O(m(log(1/ε)+log(1/α))/√α), measured at 0.53–0.57× the bound. T4: separation on one instance family, ratio 0.34→5.84 as α:1/4→2⁻¹². **The separating axiom is r ≥ 0, not the sign of η.** | CONTINUE → **I3-D** (merged with I2-E's next target): the missing half is a *class* lower bound for signed-residual methods via ω(2−ω)=Θ(√α) energy damping. |
| **I2-E** info upper bound | **CHARACTERIZATION CLOSED** | Explicit interval-envelope algorithm with an O(1)-per-probe certificate: **T ≤ (1+o(1))/ε on every spider — no 1/√α, no log**. The 5-condition certificate collapses to one scalar inequality y/(1−y²) ≤ β. **Corrected iteration 1's allocation**: the true worst case is shallow-and-wide (k≈1/(2ε) arms at depth 1), not deep-and-narrow. Measured T·ε ∈ [0.375,0.629] with no α trend; minimax worst 0.5625. Tree extension works with **no degree bound needed** (105 cells, all ε-valid). With W2's lower bound: **information complexity = Θ(1/ε), α-free, matched to factor 80.** | CONTINUE → **I3-D**: since corridor families are now provably exhausted, the ONLY remaining route to Ω(1/(√α·ε)) is a cell-probe / algebraic-decision-tree bound that charges arithmetic. |
| **I2-F** grid frontier | **BREAKTHROUGH — local multigrid breaks the grid** | Parameter map (Proved-draft): on Z^D interiors Q is exactly a positively shifted lattice Laplacian, κ_D = 2√(Dα), so **vol(S_ε) = Θ(log²(1/ε)/α) — polylog in 1/ε**, and the classic target is not vacuous but *vacuously loose* (measured T/vol(S) = 22 … 91 491). Right grid target = output-linear with α-free constants. Measured α-exponents (17 cells, 0 failures): push 0.85, Chebyshev 0.75, push→Cheb 0.81, ND-EES 0.69, **local MG (certified) 0.20, MG (oracle) −0.06** — MG-oracle reproduces the *output's own* exponents. **V-cycle rate 0.024–0.066 uniformly over α ∈ {2⁻⁴,2⁻⁸,2⁻¹²}**; cycles 1–6, a function of ε only; coarse levels/transfers/solves are 38% of the charged bill. Speedup over best incumbent 1.6×/4.4×/**10.6×** at α=2⁻⁸/2⁻¹⁰/2⁻¹², growing ~α^−1/2. The certified variant's 0.20 residual is a *certificate artefact* — the restricted-solve certificate is loose by exactly 1/(8α) on a lattice. **Multigrid is the first charged mechanism whose pass count is set by log(1/ε), not by radius.** | CONTINUE → **I3-A** (top priority): local ALGEBRAIC multigrid on the whole zoo, including the closed 3-regular family that broke EES; lattice-aware certificate; 3D crossover. |

---

## Iteration 3 slate (6 agents)

- **I3-A** (from I2-F): local algebraic multigrid (smoothed aggregation, RAP + transfers fully charged) on the entire zoo — is multilevel response output-linear and α-free on general graphs, not just lattices? Includes the closed prism family that broke EES and the hub decoy. This is the campaign's leading candidate mechanism.
- **I3-B** (from I2-A): the boundary scan — 87% of work even with a perfect gate. Build a lazy/incremental violation gate with a charged ledger; settle whether vol(shell_τ)/vol(S\*) can grow in α⁻¹ (I2-A's two construction attempts failed structurally).
- **I3-C** (from I2-C): general-graph extension — prove or refute ‖P min(u,Δv)‖ ≤ ‖P u‖ for the Perron vector; characterize which graphs absorb (K_n does, tuned paths do not) and attempt the c<1 telescope for the non-absorbing case.
- **I3-D** (from I2-D + I2-E): the lower-bound frontier — a class lower bound for signed-residual one-hop methods via the ω(2−ω)=Θ(√α) energy damping, plus scoping what an algebraic-decision-tree bound charging arithmetic would need.
- **I3-E** (SPAWN): end-to-end candidate solver — compose the campaign's winners (MG or HSEG-LDL response + safe gates + certified termination) into one algorithm with the full eleven-coordinate ledger, measured on the whole zoo against push/WY. Does the composition survive its own charging?
- **I3-F** (from I2-B): `thrash(k)` adversary against HSEG-LDL's re-carve bound; LCT/splay preferred paths if it falls.

---

## Cross-iteration flags

- **The information question is CLOSED**: Θ(1/ε), α-free, both sides matched (W2 + I2-E). The open problem is entirely computational; every corridor-style information route is provably exhausted. This corrects direction D6 of the afternoon assessment.
- **The strongest new mechanism is multilevel response** (I2-F), which is squarely in the repository's "response track" framing but was absent from all 18 note directions. If I3-A generalizes it beyond lattices, this is the campaign's main finding.
- **Two manuscript-ready packages exist**: I2-D (monotone-vs-signed separation) and the I2-C K_n absorption theorem.
- **I2-A's boundary-scan discovery** is mechanism-independent and probably the highest-value practical fix in the whole campaign.
- Nothing under the user's `notes/` was modified.

---

## Iteration 4 (6 directions) — COMPLETE

| Dir | Verdict | Headline result | Decision |
|---|---|---|---|
| **I4-D** output bound | **REFRAMES THE TARGET (framing corrected)** | **`vol(S_ε) < 1/ε` unconditionally** (classical Markov-type bound, cf. FRS19 Thm 2 / ACL — *not* a new inequality; the contribution is the systematic measurement) (proved: `ε·vol(S_ε) < Σ_{S_ε} d_v u_v ≤ Σ_v π_v = 1`; 687 cells, 14 families, 0 violations, max 0.60, tight on regular expanders/stars at critical size). Therefore output size alone can never justify the FY22 target `1/(√α·ε)`. **CORRECTED framing:** this does NOT refute FY22 — work lower bounds do not come from output size, and I4-C proved in the same iteration that R± provably needs `Ω(1/(√α·ε))` on the star, where output is only `~1/ε`. The two are consistent; the target is tight for a natural class and beatable by a stronger primitive. Corrected target: `W = O~(nnz(s) + vol(S_ε)) ⊆ O~(1/ε)`, in a model with an O(1) degree query (mandatory: `vol(∂S_ε)` is NOT bounded by 1/ε — measured 159×). Ridge measurement along `vol(S_ε) = Θ(1/ε)`: `W·ε` slope **0.000** (star, 2.5 decades, `W = 10.34/ε` exactly), 0.068 (2-D grid), 0.215-and-falling (path). Verdict: (i)-negative **proved**, (iii)-positive — `O~(1/ε)` consistent with every measurement but only at *portfolio* level. | CONTINUE → this becomes the campaign's organizing target. |
| **I4-E** value-guided frontier | **POSITIVE on the mechanism, Open by α^0.26** | Built **VGF**: admits a boundary vertex only when its certified *value* lower bound clears γ·ε — never by adjacency. **Ball trap** (clique hung off the support, correct output empty): `W·ε` slope **−0.05 (flat)** vs **+2.94 / +1.92** for ball-elimination / ball-AMG, and flat vs linear on hidden-hub. Output-boundedness is **proved** (Lemma V2: `S ⊆ {v : u_v ≥ θγε}`, no adjacency term). Region inflation exponent **+0.033 (α-free)**, mechanism +0.064; the entire residual α-dependence is the **round count** (+0.27). 45/45 certified, 0 misses. | CONTINUE → SP3: frontier Schur/response incremental factorization, predicted to zero the exponent. |
| **I4-C** signed class LB | **PROVED-draft — plus three corrections** | Class **R±** (signed residual, any ω ∈ (0,2), adaptive/randomized) pays `W ≥ 3/(128·ε·√(α(1+α)))` on the center-seeded star. **The energy-budget route is vacuous** (terminal energy may exceed Φ₀; ω→2 is empirically optimal and `ω/(2−ω)` is unbounded). What works: energy monotonicity caps the fast mode, and a center relaxation moves both modes by equal magnitude, so `|Δz_c| ≤ 2√(α(1+α))` per op while `z_c` must travel ≥ 3/8. Pathwise ⇒ randomization exempt. Numerics: `N_opt·√α` flat 0.66–0.75, 0/104 violations over 13 schedule families. **Three side-findings matter more** (see cross-cutting flags). | CONTINUE → block one-hop relaxation (the star's center-plus-leaves block escapes the coordinate lemma in O(m) — the bridge to local AMG). |
| **I4-A** face-aligned + class theorem | **MIXED → then a better fix** | Recovered and audited the safety theorem (constructive: `w := Q_S^{-1} y` for any `y>0` gives exact-rational inverse iteration with a free Collatz–Wielandt bracket — no eigensolver). Safety verified 23,100 exact checks, 0 violations. Face-aligned capping takes L-E from **127/160 → 211/211** and halves P24's inflation (`J_T` 17.89→8.60, slope 0.1255→**0.0559**) — but **absorption still does not fire**: on a proper face `α_S > α` makes the face mode underdamped, and momentum is calibrated to α while the face runs at α_S (ratio 2.19–150.3). **The closing probe is the real result**: face-calibrated `β_S = (1−q_S)/(1+q_S)` gives **0 corrections on 5/6 cells and a 100% face floor on all six**, and is *faster*. `μ₂ ≥ 2q` class theorem verified on 82 exact instances, 17/20 predicates at 100%. Correction to I3-C: `λ₂` is right in the contraction constants but the *trigger* needs `λ_max`. | CONTINUE → prove `m_S ≤ 1−q_S²` (reduces to `α_S ≥ α`); pursue face-calibrated momentum as the algorithmic fix. |
| **I4-F** certificate | **CHARACTERIZED EXACTLY; decision-ready** | The repo's rule is **not carelessly loose**: `M := D^{-1/2}Q^{-1}D^{1/2} ≥ 0` with every row sum exactly `1/α`, so `‖M‖_∞ = 1/α` **exactly** — all slack is residual-*structural*. Sharp constant derived and verified: for residual supported on T, `A(T) = max_i π^(i)(T)`, the PPR mass a walk from i places on T. On the infinite path with `|T|=1` this is **exactly √α** — that is the observed law. **One-sidedness buys nothing** (the extremal residual `r = θ·d·1_T` is itself nonnegative — refuted with a witness). Tax: push **1.00–2.16×** (nothing to recover — its threshold stop leaves exactly the extremal profile); region solves 1.15→2.54× and growing as α falls — this *is* I2-F's lattice inflation and I3-A's certified-vs-oracle exponent gap. New sound certificate (e'), profile-localized supersolution: 0 violations in 648 checks, α-free cost 1.2–8× vol(S_ε), hits the oracle radius exactly in 9/9 cells at `K = ⌈4/√α⌉`. **Uniform √α relaxation is UNSOUND.** | DELIVER → decision-ready two-tier recommendation for the repo's open residual-convention decision. |
| **I4-B** composed solver | **WORKS, does not dominate** | α-free (median exponent 0.03, max 0.18 vs push 0.96, WY 1.17), 372/372 certified, the only method never catastrophic anywhere — but median 1.20× (worst 5.78×) versus the per-cell hindsight-best, and push beats it 5.7–15.6× on high-treewidth families wherever `1/(αε)` is small. Triage costs 1.7% median and buys 1.2×–3170×; 24/84 cells flip route, always ELIM→AMG, never back, write-off ≤0.12%. Full eleven-coordinate ledger reported (`R_adj ≡ 0` in every composed run — the gate's signature). **Honest negative:** the kinetic gate wins only 0.0–6.7% here, because I3-B's 266× needs hubs that stay *unadmitted*, which ε-certified PPR does not produce. | CONTINUE (lower priority) → wire HSEG-LDL into the ELIM route; add push as a third triage route. |

## Cross-cutting flags from iteration 4

1. **A finding about the active manuscript.** I4-C reports that the long spider used in `thm:cf-spider-lower` has `S_ε = ∅` for α ≤ 2⁻⁸ — the all-zero vector is a valid output at W = 0. If this holds up, that theorem lower-bounds the cost of satisfying the *certificate*, not the cost of solving the problem. **Verify before the paper goes out.**
2. **Two `log(1/α)` factors are stopping-rule artifacts, not solver costs.** I4-C shows I2-D's T3 star bound loses its `log(1/α)` under a semantic stop (`N·√α` flat 1.375→1.348 vs certificate stop growing 2.625→4.574), and I4-F shows the certified-vs-oracle exponent gap in I2-F/I3-A *is* the certificate. Any work bound in the program that stops on the residual carries this tax.
3. **A clean complexity ladder on one instance** (I4-C), each row dropping one axiom and buying one √α: monotone `M₊ = M± = Θ(1/(αε))` → signed `R± = Θ(1/(√α·ε))` → elimination `Θ(1/ε)`.
4. **The organizing target has changed** (I4-D): `O~(vol(S_ε))` with α-free constants, not `1/(√α·ε)`. Two of the campaign's mechanisms now have proved output-boundedness (VGF's Lemma V2) or measured α-freeness (elimination, AMG); none yet has both end-to-end.

## Post-iteration-4 correction (user-prompted audit)

Two claims in the iteration-4 merge were overstated and are corrected in report §4.1:

1. **`vol(S_ε) < 1/ε` is classical, not new** — same fact as FRS19 Thm 2 and ACL's support-volume bound. The measurement programme around it stands; the inequality is folklore.
2. **"FY22 refuted as a tight target" was wrong.** Output size cannot produce work lower bounds. I4-C's `Ω(1/(√α·ε))` for R± on the star (output `~1/ε` there) is consistent with, not contradicted by, the output bound. Correct statement: output size can never *justify* the target, so the target must be settled by work arguments — and for R± it is already settled affirmatively.
3. **`vol(∂S_ε)` admits no bound `f(ε,α)`** (verified: seed with k hubs of degree D gives `vol(S_ε)=6` fixed while `vol(∂S_ε)=kD → ∞`). What is bounded is the **cardinality** `|∂S_ε| ≤ vol(S_ε) < 1/ε`, which is exactly why an O(1) degree query is required in the model. This is a different object from I3-B's shell lemma.

---

## Iteration 5 (6 directions) — COMPLETE

| Dir | Verdict | Headline result | Decision |
|---|---|---|---|
| **I5-A** manuscript spider check | **RESOLVED, with a correction to I4-C** | Claim TRUE and regime-exact: `u_max = (√α/k)·(1+λ^{2L})/(1−λ^{2L})`, so `S_ε = ∅ ⟺ √α(1+ζ)/(1−ζ) < θ` — every theorem-admissible dyadic α is empty (θ=1/8: all α ≤ 2⁻⁷, one octave earlier than I4-C said). **But I4-C's diagnosis was also wrong**: the Ω(1/(αε)) is *not* about the certificate either — the certificate is satisfiable with support radius 3 (LP, vol 7k), and the manuscript's own ω=1 push at τ=ε meets it in W=(21–42)k with W·αε → 0. **The cost is specifically the fixed-ω\* signed FIFO dynamics** (wave lemma verified push-exactly, 2.8e-15). **Repair found**: pendant-seed variant (degree-1 pendant on the center, ε=γ_α, k=Θ(θ/α)) makes z=0 invalid, keeps the triangular wave (constants ≤12% perturbed), W·αε = 0.070–0.086 flat. Recommended minimal edit: one remark after the theorem with the closed form; no change to theorem/lemma/status table needed. | DELIVER note to author. |
| **I5-B** VGF SP3 | **MIXED — prediction refuted in its stated form** | Incrementality does *not* kill rounds; **the certificate does**. On path-likes the influence region of one admission is Θ(vol(S)) up to logs, so "stalled round = O(vol(new))" is *impossible* for any exact-residual method — incrementality shaves the log at a 1.5–2× constant price. Median mechanism exponent 0.256 → 0.197 (SP3) → **0.156** (SP3+(e')), oracle 0.104. **Fan trap broken twice**: SP3 alone 103.4→5.64; (e') stops before admission, region ×516 → ×9.9 (Up-exclusion proved impossible; early certification is the sound fix). (e') collapses endgame rounds where it fires; the K=4/√α schedule provably fails below α≈2⁻¹³ — corrected K=Θ(log(1/α)/√α) (superseded again by I5-F's self-consistent exterior). | CONTINUE → (e')-first VGF with I5-F's certifier. |
| **I5-C** face-calibrated momentum | **ROUTE-B BREAKTHROUGH** | Exact Fractions, both fixes on (face-aligned cap + face-calibrated momentum, correctly calibrated to the **upper** Collatz–Wielandt end): **the safeguard never fires at all** — 0 corrections, 0 retractions in 2,550/2,550 exact stages across 9 proper-face cells including all pre-lock face changes; **J_T = 0.000 exactly on P24** (float slope 0.12552 → 0.00000). Lemma correction: I4-A's claim was **sign-backwards** — the truth is `m_S ≥ 1−q_S² ⟺ α_S ≥ α` (proved: Cauchy interlacing + exact identity, 200/200), which is the *overdamping* direction, so upper-end calibration is the safe one. **"Faster" is refuted**: fm is 25–60% *slower* to fixed accuracy (weak early momentum delays lock); wins only asymptotically. Face chain is monotone (proved, ≤\|S\*\| changes); re-tuning *releases* potential (jump < 1 at all 85 downward re-tunes) and telescopes. The face-C16 transplant fails on all six cells — the route to a theorem is the **never-triggering lemma** (trial stays subsolution under face damping), making P-stages vacuous. K_n regression bit-identical. | CONTINUE → the never-triggering lemma = the single remaining obstacle to a proper-face Route-B theorem. |
| **I5-D** ladder paper | **AUDIT-READY** | 745-line package + `verify_ladder.py`: 45 cells × 13 checks, **0 failures**. Consolidation *improved* the results: the z-scale Ψ makes the graph-free Theorem A′ (Φ constant eliminated from all main statements); new pathwise cap `‖r‖₁ ≤ 2√(α/(1+α))` for all of R± extends Theorem B to output maps; two constant slips in i2d/i4c found and fixed (0.0216→0.0190/0.0397; 3/128→3/256 general). Ladder final: `Θ(1/(αε)) → Θ(1/(√α·ε)) → Θ(1/ε)`, axioms: `r ≥ 0` = anti-acceleration, dissipative one-hop relaxation = anti-output-linearity, step sign buys nothing. Two Opens: general-graph O_B corollary (Φ=1); whether any bounded-seed-degree family with vol(S_ε)=Θ(1/ε) forces the R± bound (or seed self-return π_v/γ_α = Θ(1/α) is necessary). | DELIVER for user audit. |
| **I5-E** block relaxation class | **CAP THEORY UNIFIED** | Generalized cap (Proved-draft, 42/42 exact + 1316 inexact-update checks, 0 violations): `\|Δz_v\| ≤ 2√(γ_α·π_v·τ_v(U))` with `τ_v(U) = ((H_UU)^{-1})_vv` — locally computable, exact identity `τ_v(V) = π_v/γ_α`; covers ω-exact block steps AND any Φ-monotone U-supported update (⊇ inexact AMG corrections). Star law exact: `W(B) = Θ((1/ε)·√(x(B)/α))`, `x(B) = 1−c²(B−1)/m` — the √(α+j/m) hypothesis is **refuted**; **bounded blocks buy nothing** (g(B) = Θ(1), B ≤ m/2 still pays 0.0083/(√α·ε)); escape is a sharp phase transition at B ≈ \|S_ε\|. Path gives no block bound (blocks advance linearly, saturate at b\* ≈ 0.28/√α). **What forces block methods is VOLUME (coverage of the seed's reflecting boundary), not propagation.** The ladder restated as one cap with three τ regimes — the campaign's entire mechanism map as a single theory statement. | CONTINUE → heavy-tailed balls (rrt): prove solve cost ω(vol·polylog) there, or cap opcx and collapse the last rung. |
| **I5-F** certificate integration | **RADIUS QUESTION CLOSED; production certifier shipped** | `lib/cert.py` (sound by construction; 1,472 exact checks, 0 violations). **Lower bound (theorem, verified 1e-13)**: cycle adversary `C_{2K+4}` is indistinguishable from the infinite path on `B_K(supp r)` + degree queries, forcing `K ≥ artanh(1/C)/(2·artanh√α) − 2` for C-tight sound local certification. **Matching upper**: new *exact self-consistent exterior* (two solves + a scalar fixed point) certifies factor 2 at K = 0.25–0.30/√α — minimax-optimal within ~20%; the log(1/α) schedules are obsolete. **Θ(1/√α) is necessary and sufficient.** Tax-free headlines: bound tightness median 1.01 vs Tier-0's 8.0 (up to 22× recovered); work gap closes off-lattice (1.6–1.8× → 1.2–1.3×) but NOT on lattices with a generic certifier — the certificate system is itself 1/α-conditioned, so the certifier must reuse the mechanism's own α-free solver (engineering spelled out). Two-tier recommendation finalized, paste-ready for `docs/decisions/residual-convention.md`. | DELIVER; lattice mechanism-reuse certification as follow-up. |

## Cross-cutting flags from iteration 5

1. **The manuscript question has a clean, fair answer** (I5-A): the theorem is true as stated; in its asymptotic regime the instance is semantically trivial AND certificate-satisfiable cheaply — the Ω(1/(αε)) isolates the fixed-ω\* FIFO dynamics specifically. A one-remark edit + optional pendant-seed variant fully repairs the evidentiary value.
2. **Route B is one lemma away** (I5-C): with face-aligned cap + upper-end face-calibrated momentum, the safeguard never fires on any tested proper-face instance, exactly. The "never-triggering lemma" is now the single named obstacle.
3. **The τ functional** (I5-E) unifies every mechanism result of the campaign: work ~ (travel distance / cap) and the cap is 2√(γ_α π_v τ_v(U)). Coordinates: τ=1. Bounded blocks: τ = Θ(1). Output-scale blocks: τ = π_v/γ_α and α-dependence migrates into the solve price. This plus I5-F's radius law are the campaign's two cleanest new theory objects.
4. **Certificate story complete**: sharp constant (I4-F) + radius law Θ(1/√α) necessary & sufficient (I5-F) + production implementation + migration note. Ready for the repo decision.
5. Corrections cascade properly: I4-C's "about the certificate" diagnosis corrected by I5-A; I4-A's lemma sign corrected by I5-C; i2d/i4c constant slips fixed by I5-D. The audit chain is working.
