"""Reconcile the final core with the fixed independently validated manifest."""

from pathlib import Path
import validate_combined_solver_package as validator
from deterministic_rppr import solve_fast


if __name__ == '__main__':
    validator.main(solver=solve_fast, backend='fast',
                   reference_path=Path(__file__).with_name('packaged_combined_solver_validation.json'))
